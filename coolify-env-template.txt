# Coolify Production Environment Variables Template
# FB_APU04 - Simulador Fiscal
# VERSÃO: 1.0.0

# ========================================
# APPLICATION SETTINGS
# ========================================
PORT=8084
APP_MODULE=simulador
VITE_APP_MODULE=simu
COOKIE_SECURE=true
JWT_SECRET=change-me-super-secure-jwt-secret-2026

# ========================================
# DATABASE (próprio — não compartilhado)
# ========================================
DB_USER=postgres
DB_PASSWORD=your_secure_db_password
DB_NAME=fiscal_apu04_db
DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}?sslmode=disable

# ========================================
# REDIS
# ========================================
REDIS_ADDR=redis:6379

# ========================================
# SMTP - EMAIL (Hostinger)
# ========================================
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=contato@fortesbezerra.com.br
SMTP_PASSWORD=your_smtp_password_here
SMTP_FROM=FBTax Cloud <contato@fortesbezerra.com.br>
APP_URL=https://simu.fbtax.cloud

# ========================================
# Z.AI GLM API
# ========================================
ZAI_API_KEY=your_zai_api_key_here

# ========================================
# SECURITY
# ========================================
CORS_ORIGINS=https://simu.fbtax.cloud,https://simu.fcxlabs.com
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=1m
SESSION_TIMEOUT=24h
AUDIT_LOGS=true

# ========================================
# COOLIFY INTEGRATION
# ========================================
HEALTH_CHECK_INTERVAL=30s
METRICS_ENABLED=true

# ========================================
# DEVELOPMENT (COMMENT OUT IN PRODUCTION)
# ========================================
# DEBUG=false
# VERBOSE_LOGS=false

# ========================================
# INSTRUCOES DE CONFIGURACAO NO COOLIFY
# ========================================
# Este arquivo e um template de referencia — NAO adicione valores reais aqui.
# Os segredos reais devem ser configurados diretamente no Coolify dashboard.
#
# Passos para configurar:
# 1. Acesse o Coolify dashboard para o servico simu.fcxlabs.com (ou simu.fbtax.cloud)
# 2. Va em Environment Variables do servico de backend (FB_APU04)
# 3. Confirme que SMTP_PASSWORD esta definido com a senha real do Hostinger SMTP
#    Origem: painel Hostinger -> Email -> Gerenciar -> Senha SMTP
# 4. Confirme que ZAI_API_KEY esta definido com a chave real do Z.AI GLM
#    Origem: https://bigmodel.cn -> API Keys
# 5. Apos confirmar, o servico NAO precisa ser reimplantado (vars ja estao no container ativo)
#    O godotenv.Load() em backend/main.go nao sobrescreve vars ja injetadas pelo Coolify
#
# Para o bridge ERP no AWS:
# 6. O arquivo config-apu04.yaml no servidor AWS
#    (/opt/apps/fbtax/erp-bridge-aws/config-apu04.yaml) NAO e o arquivo do repositorio.
#    Edite diretamente no servidor e restaure as senhas Oracle reais nos campos senha: "CHANGE_ME".
#    O repositorio contem apenas CHANGE_ME como placeholder — os valores reais ficam so no servidor.
