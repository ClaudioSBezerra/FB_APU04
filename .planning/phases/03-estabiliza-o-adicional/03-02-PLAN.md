---
phase: 03-estabiliza-o-adicional
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/handlers/auth_middleware_test.go
  - backend/handlers/erp_bridge_batch_test.go
  - backend/handlers/rate_limiter_test.go
autonomous: true
requirements:
  - STAB-07

must_haves:
  truths:
    - "go test ./handlers/... executa sem erros de compilação"
    - "Cobertura de statements no pacote handlers/ atinge >= 30%"
    - "Testes não requerem banco de dados (nil db suficiente para os caminhos testados)"
    - "Testes seguem o padrão do arquivo existente admin_reset_helpers_test.go"
  artifacts:
    - path: "backend/handlers/auth_middleware_test.go"
      provides: "Testes do AuthMiddleware: sem header, header malformado, JWT inválido"
      exports: ["TestAuthMiddleware_NoHeader", "TestAuthMiddleware_MalformedHeader", "TestAuthMiddleware_InvalidJWT", "TestAuthMiddleware_ValidJWTWrongRole"]
    - path: "backend/handlers/erp_bridge_batch_test.go"
      provides: "Testes do ERPBridgeBatchImportHandler: método errado, sem API key"
      exports: ["TestERPBridgeBatchImportHandler_MethodNotAllowed", "TestERPBridgeBatchImportHandler_MissingAPIKey"]
    - path: "backend/handlers/rate_limiter_test.go"
      provides: "Testes do rateLimiter: Allow, IsLimited, Reset, GetClientIP"
      exports: ["TestRateLimiter_Allow", "TestRateLimiter_ExceedsLimit", "TestRateLimiter_Reset", "TestGetClientIP_XForwardedFor", "TestGetClientIP_RemoteAddr"]
  key_links:
    - from: "backend/handlers/auth_middleware_test.go"
      to: "backend/handlers/auth.go"
      via: "package handlers (mesmo pacote — acesso a símbolos não exportados)"
      pattern: "AuthMiddleware\\(next, \"admin\"\\)"
    - from: "backend/handlers/erp_bridge_batch_test.go"
      to: "backend/handlers/erp_bridge_batch.go"
      via: "ERPBridgeBatchImportHandler(nil) — nil db seguro para caminhos pré-DB"
      pattern: "ERPBridgeBatchImportHandler\\(nil\\)"
    - from: "backend/handlers/rate_limiter_test.go"
      to: "backend/handlers/middleware.go"
      via: "newRateLimiter() — função não exportada, acessível no mesmo pacote"
      pattern: "newRateLimiter\\("
---

<objective>
Criar três arquivos de testes Go para o pacote handlers, cobrindo os caminhos que não precisam
de banco de dados: AuthMiddleware (ausência de header, JWT inválido, role errada),
ERPBridgeBatchImportHandler (método errado, API key ausente) e rateLimiter (Allow, IsLimited,
Reset). O objetivo é atingir >= 30% de cobertura de statements em ./handlers/... usando apenas
net/http/httptest da stdlib, sem adicionar dependências externas.

Purpose: O projeto tem apenas 0.2% de cobertura (5 testes em admin_reset_helpers_test.go).
Atingir 30% bootstrap permite detectar regressões nos caminhos de autenticação e rate-limit,
que são os mais críticos pós-incidente.

Output: Três novos arquivos de teste Go, todos no package handlers, cobrindo ~30% de statements
sem exigir banco de dados nem bibliotecas de mock adicionais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-estabiliza-o-adicional/03-RESEARCH.md
@.planning/phases/03-estabiliza-o-adicional/03-PATTERNS.md

Contexto específico deste plano:

PADRÃO EXISTENTE (copiar exatamente):
  backend/handlers/admin_reset_helpers_test.go — package handlers, table-driven t.Run,
  sem import de bibliotecas externas. Arquivo tem 93 linhas.

ASSINATURAS DOS SÍMBOLOS A TESTAR:

auth.go:
  func AuthMiddleware(next http.HandlerFunc, requiredRole string) http.HandlerFunc
  func getJWTSecret() []byte  (não exportado — acessível no mesmo package)

erp_bridge_batch.go linha 77:
  func ERPBridgeBatchImportHandler(db *sql.DB) http.HandlerFunc
  — O handler verifica método (POST only → 405) e header X-API-Key (ausente → 401)
    ANTES de qualquer acesso ao *sql.DB. nil db é seguro para esses dois caminhos.

middleware.go:
  func newRateLimiter(max int, window time.Duration) *rateLimiter
  func (rl *rateLimiter) Allow(key string) bool
  func (rl *rateLimiter) IsLimited(key string) bool
  func (rl *rateLimiter) Reset(key string)
  func GetClientIP(r *http.Request) string  (linha não confirmada — verificar)

IMPORTS NECESSÁRIOS NOS NOVOS ARQUIVOS:
  "database/sql"
  "net/http"
  "net/http/httptest"
  "testing"
  "time"  (para rate_limiter_test.go)

AVISO: NÃO passar nil db para handlers que acessam o banco antes de verificar
autenticação/método. Se o handler acessa db antes do guard, o teste deve usar
t.Skip("requires DB") guarded by os.Getenv("TEST_DATABASE_URL").

COMANDO DE COBERTURA (executar a partir de backend/):
  go test -coverprofile=coverage.out ./handlers/... && go tool cover -func=coverage.out | tail -1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar auth_middleware_test.go com testes do AuthMiddleware</name>
  <files>backend/handlers/auth_middleware_test.go</files>
  <behavior>
  - TestAuthMiddleware_NoHeader: GET / sem Authorization header → 401 Unauthorized
  - TestAuthMiddleware_MalformedHeader: Authorization: "InvalidToken" (sem "Bearer ") → 401
  - TestAuthMiddleware_InvalidJWT: Authorization: "Bearer nao.e.jwt.valido" → 401
  - TestAuthMiddleware_ValidJWTWrongRole: JWT válido assinado com getJWTSecret() com role="user",
    AuthMiddleware exige "admin" → 403 Forbidden
  - TestAuthMiddleware_PassesThrough: JWT válido com role="admin" → handler inner retorna 200
  </behavior>
  <action>
  Criar backend/handlers/auth_middleware_test.go com package handlers.

  Imports: "net/http", "net/http/httptest", "testing", e o pacote de geração de JWT
  que já está em uso no projeto (verificar go.mod para o nome exato do pacote JWT —
  provavelmente "github.com/golang-jwt/jwt/v5" ou "github.com/golang-jwt/jwt").

  Para TestAuthMiddleware_ValidJWTWrongRole e TestAuthMiddleware_PassesThrough:
  gerar um JWT real usando a chave retornada por getJWTSecret(). O payload deve conter
  a claim "role" com o valor adequado. Assinar com jwt.SigningMethodHS256.

  Estrutura de cada caso de teste:
    next := http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
        w.WriteHeader(http.StatusOK)
    })
    handler := AuthMiddleware(next, "admin")
    req := httptest.NewRequest(http.MethodGet, "/", nil)
    // [configurar header se necessário]
    rr := httptest.NewRecorder()
    handler(rr, req)
    if rr.Code != http.StatusXxx { t.Errorf(...) }

  Usar table-driven tests com t.Run para os 3 casos de falha (NoHeader, Malformed, InvalidJWT).
  Os casos com JWT válido podem ser testes separados para clareza.

  NÃO usar bibliotecas de asserção externas (sem testify). Apenas comparações diretas com t.Errorf.
  </action>
  <verify>
    <automated>cd /home/claudiobezerra/projetos/FB_APU04/backend && go test ./handlers/... -run TestAuthMiddleware -v 2>&1 | tail -20</automated>
  </verify>
  <done>
- Todos os TestAuthMiddleware_* passam (PASS)
- Nenhum erro de compilação
- Cada caso valida o status HTTP correto (401 ou 403 ou 200)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar erp_bridge_batch_test.go e rate_limiter_test.go</name>
  <files>backend/handlers/erp_bridge_batch_test.go, backend/handlers/rate_limiter_test.go</files>
  <behavior>
  erp_bridge_batch_test.go:
  - TestERPBridgeBatchImportHandler_MethodNotAllowed: GET → 405 (handler só aceita POST)
  - TestERPBridgeBatchImportHandler_MissingAPIKey: POST sem header X-API-Key → 401
  - TestERPBridgeBatchImportHandler_InvalidAPIKey: POST com X-API-Key="wrong" → 401

  rate_limiter_test.go:
  - TestRateLimiter_Allow: newRateLimiter(3, time.Minute) — 3 Allow("key") retornam true
  - TestRateLimiter_ExceedsLimit: 4º Allow("key") retorna false
  - TestRateLimiter_Reset: após Reset("key"), Allow("key") retorna true novamente
  - TestRateLimiter_IsLimited: IsLimited("key") retorna true após exceder limite
  - TestGetClientIP_XForwardedFor: request com X-Forwarded-For: "1.2.3.4" retorna "1.2.3.4"
  - TestGetClientIP_RemoteAddr: request sem X-Forwarded-For, RemoteAddr="5.6.7.8:9999" retorna "5.6.7.8"
  </behavior>
  <action>
  Criar backend/handlers/erp_bridge_batch_test.go com package handlers.

  Imports: "database/sql", "net/http", "net/http/httptest", "testing".

  Para ERPBridgeBatchImportHandler_MethodNotAllowed e MissingAPIKey, passar nil como *sql.DB —
  ambos os guards (verificação de método e verificação de X-API-Key) executam antes de qualquer
  chamada ao banco. Documentar com comentário: "// nil db seguro: handler retorna antes de acessar DB".

  Para InvalidAPIKey: verificar no código de erp_bridge_batch.go qual é a lógica de validação
  da API key (provavelmente compara com os.Getenv("ERP_BRIDGE_API_KEY")). Se usar env var,
  não setar a var no teste — o header não vazio mas errado deve resultar em 401.

  Criar backend/handlers/rate_limiter_test.go com package handlers.

  Imports: "net/http", "net/http/httptest", "testing", "time".

  Para TestGetClientIP_XForwardedFor: criar httptest.NewRequest, setar header
  r.Header.Set("X-Forwarded-For", "1.2.3.4"), chamar GetClientIP(r), verificar resultado.

  Para TestGetClientIP_RemoteAddr: criar httptest.NewRequest sem X-Forwarded-For.
  O httptest.NewRequest usa "192.0.2.1:1234" como RemoteAddr padrão. Confirmar o valor
  exato que GetClientIP retorna para esse caso antes de escrever a asserção.

  Usar table-driven tests com t.Run para os casos do rateLimiter.
  </action>
  <verify>
    <automated>cd /home/claudiobezerra/projetos/FB_APU04/backend && go test ./handlers/... -v -coverprofile=coverage.out 2>&1 | tail -30 && go tool cover -func=coverage.out | grep -E "handlers|total"</automated>
  </verify>
  <done>
- Todos os testes passam (PASS, sem FAIL)
- go tool cover -func=coverage.out mostra total de statements >= 30% em ./handlers/...
- Nenhum test usa banco de dados real (sem conexão PostgreSQL necessária)
- Nenhuma biblioteca externa nova foi adicionada ao go.mod
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → AuthMiddleware | Header Authorization é entrada não confiável |
| X-API-Key header → ERPBridgeBatchImportHandler | Header pode ser forjado por qualquer cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Spoofing | AuthMiddleware | mitigate | Testes verificam que JWT inválido e ausente retornam 401 (cobertura de regressão) |
| T-03-02-02 | Elevation of Privilege | AuthMiddleware role check | mitigate | TestAuthMiddleware_ValidJWTWrongRole cobre que role errada retorna 403 |
| T-03-02-03 | Denial of Service | rateLimiter | mitigate | TestRateLimiter_ExceedsLimit verifica que o 4º request é bloqueado |
</threat_model>

<verification>
# Executar todos os testes com cobertura (a partir do diretório backend/)
cd /home/claudiobezerra/projetos/FB_APU04/backend && \
  go test ./handlers/... -v -coverprofile=coverage.out 2>&1 | grep -E "^(ok|FAIL|---)" && \
  go tool cover -func=coverage.out | tail -5

# Verificar que os 3 arquivos foram criados
ls /home/claudiobezerra/projetos/FB_APU04/backend/handlers/*_test.go

# Verificar que nenhuma dependência nova foi adicionada
cd /home/claudiobezerra/projetos/FB_APU04/backend && git diff go.mod go.sum | head -5
</verification>

<success_criteria>
- go test ./handlers/... retorna ok (zero FAIL)
- go tool cover mostra total: >= 30% de statement coverage em handlers/
- Três novos arquivos de teste existem: auth_middleware_test.go, erp_bridge_batch_test.go, rate_limiter_test.go
- Nenhuma dependência nova no go.mod (todos os imports são stdlib + módulos já presentes)
- Testes executam sem conexão com banco de dados
</success_criteria>

<output>
After completion, create `.planning/phases/03-estabiliza-o-adicional/03-02-SUMMARY.md`
</output>
