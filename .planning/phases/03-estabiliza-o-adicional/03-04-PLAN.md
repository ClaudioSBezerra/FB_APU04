---
phase: 03-estabiliza-o-adicional
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - erp-bridge-aws/bridge.py
autonomous: true
requirements:
  - STAB-09

must_haves:
  truths:
    - "Uma conexão Oracle que cai com DPY-4011 durante processar_sap reconecta e retenta a query"
    - "Uma conexão Oracle que cai durante processar_servidor reconecta no nível da query interna"
    - "Todos os 4 sites oracledb.connect() em bridge.py substituídos por _connect_oracle()"
    - "Após MAX_CONN_RETRIES=3 tentativas malsucedidas, a exceção é re-lançada normalmente"
    - "Cada tentativa de reconexão é registrada via log.warning com número da tentativa"
    - "python bridge.py --help executa sem erro de sintaxe após as modificações"
  artifacts:
    - path: "erp-bridge-aws/bridge.py"
      provides: "Bridge com retry Oracle — funções _connect_oracle() e _is_dpy4011() adicionadas"
      contains: "def _connect_oracle"
  key_links:
    - from: "_connect_oracle()"
      to: "processar_sap() linha ~628"
      via: "substituição do bloco oracledb.connect() isolado por chamada à helper"
      pattern: "conn_ora = _connect_oracle\\("
    - from: "_connect_oracle()"
      to: "processar_servidor() linha ~812"
      via: "substituição do bloco oracledb.connect() isolado por chamada à helper"
      pattern: "conn_ora = _connect_oracle\\("
    - from: "_is_dpy4011()"
      to: "processar_servidor() query loop interno"
      via: "detecção de DPY-4011 no except do cur.execute para reconexão mid-query"
      pattern: "_is_dpy4011\\(exc\\)"
---

<objective>
Adicionar retry automático nas duas conexões Oracle do bridge.py para tornar o bridge resiliente
a erros DPY-4011 ("connection lost contact") que ocorrem quando o firewall da rede derruba
conexões TCP ociosas. A mudança é cirúrgica: dois novos helpers (_connect_oracle, _is_dpy4011)
e substituição dos dois blocos oracledb.connect() existentes. Nenhuma outra lógica é alterada.

Purpose: Erros DPY-4011 hoje terminam silenciosamente o run do bridge com erros=1, exigindo
intervenção humana (restart manual). Com retry, o bridge reconecta automaticamente e o run
continua. O tracker SQLite e o UPSERT do backend garantem que re-enviar um batch já enviado
é idempotente.

Output: bridge.py com _connect_oracle() (3 tentativas, backoff linear 5s/10s) e _is_dpy4011()
como helpers privados; processar_sap e processar_servidor usando _connect_oracle no lugar do
oracledb.connect() direto; processar_servidor com detecção de DPY-4011 na query interna.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-estabiliza-o-adicional/03-RESEARCH.md
@.planning/phases/03-estabiliza-o-adicional/03-PATTERNS.md

Contexto específico deste plano:

ARQUIVO A MODIFICAR: erp-bridge-aws/bridge.py
O arquivo usa `import time as _time` (linha 25) — já disponível para sleep.
O arquivo usa `import oracledb` dentro de try/except (linha 34) — já disponível.
O logger é `log = logging.getLogger(...)` — já disponível.

LOCALIZAÇÃO DOS DOIS SITES DE CONEXÃO ORACLE:

Site 1 — processar_sap (linha 607):
  Bloco try/except nas linhas 627-640:
    try:
        conn_ora = oracledb.connect(
            user=oracle_cfg["usuario"],
            password=oracle_cfg["senha"],
            dsn=oracle_cfg["dsn"],
            expire_time=2,
        )
        log.info("Conectado ao Oracle SAP FCCORP (thin mode)")
    except Exception as exc:
        log.error("Falha ao conectar ao FCCORP: %s", exc)
        stats["sap_batch"]["erros"] = 1
        stats["sap_batch"]["erro_msg"] = str(exc)
        stats["sap_batch"]["erro_conexao"] = True
        return stats

Site 2 — processar_servidor (linha 795):
  Bloco try/except nas linhas 812-822:
    try:
        conn_ora = oracledb.connect(
            user=srv["usuario"],
            password=srv["senha"],
            dsn=srv["dsn"],
            expire_time=2,
        )
        log.info("Conectado ao Oracle (thin mode)")
    except Exception as exc:
        log.error("Falha ao conectar em %s: %s", nome, exc)
        return stats

  Query interna por tipo (linhas 834-846):
    try:
        cur = conn_ora.cursor()
        cur.execute(fonte["sql"], data_ini=data_ini, data_fim=data_fim)
        rows = [...]
        cur.close()
    except Exception as exc:
        log.error("Erro na query %s: %s", tipo, exc)
        continue

IDEMPOTÊNCIA CONFIRMADA:
  sap_s4hana: backend usa UPSERT em conflict(chave) → re-enviar batch é seguro
  oracle_xml: SQLite tracker com ja_enviado() → documentos já enviados são pulados

CONSTANTES A ADICIONAR (inserir após as constantes globais existentes, antes da def processar_sap):
  MAX_CONN_RETRIES = 3
  CONN_RETRY_DELAY = 5  # segundos

HELPERS A ADICIONAR (inserir logo após as constantes, antes da linha # ─── Processamento SAP):

def _is_dpy4011(exc: Exception) -> bool:
    Retorna True se exc é um erro DPY-4011 (connection lost).
    Checar: "DPY-4011" in str(exc)

def _connect_oracle(usuario: str, senha: str, dsn: str, nome: str) -> "oracledb.Connection":
    Tenta conectar ao Oracle com retry linear.
    Loop de 1 a MAX_CONN_RETRIES.
    Em cada falha (exceto a última): log.warning com tentativa/total/delay, _time.sleep(CONN_RETRY_DELAY * attempt).
    Na última falha: log.error e raise.
    Em caso de sucesso na tentativa > 1: log.info com o número da tentativa.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar helpers _is_dpy4011 e _connect_oracle ao bridge.py</name>
  <files>erp-bridge-aws/bridge.py</files>
  <action>
  Ler bridge.py para encontrar a linha exata do comentário "# ─── Processamento SAP S4/HANA"
  (aproximadamente linha 605). Inserir imediatamente antes desse comentário as seguintes
  definições:

  Primeiro, as constantes (se não existirem ainda — verificar se MAX_CONN_RETRIES já está
  definido em algum lugar do arquivo):
    MAX_CONN_RETRIES = 3
    CONN_RETRY_DELAY = 5  # segundos — delay base para backoff linear: 5s, 10s

  Depois, os dois helpers:

  def _is_dpy4011(exc: Exception) -> bool:
      """Retorna True se exc é um erro Oracle DPY-4011 (connection lost contact)."""
      return "DPY-4011" in str(exc)

  def _connect_oracle(usuario: str, senha: str, dsn: str, nome: str) -> "oracledb.Connection":
      """Conecta ao Oracle com retry em falhas transitórias.

      Tenta MAX_CONN_RETRIES vezes com backoff linear (CONN_RETRY_DELAY * attempt segundos).
      Lança a última exceção se todas as tentativas falharem.
      """
      last_exc: Exception = RuntimeError("connect never attempted")
      for attempt in range(1, MAX_CONN_RETRIES + 1):
          try:
              conn = oracledb.connect(
                  user=usuario,
                  password=senha,
                  dsn=dsn,
                  expire_time=2,
              )
              if attempt > 1:
                  log.info("[%s] Conectado ao Oracle na tentativa %d/%d", nome, attempt, MAX_CONN_RETRIES)
              return conn
          except Exception as exc:
              last_exc = exc
              if attempt < MAX_CONN_RETRIES:
                  delay = CONN_RETRY_DELAY * attempt
                  log.warning(
                      "[%s] Falha ao conectar (tentativa %d/%d): %s — retentando em %ds",
                      nome, attempt, MAX_CONN_RETRIES, exc, delay,
                  )
                  _time.sleep(delay)
              else:
                  log.error(
                      "[%s] Falha ao conectar após %d tentativas: %s",
                      nome, MAX_CONN_RETRIES, exc,
                  )
      raise last_exc

  Verificar que a indentação está correta (0 espaços — funções de nível de módulo).
  Verificar que não há conflito de nomes com funções existentes.
  </action>
  <verify>
    <automated>cd /home/claudiobezerra/projetos/FB_APU04/erp-bridge-aws && python -c "import ast; ast.parse(open('bridge.py').read()); print('OK: sem erros de sintaxe')"</automated>
  </verify>
  <done>
- python -c "import ast; ast.parse(...)" retorna "OK: sem erros de sintaxe"
- grep encontra "def _is_dpy4011" e "def _connect_oracle" em bridge.py
- As funções estão definidas antes da def processar_sap (linha ~607)
  </done>
</task>

<task type="auto">
  <name>Task 2: Substituir oracledb.connect() direto em processar_sap e processar_servidor</name>
  <files>erp-bridge-aws/bridge.py</files>
  <action>
  MODIFICAÇÃO 1 — processar_sap (linhas ~627-640):
  Substituir o bloco try/except completo de conexão pelo seguinte padrão:

    try:
        conn_ora = _connect_oracle(
            oracle_cfg["usuario"],
            oracle_cfg["senha"],
            oracle_cfg["dsn"],
            "FCCORP",
        )
        log.info("Conectado ao Oracle SAP FCCORP (thin mode)")
    except Exception as exc:
        log.error("Falha ao conectar ao FCCORP após %d tentativas: %s", MAX_CONN_RETRIES, exc)
        stats["sap_batch"]["erros"] = 1
        stats["sap_batch"]["erro_msg"] = str(exc)
        stats["sap_batch"]["erro_conexao"] = True
        return stats

  O log.info "Conectado ao Oracle SAP FCCORP (thin mode)" pode ser mantido — _connect_oracle
  só loga quando attempt > 1, então no caso de sucesso na primeira tentativa o log.info
  abaixo é o que aparece.

  MODIFICAÇÃO 2 — processar_servidor (linhas ~812-822):
  Substituir o bloco try/except de conexão pelo padrão:

    try:
        conn_ora = _connect_oracle(
            srv["usuario"],
            srv["senha"],
            srv["dsn"],
            nome,
        )
        log.info("Conectado ao Oracle (thin mode)")
    except Exception as exc:
        log.error("Falha ao conectar em %s após %d tentativas: %s", nome, MAX_CONN_RETRIES, exc)
        return stats

  MODIFICAÇÃO 3 — processar_servidor query interna (linhas ~834-846):
  O bloco try/except interno que executa cur.execute(fonte["sql"], ...) deve ser modificado
  para detectar DPY-4011 e reconectar. Substituir por:

    _query_retry = 0
    while True:
        try:
            cur = conn_ora.cursor()
            cur.execute(fonte["sql"], data_ini=data_ini, data_fim=data_fim)
            rows = []
            for raw_row in cur:
                rows.append((
                    str(raw_row[fonte["chave_col"]]).strip(),
                    clob_para_str(raw_row[fonte["xml_col"]]),
                ))
            cur.close()
            break  # sucesso — sair do while
        except Exception as exc:
            if _is_dpy4011(exc) and _query_retry < 1:
                _query_retry += 1
                log.warning("  [%s] DPY-4011 na query %s — reconectando (tentativa %d)...",
                            nome, tipo, _query_retry)
                try:
                    conn_ora.close()
                except Exception:
                    pass
                try:
                    conn_ora = _connect_oracle(srv["usuario"], srv["senha"], srv["dsn"], nome)
                except Exception as reconnect_exc:
                    log.error("  [%s] Falha ao reconectar: %s", nome, reconnect_exc)
                    break
                continue  # retry the query
            else:
                log.error("Erro na query %s: %s", tipo, exc)
                break  # sair sem retry adicional

  MODIFICAÇÃO 4 — run_daemon parceiros (linha ~1111):
  Localizar o bloco oracledb.connect() dentro da função run_daemon (o branch que processa
  parceiros/integração Oracle adicional). Substituir por chamada a _connect_oracle() com
  os mesmos parâmetros de usuário/senha/dsn. Se não existir um bloco try/except isolado,
  criar um equivalente ao padrão das Modificações 1 e 2.

  MODIFICAÇÃO 5 — main() parceiros (linha ~1299):
  Localizar o bloco oracledb.connect() em main() no modo one-shot de parceiros.
  Substituir por chamada a _connect_oracle() com os mesmos parâmetros.

  Para as Modificações 4 e 5: ler as linhas exatas do arquivo antes de editar, pois os
  números de linha podem ter deslocado após as Modificações 1-3. Usar grep para localizar
  os blocos: grep -n "oracledb.connect(" bridge.py

  ATENÇÃO: Preservar exatamente toda a lógica DEPOIS do bloco rows = [...] (incluindo
  total_rows, loop de envio, tracking, etc.). Apenas o bloco try/except interno do cur.execute
  é substituído. Usar o arquivo bridge.py atual como referência — não inventar código.

  Verificar que a indentação está alinhada com o código circundante (o bloco está dentro
  de "for tipo in tipos:" que está dentro de "try:" em processar_servidor).
  </action>
  <verify>
    <automated>cd /home/claudiobezerra/projetos/FB_APU04/erp-bridge-aws && python -c "import ast; ast.parse(open('bridge.py').read()); print('OK: sem erros de sintaxe')" && python bridge.py --help 2>&1 | head -5</automated>
  </verify>
  <done>
- python bridge.py --help executa sem erro (mostra usage)
- python -c "ast.parse(...)" retorna OK
- grep confirma "_connect_oracle(" aparece em pelo menos 5 locais (4 sites + definição)
- grep confirma "_is_dpy4011(" aparece na query interna de processar_servidor
- O bloco try/except original de oracledb.connect() em processar_sap e processar_servidor foi substituído
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bridge.py → Oracle DB | Credenciais Oracle em config-apu04.yaml; conexão via rede potencialmente instável |
| bridge.py → fbtax backend API | Re-envio de batches em retry é idempotente via UPSERT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Denial of Service | _connect_oracle retry loop | mitigate | MAX_CONN_RETRIES=3 com raise na última tentativa — não loop infinito |
| T-03-04-02 | Information Disclosure | log.warning com exc em retry | accept | Logs são internos ao container Docker, não expostos publicamente |
| T-03-04-03 | Tampering | Re-envio de batch após reconnect | accept | Backend UPSERT garante idempotência — re-enviar batch não duplica registros |
| T-03-04-04 | Elevation of Privilege | _connect_oracle não altera credenciais | accept | Helper apenas retry a conexão com as mesmas credenciais do cfg passado |
</threat_model>

<verification>
# Verificar sintaxe Python
cd /home/claudiobezerra/projetos/FB_APU04/erp-bridge-aws && \
  python -c "import ast; ast.parse(open('bridge.py').read()); print('OK: sintaxe válida')"

# Verificar que o --help ainda funciona
python /home/claudiobezerra/projetos/FB_APU04/erp-bridge-aws/bridge.py --help 2>&1 | head -3

# Verificar que os helpers foram adicionados
grep -n "def _is_dpy4011\|def _connect_oracle\|MAX_CONN_RETRIES\|CONN_RETRY_DELAY" \
  /home/claudiobezerra/projetos/FB_APU04/erp-bridge-aws/bridge.py

# Verificar que _connect_oracle é chamado nos dois sites de conexão
grep -n "_connect_oracle(" /home/claudiobezerra/projetos/FB_APU04/erp-bridge-aws/bridge.py

# Verificar que oracledb.connect() direto não existe mais nos dois sites originais
# (pode ainda existir dentro de _connect_oracle — isso é esperado)
grep -n "oracledb.connect(" /home/claudiobezerra/projetos/FB_APU04/erp-bridge-aws/bridge.py
</verification>

<success_criteria>
- python -c "ast.parse(bridge.py)" retorna sem erro
- python bridge.py --help executa corretamente
- grep encontra "def _is_dpy4011" e "def _connect_oracle" definidos antes de processar_sap
- grep encontra "_connect_oracle(" em processar_sap (linha ~628) e processar_servidor (linha ~812)
- grep encontra "_is_dpy4011(exc)" no bloco de query interna de processar_servidor
- grep encontra "oracledb.connect(" APENAS dentro de _connect_oracle (não nos dois sites originais)
- MAX_CONN_RETRIES = 3 e CONN_RETRY_DELAY = 5 definidos como constantes de módulo
</success_criteria>

<output>
After completion, create `.planning/phases/03-estabiliza-o-adicional/03-04-SUMMARY.md`
</output>
