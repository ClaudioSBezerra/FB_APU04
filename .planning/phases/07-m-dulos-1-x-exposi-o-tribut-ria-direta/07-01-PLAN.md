---
phase: 07-modulos-1x-exposicao-tributaria-direta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/handlers/reforma_modulo1.go
  - backend/main.go
autonomous: true
requirements: [RFMB-01, RFMB-02, RFMB-03, RFMB-04]

must_haves:
  truths:
    - "GET /api/reforma/modulo1/creditos retorna JSON com créditos ICMS bloqueados agrupados por CFOP + projeção IBS/CBS, isolado por company_id"
    - "GET /api/reforma/modulo1/ranking retorna ranking de fornecedores Simples Nacional com IBS/CBS estimado e flag Simples"
    - "GET /api/reforma/modulo1/reprecificacao retorna linhas por produto com três caminhos de CST e variação percentual via LATERAL NCM"
    - "GET /api/reforma/modulo1/split retorna float tributário, custo CDI e matriz de sensibilidade DSO × CDI"
    - "Cada endpoint JSON de créditos/ranking/reprecificacao tem endpoint /csv correspondente com Content-Disposition attachment"
    - "Todos os handlers filtram cancelados (EFD: cod_sit NOT IN ('02','03','04','05'); XML: cancelado = 'N') e transferências (cfop tipo != 'T')"
  artifacts:
    - path: "backend/handlers/reforma_modulo1.go"
      provides: "4 handlers JSON + 3 handlers CSV da Reforma Módulos 1.x"
      contains: "func CreditosBloqueadosHandler"
      min_lines: 350
    - path: "backend/main.go"
      provides: "Registro de 7 rotas /api/reforma/modulo1/*"
      contains: "/api/reforma/modulo1/creditos"
  key_links:
    - from: "backend/handlers/reforma_modulo1.go"
      to: "reg_c190 → reg_c100 → import_jobs"
      via: "JOIN chain para company_id (reg_c190 não tem company_id)"
      pattern: "JOIN reg_c100 c100 ON c100.id = c190.id_pai_c100"
    - from: "backend/handlers/reforma_modulo1.go"
      to: "reforma_parametros"
      via: "QueryRow com sql.ErrNoRows guard + defaults"
      pattern: "FROM reforma_parametros WHERE company_id = \\$1"
    - from: "backend/main.go"
      to: "handlers.CreditosBloqueadosHandler"
      via: "AuthMiddleware(handler, \"\")"
      pattern: "handlers.AuthMiddleware\\(handlers\\.CreditosBloqueadosHandler"
---

<objective>
Implementar o backend da Phase 7 — 4 handlers analíticos da Reforma Tributária (Módulos 1.x) em um único arquivo `backend/handlers/reforma_modulo1.go`, mais o registro das rotas em `backend/main.go`.

Purpose: Expor a "exposição tributária direta" da empresa via 4 endpoints REST que consomem a infraestrutura entregue na Phase 6 (`reforma_parametros`, `cst_icms`/`aliq_icms` em `reg_c190`, `ind_final` em `nfe_saidas`, seed de CFOPs de transferência). Toda a lógica fiscal (joins, agregações, projeções IBS/CBS, float tributário) roda no SQL/Go; o frontend (07-02) apenas renderiza.

Output: `reforma_modulo1.go` com 4 handlers JSON (créditos, ranking, reprecificacao, split) + 3 handlers CSV (créditos, ranking, reprecificacao — split não tem CSV por UI-SPEC) + 7 rotas registradas em `main.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-m-dulos-1-x-exposi-o-tribut-ria-direta/07-RESEARCH.md
@.planning/phases/07-m-dulos-1-x-exposi-o-tribut-ria-direta/07-PATTERNS.md

<interfaces>
<!-- Contratos que o executor DEVE replicar. Extraídos do codebase (2026-05-22). -->

Auth + companyID (de backend/handlers/creditos_perdidos.go linhas 93-113):
  claims, ok := r.Context().Value(ClaimsKey).(jwt.MapClaims)  // ok==false → 401
  userID := claims["user_id"].(string)
  companyID, err := GetEffectiveCompanyID(db, userID, r.Header.Get("X-Company-ID"))  // IDOR protection

Helpers já existentes no pacote handlers:
  func jsonErr(w http.ResponseWriter, status int, msg string, extra ...map[string]string)  // ai_query.go:55
  func GetEffectiveCompanyID(db *sql.DB, userID, headerCompanyID string) (string, error)

Struct de parâmetros (de backend/handlers/reforma_config.go linhas 11-19):
  type ReformaParametros struct {
    CompanyID, TargetAno, AliqIBSPct, AliqCBSPct, FatorSimplesPct, TaxaCDIAnualPct, PrazoMedioDias
  }
  Leitura: SELECT company_id, target_ano, aliq_ibs_pct, aliq_cbs_pct, fator_simples_pct,
           taxa_cdi_anual_pct, prazo_medio_dias FROM reforma_parametros WHERE company_id = $1
  Defaults quando sql.ErrNoRows: aliq_ibs=26.5, aliq_cbs=9.9, fator_simples=20.0, taxa_cdi=10.5, prazo_medio=30

Schema verificado (migrations):
  reg_c190(job_id, id_pai_c100, cfop, vl_opr, vl_bc_icms, vl_icms, cst_icms, aliq_icms)  -- NÃO tem company_id
  reg_c100(id, job_id, cod_sit)  -- cod_sit aqui, NÃO em reg_c190
  import_jobs(id, company_id)
  cfop(cfop, tipo)  -- tipo='T' para transferências (migration 089)
  nfe_entradas(id, company_id, cancelado, cfop, tipo_cfop, forn_cnpj, forn_nome, v_nf)  -- cfop NO cabeçalho (migration 066), populado pelo XML
  nfe_saidas(id, company_id, cancelado, cfop, tipo_cfop, v_nf, ind_final)  -- cfop NO cabeçalho
  nfe_entradas_itens(nfe_id, company_id, x_prod, ncm, cfop, cst_icms, v_prod, v_bc_icms, v_icms)
  forn_simples(cnpj VARCHAR(14) PK)  -- 14 dígitos puros, sem pontuação, sem nome
  participants(job_id, cod_part, nome, cnpj VARCHAR(20))
  ncm_cclasstrib_reforma(ncm_digits VARCHAR(10), ibs_reducao_pct, cbs_reducao_pct, cclasstrib)  -- idx em ncm_digits

Registro de rota (de backend/main.go linhas 524-538, padrão /api/reforma/parametros):
  http.HandleFunc("/api/reforma/modulo1/creditos", func(w, r) {
    database := getDB(); if database == nil { jsonServiceUnavailable(w); return }
    handlers.AuthMiddleware(handlers.CreditosBloqueadosHandler(database), "")(w, r)
  })
  // role "" = qualquer usuário autenticado (analytics read-only). NÃO usar "admin".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handlers Módulo 1.1 (créditos bloqueados) e 1.3 (ranking fornecedores) — JSON + CSV</name>
  <files>backend/handlers/reforma_modulo1.go</files>
  <read_first>
    - backend/handlers/creditos_perdidos.go (padrão de structs, multi-handler em arquivo único, auth+companyID, scan de rows, empty-slice guard, leitura de alíquotas)
    - backend/handlers/reforma_config.go (struct ReformaParametros, leitura com sql.ErrNoRows guard)
    - backend/handlers/xml_conciliacao.go (linhas 305-386: padrão CSV — csv.NewWriter, Content-Disposition, header PT-BR, loop com fmt.Sprintf("%.2f", ...))
    - .planning/phases/07-m-dulos-1-x-exposi-o-tribut-ria-direta/07-PATTERNS.md (seções "reforma_modulo1.go" e "Shared Patterns")
  </read_first>
  <action>
    Criar `backend/handlers/reforma_modulo1.go` no package `handlers`, importando database/sql, encoding/csv, encoding/json, fmt, log, net/http, github.com/golang-jwt/jwt/v5.

    Declarar no topo do arquivo as structs de resposta (JSON tags snake_case):
    - `Modulo11Row{ TipoCFOP, CFOP string; VlIcmsTotal, VlOprTotal, IBSEquiv, CBSEquiv float64; QtdRegistros int }`
    - `Modulo11Response{ Rows []Modulo11Row; TotalIcms, TotalIBS, TotalCBS float64 }`
    - `Modulo13Row{ FornCNPJ, FornNome string; QtdNotas int; ValorTotal, IBSPerdidoEst, CBSPerdidoEst float64; Simples bool }`
    - `Modulo13Response{ Rows []Modulo13Row; FatorSimplesPct float64 }`

    Implementar 4 funções com assinatura `func XxxHandler(db *sql.DB) http.HandlerFunc`:

    `CreditosBloqueadosHandler` (GET /api/reforma/modulo1/creditos — RFMB-01):
    - Bloco padrão: Content-Type application/json; se r.Method != GET → jsonErr 405; extrair claims/userID; GetEffectiveCompanyID; em erro → jsonErr 500.
    - Ler aliq_ibs_pct e aliq_cbs_pct de reforma_parametros via db.QueryRow com guard sql.ErrNoRows (defaults 26.5 e 9.9).
    - Query principal (fonte EFD reg_c190): SELECT COALESCE(cf.tipo,'O') AS tipo_cfop, c190.cfop, SUM(c190.vl_icms) AS vl_icms_total, SUM(c190.vl_opr) AS vl_opr_total, COUNT(DISTINCT c100.id) AS qtd_notas FROM reg_c190 c190 JOIN reg_c100 c100 ON c100.id = c190.id_pai_c100 JOIN import_jobs j ON j.id = c100.job_id LEFT JOIN cfop cf ON cf.cfop = c190.cfop WHERE j.company_id = $1 AND c100.cod_sit NOT IN ('02','03','04','05') AND COALESCE(cf.tipo,'O') != 'T' GROUP BY cf.tipo, c190.cfop ORDER BY vl_icms_total DESC. company_id sempre via $1 parametrizado (IDOR).
    - Para cada row: calcular projeção IBS/CBS usando vl_opr_total como base (decisão A1/Open Question 1 do RESEARCH: IBSEquiv = vl_opr_total * aliqIBS/100.0; CBSEquiv = vl_opr_total * aliqCBS/100.0). Acumular TotalIcms, TotalIBS, TotalCBS.
    - Empty-slice guard: se rows == nil → rows = []Modulo11Row{} (nunca retornar null). Encode Modulo11Response.

    `CreditosBloqueadosCSVHandler` (GET /api/reforma/modulo1/creditos/csv): mesma autenticação e mesma query da versão JSON; setar Content-Type "text/csv; charset=utf-8" e Content-Disposition `attachment; filename="creditos-icms-bloqueados.csv"`; csv.NewWriter; header []string{"Tipo CFOP","CFOP","ICMS Bloqueado (R$)","VL Operações (R$)","IBS Equiv. (R$)","CBS Equiv. (R$)","Qtd Registros"}; escrever cada row com fmt.Sprintf("%.2f", ...) para floats e fmt.Sprintf("%d", ...) para int; cw.Flush() + checar cw.Error().

    `RankingFornecedoresHandler` (GET /api/reforma/modulo1/ranking — RFMB-02):
    - Bloco padrão de auth idêntico.
    - Ler fator_simples_pct, aliq_ibs_pct, aliq_cbs_pct de reforma_parametros (guard sql.ErrNoRows, defaults 20.0, 26.5, 9.9).
    - Query principal (fonte XML nfe_entradas — decisão A5 do RESEARCH): SELECT ne.forn_cnpj, COALESCE(ne.forn_nome,'') AS forn_nome, COUNT(*) AS qtd_notas, SUM(ne.v_nf) AS valor_total FROM nfe_entradas ne JOIN forn_simples fs ON fs.cnpj = ne.forn_cnpj LEFT JOIN cfop cf ON cf.cfop = ne.cfop WHERE ne.company_id = $1 AND ne.cancelado = 'N' AND COALESCE(cf.tipo,'O') != 'T' GROUP BY ne.forn_cnpj, ne.forn_nome ORDER BY valor_total DESC LIMIT 100. (Notas: forn_simples.cnpj é 14 dígitos puros; nfe_entradas.forn_cnpj também é VARCHAR(14) puro — JOIN direto, sem REGEXP_REPLACE necessário nesta fonte.)
    - Para cada row: Simples = true (todos vêm do JOIN com forn_simples); IBSPerdidoEst = valor_total * fatorSimples/100.0 * aliqIBS/100.0; CBSPerdidoEst = valor_total * fatorSimples/100.0 * aliqCBS/100.0. (O fator_simples representa a fração de crédito não aproveitável de fornecedor Simples.)
    - Empty-slice guard. Encode Modulo13Response com FatorSimplesPct preenchido.

    `RankingFornecedoresCSVHandler` (GET /api/reforma/modulo1/ranking/csv): mesma query; Content-Disposition `attachment; filename="ranking-fornecedores-simples.csv"`; header []string{"CNPJ","Fornecedor","Qtd Notas","Valor Total (R$)","IBS Estimado (R$)","CBS Estimado (R$)","Simples Nacional"}; coluna Simples sempre "Sim".

    Usar `jsonErr(w, status, msg)` para erros JSON e `http.Error(w, msg, status)` nos handlers CSV (consistente com xml_conciliacao.go). Logar erros de query com log.Printf antes de retornar 500 genérico.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./handlers/</automated>
  </verify>
  <acceptance_criteria>
    - `backend/handlers/reforma_modulo1.go` contém `func CreditosBloqueadosHandler(db *sql.DB) http.HandlerFunc`
    - `reforma_modulo1.go` contém `func CreditosBloqueadosCSVHandler(`, `func RankingFornecedoresHandler(`, `func RankingFornecedoresCSVHandler(`
    - `reforma_modulo1.go` contém a string `JOIN reg_c100 c100 ON c100.id = c190.id_pai_c100` (join chain para company_id)
    - `reforma_modulo1.go` contém `c100.cod_sit NOT IN ('02','03','04','05')` (filtro cancelados EFD)
    - `reforma_modulo1.go` contém `JOIN forn_simples fs ON fs.cnpj = ne.forn_cnpj` e `ne.cancelado = 'N'`
    - `reforma_modulo1.go` contém `COALESCE(cf.tipo,'O') != 'T'` (filtro transferências) em ambas as queries
    - `reforma_modulo1.go` contém `attachment; filename="creditos-icms-bloqueados.csv"` e `attachment; filename="ranking-fornecedores-simples.csv"`
    - Nenhuma interpolação de company_id em SQL: `grep -nE "Sprintf.*company_id|company_id.*%s" backend/handlers/reforma_modulo1.go` retorna 0 linhas
    - `cd backend && go build ./...` sai com código 0
  </acceptance_criteria>
  <done>Os 4 handlers (créditos JSON+CSV, ranking JSON+CSV) compilam; queries usam join chains corretos, filtros de cancelados e transferências, e company_id parametrizado.</done>
</task>

<task type="auto">
  <name>Task 2: Handlers Módulo 1.2 (reprecificação) e 1.4 (split payment) + registro de rotas em main.go</name>
  <files>backend/handlers/reforma_modulo1.go, backend/main.go</files>
  <read_first>
    - backend/handlers/reforma_modulo1.go (estado após Task 1 — structs, helpers, padrão dos handlers já criados)
    - backend/handlers/reforma_config.go (leitura de reforma_parametros completa)
    - backend/main.go (linhas 520-540: bloco /api/reforma/parametros — padrão exato de registro com getDB/jsonServiceUnavailable/AuthMiddleware)
    - .planning/phases/07-m-dulos-1-x-exposi-o-tribut-ria-direta/07-RESEARCH.md (Pattern 5 LATERAL NCM, Pattern 6 três caminhos CST, Pattern 7 split payment, Code Examples)
  </read_first>
  <action>
    Adicionar ao `backend/handlers/reforma_modulo1.go` (mesmo arquivo) as structs e handlers dos Módulos 1.2 e 1.4.

    Structs:
    - `Modulo12Row{ NCM, XProd, CSTICMS, CSTPath string; PrecoAtual, IcmsAtual, IBSProjetado, CBSProjetado, VariacaoPct float64 }`
    - `Modulo12Response{ Rows []Modulo12Row; AliqIBSPct, AliqCBSPct float64 }`
    - `SensibilidadeRow{ DSO int; Custos []float64 }` (um valor por coluna CDI)
    - `Modulo14Response{ FloatTributario, CustoCDI, TotalSaidas, AliqTotal, TaxaCDIAnualPct float64; PrazoMedioDias int; CDIColunas []float64; DSOLinhas []int; Sensibilidade []SensibilidadeRow }`

    `ReprecificacaoHandler` (GET /api/reforma/modulo1/reprecificacao — RFMB-03):
    - Auth padrão; ler aliq_ibs_pct, aliq_cbs_pct (guard sql.ErrNoRows, defaults 26.5, 9.9).
    - Query principal (fonte XML nfe_entradas_itens com LATERAL NCM): SELECT nit.ncm, nit.x_prod, COALESCE(nit.cst_icms,'') AS cst_icms, nit.v_prod, nit.v_icms, ncmr.ibs_reducao_pct, ncmr.cbs_reducao_pct FROM nfe_entradas_itens nit JOIN nfe_entradas ne ON ne.id = nit.nfe_id LEFT JOIN cfop cf ON cf.cfop = nit.cfop LEFT JOIN LATERAL (SELECT ibs_reducao_pct, cbs_reducao_pct, cclasstrib FROM ncm_cclasstrib_reforma WHERE nit.ncm LIKE ncm_digits || '%' ORDER BY length(ncm_digits) DESC LIMIT 1) ncmr ON true WHERE nit.company_id = $1 AND ne.cancelado = 'N' AND COALESCE(cf.tipo,'O') != 'T' ORDER BY nit.v_prod DESC LIMIT 500. (CFOP do item via nfe_entradas_itens.cfop — para reprecificação por produto sempre usar a tabela de itens.)
    - Classificar CST em 3 caminhos (decisão A2/Open Question 3 do RESEARCH — interpretação documentada, não validada com especialista fiscal): CSTPath = "normal" se cst_icms == "00"; "st" se cst_icms ∈ {"10","30","60","70"}; "base_reduzida" se cst_icms ∈ {"20"}; default "outro". Tratar NULL/"" como "normal" via COALESCE.
    - Cálculo de reprecificação (interpretação documentada): IBSProjetado = v_prod * aliqIBS/100.0 * (1 - COALESCE(ibs_reducao_pct,0)/100.0); CBSProjetado = v_prod * aliqCBS/100.0 * (1 - COALESCE(cbs_reducao_pct,0)/100.0); IcmsAtual = v_icms; VariacaoPct = se v_prod>0: (IBSProjetado+CBSProjetado-IcmsAtual)/v_prod*100.0, senão 0.
    - Empty-slice guard. Encode Modulo12Response.

    `ReprecificacaoCSVHandler` (GET /api/reforma/modulo1/reprecificacao/csv): mesma query; Content-Disposition `attachment; filename="reprecificacao-produtos.csv"`; header []string{"NCM","Descrição Produto","CST ICMS","Preço Atual (R$)","ICMS Atual (R$)","IBS Projetado (R$)","CBS Projetado (R$)","Variação (%)"}.

    `SplitPaymentHandler` (GET /api/reforma/modulo1/split — RFMB-04, SEM CSV):
    - Auth padrão; ler aliq_ibs_pct, aliq_cbs_pct, taxa_cdi_anual_pct, prazo_medio_dias (guard sql.ErrNoRows, defaults 26.5, 9.9, 10.5, 30).
    - Total de saídas (fonte XML nfe_saidas): SELECT COALESCE(SUM(ns.v_nf),0) FROM nfe_saidas ns LEFT JOIN cfop cf ON cf.cfop = ns.cfop WHERE ns.company_id = $1 AND ns.cancelado = 'N' AND COALESCE(cf.tipo,'O') != 'T'. Scan em totalSaidas.
    - Cálculo (Pattern 7 do RESEARCH, interpretação A3 documentada): aliqTotal = aliqIBS + aliqCBS; FloatTributario = aliqTotal/100.0 * totalSaidas * float64(prazoMedio)/365.0; CustoCDI = FloatTributario * taxaCDI/100.0.
    - Gerar matriz de sensibilidade no Go: DSOLinhas = []int{15,30,45,60,90}; CDIColunas = []float64{8,10,12,14}; para cada dso e cada cdi: custo = aliqTotal/100.0 * totalSaidas * float64(dso)/365.0 * cdi/100.0. Montar []SensibilidadeRow.
    - Encode Modulo14Response (incluir TaxaCDIAnualPct e PrazoMedioDias atuais para o frontend destacar a célula corrente).

    Em `backend/main.go`, após o bloco `/api/reforma/parametros` (que termina ~linha 539), adicionar um bloco comentado `// ── Reforma Tributária — Módulos 1.x (Phase 7) ──` registrando 7 rotas usando o padrão exato do bloco existente (getDB → jsonServiceUnavailable se nil → handlers.AuthMiddleware(handler, "")):
    - /api/reforma/modulo1/creditos → CreditosBloqueadosHandler
    - /api/reforma/modulo1/creditos/csv → CreditosBloqueadosCSVHandler
    - /api/reforma/modulo1/ranking → RankingFornecedoresHandler
    - /api/reforma/modulo1/ranking/csv → RankingFornecedoresCSVHandler
    - /api/reforma/modulo1/reprecificacao → ReprecificacaoHandler
    - /api/reforma/modulo1/reprecificacao/csv → ReprecificacaoCSVHandler
    - /api/reforma/modulo1/split → SplitPaymentHandler
    Todas com role "" (AuthMiddleware(handler, "")). NÃO registrar rota /split/csv (split payment não tem CSV por UI-SPEC).
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./handlers/ && grep -c "api/reforma/modulo1/" main.go</automated>
  </verify>
  <acceptance_criteria>
    - `reforma_modulo1.go` contém `func ReprecificacaoHandler(`, `func ReprecificacaoCSVHandler(`, `func SplitPaymentHandler(`
    - `reforma_modulo1.go` contém `LEFT JOIN LATERAL` e `ORDER BY length(ncm_digits) DESC` (LATERAL prefix-match NCM)
    - `reforma_modulo1.go` contém a classificação dos três caminhos CST (strings "normal", "st", "base_reduzida")
    - `reforma_modulo1.go` contém `[]int{15, 30, 45, 60, 90}` e `[]float64{8, 10, 12, 14}` (matriz de sensibilidade)
    - `reforma_modulo1.go` NÃO contém `SplitPaymentCSVHandler` (split não tem CSV): `grep -c "SplitPaymentCSVHandler" backend/handlers/reforma_modulo1.go` retorna 0
    - `backend/main.go` contém `/api/reforma/modulo1/creditos`, `/api/reforma/modulo1/ranking`, `/api/reforma/modulo1/reprecificacao`, `/api/reforma/modulo1/split`
    - `grep -c "api/reforma/modulo1/" backend/main.go` retorna 7 (4 JSON + 3 CSV)
    - `backend/main.go` registra split SEM /csv: `grep -c "modulo1/split/csv" backend/main.go` retorna 0
    - Todas as rotas usam role "": `grep -c "AuthMiddleware(handlers\..*Handler(database), \"admin\")" backend/main.go` não aumenta para rotas modulo1
    - `cd backend && go build ./...` sai com código 0; `go vet ./handlers/` sem erros
  </acceptance_criteria>
  <done>Os handlers de reprecificação (JSON+CSV) e split payment (JSON) compilam; as 7 rotas estão registradas em main.go com role "" e o build/vet passa.</done>
</task>

<task type="auto">
  <name>Task 3: Testes de criação dos handlers Módulo 1.x (guard tests sem DB)</name>
  <files>backend/handlers/reforma_modulo1_test.go</files>
  <read_first>
    - backend/handlers/handlers_guards13_test.go (padrão exato de guard test: HandlerName(nil) retorna non-nil; method-not-allowed retorna 405; httptest.NewRequest/NewRecorder)
    - backend/handlers/reforma_modulo1.go (assinaturas dos 7 handlers criados nas Tasks 1-2)
  </read_first>
  <action>
    Criar `backend/handlers/reforma_modulo1_test.go` no package `handlers`, importando net/http, net/http/httptest, testing.

    Para cada um dos 7 handlers, escrever um teste de criação no padrão de handlers_guards13_test.go: `func TestCreditosBloqueadosHandler_Creation(t *testing.T) { h := CreditosBloqueadosHandler(nil); if h == nil { t.Error("expected non-nil handler") } }`. Repetir para CreditosBloqueadosCSVHandler, RankingFornecedoresHandler, RankingFornecedoresCSVHandler, ReprecificacaoHandler, ReprecificacaoCSVHandler, SplitPaymentHandler.

    Adicionar um teste de method-not-allowed para um handler JSON sem tocar DB: `func TestCreditosBloqueadosHandler_MethodNotAllowed(t *testing.T)` — criar handler com nil, fazer httptest.NewRequest(http.MethodPost, "/api/reforma/modulo1/creditos", nil) + NewRecorder, chamar handler(rr, req), assertar rr.Code == http.StatusMethodNotAllowed. (POST é rejeitado antes de qualquer acesso ao DB, então nil db é seguro.)

    Não escrever testes que executem queries (exigiriam DB real). Os guard tests cobrem a criação e a branch de método inválido, consistente com o padrão do codebase.
  </action>
  <verify>
    <automated>cd backend && go test ./handlers/ -run "TestCreditosBloqueadosHandler|TestRankingFornecedoresHandler|TestReprecificacaoHandler|TestSplitPaymentHandler" -v</automated>
  </verify>
  <acceptance_criteria>
    - `backend/handlers/reforma_modulo1_test.go` contém `func TestCreditosBloqueadosHandler_Creation(`
    - Arquivo de teste contém testes de criação para os 7 handlers (creditos+csv, ranking+csv, reprecificacao+csv, split)
    - Arquivo contém `func TestCreditosBloqueadosHandler_MethodNotAllowed(` assertando `http.StatusMethodNotAllowed`
    - `cd backend && go test ./handlers/ -run "TestCreditosBloqueadosHandler|TestRankingFornecedoresHandler|TestReprecificacaoHandler|TestSplitPaymentHandler"` sai com código 0 (todos PASS)
  </acceptance_criteria>
  <done>Os guard tests dos 7 handlers passam; cobertura confirma que cada handler é criável e rejeita método inválido sem tocar o banco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → API (Go handler) | JWT do usuário atravessa; X-Company-ID pode ser manipulado pelo cliente |
| API → PostgreSQL | queries com company_id derivado do JWT, nunca do body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | Todos os 7 handlers de reforma_modulo1.go | mitigate | company_id obtido via `GetEffectiveCompanyID(db, userID, X-Company-ID)` — IDOR protection já valida que o usuário pertence à empresa; nunca usar company_id do body/query. company_id sempre via `$1` parametrizado. |
| T-07-02 | Tampering (SQL injection) | Queries SQL dos handlers | mitigate | company_id e demais filtros via placeholders `$N`; `cf.tipo` lido do banco (não interpolado); nenhum nome de tabela ou valor concatenado em string SQL. |
| T-07-03 | Information Disclosure | CSV handlers (creditos/ranking/reprecificacao) | mitigate | Handlers CSV extraem company_id via JWT idêntico aos handlers JSON — mesma proteção de isolamento; impossível exportar dados de outra empresa. |
| T-07-04 | Information Disclosure | Endpoints analytics (dados fiscais sensíveis) | mitigate | Todos os handlers protegidos por `AuthMiddleware(handler, "")` — exigem JWT válido; usuário não autenticado recebe 401. |
| T-07-05 | Denial of Service | ReprecificacaoHandler (LATERAL join sem limite agressivo) | accept | Query limitada a `LIMIT 500` linhas; LATERAL usa índice `idx_ncm_cclasstrib_reforma_digits`; volume controlado por empresa. Baixo risco para usuário interno autenticado. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova instalada nesta fase (RESEARCH "Nenhuma dependência nova necessária") — sem superfície de supply chain. |
</threat_model>

<verification>
- `cd backend && go build ./...` compila sem erros
- `cd backend && go vet ./handlers/` sem warnings
- `cd backend && go test ./handlers/ -run "TestCreditosBloqueadosHandler|TestRankingFornecedoresHandler|TestReprecificacaoHandler|TestSplitPaymentHandler"` todos PASS
- `grep -c "api/reforma/modulo1/" backend/main.go` retorna 7
- Nenhuma interpolação de company_id: `grep -nE "Sprintf.*company_id" backend/handlers/reforma_modulo1.go` retorna 0 linhas
</verification>

<success_criteria>
- 4 handlers JSON (creditos, ranking, reprecificacao, split) + 3 handlers CSV implementados em reforma_modulo1.go
- Join chain reg_c190 → reg_c100 → import_jobs para company_id (Módulo 1.1)
- Filtros de cancelados (EFD cod_sit + XML cancelado) e transferências (cfop tipo != 'T') em todos os handlers
- LATERAL NCM longest-prefix-wins no Módulo 1.2
- Matriz de sensibilidade DSO × CDI gerada no Go no Módulo 1.4
- 7 rotas registradas em main.go com role "" (read-only analytics)
- Guard tests passam para os 7 handlers
- company_id sempre parametrizado (IDOR protection)
</success_criteria>

<output>
Create `.planning/phases/07-m-dulos-1-x-exposi-o-tribut-ria-direta/07-01-SUMMARY.md` when done
</output>
