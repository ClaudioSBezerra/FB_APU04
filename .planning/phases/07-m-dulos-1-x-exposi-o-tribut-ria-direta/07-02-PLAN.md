---
phase: 07-modulos-1x-exposicao-tributaria-direta
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/pages/Reforma11CreditosBloqueados.tsx
  - frontend/src/pages/Reforma13RankingFornecedores.tsx
  - frontend/src/pages/Reforma12Reprecificacao.tsx
  - frontend/src/pages/Reforma14SplitPayment.tsx
  - frontend/src/lib/navigation.ts
  - frontend/src/App.tsx
autonomous: false
requirements: [RFMB-01, RFMB-02, RFMB-03, RFMB-04]

must_haves:
  truths:
    - "Usuário navega para a aba 'Créditos IBS/CBS' e vê gráfico de barras + tabela de créditos ICMS bloqueados por CFOP"
    - "Usuário navega para 'Ranking Fornecedores' e vê disclaimer regulatório + ranking com badge Simples Nacional"
    - "Usuário navega para 'Reprecificação' e vê tabela por produto com badge de caminho CST e variação percentual colorida"
    - "Usuário navega para 'Split Payment' e vê 2 KPI cards (float, custo CDI) + matriz de sensibilidade DSO × CDI"
    - "Botão 'Exportar CSV' baixa o arquivo nos módulos 1.1, 1.2 e 1.3 (1.4 não tem CSV)"
    - "As 4 tabs Phase 7 estão ativas; as 4 tabs Phase 8 (cfop, ncm, uf-destino, b2b-b2c) permanecem disabled"
  artifacts:
    - path: "frontend/src/pages/Reforma11CreditosBloqueados.tsx"
      provides: "Página Módulo 1.1 — KPI cards + bar chart + tabela + CSV"
      contains: "/api/reforma/modulo1/creditos"
      min_lines: 100
    - path: "frontend/src/pages/Reforma13RankingFornecedores.tsx"
      provides: "Página Módulo 1.3 — disclaimer + bar chart + tabela ranking"
      contains: "/api/reforma/modulo1/ranking"
    - path: "frontend/src/pages/Reforma12Reprecificacao.tsx"
      provides: "Página Módulo 1.2 — filtro CST + tabela reprecificação"
      contains: "/api/reforma/modulo1/reprecificacao"
    - path: "frontend/src/pages/Reforma14SplitPayment.tsx"
      provides: "Página Módulo 1.4 — KPI cards + matriz sensibilidade"
      contains: "/api/reforma/modulo1/split"
    - path: "frontend/src/App.tsx"
      provides: "4 rotas dos módulos 1.x"
      contains: "/reforma/creditos"
    - path: "frontend/src/lib/navigation.ts"
      provides: "4 tabs Phase 7 ativadas"
  key_links:
    - from: "frontend/src/pages/Reforma11CreditosBloqueados.tsx"
      to: "/api/reforma/modulo1/creditos"
      via: "useQuery + fetch"
      pattern: "fetch\\('/api/reforma/modulo1/creditos'\\)"
    - from: "frontend/src/App.tsx"
      to: "Reforma11CreditosBloqueados"
      via: "<Route path=/reforma/creditos>"
      pattern: "path=\"/reforma/creditos\""
    - from: "frontend/src/lib/navigation.ts"
      to: "/reforma/creditos"
      via: "tab sem disabled"
      pattern: "path: '/reforma/creditos'"
---

<objective>
Implementar o frontend da Phase 7 — 4 páginas React (Módulos 1.1, 1.2, 1.3, 1.4) que consomem os endpoints criados em 07-01, mais a ativação das 4 tabs em `navigation.ts` e o registro das 4 rotas em `App.tsx`.

Purpose: Tornar visível ao usuário a "exposição tributária direta" — créditos ICMS bloqueados, ranking de fornecedores Simples Nacional, reprecificação por produto e impacto de capital de giro do split payment. Cada página segue o contrato visual de 07-UI-SPEC.md (layout, cores, tipografia, estados, disclaimers).

Output: 4 arquivos `.tsx` em frontend/src/pages/ + navigation.ts (remover disabled de 4 tabs) + App.tsx (4 imports + 4 rotas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-m-dulos-1-x-exposi-o-tribut-ria-direta/07-PATTERNS.md
@.planning/phases/07-m-dulos-1-x-exposi-o-tribut-ria-direta/07-UI-SPEC.md
@.planning/phases/07-m-dulos-1-x-exposi-o-tribut-ria-direta/07-01-SUMMARY.md

<interfaces>
<!-- Contratos da API (de 07-01) que o frontend consome. -->

Endpoints (todos GET, JWT injetado automaticamente pelo AuthContext via window.fetch):
  GET /api/reforma/modulo1/creditos        → Modulo11Response
  GET /api/reforma/modulo1/creditos/csv    → text/csv attachment
  GET /api/reforma/modulo1/ranking         → Modulo13Response
  GET /api/reforma/modulo1/ranking/csv     → text/csv attachment
  GET /api/reforma/modulo1/reprecificacao  → Modulo12Response
  GET /api/reforma/modulo1/reprecificacao/csv → text/csv attachment
  GET /api/reforma/modulo1/split           → Modulo14Response  (SEM /csv)

Tipos de resposta (JSON snake_case — declarar interface TS em cada página):
  Modulo11Response { rows: Modulo11Row[]; total_icms, total_ibs, total_cbs: number }
  Modulo11Row { tipo_cfop, cfop: string; vl_icms_total, vl_opr_total, ibs_equiv, cbs_equiv: number; qtd_registros: number }
  Modulo13Response { rows: Modulo13Row[]; fator_simples_pct: number }
  Modulo13Row { forn_cnpj, forn_nome: string; qtd_notas: number; valor_total, ibs_perdido_est, cbs_perdido_est: number; simples: boolean }
  Modulo12Response { rows: Modulo12Row[]; aliq_ibs_pct, aliq_cbs_pct: number }
  Modulo12Row { ncm, x_prod, cst_icms, cst_path: string; preco_atual, icms_atual, ibs_projetado, cbs_projetado, variacao_pct: number }
  Modulo14Response { float_tributario, custo_cdi, total_saidas, aliq_total, taxa_cdi_anual_pct: number; prazo_medio_dias: number; cdi_colunas: number[]; dso_linhas: number[]; sensibilidade: { dso: number; custos: number[] }[] }

Padrões frontend (de frontend/src/pages/ConciliacaoBridgeXML.tsx — analog exato):
  - fmtBRL(v) = v.toLocaleString('pt-BR', {style:'currency',currency:'BRL'}); null → '—'
  - fmtCNPJ(v) — formata 14 dígitos como NN.NNN.NNN/NNNN-NN
  - useQuery({ queryKey, queryFn: async () => { const res = await fetch(url); if(!res.ok) throw...; return res.json() }})
  - 4-state machine: isLoading (Skeleton x6) | isError (Alert destructive) | empty (texto centrado) | data (tabela/chart)
  - CSV download: setDownloadingCSV(true); fetch(url+'/csv'); res.blob(); createObjectURL; <a download>; revokeObjectURL
  - recharts Tooltip conflita com shadcn Tooltip → `import { Tooltip as UITooltip } from '@/components/ui/tooltip'`

Pontos de inserção (verificados 2026-05-22):
  navigation.ts linhas 49-52: 4 tabs Phase 7 com `disabled: true` (REMOVER); linhas 53-56: 4 tabs Phase 8 (MANTER disabled)
  App.tsx linha 31: import ReformaParametros; linhas 174-176: bloco "Análise Reforma Tributária" (inserir 4 rotas após linha 176)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Páginas Módulo 1.1 (créditos bloqueados) e 1.3 (ranking fornecedores)</name>
  <files>frontend/src/pages/Reforma11CreditosBloqueados.tsx, frontend/src/pages/Reforma13RankingFornecedores.tsx</files>
  <read_first>
    - frontend/src/pages/ConciliacaoBridgeXML.tsx (analog exato — imports, fmtBRL/fmtCNPJ, useQuery, BarChart, Table, 4-state machine, downloadingCSV/handleExportCSV, Badge)
    - .planning/phases/07-m-dulos-1-x-exposi-o-tribut-ria-direta/07-UI-SPEC.md (Page Layout Contract, Módulo 1.1 e 1.3 Interaction Contracts, Copywriting Contract, State Machine, Accessibility)
    - .planning/phases/07-m-dulos-1-x-exposi-o-tribut-ria-direta/07-PATTERNS.md (seções Reforma11 e Reforma13)
    - frontend/src/pages/ReformaParametros.tsx (padrão de KPI cards e layout de página da reforma)
  </read_first>
  <action>
    Criar `Reforma11CreditosBloqueados.tsx` (Módulo 1.1 — RFMB-01):
    - Imports: useState (react); useQuery (@tanstack/react-query); Card/CardHeader/CardTitle/CardContent, Badge, Button, Separator, Alert/AlertDescription, Skeleton, Table/TableBody/TableCell/TableHead/TableHeader/TableRow (@/components/ui/*); BarChart/Bar/XAxis/YAxis/CartesianGrid/Tooltip/Legend/ResponsiveContainer (recharts); Download (lucide-react). Aliasar shadcn Tooltip como UITooltip apenas se usar tooltip de UI.
    - Declarar interfaces Modulo11Row e Modulo11Response conforme <interfaces>.
    - Copiar helpers fmtBRL e fmtCNPJ verbatim de ConciliacaoBridgeXML.tsx.
    - useQuery<Modulo11Response> com queryKey ['reforma/modulo1/creditos'] e fetch('/api/reforma/modulo1/creditos').
    - Estado downloadingCSV + handleExportCSV apontando para '/api/reforma/modulo1/creditos/csv', filename 'creditos-icms-bloqueados.csv'.
    - Layout (Page Layout Contract): div space-y-6 p-6 → header row (h1 text-xl font-semibold "Créditos ICMS Bloqueados — Módulo 1.1"; p text-sm text-muted-foreground "Créditos ICMS não aproveitáveis na transição + equivalente IBS/CBS recuperável"; Button "Exportar CSV" com Download icon, variant default quando há dados/outline quando vazio, disabled quando downloadingCSV ou sem dados, aria-label "Exportar tabela de créditos como CSV") → Separator.
    - KPI summary: grid grid-cols-3 gap-4 com 3 cards (Total ICMS Bloqueado=total_icms, Total Equiv. IBS=total_ibs, Total Equiv. CBS=total_cbs), valor em text-2xl font-semibold via fmtBRL, label text-sm text-muted-foreground.
    - Card principal "Créditos por CFOP": 4-state machine (isLoading → 6 Skeleton h-8 w-full; isError → Alert variant destructive "Erro ao carregar dados. Verifique sua conexão e tente novamente."; empty rows.length===0 → texto centrado "Nenhum crédito ICMS encontrado para o período selecionado."; data → BarChart + Table).
    - BarChart: ResponsiveContainer height 280 com aria-label "Gráfico de créditos ICMS bloqueados por CFOP"; XAxis dataKey="cfop"; YAxis tickFormatter fmtBRL; recharts Tooltip formatter fmtBRL; Legend; Bar dataKey="vl_icms_total" name="ICMS Bloqueado" fill="var(--pis-cofins)"; Bar dataKey="ibs_equiv" name="Equiv. IBS" fill="var(--ibs-cbs)".
    - Table colunas: Tipo CFOP | CFOP | ICMS Bloqueado (R$) | Valor Operação (R$) | IBS Equiv. (R$) | CBS Equiv. (R$) | Qtd Registros. Headers text-xs font-semibold uppercase tracking-wide (numéricos text-right). Células numéricas text-xs font-mono text-right via fmtBRL.
    - export default function.

    Criar `Reforma13RankingFornecedores.tsx` (Módulo 1.3 — RFMB-02):
    - Mesmos imports base + AlertTriangle (lucide-react) para o disclaimer.
    - Interfaces Modulo13Row e Modulo13Response. Helpers fmtBRL/fmtCNPJ.
    - useQuery queryKey ['reforma/modulo1/ranking'], fetch('/api/reforma/modulo1/ranking'). CSV → '/api/reforma/modulo1/ranking/csv', filename 'ranking-fornecedores-simples.csv'.
    - Header: h1 "Ranking de Fornecedores — Módulo 1.3"; subtitle "Fornecedores Simples Nacional com crédito IBS/CBS estimado por empresa".
    - Disclaimer banner (RFMB-02 obrigatório): Alert variant default com className "border-warning text-warning-foreground bg-warning/10", AlertTriangle icon, AlertDescription "Valores estimados. A alíquota definitiva do Fator Simples Nacional não foi publicada pelo CG-IBS. Use como referência indicativa." — posicionado após Separator, antes do Card.
    - Card com bar chart top 10 (ResponsiveContainer height 220, aria-label "Ranking de fornecedores Simples Nacional"; data = rows.slice(0,10); Bar dataKey="ibs_perdido_est" name="IBS+CBS Estimado" fill="var(--ibs-cbs)") + Table.
    - Table colunas: # | CNPJ | Nome Fornecedor | Qtd Notas | Valor Total (R$) | IBS Estimado (R$) | CBS Estimado (R$) | Simples Nacional. CNPJ via fmtCNPJ. Coluna Simples Nacional: Badge variant outline className "text-xs px-1.5 py-0 bg-yellow-50 text-yellow-700 border-yellow-200" texto "Simples" quando row.simples, célula vazia caso contrário.
    - Empty state: "Nenhum fornecedor Simples Nacional encontrado. Verifique se a tabela forn_simples está populada."
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "Reforma11|Reforma13" || echo "no type errors in module pages"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/Reforma11CreditosBloqueados.tsx` contém `fetch('/api/reforma/modulo1/creditos')` e `'creditos-icms-bloqueados.csv'`
    - Reforma11 contém `fill="var(--pis-cofins)"` e `fill="var(--ibs-cbs)"` (cores do BarChart por UI-SPEC)
    - Reforma11 contém o empty-state copy exato "Nenhum crédito ICMS encontrado para o período selecionado."
    - `frontend/src/pages/Reforma13RankingFornecedores.tsx` contém `fetch('/api/reforma/modulo1/ranking')` e `'ranking-fornecedores-simples.csv'`
    - Reforma13 contém o disclaimer regulatório exato "A alíquota definitiva do Fator Simples Nacional não foi publicada pelo CG-IBS"
    - Reforma13 contém Badge com texto "Simples"
    - Ambos os arquivos contêm o tratamento de 4 estados (isLoading, isError, empty, data) e o botão "Exportar CSV"
    - `cd frontend && npx tsc --noEmit` não reporta erros de tipo nestes 2 arquivos
  </acceptance_criteria>
  <done>As páginas 1.1 e 1.3 renderizam KPI/chart/tabela, consomem os endpoints corretos, exibem disclaimer (1.3) e exportam CSV; sem erros de tipo.</done>
</task>

<task type="auto">
  <name>Task 2: Páginas Módulo 1.2 (reprecificação) e 1.4 (split payment) + ativação de tabs e rotas</name>
  <files>frontend/src/pages/Reforma12Reprecificacao.tsx, frontend/src/pages/Reforma14SplitPayment.tsx, frontend/src/lib/navigation.ts, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/pages/ConciliacaoBridgeXML.tsx (filtro Select, Table, helpers — analog para 1.2)
    - frontend/src/pages/ReformaParametros.tsx (KPI card layout — analog para 1.4)
    - .planning/phases/07-m-dulos-1-x-exposi-o-tribut-ria-direta/07-UI-SPEC.md (Módulo 1.2 e 1.4 Interaction Contracts, Copywriting, State Machine, Accessibility)
    - .planning/phases/07-m-dulos-1-x-exposi-o-tribut-ria-direta/07-PATTERNS.md (seções Reforma12, Reforma14, navigation.ts, App.tsx)
    - frontend/src/lib/navigation.ts (estado atual — linhas 45-56, bloco reforma)
    - frontend/src/App.tsx (estado atual — linha 31 import, linhas 174-176 bloco reforma)
  </read_first>
  <action>
    Criar `Reforma12Reprecificacao.tsx` (Módulo 1.2 — RFMB-03):
    - Imports: useState; useQuery; Card/etc; Badge; Button; Separator; Alert/AlertDescription; Skeleton; Table/etc; Select/SelectContent/SelectItem/SelectTrigger/SelectValue (@/components/ui/select); Download (lucide-react). Sem BarChart (table-only por UI-SPEC).
    - Interfaces Modulo12Row e Modulo12Response. Helper fmtBRL.
    - Helper CSTBadge({cst}): se !cst → span "—" muted; isNormal = cst==='00' → Badge variant secondary; isST = ['10','30','60','70'].includes(cst) → variant outline; senão variant default; font-mono text-xs.
    - Helper fmtVariacao(v): null → "—" muted; positivo → text-green-600 com prefixo "+"; zero → text-muted-foreground; negativo → text-red-600; sempre toFixed(2)+"%" font-mono text-xs.
    - Estado cstFilter (default 'todos'); Select de CST: Todos / Normal (00) / Substituição Tributária (value 'st') / Base Reduzida (value 'base_reduzida'). Filtrar rows no cliente por cst_path quando cstFilter != 'todos'.
    - useQuery queryKey ['reforma/modulo1/reprecificacao'], fetch('/api/reforma/modulo1/reprecificacao'). CSV → '/api/reforma/modulo1/reprecificacao/csv', filename 'reprecificacao-produtos.csv'.
    - Header: h1 "Reprecificação de Produtos — Módulo 1.2"; subtitle "Impacto da troca ICMS por dentro por IBS/CBS por fora por produto".
    - Card: filtro row (flex gap-4) com o Select + Separator; Table colunas: NCM | Descrição Produto | CST ICMS (CSTBadge) | Preço Atual (R$) | ICMS Atual (R$) | IBS Projetado (R$) | CBS Projetado (R$) | Variação (%) (fmtVariacao). NCM font-mono. Linhas com cst_icms NULL/"" exibem badge "—" e colunas IBS/CBS "—" — NÃO ocultar (representam dados históricos sem CST).
    - 4-state machine. Empty: "Nenhum produto encontrado. Verifique se há notas fiscais de entrada importadas."

    Criar `Reforma14SplitPayment.tsx` (Módulo 1.4 — RFMB-04, SEM CSV):
    - Imports: useQuery; Card/etc; Separator; Alert/AlertDescription; Skeleton; Table/etc; Info (lucide-react). Sem Button de CSV, sem BarChart.
    - Interface Modulo14Response (incluir sensibilidade: {dso:number; custos:number[]}[]). Helper fmtBRL.
    - useQuery queryKey ['reforma/modulo1/split'], fetch('/api/reforma/modulo1/split').
    - Header: h1 "Split Payment e Capital de Giro — Módulo 1.4"; subtitle "Float tributário e custo de reposição CDI com tabela de sensibilidade DSO × CDI". (Sem botão Exportar CSV.)
    - 2 KPI cards grid grid-cols-2 gap-4: Card 1 "Float Tributário" valor fmtBRL(float_tributario) text-2xl font-semibold + subtitle text-sm text-muted-foreground "IBS+CBS × Saídas × {prazo_medio_dias} dias / 365"; Card 2 "Custo CDI Estimado (R$/ano)" valor fmtBRL(custo_cdi) + subtitle "Float × {taxa_cdi_anual_pct}% CDI".
    - Disclaimer banner Alert variant default com Info icon: "Split payment entra em vigor gradualmente entre 2026 e 2033 conforme cronograma da Reforma. Os valores simulam o impacto no regime de transição plena."
    - Card "Tabela de Sensibilidade — DSO × CDI": Table com header DSO (dias) + uma coluna por cdi_colunas (rótulo "CDI {cdi}%"); body itera sensibilidade: primeira célula = dso font-mono; demais células = custos[i] via fmtBRL font-mono text-xs text-right. Destacar a célula corrente (dso === prazo_medio_dias && cdi_colunas[i] === taxa_cdi_anual_pct) com className "bg-primary/10 font-semibold" e aria-current="true".
    - 4-state machine. Empty (sem saídas): "Nenhuma nota fiscal de saída encontrada. Os módulos de split payment requerem dados de NF-e de saída importados via XML."

    Modificar `frontend/src/lib/navigation.ts`: nas linhas 49-52, remover `disabled: true` das 4 tabs Phase 7 — '/reforma/creditos', '/reforma/reprecificacao', '/reforma/ranking', '/reforma/split-payment'. MANTER `disabled: true` nas 4 tabs Phase 8 (linhas 53-56: '/reforma/cfop', '/reforma/ncm', '/reforma/uf-destino', '/reforma/b2b-b2c'). Não alterar a tab 'Parâmetros' (já ativa).

    Modificar `frontend/src/App.tsx`: após o import ReformaParametros (linha 31), adicionar 4 imports default: Reforma11CreditosBloqueados, Reforma12Reprecificacao, Reforma13RankingFornecedores, Reforma14SplitPayment (de './pages/...'). No bloco "Análise Reforma Tributária" (após linha 176), adicionar 4 <Route>: path="/reforma/creditos" element Reforma11CreditosBloqueados; path="/reforma/reprecificacao" element Reforma12Reprecificacao; path="/reforma/ranking" element Reforma13RankingFornecedores; path="/reforma/split-payment" element Reforma14SplitPayment.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/navigation.test.ts && npx tsc --noEmit 2>&1 | grep -E "Reforma1[24]|navigation|App.tsx" || echo "no type errors in changed files"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/Reforma12Reprecificacao.tsx` contém `fetch('/api/reforma/modulo1/reprecificacao')` e `'reprecificacao-produtos.csv'`
    - Reforma12 contém o filtro Select com itens value 'st' e 'base_reduzida' e o helper de variação colorida (text-green-600 e text-red-600)
    - `frontend/src/pages/Reforma14SplitPayment.tsx` contém `fetch('/api/reforma/modulo1/split')`
    - Reforma14 NÃO contém referência a CSV: `grep -c "csv" frontend/src/pages/Reforma14SplitPayment.tsx` retorna 0
    - Reforma14 contém `bg-primary/10` e `aria-current` (destaque da célula corrente da matriz)
    - Reforma14 contém o disclaimer "Split payment entra em vigor gradualmente entre 2026 e 2033"
    - `frontend/src/lib/navigation.ts`: as 4 tabs Phase 7 não têm `disabled: true`; verificação — `grep -c "disabled: true" frontend/src/lib/navigation.ts` reflete remoção de exatamente 4 entradas do bloco reforma (4 Phase 8 permanecem)
    - navigation.ts ainda contém `path: '/reforma/cfop'` COM `disabled: true` na mesma linha (Phase 8 mantida)
    - `frontend/src/App.tsx` contém `path="/reforma/creditos"`, `path="/reforma/reprecificacao"`, `path="/reforma/ranking"`, `path="/reforma/split-payment"` e os 4 imports
    - `cd frontend && npx vitest run src/lib/navigation.test.ts` sai com código 0
    - `cd frontend && npx tsc --noEmit` não reporta erros de tipo nos arquivos alterados
  </acceptance_criteria>
  <done>As páginas 1.2 e 1.4 renderizam corretamente; as 4 tabs Phase 7 estão ativas e as 4 Phase 8 permanecem disabled; as 4 rotas estão registradas; testes de navegação e tipos passam.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual das 4 páginas dos Módulos 1.x</name>
  <what-built>
    4 páginas dos Módulos 1.x da Reforma Tributária, ativadas no menu e roteadas:
    - Módulo 1.1 Créditos ICMS Bloqueados (KPI + bar chart + tabela + CSV)
    - Módulo 1.3 Ranking Fornecedores (disclaimer + bar chart + tabela + badge Simples + CSV)
    - Módulo 1.2 Reprecificação (filtro CST + tabela + variação colorida + CSV)
    - Módulo 1.4 Split Payment (2 KPI cards + matriz de sensibilidade DSO × CDI, sem CSV)
  </what-built>
  <action>
    Tarefa de verificação humana — não há código a escrever. O executor garante que o frontend está rodando (cd frontend && npm run dev) e o backend acessível, então pausa para o usuário inspecionar as 4 páginas conforme os passos em <how-to-verify>.
  </action>
  <how-to-verify>
    1. Rodar o frontend localmente (`cd frontend && npm run dev`) e o backend (ou usar o ambiente já provisionado).
    2. Logar e abrir o módulo "Análise Reforma Tributária" no menu lateral.
    3. Confirmar que aparecem as tabs: Parâmetros, Créditos IBS/CBS, Reprecificação, Ranking Fornecedores, Split Payment (ativas) e que Análise CFOP / Análise NCM / UF Destino / B2B vs B2C continuam desabilitadas (cinza, não clicáveis).
    4. Abrir cada uma das 4 tabs Phase 7:
       - 1.1: gráfico de barras (ICMS Bloqueado vs Equiv. IBS) + tabela por CFOP + 3 KPI cards no topo. Clicar "Exportar CSV" → baixa creditos-icms-bloqueados.csv.
       - 1.3: banner amarelo de disclaimer do Fator Simples no topo; ranking com badge "Simples"; "Exportar CSV" baixa ranking-fornecedores-simples.csv.
       - 1.2: Select de filtro CST (Todos/Normal/ST/Base Reduzida) filtra a tabela; coluna Variação com + verde / - vermelho; "Exportar CSV" baixa reprecificacao-produtos.csv.
       - 1.4: 2 cards (Float Tributário, Custo CDI); matriz de sensibilidade DSO × CDI com a célula correspondente aos parâmetros atuais destacada; NÃO há botão Exportar CSV.
    5. Se uma empresa não tiver dados, confirmar que cada página mostra o empty-state em PT-BR correto (não erro nem tela em branco).
    6. Confirmar valores monetários formatados em R$ (pt-BR) e CNPJs formatados com pontuação no ranking.
  </how-to-verify>
  <resume-signal>Digite "approved" se as 4 páginas renderizam, navegam, filtram, exportam (1.1/1.2/1.3) e exibem disclaimers/empty-states corretamente; ou descreva os problemas encontrados.</resume-signal>
  <verify>
    <human-check>Usuário confirma renderização, navegação, filtros, exportação CSV (1.1/1.2/1.3) e disclaimers/empty-states das 4 páginas.</human-check>
  </verify>
  <done>Usuário respondeu "approved"; as 4 páginas dos Módulos 1.x funcionam end-to-end e as tabs Phase 8 permanecem desabilitadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (React) → API | fetch envia JWT + X-Company-ID injetados pelo AuthContext (window.fetch interceptado) |
| usuário → UI | dados fiscais sensíveis renderizados; CSV baixado no navegador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Disclosure | 4 páginas + downloads CSV | mitigate | Páginas usam fetch sem expor company_id no client; AuthContext injeta JWT/X-Company-ID; o backend (07-01) faz o isolamento por empresa via GetEffectiveCompanyID. O frontend não decide de qual empresa são os dados. |
| T-07-07 | Spoofing | Acesso direto às rotas /reforma/* sem login | mitigate | App.tsx aninha as rotas sob o guard de autenticação existente; usuário não autenticado é redirecionado ao login (padrão já em vigor para /reforma/parametros). |
| T-07-08 | Tampering | XSS via dados fiscais (forn_nome, x_prod) renderizados | mitigate | React escapa por padrão todo conteúdo em JSX ({value}); nenhum uso de dangerouslySetInnerHTML nas novas páginas. |
| T-07-09 | Information Disclosure | Disclaimer regulatório ausente (RFMB-02) | mitigate | Banner obrigatório do Fator Simples Nacional implementado na Reforma13 (Task 1) — requisito de conformidade regulatória explícito. |
| T-07-SC | Tampering | npm installs | mitigate | Nenhuma dependência nova (RESEARCH "zero new installations") — todos os componentes shadcn/recharts/lucide já instalados. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` sem erros de tipo nas 6 alterações
- `cd frontend && npx vitest run src/lib/navigation.test.ts` PASS
- `cd frontend && npm run build` compila (bundle gerado)
- navigation.ts: 4 tabs Phase 7 ativas, 4 tabs Phase 8 ainda disabled
- App.tsx: 4 rotas + 4 imports presentes
- Checkpoint humano confirma renderização, navegação, filtros, CSV e disclaimers
</verification>

<success_criteria>
- 4 páginas React (1.1, 1.2, 1.3, 1.4) consumindo os endpoints de 07-01
- Layout, cores, tipografia e estados conforme 07-UI-SPEC.md
- Disclaimer regulatório do Fator Simples (1.3) e do cronograma split payment (1.4) presentes
- CSV export funcional em 1.1, 1.2, 1.3; ausente em 1.4 (por design)
- Matriz de sensibilidade DSO × CDI com célula corrente destacada (1.4)
- 4 tabs Phase 7 ativas; 4 tabs Phase 8 permanecem disabled
- 4 rotas registradas em App.tsx
- Sem erros de tipo; teste de navegação passa; checkpoint humano aprovado
</success_criteria>

<output>
Create `.planning/phases/07-m-dulos-1-x-exposi-o-tribut-ria-direta/07-02-SUMMARY.md` when done
</output>
