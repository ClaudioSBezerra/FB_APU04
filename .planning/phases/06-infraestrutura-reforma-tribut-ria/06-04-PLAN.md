---
phase: 06-infraestrutura-reforma-tribut-ria
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - frontend/src/hooks/useReformaParametros.ts
  - frontend/src/pages/ReformaParametros.tsx
  - frontend/src/lib/navigation.ts
  - frontend/src/App.tsx
  - frontend/package.json
  - frontend/public/brazil-states.json
autonomous: true
requirements: [RFMA-06, RFMA-07, RFMA-08]
user_setup: []
must_haves:
  truths:
    - "Sidebar exibe módulo 'Análise Reforma Tributária' com tab ativa 'Parâmetros' e tabs placeholder disabled para módulos 1.x/2.x"
    - "Página /reforma/parametros e /config/reforma-parametros renderizam o mesmo componente ReformaParametros"
    - "Admin edita e salva os parâmetros; não-admin vê campos desabilitados sem botão Salvar"
    - "Campo fator_simples_pct exibe tooltip ⓘ com disclaimer do CG-IBS"
    - "react-simple-maps v3.0.0 instalado e frontend/public/brazil-states.json commitado"
  artifacts:
    - path: "frontend/src/hooks/useReformaParametros.ts"
      provides: "useReformaParametros (useQuery) + useUpdateReformaParametros (useMutation)"
      exports: ["useReformaParametros", "useUpdateReformaParametros", "ReformaParametros"]
    - path: "frontend/src/pages/ReformaParametros.tsx"
      provides: "Página card com campos editáveis + tooltip + botão Salvar admin-only"
      contains: "useReformaParametros"
    - path: "frontend/src/lib/navigation.ts"
      provides: "Módulo reforma + tab config + getActiveModule"
      contains: "reforma:"
    - path: "frontend/src/App.tsx"
      provides: "2 Routes para ReformaParametros"
      contains: "ReformaParametros"
    - path: "frontend/public/brazil-states.json"
      provides: "TopoJSON estados brasileiros para Phase 8"
      contains: "Feature"
  key_links:
    - from: "frontend/src/pages/ReformaParametros.tsx"
      to: "/api/reforma/parametros"
      via: "useReformaParametros / useUpdateReformaParametros"
      pattern: "useReformaParametros"
    - from: "frontend/src/App.tsx"
      to: "ReformaParametros"
      via: "Route path /reforma/parametros e /config/reforma-parametros"
      pattern: "path=\"/reforma/parametros\""
    - from: "frontend/src/lib/navigation.ts (getActiveModule)"
      to: "'reforma'"
      via: "pathname.startsWith('/reforma')"
      pattern: "startsWith\\('/reforma'\\)"
---

<objective>
Entregar a camada frontend da infraestrutura da Reforma: hook react-query (`useReformaParametros.ts`), página de configuração (`ReformaParametros.tsx`), entrada de navegação (`navigation.ts`), rotas (`App.tsx`), e o setup da dependência de mapa (`react-simple-maps` + `brazil-states.json`) que a Phase 8 consumirá.

Purpose: Esta é a única saída visível ao usuário na Phase 6 — a aba "Análise Reforma Tributária" na sidebar e a página onde admins configuram alíquotas IBS/CBS, fator Simples, CDI e prazo médio. O hook compartilha esses parâmetros globalmente para todos os módulos das Phases 7/8. O setup do mapa é puro pré-requisito de dependência (nenhum componente de mapa é criado agora).

Output: 2 arquivos frontend novos, 2 modificados, package.json atualizado, e o TopoJSON commitado. Build Vite (`npm run build`) deve passar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infraestrutura-reforma-tribut-ria/06-CONTEXT.md
@.planning/phases/06-infraestrutura-reforma-tribut-ria/06-RESEARCH.md
@.planning/phases/06-infraestrutura-reforma-tribut-ria/06-PATTERNS.md

<interfaces>
<!-- Estruturas verificadas no codebase em 2026-05-22 -->

API backend (criada na plan 06-03):
  GET /api/reforma/parametros → 200 `{"parametros": ReformaParametros | null}`
  PUT /api/reforma/parametros (admin) → 200 `{"message": "..."}`; 403 para não-admin
  Campos JSON snake_case: company_id, target_ano, aliq_ibs_pct, aliq_cbs_pct, fator_simples_pct, taxa_cdi_anual_pct, prazo_medio_dias

frontend/src/contexts/AuthContext.tsx:
  - fetch() global interceptado — injeta Authorization + X-Company-ID automaticamente (NÃO passar headers manualmente)
  - useAuth() expõe { user, isAuthenticated, loading }; user?.role === 'admin' para checar admin

frontend/src/lib/navigation.ts:
  - interface ModuleTab { label, path, disabled?, danger?, adminOnly? }
  - interface ModuleConfig { label, tabs: ModuleTab[] }
  - export const modules: Record<string, ModuleConfig> com chaves: simulador, notas, painel, config (nessa ordem)
  - export function getActiveModule(pathname): usa cadeia de if pathname.startsWith(...); '/config/' → 'config'; default 'simulador'

frontend/src/App.tsx:
  - imports de páginas no topo (ex.: import TabelaAliquotas from './pages/TabelaAliquotas')
  - Routes simples sem AdminRoute: <Route path="/config/aliquotas" element={<TabelaAliquotas />} />
  - AdminRoute existe mas NÃO deve ser usado aqui (não-admin vê em modo leitura, D-06)
  - @tanstack/react-query v5.90.20 e QueryClientProvider já configurados

frontend/src/pages/ERPBridgeConfig.tsx (analog de página com useQuery + card + inputs + Salvar)
frontend/src/pages/ConciliacaoBridgeXML.tsx (analog de Tooltip Radix — imports e uso, linhas 29-34 e 198-224)
frontend/src/components/ui/tooltip.tsx (componente existe; Tooltip/TooltipContent/TooltipProvider/TooltipTrigger)

Package legitimacy (RESEARCH.md Package Legitimacy Audit):
  react-simple-maps@3.0.0 — npm, ~8 anos, MIT, sem postinstall — [VERIFIED: npm registry], disposição Aprovado.
  Não requer checkpoint humano (não é [ASSUMED]/[SUS]/[SLOP]).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hook useReformaParametros.ts (RFMA-06)</name>
  <files>frontend/src/hooks/useReformaParametros.ts</files>
  <read_first>
    - frontend/src/pages/ERPBridgeConfig.tsx (padrão useQuery + useMutation + useQueryClient, linhas ~114-234)
    - frontend/src/contexts/AuthContext.tsx (confirmar que fetch() global injeta headers — não passar authHeaders manualmente)
    - frontend/src/hooks/use-mobile.tsx (confirmar convenção de arquivo de hook no diretório)
  </read_first>
  <action>
    Criar `frontend/src/hooks/useReformaParametros.ts` exportando:

    1. interface `ReformaParametros` com os 7 campos snake_case: company_id (string), target_ano (number), aliq_ibs_pct (number), aliq_cbs_pct (number), fator_simples_pct (number), taxa_cdi_anual_pct (number), prazo_medio_dias (number).

    2. `useReformaParametros()` → `useQuery<{ parametros: ReformaParametros | null }>` com `queryKey: ['reforma-parametros']`, `queryFn` fazendo `fetch('/api/reforma/parametros')`, checando `res.ok` (throw em erro), retornando `res.json()`. NÃO passar headers manualmente — o fetch global do AuthContext injeta Authorization e X-Company-ID.

    3. `useUpdateReformaParametros()` → `useMutation` com `mutationFn` fazendo `fetch('/api/reforma/parametros', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) })` (data: Partial<ReformaParametros>), checando res.ok (throw com res.text() em erro); `onSuccess` invalida `queryKey: ['reforma-parametros']` via useQueryClient.

    Imports: `useQuery, useMutation, useQueryClient` de `@tanstack/react-query`. Hook compartilhado globalmente (não por módulo) — queryKey sem companyId (o backend já isola por JWT). Apenas o header Content-Type é necessário no PUT (o resto vem do interceptor).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "useReformaParametros" | grep -q "error TS" && echo "TSC_ERRORS" || (grep -q "export function useReformaParametros" src/hooks/useReformaParametros.ts && grep -q "export function useUpdateReformaParametros" src/hooks/useReformaParametros.ts && grep -q "queryKey: \['reforma-parametros'\]" src/hooks/useReformaParametros.ts && echo OK)</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo exporta `interface ReformaParametros` com os 7 campos number/string snake_case
    - Exporta `useReformaParametros()` usando `useQuery` com queryKey `['reforma-parametros']` e fetch GET `/api/reforma/parametros`
    - Exporta `useUpdateReformaParametros()` usando `useMutation` com PUT e `onSuccess` que invalida `['reforma-parametros']`
    - Nenhum header de Authorization/X-Company-ID passado manualmente (apenas Content-Type no PUT)
    - Tipagem do retorno do useQuery é `{ parametros: ReformaParametros | null }`
  </acceptance_criteria>
  <done>Hook react-query exportando query e mutation dos parâmetros da Reforma, compartilhado globalmente, sem headers manuais.</done>
</task>

<task type="auto">
  <name>Task 2: Página ReformaParametros.tsx + navegação + rotas (RFMA-07)</name>
  <files>frontend/src/pages/ReformaParametros.tsx, frontend/src/lib/navigation.ts, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/pages/ERPBridgeConfig.tsx (estrutura card + Input + Button Salvar + useEffect de sync de state com dados do hook)
    - frontend/src/pages/ConciliacaoBridgeXML.tsx (imports e uso do Tooltip Radix, linhas 29-34 e 198-224)
    - frontend/src/lib/navigation.ts (estrutura modules Record + getActiveModule — ler arquivo inteiro)
    - frontend/src/App.tsx (padrão de import de página e <Route>; localizar AdminRoute mas NÃO usá-lo aqui)
    - frontend/src/components/ui/tooltip.tsx (exports do componente tooltip)
    - frontend/src/hooks/useReformaParametros.ts (hook criado na Task 1 — consumir useReformaParametros e useUpdateReformaParametros)
  </read_first>
  <action>
    Três arquivos:

    A) Criar `frontend/src/pages/ReformaParametros.tsx` (D-04: mesmo componente para ambas as rotas):
    - importar Card/CardContent/CardHeader/CardTitle, Input, Button de @/components/ui/*; Tooltip/TooltipContent/TooltipProvider/TooltipTrigger de @/components/ui/tooltip; Info, Loader2 de lucide-react; toast de sonner; useAuth de @/contexts/AuthContext; useReformaParametros e useUpdateReformaParametros do hook.
    - `const { user } = useAuth(); const isAdmin = user?.role === 'admin'` (D-06/D-07).
    - `const { data, isLoading } = useReformaParametros()`; estado local para os 6 campos editáveis (target_ano, aliq_ibs_pct, aliq_cbs_pct, fator_simples_pct, taxa_cdi_anual_pct, prazo_medio_dias). `useEffect` que popula o estado quando `data?.parametros` chega (padrão ERPBridgeConfig). Se `data.parametros` for null, usar defaults sensatos (ex.: target_ano 2027, fator_simples_pct 20).
    - Card "Parâmetros da Reforma Tributária" com cada campo como `<Input type="number" value={...} onChange={...} disabled={!isAdmin} />` (D-05: card inline, não modal; D-06: disabled para não-admin).
    - Campo `fator_simples_pct`: label envolvido em `<TooltipProvider delayDuration={200}><Tooltip><TooltipTrigger asChild><span className="inline-flex items-center gap-1 cursor-help">Fator Simples Nacional (%) <Info className="h-3.5 w-3.5 ..." /></span></TooltipTrigger><TooltipContent>Valor estimado. Alíquota definitiva ainda não publicada pelo CG-IBS.</TooltipContent></Tooltip></TooltipProvider>` (D-08: texto exato).
    - Botão Salvar renderizado SOMENTE `{isAdmin && (...)}` (D-06): chama `useUpdateReformaParametros().mutate(...)` com os valores; toast.success/error no onSuccess/onError; disabled enquanto isPending com Loader2.
    - NÃO usar AdminRoute — não-admin vê a página em leitura.

    B) Modificar `frontend/src/lib/navigation.ts`:
    - Adicionar nova chave `reforma` ao record `modules` (após `painel`, antes de `config`) com `label: 'Análise Reforma Tributária'` (RFMA-07/D-01) e tabs:
      - `{ label: 'Parâmetros', path: '/reforma/parametros' }` (tab ativa — D-02)
      - tabs placeholder com `disabled: true` para os módulos futuros (D-02): Créditos IBS/CBS → /reforma/creditos; Reprecificação → /reforma/reprecificacao; Ranking Fornecedores → /reforma/ranking; Split Payment → /reforma/split-payment; Análise CFOP → /reforma/cfop; Análise NCM → /reforma/ncm; UF Destino → /reforma/uf-destino; B2B vs B2C → /reforma/b2b-b2c (labels/paths são discrição mas devem seguir o ROADMAP; todos disabled: true).
    - Adicionar tab no módulo `config` após 'Alíquotas': `{ label: 'Parâmetros Reforma', path: '/config/reforma-parametros' }` (D-04).
    - Em `getActiveModule`: adicionar `if (pathname.startsWith('/reforma')) return 'reforma'` ANTES da linha `if (pathname.startsWith('/config/')) return 'config'` (D-03). A rota /config/reforma-parametros continua resolvendo para 'config' via a regra /config/ existente.

    C) Modificar `frontend/src/App.tsx`:
    - Adicionar `import ReformaParametros from './pages/ReformaParametros'` junto aos demais imports de páginas.
    - Adicionar AMBAS as rotas (D-04 + Pitfall 7): `<Route path="/reforma/parametros" element={<ReformaParametros />} />` e `<Route path="/config/reforma-parametros" element={<ReformaParametros />} />`. NÃO envolver em AdminRoute (D-06).
  </action>
  <verify>
    <automated>cd frontend && grep -q "reforma:" src/lib/navigation.ts && grep -q "startsWith('/reforma')" src/lib/navigation.ts && grep -q "path: '/config/reforma-parametros'" src/lib/navigation.ts && grep -q "import ReformaParametros" src/App.tsx && grep -c 'path="/reforma/parametros"\|path="/config/reforma-parametros"' src/App.tsx | grep -q 2 && grep -q "Alíquota definitiva ainda não publicada pelo CG-IBS" src/pages/ReformaParametros.tsx && grep -q "isAdmin" src/pages/ReformaParametros.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `navigation.ts` contém chave `reforma` com label 'Análise Reforma Tributária', uma tab ativa 'Parâmetros' (/reforma/parametros) e demais tabs com `disabled: true`
    - `navigation.ts` config module contém tab `{ label: 'Parâmetros Reforma', path: '/config/reforma-parametros' }`
    - `getActiveModule` retorna 'reforma' para pathname iniciando com '/reforma' (regra antes de '/config/')
    - `App.tsx` importa ReformaParametros e registra exatamente 2 Routes (/reforma/parametros e /config/reforma-parametros), nenhuma envolta em AdminRoute
    - `ReformaParametros.tsx` usa useReformaParametros + useUpdateReformaParametros, deriva `isAdmin = user?.role === 'admin'`, desabilita inputs e oculta Salvar para não-admin
    - O tooltip do fator_simples_pct contém o texto exato "Valor estimado. Alíquota definitiva ainda não publicada pelo CG-IBS."
    - Todos os 6 campos editáveis presentes: target_ano, aliq_ibs_pct, aliq_cbs_pct, fator_simples_pct, taxa_cdi_anual_pct, prazo_medio_dias
  </acceptance_criteria>
  <done>Sidebar exibe módulo Reforma com tab Parâmetros (e placeholders disabled); página renderiza nas 2 rotas; admin edita/salva, não-admin lê; tooltip do fator Simples presente.</done>
</task>

<task type="auto">
  <name>Task 3: Instalar react-simple-maps + commitar brazil-states.json (RFMA-08)</name>
  <files>frontend/package.json, frontend/public/brazil-states.json</files>
  <read_first>
    - frontend/package.json (confirmar dependências atuais e versão de react antes de instalar)
    - .planning/phases/06-infraestrutura-reforma-tribut-ria/06-RESEARCH.md (RFMA-08 — versão 3.0.0; Pitfall 5 — ESM/Vite; Package Legitimacy Audit — Aprovado/VERIFIED)
  </read_first>
  <action>
    Setup puro de dependência para a Phase 8 (nenhum componente de mapa criado agora):

    1. Instalar a dependência exata: rodar `npm install react-simple-maps@3.0.0` no diretório `frontend/`. O pacote está [VERIFIED: npm registry] no audit (MIT, ~8 anos, sem postinstall) — não requer checkpoint humano. Confirmar que `react-simple-maps` aparece em `dependencies` de `package.json` com versão 3.0.0.

    2. Obter e commitar `frontend/public/brazil-states.json`: um arquivo TopoJSON/GeoJSON dos estados brasileiros compatível com react-simple-maps v3.0.0. Baixar de uma fonte pública (ex.: https://raw.githubusercontent.com/codeforamerica/click_that_hood/master/public/data/brazil-states.geojson) e salvar em `frontend/public/brazil-states.json`. O arquivo deve ser JSON válido contendo features dos 27 estados (26 + DF). Validar que é JSON parseável e contém geometria de estados.

    3. NÃO criar componente de mapa nem importar react-simple-maps em nenhum arquivo .tsx nesta plan — apenas instalar e commitar o asset. Build errors de ESM/Vite (Pitfall 5) só apareceriam ao usar o componente na Phase 8.
  </action>
  <verify>
    <automated>cd frontend && grep -q "\"react-simple-maps\": \"3.0.0\"\|\"react-simple-maps\": \"\\^3.0.0\"" package.json && test -f public/brazil-states.json && node -e "const d=require('./public/brazil-states.json'); if(JSON.stringify(d).length<1000) process.exit(1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lista `react-simple-maps` versão 3.0.0 em dependencies
    - `node_modules/react-simple-maps` existe após install
    - `frontend/public/brazil-states.json` existe, é JSON válido (parseável por node) e tem tamanho substancial (>1KB, contém geometria dos estados)
    - Nenhum arquivo .tsx importa react-simple-maps nesta plan (setup apenas)
  </acceptance_criteria>
  <done>react-simple-maps@3.0.0 instalado e brazil-states.json commitado em frontend/public/, prontos para consumo na Phase 8; nenhum componente de mapa criado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (cliente) → API Go | Página chama GET/PUT /api/reforma/parametros via fetch interceptado |
| npm registry → build local | Instalação de react-simple-maps traz código de terceiro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Elevation of Privilege | Não-admin tenta salvar parâmetros pela UI | mitigate | UI oculta o botão Salvar para não-admin (D-06) E o backend rejeita PUT não-admin com 403 (defesa em profundidade, plan 06-03) |
| T-06-13 | Information Disclosure | Disclaimer ausente do fator Simples | mitigate | Tooltip ⓘ obrigatório com texto exato sobre o fator não publicado (D-08) — verificado por grep do texto |
| T-06-14 | Tampering | Pacote npm malicioso (supply chain) | mitigate | react-simple-maps [VERIFIED: npm registry] no Package Legitimacy Audit — MIT, ~8 anos, sem script postinstall; versão fixada em 3.0.0 |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Pacote único auditado e VERIFIED; não [ASSUMED]/[SUS]/[SLOP] — checkpoint humano não exigido |
</threat_model>

<verification>
- `npm run build` (ou `npx tsc --noEmit`) no frontend compila sem novos erros de tipo
- Sidebar exibe módulo 'Análise Reforma Tributária' com tab 'Parâmetros' ativa e placeholders disabled
- /reforma/parametros e /config/reforma-parametros renderizam ReformaParametros
- Admin vê inputs editáveis + botão Salvar; não-admin vê inputs disabled sem botão
- Tooltip do fator_simples_pct contém o disclaimer exato
- react-simple-maps@3.0.0 em package.json; brazil-states.json válido em public/
</verification>

<success_criteria>
- RFMA-06: hook useReformaParametros compartilhado via react-query
- RFMA-07: navegação + rotas + página de configuração editável por admins com disclaimer
- RFMA-08: react-simple-maps v3.0.0 instalado + brazil-states.json commitado
</success_criteria>

<output>
Create `.planning/phases/06-infraestrutura-reforma-tribut-ria/06-04-SUMMARY.md` when done
</output>
