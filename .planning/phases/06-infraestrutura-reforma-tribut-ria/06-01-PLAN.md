---
phase: 06-infraestrutura-reforma-tribut-ria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/086_add_cst_aliq_icms_to_reg_c190.sql
  - backend/migrations/087_create_reforma_parametros.sql
  - backend/migrations/088_add_ind_final_to_nfe_saidas.sql
  - backend/migrations/089_seed_cfop_transferencias.sql
autonomous: true
requirements: [RFMA-01, RFMA-02, RFMA-03, RFMA-04]
must_haves:
  truths:
    - "reg_c190 possui colunas cst_icms (VARCHAR 3) e aliq_icms (NUMERIC 6,2)"
    - "Tabela reforma_parametros existe com PK company_id e 6 campos de configuração"
    - "nfe_saidas possui coluna ind_final (SMALLINT, nullable)"
    - "CFOPs 1151/1152/2151/2152/5151/5152/6151/6152 têm tipo='T' na tabela cfop"
  artifacts:
    - path: "backend/migrations/086_add_cst_aliq_icms_to_reg_c190.sql"
      provides: "ADD COLUMN cst_icms + aliq_icms em reg_c190"
      contains: "ALTER TABLE reg_c190"
    - path: "backend/migrations/087_create_reforma_parametros.sql"
      provides: "CREATE TABLE reforma_parametros"
      contains: "CREATE TABLE IF NOT EXISTS reforma_parametros"
    - path: "backend/migrations/088_add_ind_final_to_nfe_saidas.sql"
      provides: "ADD COLUMN ind_final em nfe_saidas"
      contains: "ALTER TABLE nfe_saidas"
    - path: "backend/migrations/089_seed_cfop_transferencias.sql"
      provides: "Seed 8 CFOPs de transferência com tipo='T'"
      contains: "ON CONFLICT (cfop) DO UPDATE SET tipo = 'T'"
  key_links:
    - from: "reforma_parametros.company_id"
      to: "companies.id"
      via: "FOREIGN KEY ON DELETE CASCADE"
      pattern: "REFERENCES companies\\(id\\)"
---

<objective>
Criar a fundação de schema da Reforma Tributária: quatro migrations PostgreSQL numeradas 086–089 que adicionam colunas a `reg_c190` e `nfe_saidas`, criam a tabela `reforma_parametros` e fazem seed dos CFOPs de transferência com `tipo='T'`.

Purpose: Toda a infraestrutura analítica das Phases 7 e 8 depende destas mudanças de schema. Sem `cst_icms`/`aliq_icms` em `reg_c190` não há análise de créditos ICMS; sem `reforma_parametros` não há parâmetros configuráveis; sem `ind_final` em `nfe_saidas` não há segmentação B2B/B2C; sem CFOPs `tipo='T'` os módulos não conseguem excluir transferências.

Output: 4 arquivos de migration DDL idempotentes. Nenhuma mudança de código Go nesta plan — apenas DDL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infraestrutura-reforma-tribut-ria/06-CONTEXT.md
@.planning/phases/06-infraestrutura-reforma-tribut-ria/06-RESEARCH.md
@.planning/phases/06-infraestrutura-reforma-tribut-ria/06-PATTERNS.md

<interfaces>
<!-- Fatos verificados no codebase — usar diretamente, sem explorar -->

companies (migration 013_create_environment_hierarchy.sql): PK `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`.
  → reforma_parametros.company_id deve ser FK para companies(id) ON DELETE CASCADE.

reg_c190 (migration 010_update_schema_efd_icms.sql): tabela já existe; colunas atuais incluem
  job_id, id_pai_c100, cfop, vl_opr, vl_bc_icms, vl_icms, vl_bc_icms_st, vl_icms_st, vl_red_bc, vl_ipi, cod_obs.
  → Adicionar cst_icms VARCHAR(3) e aliq_icms NUMERIC(6,2) (RFMA-01 especifica esses tipos exatos).

cfop (migration 009_create_cfop_table.sql + 062_reseed_cfops.sql): colunas cfop (PK), descricao_cfop, tipo.
  Valores válidos de tipo: A/C/R/T/O/S. Seed pattern usa INSERT ... ON CONFLICT (cfop).

nfe_saidas: tabela existe; constraint de conflito é uq_nfe_saidas_company_chave.
  → Adicionar ind_final SMALLINT nullable.

Migration mais recente confirmada: 085_fix_vw_xml_entradas_informativos_pis_cofins.sql.
  → Novas migrations são 086, 087, 088, 089.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations de coluna — 086 (reg_c190) e 088 (nfe_saidas)</name>
  <files>backend/migrations/086_add_cst_aliq_icms_to_reg_c190.sql, backend/migrations/088_add_ind_final_to_nfe_saidas.sql</files>
  <read_first>
    - backend/migrations/084_add_competencia_and_fix_vw_xml_operacoes.sql (analog exato de ADD COLUMN IF NOT EXISTS — replicar cabeçalho e estilo)
    - backend/migrations/085_fix_vw_xml_entradas_informativos_pis_cofins.sql (confirma numeração mais recente = 085)
    - backend/migrations/010_update_schema_efd_icms.sql (estrutura atual de reg_c190)
  </read_first>
  <action>
    Criar `086_add_cst_aliq_icms_to_reg_c190.sql` (RFMA-01, parte DDL): `ALTER TABLE reg_c190 ADD COLUMN IF NOT EXISTS cst_icms VARCHAR(3), ADD COLUMN IF NOT EXISTS aliq_icms NUMERIC(6,2);`. Usar exatamente os tipos `VARCHAR(3)` e `NUMERIC(6,2)` especificados em RFMA-01. Colunas nullable (sem NOT NULL, sem DEFAULT) — registros históricos ficam NULL conforme D-09.

    Criar `088_add_ind_final_to_nfe_saidas.sql` (RFMA-03, parte DDL): `ALTER TABLE nfe_saidas ADD COLUMN IF NOT EXISTS ind_final SMALLINT;`. Tipo `SMALLINT` conforme RFMA-03. Nullable — notas históricas ficam NULL (D-09).

    Cada arquivo deve ter cabeçalho comentado no estilo do codebase (ver 084/085): linha `-- Migration NNN: <descrição> (RFMA-XX)` seguida de motivo. Usar `IF NOT EXISTS` para idempotência. Não inserir blocos de código fora do escopo DDL descrito.
  </action>
  <verify>
    <automated>grep -q "ADD COLUMN IF NOT EXISTS cst_icms" backend/migrations/086_add_cst_aliq_icms_to_reg_c190.sql && grep -q "aliq_icms NUMERIC(6,2)" backend/migrations/086_add_cst_aliq_icms_to_reg_c190.sql && grep -q "ind_final SMALLINT" backend/migrations/088_add_ind_final_to_nfe_saidas.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `086_add_cst_aliq_icms_to_reg_c190.sql` contém `ALTER TABLE reg_c190` e ambas `cst_icms VARCHAR(3)` e `aliq_icms NUMERIC(6,2)` com `IF NOT EXISTS`
    - `088_add_ind_final_to_nfe_saidas.sql` contém `ALTER TABLE nfe_saidas` e `ind_final SMALLINT` com `IF NOT EXISTS`
    - Nenhuma coluna nova tem `NOT NULL` nem `DEFAULT` (são nullable por D-09)
    - Cada arquivo inicia com comentário `-- Migration 086:` / `-- Migration 088:` referenciando RFMA-01 / RFMA-03
  </acceptance_criteria>
  <done>Migrations 086 e 088 existem, são idempotentes (IF NOT EXISTS), adicionam as 3 colunas nullable nos tipos exatos de RFMA-01 e RFMA-03.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 087 — CREATE TABLE reforma_parametros</name>
  <files>backend/migrations/087_create_reforma_parametros.sql</files>
  <read_first>
    - backend/migrations/010_update_schema_efd_icms.sql (analog de CREATE TABLE IF NOT EXISTS com created_at/updated_at)
    - backend/migrations/013_create_environment_hierarchy.sql (confirma companies.id UUID PK — alvo da FK)
    - .planning/REQUIREMENTS.md (RFMA-02 — campos e tipos exatos da tabela)
  </read_first>
  <action>
    Criar `087_create_reforma_parametros.sql` (RFMA-02): `CREATE TABLE IF NOT EXISTS reforma_parametros` com:
    - `company_id UUID PRIMARY KEY REFERENCES companies(id) ON DELETE CASCADE` (PK = FK, conforme RFMA-02 e key_link verificado)
    - `target_ano INTEGER NOT NULL DEFAULT 2027`
    - `aliq_ibs_pct NUMERIC(5,2)` — RFMA-02 especifica NUMERIC(5,2)
    - `aliq_cbs_pct NUMERIC(5,2)`
    - `fator_simples_pct NUMERIC(5,2)` com `DEFAULT 20.00` (padrão estimado 20% por RFMA-02, pendente publicação CG-IBS)
    - `taxa_cdi_anual_pct NUMERIC(5,2)`
    - `prazo_medio_dias INTEGER`
    - `created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP`
    - `updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP`

    IMPORTANTE: usar os tipos `NUMERIC(5,2)` e `INT/INTEGER` EXATAMENTE como RFMA-02 define (NUMERIC(5,2) para os percentuais, não NUMERIC(6,4) como sugerido em PATTERNS.md). Os valores são percentuais inteiros com 2 casas (ex.: 26.00 para IBS, 20.00 para fator Simples), não frações. Cabeçalho comentado `-- Migration 087:` referenciando RFMA-02. Não inserir índices extras nem triggers.
  </action>
  <verify>
    <automated>grep -q "CREATE TABLE IF NOT EXISTS reforma_parametros" backend/migrations/087_create_reforma_parametros.sql && grep -q "company_id UUID PRIMARY KEY REFERENCES companies(id) ON DELETE CASCADE" backend/migrations/087_create_reforma_parametros.sql && grep -cE "aliq_ibs_pct|aliq_cbs_pct|fator_simples_pct|taxa_cdi_anual_pct|target_ano|prazo_medio_dias" backend/migrations/087_create_reforma_parametros.sql | grep -q 6 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo contém `CREATE TABLE IF NOT EXISTS reforma_parametros`
    - PK definida como `company_id UUID PRIMARY KEY REFERENCES companies(id) ON DELETE CASCADE`
    - As 6 colunas de parâmetro presentes: `target_ano`, `aliq_ibs_pct`, `aliq_cbs_pct`, `fator_simples_pct`, `taxa_cdi_anual_pct`, `prazo_medio_dias`
    - Percentuais usam tipo `NUMERIC(5,2)` (conforme RFMA-02), `target_ano`/`prazo_medio_dias` usam `INTEGER`/`INT`
    - `fator_simples_pct` tem `DEFAULT 20.00` (ou 20) — padrão estimado pendente CG-IBS
    - Colunas `created_at` e `updated_at` presentes com `DEFAULT CURRENT_TIMESTAMP`
  </acceptance_criteria>
  <done>Tabela reforma_parametros criada com PK/FK company_id, 6 campos nos tipos exatos de RFMA-02, timestamps, idempotente via IF NOT EXISTS.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 089 — Seed CFOPs de transferência</name>
  <files>backend/migrations/089_seed_cfop_transferencias.sql</files>
  <read_first>
    - backend/migrations/062_reseed_cfops.sql (analog de seed INSERT ... ON CONFLICT (cfop) — replicar formato de colunas cfop/descricao_cfop/tipo)
    - backend/migrations/009_create_cfop_table.sql (confirma colunas e valores válidos de tipo)
    - .planning/phases/06-infraestrutura-reforma-tribut-ria/06-RESEARCH.md (Pitfall 4 — usar DO UPDATE, nunca DO NOTHING)
  </read_first>
  <action>
    Criar `089_seed_cfop_transferencias.sql` (RFMA-04): `INSERT INTO cfop (cfop, descricao_cfop, tipo) VALUES (...)` para os 8 CFOPs de transferência, todos com `tipo='T'`:
    - 1151 'Transferência para industrialização'
    - 1152 'Transferência para comercialização'
    - 2151 'Transferência para industrialização - interestadual'
    - 2152 'Transferência para comercialização - interestadual'
    - 5151 'Transferência de produção do estabelecimento'
    - 5152 'Transferência de mercadoria adquirida ou recebida de terceiros'
    - 6151 'Transferência interestadual de produção do estabelecimento'
    - 6152 'Transferência interestadual de mercadoria adquirida ou recebida de terceiros'

    Fechar com `ON CONFLICT (cfop) DO UPDATE SET tipo = 'T', descricao_cfop = EXCLUDED.descricao_cfop;` — usar DO UPDATE (NÃO DO NOTHING) por Pitfall 4: se algum desses CFOPs já existir com tipo errado (ex.: 'R'), o seed deve corrigir para 'T'. Cabeçalho `-- Migration 089:` referenciando RFMA-04.
  </action>
  <verify>
    <automated>grep -cE "^\('(1151|1152|2151|2152|5151|5152|6151|6152)'" backend/migrations/089_seed_cfop_transferencias.sql | grep -q 8 && grep -q "ON CONFLICT (cfop) DO UPDATE SET tipo = 'T'" backend/migrations/089_seed_cfop_transferencias.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo contém INSERT com os 8 CFOPs exatos: 1151, 1152, 2151, 2152, 5151, 5152, 6151, 6152
    - Todos com `tipo='T'`
    - Cláusula final é `ON CONFLICT (cfop) DO UPDATE SET tipo = 'T', descricao_cfop = EXCLUDED.descricao_cfop` (DO UPDATE, não DO NOTHING)
    - Cabeçalho referencia RFMA-04
  </acceptance_criteria>
  <done>Migration 089 faz seed idempotente dos 8 CFOPs de transferência com tipo='T', corrigindo tipos errados via DO UPDATE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration runner → PostgreSQL | DDL aplicada ao schema; sem input de usuário em runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Re-execução de migration alterando dados | mitigate | Todas as migrations usam `IF NOT EXISTS` (DDL) e `ON CONFLICT` (seed) — idempotentes; reaplicação não corrompe estado |
| T-06-02 | Denial of Service | FK CASCADE em reforma_parametros | accept | ON DELETE CASCADE remove parâmetros apenas quando a própria company é deletada — comportamento desejado, não vetor de DoS |
| T-06-03 | Information Disclosure | Colunas cst_icms/aliq_icms/ind_final nullable | accept | Dados fiscais já protegidos pelo isolamento company_id existente; novas colunas seguem o mesmo escopo |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | N/A nesta plan — nenhuma instalação de pacote (RFMA-08 está na plan 06-04) |
</threat_model>

<verification>
- Todas as 4 migrations existem em `backend/migrations/` com numeração 086–089
- Migrations 086 e 088 são ADD COLUMN IF NOT EXISTS (nullable, sem default exceto reforma_parametros)
- Migration 087 cria reforma_parametros com PK/FK company_id e 6 campos nos tipos de RFMA-02
- Migration 089 faz seed dos 8 CFOPs com tipo='T' via DO UPDATE
- Reaplicar qualquer migration não causa erro (idempotência verificada por IF NOT EXISTS / ON CONFLICT)
</verification>

<success_criteria>
- RFMA-01 (parte DDL): reg_c190 ganha cst_icms VARCHAR(3) + aliq_icms NUMERIC(6,2)
- RFMA-02: tabela reforma_parametros existe com schema completo
- RFMA-03 (parte DDL): nfe_saidas ganha ind_final SMALLINT
- RFMA-04: 8 CFOPs de transferência com tipo='T' na tabela cfop
</success_criteria>

<output>
Create `.planning/phases/06-infraestrutura-reforma-tribut-ria/06-01-SUMMARY.md` when done
</output>
