---
phase: 06-infraestrutura-reforma-tribut-ria
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/handlers/reforma_config.go
  - backend/main.go
autonomous: true
requirements: [RFMA-05]
must_haves:
  truths:
    - "GET /api/reforma/parametros retorna os parâmetros da empresa autenticada (ou null se ainda não configurada)"
    - "PUT /api/reforma/parametros faz upsert dos parâmetros e exige role admin (403 para não-admin)"
    - "company_id é extraído do JWT, nunca do body (proteção IDOR)"
  artifacts:
    - path: "backend/handlers/reforma_config.go"
      provides: "GetReformaParametrosHandler + PutReformaParametrosHandler + struct ReformaParametros"
      exports: ["GetReformaParametrosHandler", "PutReformaParametrosHandler"]
      contains: "ON CONFLICT (company_id) DO UPDATE"
    - path: "backend/main.go"
      provides: "Rota /api/reforma/parametros (GET sem role, PUT role admin)"
      contains: "/api/reforma/parametros"
  key_links:
    - from: "backend/main.go (rota PUT)"
      to: "handlers.PutReformaParametrosHandler"
      via: "AuthMiddleware(..., \"admin\")"
      pattern: "PutReformaParametrosHandler"
    - from: "reforma_config.go handlers"
      to: "reforma_parametros table"
      via: "GetEffectiveCompanyID + SELECT/UPSERT por company_id"
      pattern: "GetEffectiveCompanyID"
---

<objective>
Criar o handler Go `reforma_config.go` com `GET /api/reforma/parametros` (qualquer usuário autenticado lê) e `PUT /api/reforma/parametros` (somente admin grava via upsert), e registrar a rota multi-método em `main.go`.

Purpose: O frontend (plan 06-04) precisa de uma API para ler e salvar os parâmetros da Reforma. Sem o handler, a página de configuração não tem como persistir. RFMA-05 e a regra de acesso D-07 (PUT só admin → 403) são entregues aqui.

Output: 1 arquivo Go novo (`reforma_config.go`) + 1 modificação em `main.go`. Build Go passa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infraestrutura-reforma-tribut-ria/06-CONTEXT.md
@.planning/phases/06-infraestrutura-reforma-tribut-ria/06-RESEARCH.md
@.planning/phases/06-infraestrutura-reforma-tribut-ria/06-PATTERNS.md

<interfaces>
<!-- Padrões verificados no codebase em 2026-05-22 -->

backend/handlers/rfb_credentials.go (analog EXATO de GET+PUT com UPSERT):
  - GetRFBCredentialHandler(db *sql.DB) http.HandlerFunc — extrai claims, userID, GetEffectiveCompanyID, SELECT, trata sql.ErrNoRows
  - SaveRFBCredentialHandler(db *sql.DB) http.HandlerFunc — INSERT ... ON CONFLICT (company_id) DO UPDATE SET ... updated_at=CURRENT_TIMESTAMP

backend/handlers/auth.go:
  - ClaimsKey — chave de contexto para jwt.MapClaims
  - GetEffectiveCompanyID(db *sql.DB, userID string, headerCompanyID string) (string, error) — resolve company_id efetivo
  - AuthMiddleware(h http.HandlerFunc, requiredRole string) — rejeita com 403 se role insuficiente (linha 253: requiredRole != "" && userRole != requiredRole && userRole != "admin")

backend/main.go:
  - getDB() *sql.DB (linha 57)
  - jsonServiceUnavailable(w) helper (linha 341)
  - withAuth(handlerFactory, role) closure (linha 358) — wrap single-method
  - Multi-method routes usam http.HandleFunc com switch r.Method + handlers.AuthMiddleware(handler(database), role)(w, r) — padrão em /api/rfb/credentials (linha 526)
  - IMPORTANTE: /api/rfb/credentials está DENTRO de `if appModule != "simulador"`. A rota da Reforma NÃO deve ficar nesse bloco — Reforma faz parte do produto simulador. Registrar junto das rotas gerais (próximo às linhas 371–377, fora de qualquer condicional de appModule).

reforma_parametros (tabela criada na plan 06-01, migration 087):
  PK company_id UUID; campos target_ano INT, aliq_ibs_pct NUMERIC(5,2), aliq_cbs_pct NUMERIC(5,2),
  fator_simples_pct NUMERIC(5,2), taxa_cdi_anual_pct NUMERIC(5,2), prazo_medio_dias INT; created_at/updated_at.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reforma_config.go — handlers GET e PUT com UPSERT</name>
  <files>backend/handlers/reforma_config.go</files>
  <behavior>
    - GET com empresa sem parâmetros configurados (sql.ErrNoRows): responde 200 com JSON `{"parametros": null}`
    - GET com empresa configurada: responde 200 com `{"parametros": {company_id, target_ano, aliq_ibs_pct, aliq_cbs_pct, fator_simples_pct, taxa_cdi_anual_pct, prazo_medio_dias}}`
    - GET sem claims válidas no contexto: responde 401
    - PUT com body válido: faz upsert e responde 200 com mensagem de sucesso; segunda chamada com valores diferentes atualiza (não duplica) a linha da company
    - PUT com aliq_ibs_pct fora de [0,100] ou prazo_medio_dias fora de [1,3650]: responde 400
    - company_id sempre derivado de GetEffectiveCompanyID (JWT), nunca do body (IDOR)
  </behavior>
  <read_first>
    - backend/handlers/rfb_credentials.go (analog completo GET+PUT com UPSERT — replicar estrutura de imports, extração de claims, sql.ErrNoRows, INSERT ON CONFLICT DO UPDATE)
    - backend/handlers/config.go (padrão GET simples com db *sql.DB closure)
    - backend/handlers/auth.go (ClaimsKey, GetEffectiveCompanyID — confirmar assinaturas reais)
    - .planning/phases/06-infraestrutura-reforma-tribut-ria/06-PATTERNS.md (seção reforma_config.go — código de referência para struct e ambos handlers)
  </read_first>
  <action>
    Criar `backend/handlers/reforma_config.go` no package `handlers`. Definir:

    1. struct `ReformaParametros` com tags JSON: `CompanyID string \`json:"company_id"\``, `TargetAno int \`json:"target_ano"\``, `AliqIBSPct float64 \`json:"aliq_ibs_pct"\``, `AliqCBSPct float64 \`json:"aliq_cbs_pct"\``, `FatorSimplesPct float64 \`json:"fator_simples_pct"\``, `TaxaCDIAnualPct float64 \`json:"taxa_cdi_anual_pct"\``, `PrazoMedioDias int \`json:"prazo_medio_dias"\``.

    2. `func GetReformaParametrosHandler(db *sql.DB) http.HandlerFunc`: extrai `claims := r.Context().Value(ClaimsKey).(jwt.MapClaims)` (401 se ok==false); `userID := claims["user_id"].(string)`; `companyID, err := GetEffectiveCompanyID(db, userID, r.Header.Get("X-Company-ID"))` (500 em erro); `db.QueryRow("SELECT company_id, target_ano, aliq_ibs_pct, aliq_cbs_pct, fator_simples_pct, taxa_cdi_anual_pct, prazo_medio_dias FROM reforma_parametros WHERE company_id = $1", companyID).Scan(...)`. Se `err == sql.ErrNoRows`: responder `json.NewEncoder(w).Encode(map[string]interface{}{"parametros": nil})` e retornar. Se sucesso: `Encode(map[string]interface{}{"parametros": p})`.

    3. `func PutReformaParametrosHandler(db *sql.DB) http.HandlerFunc`: mesma extração de claims/companyID; `json.NewDecoder(r.Body).Decode(&req)` (400 se erro); validar ranges (V5 ASVS): aliq_ibs_pct/aliq_cbs_pct/fator_simples_pct/taxa_cdi_anual_pct ∈ [0,100], prazo_medio_dias ∈ [1,3650], target_ano ∈ [2024,2100] — responder 400 com mensagem clara se inválido; UPSERT: `INSERT INTO reforma_parametros (company_id, target_ano, aliq_ibs_pct, aliq_cbs_pct, fator_simples_pct, taxa_cdi_anual_pct, prazo_medio_dias) VALUES ($1,$2,$3,$4,$5,$6,$7) ON CONFLICT (company_id) DO UPDATE SET target_ano=$2, aliq_ibs_pct=$3, aliq_cbs_pct=$4, fator_simples_pct=$5, taxa_cdi_anual_pct=$6, prazo_medio_dias=$7, updated_at=CURRENT_TIMESTAMP`. Usar `companyID` (do JWT) como $1 — nunca req.CompanyID. Responder 200 com `{"message": "Parâmetros salvos com sucesso"}`.

    Imports: `database/sql`, `encoding/json`, `net/http`, `github.com/golang-jwt/jwt/v5`. NÃO verificar role dentro do handler — a checagem admin acontece no AuthMiddleware (registrado em main.go na plan task 2). NÃO usar DO NOTHING (Pitfall 2). NÃO interpolar strings em SQL — só placeholders parametrizados.
  </action>
  <verify>
    <automated>cd backend && go build ./handlers/ && grep -q "func GetReformaParametrosHandler(db \*sql.DB) http.HandlerFunc" handlers/reforma_config.go && grep -q "func PutReformaParametrosHandler(db \*sql.DB) http.HandlerFunc" handlers/reforma_config.go && grep -q "ON CONFLICT (company_id) DO UPDATE" handlers/reforma_config.go && grep -q "GetEffectiveCompanyID" handlers/reforma_config.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./handlers/` compila sem erros
    - `reforma_config.go` exporta `GetReformaParametrosHandler` e `PutReformaParametrosHandler` com assinatura `(db *sql.DB) http.HandlerFunc`
    - struct `ReformaParametros` tem os 7 campos com tags JSON snake_case exatas
    - GET trata `sql.ErrNoRows` retornando `{"parametros": null}` (não 500)
    - PUT usa `INSERT ... ON CONFLICT (company_id) DO UPDATE SET ... updated_at=CURRENT_TIMESTAMP` com `companyID` (do JWT) em $1
    - PUT valida ranges numéricos e responde 400 para valores fora do intervalo
    - Nenhum SQL usa interpolação de string (apenas $1..$7)
  </acceptance_criteria>
  <done>Handler reforma_config.go criado com GET (lê parâmetros, trata empresa não-configurada) e PUT (upsert validado, company_id do JWT); build Go passa.</done>
</task>

<task type="auto">
  <name>Task 2: main.go — registrar rota /api/reforma/parametros</name>
  <files>backend/main.go</files>
  <read_first>
    - backend/main.go (ler o bloco multi-method de /api/rfb/credentials ~linha 526 e as rotas gerais ~371–377; confirmar onde termina o bloco `if appModule != "simulador"` para NÃO inserir dentro dele)
    - backend/handlers/auth.go (AuthMiddleware — confirmar que role "admin" gera 403 para não-admin)
    - .planning/phases/06-infraestrutura-reforma-tribut-ria/06-PATTERNS.md (seção main.go — padrão multi-method com switch r.Method)
  </read_first>
  <action>
    Adicionar em `main.go`, junto às rotas gerais (próximo às linhas 371–377, FORA do bloco condicional `if appModule != "simulador"` — Reforma é parte do produto simulador), uma rota multi-método:

    `http.HandleFunc("/api/reforma/parametros", func(w http.ResponseWriter, r *http.Request) { ... })` que:
    - obtém `database := getDB()`; se nil, chama `jsonServiceUnavailable(w)` e retorna
    - `switch r.Method`:
      - `case http.MethodGet:` → `handlers.AuthMiddleware(handlers.GetReformaParametrosHandler(database), "")(w, r)` (sem restrição de role — qualquer autenticado lê, D-06)
      - `case http.MethodPut:` → `handlers.AuthMiddleware(handlers.PutReformaParametrosHandler(database), "admin")(w, r)` (somente admin grava — D-07, retorna 403 para não-admin)
      - `default:` → `http.Error(w, "Method not allowed", http.StatusMethodNotAllowed)`

    Replicar exatamente o estilo do bloco `/api/rfb/credentials` (linha 526), mas posicionado fora do `if appModule != "simulador"`. Não criar handler novo — reutilizar os de reforma_config.go.
  </action>
  <verify>
    <automated>cd backend && go build . && grep -q "/api/reforma/parametros" main.go && grep -q "handlers.GetReformaParametrosHandler(database), \"\"" main.go && grep -q "handlers.PutReformaParametrosHandler(database), \"admin\"" main.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `go build .` (no diretório backend) compila o binário sem erros
    - `main.go` registra `/api/reforma/parametros` com switch GET/PUT
    - GET usa `AuthMiddleware(handlers.GetReformaParametrosHandler(database), "")` (role vazia)
    - PUT usa `AuthMiddleware(handlers.PutReformaParametrosHandler(database), "admin")`
    - A rota NÃO está dentro do bloco `if appModule != "simulador"` (está nas rotas gerais)
    - Método não suportado retorna 405
  </acceptance_criteria>
  <done>Rota /api/reforma/parametros registrada em main.go com GET aberto a autenticados e PUT restrito a admin; binário backend compila.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (cliente, untrusted) → API Go | Requisições GET/PUT com JWT cruzam para o handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Elevation of Privilege | Usuário não-admin tenta PUT alíquotas | mitigate | `AuthMiddleware(..., "admin")` no main.go rejeita com 403 ANTES do handler executar (D-07) |
| T-06-09 | Elevation of Privilege | PUT com company_id de outra empresa no body (IDOR) | mitigate | company_id derivado de `GetEffectiveCompanyID` (JWT), $1 do UPSERT; req.CompanyID do body é ignorado |
| T-06-10 | Tampering | Injeção SQL nos campos numéricos | mitigate | Todos os valores via placeholders $1..$7 parametrizados; nunca interpolados |
| T-06-11 | Input Validation | PUT com alíquotas absurdas (negativas/>100) | mitigate | Validação de range [0,100] / [1,3650] no handler retorna 400 (V5 ASVS) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | N/A nesta plan — nenhuma instalação de pacote |
</threat_model>

<verification>
- `go build ./...` no diretório backend compila handler + binário main
- GET /api/reforma/parametros responde a usuário autenticado; empresa não-configurada → {"parametros": null}
- PUT /api/reforma/parametros exige role admin (403 caso contrário) e faz upsert idempotente por company_id
- company_id sempre do JWT (IDOR mitigado); SQL parametrizado; ranges validados
</verification>

<success_criteria>
- RFMA-05: endpoints GET e PUT /api/reforma/parametros funcionais com validação de company_id e role admin no PUT
- Backend Go compila integralmente
</success_criteria>

<output>
Create `.planning/phases/06-infraestrutura-reforma-tribut-ria/06-03-SUMMARY.md` when done
</output>
