---
phase: 05-observabilidade-e-alertas
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - monitoring/prometheus/prometheus.yml
  - monitoring/prometheus/rules/fiscal.yml
  - monitoring/alertmanager/alertmanager.yml
  - docs/runbooks/dpy4011-consecutivos.md
  - docs/runbooks/xml-upload-falha.md
  - docs/runbooks/reset-banco-executado.md
  - docs/runbooks/bridge-offline.md
  - docs/runbooks/db-tamanho.md
  - docs/runbooks/README.md
autonomous: true
requirements:
  - OBS-02

must_haves:
  truths:
    - "Prometheus avalia regras de alerta a cada 15s usando os counters expostos pelo backend e bridge no Plan 01"
    - "Alertmanager recebe alertas do Prometheus e envia email via SMTP usando as variáveis ${SMTP_*} já configuradas no stack"
    - "Cada alerta crítico dispara em menos de 1 minuto após o evento (verificável manualmente injetando um incremento)"
    - "Cada tipo de alerta tem um runbook Markdown em docs/runbooks/ linkado via annotation runbook_url"
    - "Reset de banco gera alerta imediato (for: 0m) — repetir o incidente de 2026-05-07 dispara email para a equipe fiscal"
    - "DPY-4011 consecutivos (>= 3 em 5min) dispara alerta com link para o runbook que descreve passos de mitigação"
    - "Email de alerta inclui description, severity label e runbook_url clicável"
  artifacts:
    - path: "monitoring/prometheus/rules/fiscal.yml"
      provides: "5 regras de alerta agrupadas (bridge_critico + db + reset)"
      contains: "alert: BridgeDPY4011Consecutivos"
    - path: "monitoring/alertmanager/alertmanager.yml"
      provides: "Roteamento SMTP com group_by alertname, deduplicação, repeat_interval"
      contains: "smtp_smarthost"
    - path: "docs/runbooks/dpy4011-consecutivos.md"
      provides: "Procedimento operacional para falha Oracle DPY-4011"
      min_lines: 30
    - path: "docs/runbooks/xml-upload-falha.md"
      provides: "Procedimento para falhas de upload XML"
      min_lines: 30
    - path: "docs/runbooks/reset-banco-executado.md"
      provides: "Procedimento de auditoria após reset de banco"
      min_lines: 30
    - path: "docs/runbooks/bridge-offline.md"
      provides: "Procedimento para Bridge daemon offline (last_run > 65min)"
      min_lines: 25
    - path: "docs/runbooks/db-tamanho.md"
      provides: "Procedimento para crescimento anormal de tamanho do banco"
      min_lines: 25
    - path: "docs/runbooks/README.md"
      provides: "Índice mapeando cada alerta para seu runbook"
      contains: "BridgeDPY4011Consecutivos"
  key_links:
    - from: "monitoring/prometheus/rules/fiscal.yml"
      to: "monitoring/prometheus/prometheus.yml"
      via: "rule_files glob inclui rules/*.yml"
      pattern: "rule_files:"
    - from: "monitoring/prometheus/prometheus.yml"
      to: "monitoring/alertmanager/alertmanager.yml"
      via: "alerting.alertmanagers static_configs targets ['alertmanager:9093']"
      pattern: "alertmanager:9093"
    - from: "monitoring/alertmanager/alertmanager.yml"
      to: "SMTP env vars"
      via: "global.smtp_smarthost ${SMTP_HOST}:${SMTP_PORT}"
      pattern: "smtp_smarthost"
    - from: "monitoring/prometheus/rules/fiscal.yml"
      to: "docs/runbooks/*.md"
      via: "annotations.runbook_url"
      pattern: "runbook_url:"
    - from: "alertas"
      to: "counters do Plan 01 (bridge_dpy4011_total, xml_upload_errors_total, database_reset_total, bridge_daemon_online)"
      via: "expressões PromQL"
      pattern: "bridge_dpy4011_total|xml_upload_errors_total|database_reset_total|bridge_daemon_online"
---

<objective>
Concluir OBS-02 entregando o pipeline completo de alertas: regras Prometheus que consomem os counters expostos no Plan 01, configuração Alertmanager com roteamento SMTP, e runbooks Markdown versionados linkados aos alertas via runbook_url.

Purpose: Sem regras de alerta, os counters do Plan 01 são apenas histórico passivo. OBS-02 exige detecção em menos de 1 minuto de DPY-4011 consecutivos, falhas de upload XML e reset de banco — eventos que repetiriam o incidente de 2026-05-07. Runbooks garantem que quem receber o email saiba o que fazer.

Output: arquivo de regras Prometheus com 5 alertas, alertmanager.yml com SMTP usando as env vars já existentes, 5 runbooks Markdown + um índice README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-observabilidade-e-alertas/05-RESEARCH.md
@.planning/phases/05-observabilidade-e-alertas/05-PATTERNS.md
@.planning/phases/05-observabilidade-e-alertas/05-01-PLAN.md

<interfaces>
Métricas disponíveis (criadas no Plan 01, este plan APENAS consome):

Backend Go (api:8084/metrics):
- http_request_duration_seconds (Histogram, labels method/path/status)
- http_requests_total (Counter, labels method/path/status)
- bridge_run_errors_total (Counter, sem label) — incrementa quando PATCH /runs recebe status=error
- xml_upload_errors_total (Counter, sem label) — incrementa em rejeição de batch ou parse falho
- database_reset_total (Counter, sem label) — incrementa ao concluir ResetDatabaseHandler

Bridge Python (bridge:8086/metrics):
- bridge_runs_total (Counter, label status: success|partial|error)
- bridge_dpy4011_total (Counter, sem label)
- bridge_daemon_online (Gauge, 0 ou 1)
- bridge_last_run_timestamp_seconds (Gauge, timestamp Unix)
- bridge_docs_sent_total (Counter, label tipo)

PostgreSQL (postgres-exporter:9187/metrics):
- pg_database_size_bytes (Gauge, label datname)
- pg_stat_activity_count (Gauge, label datname)
- pg_up (Gauge, 0 ou 1)

Variáveis SMTP já disponíveis ao container alertmanager (Plan 01 Task 1):
- SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD, SMTP_FROM
- Recipient: claudio.bezerra@ferreiracosta.com.br (email do usuário, confirmado em userEmail/MEMORY)
</interfaces>

<smtp_topology>
Topologia SMTP (Open Question 2 do RESEARCH.md):

Alertmanager assume STARTTLS na porta especificada por SMTP_PORT. Configurar:
- smtp_smarthost: ${SMTP_HOST}:${SMTP_PORT}
- smtp_require_tls: true (default — funciona com STARTTLS na 587)

Se em produção o SMTP_PORT for 465 (SSL implícito), descomentar a linha smtp_hello e setar smtp_require_tls: false acompanhado de proxy SSL externo. Documentar essa condição como nota no alertmanager.yml.

Test gate: Task 3 inclui amtool check-config + envio de alerta de teste via curl POST /api/v1/alerts no Alertmanager para confirmar que o email chega antes de o plan ser considerado concluído.
</smtp_topology>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar regras Prometheus para os 5 alertas críticos</name>
  <files>monitoring/prometheus/rules/fiscal.yml, monitoring/prometheus/prometheus.yml</files>
  <read_first>
    - monitoring/prometheus/prometheus.yml (criado no Plan 01 — confirmar que rule_files: ["rules/*.yml"] está presente)
    - .planning/phases/05-observabilidade-e-alertas/05-RESEARCH.md (Pattern 4 — exemplos de alert rules e tabelas de métricas com colunas Alerta)
  </read_first>
  <action>
    Criar monitoring/prometheus/rules/fiscal.yml com 2 grupos contendo as 5 regras abaixo. Todas as regras devem ter os labels severity (critical|warning) e team ("fiscal"), e annotations summary, description, runbook_url.

    Grupo 1: bridge_critico — interval 15s. Contém:

    1. BridgeDPY4011Consecutivos
       - expr: increase(bridge_dpy4011_total[5m]) >= 3
       - for: 30s (ajustado de 1m do RESEARCH para garantir <1min total: scrape 15s + eval 15s + for 30s = 60s)
       - labels: severity=critical, team=fiscal
       - annotations.summary: "Bridge com 3 ou mais erros DPY-4011 em 5 minutos"
       - annotations.description: "{{ $value }} erros DPY-4011 detectados em 5min. Bridge pode estar perdendo conexão Oracle repetidamente."
       - annotations.runbook_url: "https://github.com/<repo>/blob/main/docs/runbooks/dpy4011-consecutivos.md" (placeholder — usar string fixa /docs/runbooks/dpy4011-consecutivos.md se o repo não for público no GitHub)

    2. BridgeOffline
       - expr: time() - bridge_last_run_timestamp_seconds > 3900 (65 minutos — bridge roda a cada 60min então tolerância de 5min)
       - for: 0s (a métrica já reflete o estado real; for não acumula tempo extra)
       - labels: severity=critical, team=fiscal
       - annotations.summary: "Bridge não executou nenhum run há mais de 65 minutos"
       - annotations.description: "Último run em {{ $value | humanizeDuration }} atrás. Bridge daemon pode estar travado ou crashed."
       - annotations.runbook_url: "/docs/runbooks/bridge-offline.md"

    3. BridgeDaemonDown
       - expr: bridge_daemon_online == 0 OR up{job="fb_apu04_bridge"} == 0
       - for: 1m (evitar falso positivo em redeploy curto)
       - labels: severity=critical, team=fiscal
       - annotations.summary: "Bridge daemon offline"
       - annotations.description: "Métricas do bridge não acessíveis ou flag bridge_daemon_online == 0"
       - annotations.runbook_url: "/docs/runbooks/bridge-offline.md"

    Grupo 2: ops_critico — interval 15s. Contém:

    4. XMLUploadFalha
       - expr: increase(xml_upload_errors_total[5m]) > 0
       - for: 0s (qualquer erro de upload XML é evento de interesse imediato)
       - labels: severity=warning, team=fiscal
       - annotations.summary: "Upload XML rejeitado ou com erro de parse"
       - annotations.description: "{{ $value }} uploads XML falharam nos últimos 5min"
       - annotations.runbook_url: "/docs/runbooks/xml-upload-falha.md"

    5. ResetBancoExecutado
       - expr: increase(database_reset_total[5m]) > 0
       - for: 0s (reset é evento crítico imediato — repete cenário de 2026-05-07)
       - labels: severity=critical, team=fiscal
       - annotations.summary: "ResetDatabase executado"
       - annotations.description: "{{ $value }} reset(s) de banco executado(s) nos últimos 5min. Verificar admin_destructive_actions imediatamente."
       - annotations.runbook_url: "/docs/runbooks/reset-banco-executado.md"

    6. DBTamanhoAlto (grupo db_critico, interval 1m)
       - expr: pg_database_size_bytes{datname="fb_apu04"} > 50 * 1024 * 1024 * 1024 (50 GB — ajustar conforme growth rate observado)
       - for: 5m (evitar falso positivo em bulk import temporário)
       - labels: severity=warning, team=fiscal
       - annotations.summary: "Banco fb_apu04 com mais de 50 GB"
       - annotations.description: "Tamanho atual: {{ $value | humanize1024 }}B. Avaliar vacuum, archive ou cleanup."
       - annotations.runbook_url: "/docs/runbooks/db-tamanho.md"

    Confirmar que monitoring/prometheus/prometheus.yml já contém rule_files: ["rules/*.yml"] (criado no Plan 01). Se não estiver, adicionar.

    Os 5 alertas listados acima (regras 1-5) são os alertas críticos exigidos por OBS-02. A regra 6 (DBTamanhoAlto) é warning adicional alinhada ao success criterion "tamanho DB" de OBS-01.

    Cada regra deve ser validável via promtool check rules monitoring/prometheus/rules/fiscal.yml.
  </action>
  <verify>
    <automated>python3 -c "import yaml; doc=yaml.safe_load(open('monitoring/prometheus/rules/fiscal.yml')); assert 'groups' in doc; assert sum(len(g.get('rules',[])) for g in doc['groups']) >= 5" &amp;&amp; docker run --rm -v $(pwd)/monitoring/prometheus:/etc/prometheus --entrypoint promtool prom/prometheus:v2.55.1 check rules /etc/prometheus/rules/fiscal.yml &amp;&amp; grep -q "rule_files:" monitoring/prometheus/prometheus.yml</automated>
  </verify>
  <acceptance_criteria>
    - python3 -c "import yaml; yaml.safe_load(open('monitoring/prometheus/rules/fiscal.yml'))" retorna exit 0
    - promtool check rules monitoring/prometheus/rules/fiscal.yml (via docker run) retorna "SUCCESS: 6 rules found" ou equivalente >= 5
    - grep -c "alert:" monitoring/prometheus/rules/fiscal.yml >= 5
    - grep -c "BridgeDPY4011Consecutivos\|BridgeOffline\|BridgeDaemonDown\|XMLUploadFalha\|ResetBancoExecutado" monitoring/prometheus/rules/fiscal.yml >= 5
    - grep -c "runbook_url:" monitoring/prometheus/rules/fiscal.yml >= 5 (cada alerta com link de runbook)
    - grep -c "severity:" monitoring/prometheus/rules/fiscal.yml >= 5
    - grep -c "team: fiscal\|team: \"fiscal\"" monitoring/prometheus/rules/fiscal.yml >= 5
    - ResetBancoExecutado tem "for: 0s" ou "for: 0m" verificável via grep -A2 "alert: ResetBancoExecutado" monitoring/prometheus/rules/fiscal.yml | grep "for: 0"
    - BridgeDPY4011Consecutivos tem expr usando increase(bridge_dpy4011_total[5m])
    - XMLUploadFalha tem expr usando xml_upload_errors_total
    - Cada regra referencia uma das métricas declaradas em interfaces (bridge_dpy4011_total, xml_upload_errors_total, database_reset_total, bridge_daemon_online, bridge_last_run_timestamp_seconds, pg_database_size_bytes)
    - Behavior: ao recarregar Prometheus (curl -X POST http://localhost:9090/-/reload com web.enable-lifecycle), as 5+ regras aparecem em http://localhost:9090/rules sem erros de parsing
  </acceptance_criteria>
  <done>5 alertas operacionais validados por promtool, todos linkados a runbooks, latência total de detecção menor ou igual a 60s para os critical (DPY-4011, Reset, BridgeOffline).</done>
</task>

<task type="auto">
  <name>Task 2: Configurar Alertmanager com roteamento SMTP usando env vars existentes</name>
  <files>monitoring/alertmanager/alertmanager.yml, docker-compose.yml, docker-compose.prod.yml</files>
  <read_first>
    - monitoring/alertmanager/alertmanager.yml (stub criado no Plan 01 Task 1)
    - docker-compose.yml (linhas do serviço alertmanager — confirmar que as env vars SMTP_* estão sendo passadas)
    - backend/services/email.go (padrão de como o backend já usa SMTP — manter mesma porta e tipo TLS)
    - .planning/phases/05-observabilidade-e-alertas/05-RESEARCH.md Pattern 6 e Pitfall 4
  </read_first>
  <action>
    Substituir o stub monitoring/alertmanager/alertmanager.yml por configuração completa:

    1. Bloco global:
       - smtp_smarthost: ${SMTP_HOST}:${SMTP_PORT}
       - smtp_from: ${SMTP_FROM}
       - smtp_auth_username: ${SMTP_USER}
       - smtp_auth_password: ${SMTP_PASSWORD}
       - smtp_require_tls: true (default; comentário no arquivo explicando que setar para false se SMTP usar SSL implícito na 465)
       - resolve_timeout: 5m
       Nota inline: Alertmanager substitui ${VAR} apenas se foi compilado com --enable-feature=expand-external-labels OU se as vars forem passadas no command line. Como o Alertmanager v0.27 NÃO expande env vars no YAML por padrão, usar uma abordagem alternativa: o docker-compose deve passar as variáveis e o alertmanager.yml deve referenciar os valores LITERAIS via templates de envsubst no entrypoint OU usar a feature receiver template. Solução pragmática: criar um wrapper de entrypoint no docker-compose ou usar gomplate. Para simplicidade, adotar a abordagem: no docker-compose service alertmanager, sobrescrever entrypoint com sh -c "envsubst < /etc/alertmanager/alertmanager.yml.tpl > /tmp/alertmanager.yml && /bin/alertmanager --config.file=/tmp/alertmanager.yml --storage.path=/alertmanager" e renomear o arquivo para alertmanager.yml.tpl. Atualizar o command/entrypoint do serviço alertmanager em docker-compose.yml e docker-compose.prod.yml conforme essa decisão. Adicionar comentário no topo do arquivo .yml.tpl explicando o mecanismo.

    2. Bloco route:
       - group_by: ["alertname", "severity"]
       - group_wait: 10s (rápido para critical; 30s seria conservador demais para Reset)
       - group_interval: 5m
       - repeat_interval: 4h
       - receiver: "fiscal-team-default"
       - routes (sub-roteamento):
         - match: { severity: critical }
           receiver: "fiscal-team-critical"
           group_wait: 5s (ainda mais rápido para critical)
           repeat_interval: 1h
         - match: { severity: warning }
           receiver: "fiscal-team-warning"
           repeat_interval: 12h

    3. Bloco receivers (3 receivers, todos email_configs):
       - name: fiscal-team-default
         email_configs: [{ to: "claudio.bezerra@ferreiracosta.com.br", send_resolved: true, headers: { Subject: "[FB_APU04][{{ .GroupLabels.severity }}] {{ .GroupLabels.alertname }}" }, html: "<h2>{{ .GroupLabels.alertname }}</h2>{{ range .Alerts }}<p><b>{{ .Annotations.summary }}</b></p><p>{{ .Annotations.description }}</p><p>Runbook: <a href='{{ .Annotations.runbook_url }}'>{{ .Annotations.runbook_url }}</a></p>{{ end }}" }]
       - name: fiscal-team-critical
         email_configs: [{ to: "claudio.bezerra@ferreiracosta.com.br", send_resolved: true, headers: { Subject: "[FB_APU04][CRITICAL] {{ .GroupLabels.alertname }}" }, html: "<h1 style='color:red'>CRITICAL: {{ .GroupLabels.alertname }}</h1>{{ range .Alerts }}<p><b>{{ .Annotations.summary }}</b></p><p>{{ .Annotations.description }}</p><p><a href='{{ .Annotations.runbook_url }}'>RUNBOOK</a></p>{{ end }}" }]
       - name: fiscal-team-warning
         email_configs: [{ to: "claudio.bezerra@ferreiracosta.com.br", send_resolved: false, headers: { Subject: "[FB_APU04][WARNING] {{ .GroupLabels.alertname }}" }, html: "<h2>WARNING: {{ .GroupLabels.alertname }}</h2>{{ range .Alerts }}<p>{{ .Annotations.description }}</p><p><a href='{{ .Annotations.runbook_url }}'>Runbook</a></p>{{ end }}" }]

    4. Bloco inhibit_rules (opcional mas recomendado):
       - source_match: { severity: critical, alertname: BridgeDaemonDown }
         target_match: { severity: critical, alertname: BridgeDPY4011Consecutivos }
         equal: ["team"]
       Justificativa: se o bridge daemon estiver completamente down, suprimir alertas de DPY-4011 (causa raiz é o daemon, não a conexão Oracle).

    Validar via docker run --rm prom/alertmanager:v0.27.0 amtool check-config /etc/alertmanager/alertmanager.yml após renderizar o template com envsubst usando valores dummy.

    Decisão de TLS: assumir STARTTLS via porta 587 (padrão do backend services/email.go). Se ambiente real usar 465, deixar nota inline para futura correção.
  </action>
  <verify>
    <automated>(test -f monitoring/alertmanager/alertmanager.yml || test -f monitoring/alertmanager/alertmanager.yml.tpl) &amp;&amp; SMTP_HOST=smtp.test.com SMTP_PORT=587 SMTP_USER=test SMTP_PASSWORD=test SMTP_FROM=test@test.com envsubst &lt; monitoring/alertmanager/alertmanager.yml.tpl &gt; /tmp/am-test.yml &amp;&amp; docker run --rm -v /tmp/am-test.yml:/tmp/am-test.yml --entrypoint amtool prom/alertmanager:v0.27.0 check-config /tmp/am-test.yml &amp;&amp; grep -q "smtp_smarthost" monitoring/alertmanager/alertmanager.yml.tpl &amp;&amp; grep -q "fiscal-team-critical" monitoring/alertmanager/alertmanager.yml.tpl &amp;&amp; grep -q "runbook_url" monitoring/alertmanager/alertmanager.yml.tpl</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo monitoring/alertmanager/alertmanager.yml.tpl existe com sintaxe YAML válida após envsubst
    - amtool check-config sobre o YAML renderizado retorna "SUCCESS"
    - grep -c "receiver:" monitoring/alertmanager/alertmanager.yml.tpl >= 4 (default route + 3 receivers explícitos)
    - grep -c "email_configs:" monitoring/alertmanager/alertmanager.yml.tpl >= 3 (um por receiver)
    - grep -c "claudio.bezerra@ferreiracosta.com.br" monitoring/alertmanager/alertmanager.yml.tpl >= 3
    - grep -q "smtp_smarthost: \${SMTP_HOST}:\${SMTP_PORT}" monitoring/alertmanager/alertmanager.yml.tpl OR grep -q 'smtp_smarthost: ${SMTP_HOST}' monitoring/alertmanager/alertmanager.yml.tpl
    - grep -q "match:" monitoring/alertmanager/alertmanager.yml.tpl verdadeiro (sub-rotas critical/warning)
    - grep -q "repeat_interval: 1h" monitoring/alertmanager/alertmanager.yml.tpl verdadeiro (critical mais frequente)
    - grep -q "inhibit_rules:" monitoring/alertmanager/alertmanager.yml.tpl verdadeiro (regra BridgeDaemonDown inibe DPY-4011)
    - docker-compose.yml e docker-compose.prod.yml têm entrypoint customizado do serviço alertmanager usando envsubst + alertmanager.yml.tpl (verificável via grep -A5 "alertmanager:" docker-compose.yml | grep "envsubst")
    - Behavior: subindo o stack via docker compose up -d alertmanager (com SMTP_* setados no .env de teste), curl -s http://localhost:9093/-/healthy retorna OK e os logs do container não contêm "ERROR loading config" nem "no such variable"
  </acceptance_criteria>
  <done>Alertmanager configurado com 3 receivers (default + critical + warning), SMTP usando ${SMTP_*} via envsubst, inhibit rule para BridgeDaemonDown, validado por amtool e por boot real do container.</done>
</task>

<task type="auto">
  <name>Task 3: Escrever 5 runbooks + índice README em docs/runbooks/</name>
  <files>docs/runbooks/README.md, docs/runbooks/dpy4011-consecutivos.md, docs/runbooks/xml-upload-falha.md, docs/runbooks/reset-banco-executado.md, docs/runbooks/bridge-offline.md, docs/runbooks/db-tamanho.md</files>
  <read_first>
    - monitoring/prometheus/rules/fiscal.yml (criado na Task 1 — confirmar que cada alerta tem runbook_url que mapeia para os arquivos abaixo)
    - .planning/phases/05-observabilidade-e-alertas/05-RESEARCH.md (seção Runbooks)
    - backend/handlers/admin.go (procurar como ResetDatabaseHandler grava em admin_destructive_actions — info necessária para o runbook reset-banco-executado)
    - erp-bridge-aws/bridge.py (procurar onde DPY-4011 é detectado via _is_dpy4011 — info para o runbook DPY-4011)
  </read_first>
  <action>
    Criar 6 arquivos Markdown em docs/runbooks/. Cada runbook deve seguir esta estrutura mínima (substituindo placeholders):

    Estrutura padrão de cada runbook (mínimo 30 linhas para os 3 critical, 25 para os 2 warning):
    - Título: # Runbook: [Nome do Alerta]
    - Severidade: critical ou warning
    - Dispara quando: expressão PromQL exata da regra
    - Detectado em: tempo total (scrape + eval + for)
    - Sintomas: 3-5 bullets do que o operador vê
    - Causa Mais Provável: lista ordenada das hipóteses
    - Passos de Mitigação: lista numerada de ações concretas COM comandos
    - Verificação Pós-Mitigação: comandos para confirmar fix
    - Escalar Para: nome ou email
    - Histórico: link para incidentes passados (vazio inicialmente exceto reset-banco que referencia 2026-05-07)

    Conteúdos específicos:

    docs/runbooks/README.md — Índice:
    - Tabela mapeando Alerta -> Severidade -> Runbook -> Métrica
    - Linhas para os 5 alertas + nota sobre como adicionar novos
    - Link para http://localhost:9090/alerts (Prometheus) e http://localhost:9093 (Alertmanager)

    docs/runbooks/dpy4011-consecutivos.md (>= 30 linhas):
    - Dispara quando: increase(bridge_dpy4011_total[5m]) >= 3
    - Sintomas: emails de alerta, logs do bridge mostrando "ORA-DPY-4011" ou "DPY-4011", runs com status=error em /api/erp-bridge/runs
    - Causa: rede Oracle instável, listener restart, firewall reset, credenciais expiradas
    - Passos: (1) docker compose logs --tail 200 bridge | grep DPY-4011; (2) ping oracle host; (3) sqlplus user/pass@host:port/service para testar conexão; (4) verificar tnsnames.ora; (5) se persistir, restart do container bridge: docker compose restart bridge; (6) confirmar que retry/reconnect (STAB-09) está ativo
    - Verificação: bridge_dpy4011_total deve parar de crescer; próximo run deve retornar status=success

    docs/runbooks/xml-upload-falha.md (>= 30 linhas):
    - Dispara quando: increase(xml_upload_errors_total[5m]) > 0
    - Sintomas: usuário reporta upload rejeitado; xml_upload_batches.status = "rejeitado" ou "parcial"
    - Causa: ZIP corrompido, NF-e fora do schema v4.00, arquivo > 100MB ou > 5000 XMLs, NCM não cadastrado
    - Passos: (1) SELECT id, file_name, error_msg FROM xml_upload_batches WHERE created_at > NOW() - INTERVAL '1 hour' AND status != 'success' ORDER BY created_at DESC LIMIT 20; (2) inspecionar error_msg do batch mais recente; (3) se schema invalido, validar XML manualmente; (4) se arquivo muito grande, orientar usuario a dividir o ZIP
    - Verificação: novo upload do mesmo arquivo (depois de corrigido) marca status=success

    docs/runbooks/reset-banco-executado.md (>= 30 linhas):
    - Dispara quando: increase(database_reset_total[5m]) > 0 (for: 0s — imediato)
    - **AÇÃO IMEDIATA:** Verificar admin_destructive_actions
    - Histórico: REFERENCIAR INCIDENTE 2026-05-07 (perda de 4 meses de produção APU02). Esse alerta existe especificamente para detectar a repetição desse cenário em <1min
    - Sintomas: email critical chega; tabelas zeradas; usuário reporta "perdi dados"
    - Causa: execução intencional de ResetDatabaseHandler (5 gates passados); deve haver registro em admin_destructive_actions
    - Passos: (1) SELECT * FROM admin_destructive_actions ORDER BY executed_at DESC LIMIT 1; (2) confirmar usuario, ip, scope; (3) se NÃO autorizado: rotacionar JWT secret imediatamente, revogar tokens, ler audit completo; (4) restaurar de /backups/reset-{timestamp}.sql; (5) abrir post-mortem
    - Verificação: contagens de tabelas restauradas batem com snapshot pré-reset

    docs/runbooks/bridge-offline.md (>= 25 linhas):
    - Dispara quando: time() - bridge_last_run_timestamp_seconds > 3900 OU bridge_daemon_online == 0
    - Sintomas: dashboard Bridge mostra "Daemon offline", sem novos registros em erp_bridge_runs
    - Causa: container parou, conexao Oracle perdida sem retry, kill manual
    - Passos: (1) docker compose ps bridge; (2) docker compose logs --tail 100 bridge; (3) docker compose restart bridge; (4) confirmar /api/erp-bridge/heartbeat sendo chamado
    - Verificação: bridge_daemon_online == 1 e bridge_last_run_timestamp_seconds atualiza no próximo ciclo

    docs/runbooks/db-tamanho.md (>= 25 linhas):
    - Dispara quando: pg_database_size_bytes{datname="fb_apu04"} > 50 GB
    - Sintomas: warning email; lentidão em queries; backups demorados
    - Causa: bulk import sem cleanup, materialized views sem REFRESH, bloat de tabelas
    - Passos: (1) SELECT pg_size_pretty(pg_database_size('fb_apu04')); (2) listar top tables: SELECT schemaname, tablename, pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) FROM pg_tables WHERE schemaname='public' ORDER BY pg_total_relation_size(schemaname||'.'||tablename) DESC LIMIT 10; (3) VACUUM FULL nas top tables fora do horário fiscal; (4) avaliar particionamento ou archive de dados antigos
    - Verificação: pg_database_size_bytes baixa após VACUUM e dashboard reflete

    Todos os runbooks devem estar em pt-BR (response_language: pt-BR).
  </action>
  <verify>
    <automated>test -f docs/runbooks/README.md &amp;&amp; test -f docs/runbooks/dpy4011-consecutivos.md &amp;&amp; test -f docs/runbooks/xml-upload-falha.md &amp;&amp; test -f docs/runbooks/reset-banco-executado.md &amp;&amp; test -f docs/runbooks/bridge-offline.md &amp;&amp; test -f docs/runbooks/db-tamanho.md &amp;&amp; test $(wc -l < docs/runbooks/dpy4011-consecutivos.md) -ge 30 &amp;&amp; test $(wc -l < docs/runbooks/reset-banco-executado.md) -ge 30 &amp;&amp; test $(wc -l < docs/runbooks/xml-upload-falha.md) -ge 30 &amp;&amp; test $(wc -l < docs/runbooks/bridge-offline.md) -ge 25 &amp;&amp; test $(wc -l < docs/runbooks/db-tamanho.md) -ge 25 &amp;&amp; grep -q "2026-05-07" docs/runbooks/reset-banco-executado.md</automated>
  </verify>
  <acceptance_criteria>
    - Os 6 arquivos existem em docs/runbooks/
    - docs/runbooks/dpy4011-consecutivos.md tem >= 30 linhas e contém os tokens "DPY-4011", "Passos de Mitigação" (ou "Mitigação"), "docker compose"
    - docs/runbooks/xml-upload-falha.md tem >= 30 linhas e contém "xml_upload_batches", "v4.00" ou "schema"
    - docs/runbooks/reset-banco-executado.md tem >= 30 linhas e contém "2026-05-07", "admin_destructive_actions", "/backups/reset-"
    - docs/runbooks/bridge-offline.md tem >= 25 linhas e contém "bridge_daemon_online", "docker compose restart"
    - docs/runbooks/db-tamanho.md tem >= 25 linhas e contém "pg_database_size_bytes", "VACUUM"
    - docs/runbooks/README.md contém os 5 nomes de alerta como linhas/links de tabela
    - Cada runbook tem seção "Sintomas", "Causa", "Passos" (case-insensitive)
    - grep -L "claude\|gerado por" docs/runbooks/*.md retorna 6 arquivos (nenhum runbook contém atribuição automática indesejada — runbooks são propriedade da equipe)
    - Cada referência runbook_url em monitoring/prometheus/rules/fiscal.yml aponta para um arquivo existente em docs/runbooks/ (verificável via shell: for url in $(grep -oP 'runbook_url:.*\K/docs/runbooks/[^"]+' monitoring/prometheus/rules/fiscal.yml); do test -f ".${url}"; done)
  </acceptance_criteria>
  <done>5 runbooks + README criados em docs/runbooks/, cada um com Sintomas/Causa/Passos/Verificação concretos, todos linkados pelas regras Prometheus via runbook_url, runbook de reset referencia explicitamente o incidente 2026-05-07.</done>
</task>

<task type="auto">
  <name>Task 4: Validação end-to-end — injetar alerta de teste e confirmar entrega SMTP</name>
  <files>monitoring/alertmanager/alertmanager.yml.tpl (sem mudanças — apenas verificação)</files>
  <read_first>
    - monitoring/prometheus/rules/fiscal.yml (regras finalizadas na Task 1)
    - monitoring/alertmanager/alertmanager.yml.tpl (config finalizada na Task 2)
    - docs/runbooks/*.md (criados na Task 3)
    - docker-compose.yml serviços alertmanager + prometheus
  </read_first>
  <action>
    Verificação end-to-end do pipeline Plan 01 + Plan 02 sem necessidade de aguardar evento real. Esta task NÃO cria nem modifica arquivos novos — apenas executa os comandos de validação e captura output para o SUMMARY.

    Passos a executar e documentar:

    1. Subir o stack completo de monitoramento (assumindo SMTP_* setados no .env de teste com smtp.mailtrap.io ou similar para captura sem entregar email real):
       docker compose up -d prometheus alertmanager grafana postgres-exporter
       docker compose logs --tail 30 alertmanager | grep -i "loaded\|ready" — confirma config carregou

    2. Listar targets Prometheus:
       curl -s http://localhost:9090/api/v1/targets | jq ".data.activeTargets[] | {job: .labels.job, health: .health}"
       Esperado: 4 jobs (fb_apu04_api, fb_apu04_bridge, fb_apu04_postgres, prometheus_self) com health "up" (bridge pode estar "down" se não estiver rodando localmente — aceitável neste smoke test, documentar no SUMMARY)

    3. Confirmar regras carregadas:
       curl -s http://localhost:9090/api/v1/rules | jq ".data.groups[].rules[] | .name" deve listar BridgeDPY4011Consecutivos, BridgeOffline, BridgeDaemonDown, XMLUploadFalha, ResetBancoExecutado, DBTamanhoAlto

    4. Injetar alerta de teste DIRETAMENTE no Alertmanager (não depende do Prometheus disparar):
       curl -s -X POST http://localhost:9093/api/v2/alerts -H "Content-Type: application/json" -d '[{"labels":{"alertname":"TesteSMOKEPlan02","severity":"critical","team":"fiscal"},"annotations":{"summary":"Teste de pipeline SMTP","description":"Alerta sintetico para validar entrega de email","runbook_url":"/docs/runbooks/dpy4011-consecutivos.md"},"startsAt":"'$(date -u +%Y-%m-%dT%H:%M:%S.000Z)'"}]'
       Esperado: HTTP 200 vazio

    5. Confirmar alerta visível no Alertmanager:
       curl -s http://localhost:9093/api/v2/alerts | jq ".[] | select(.labels.alertname==\"TesteSMOKEPlan02\")" — esperar 1 entrada

    6. Verificar logs do Alertmanager para entrega SMTP:
       docker compose logs --tail 50 alertmanager | grep -i "notif\|smtp"
       Esperado: linha de sucesso ou linha de erro detalhado (sem credenciais)

    7. Resolver o alerta de teste:
       curl -s -X POST http://localhost:9093/api/v2/alerts -H "Content-Type: application/json" -d '[{"labels":{"alertname":"TesteSMOKEPlan02","severity":"critical","team":"fiscal"},"startsAt":"'$(date -u -d "5 minutes ago" +%Y-%m-%dT%H:%M:%S.000Z)'","endsAt":"'$(date -u +%Y-%m-%dT%H:%M:%S.000Z)'"}]'

    8. Documentar no SUMMARY:
       - Output do curl /api/v1/targets (jobs e health)
       - Output do curl /api/v1/rules (lista de alertas)
       - Confirmação de que o POST no Alertmanager retornou 200
       - Linha do log SMTP (sucesso OU motivo de falha)
       - Tempo total entre POST e log de notif (deve ser < 30s sob group_wait 5s para critical)

    Critério de aceitação: se SMTP estiver indisponível no ambiente de teste (mailtrap não configurado), o teste considera-se PARCIAL — Alertmanager aceita o alerta e tenta entregar (log mostra erro de conexão SMTP). Isso é aceitável e documentado. O que NÃO é aceitável: Alertmanager retornar erro de config OU não aceitar o POST.
  </action>
  <verify>
    <automated>docker compose up -d prometheus alertmanager grafana postgres-exporter 2>/dev/null; i=0; until curl -fs http://localhost:9093/-/ready; do i=$((i+1)); if [ $i -ge 30 ]; then echo "Alertmanager nao ficou pronto em 30s" &amp;&amp; exit 1; fi; sleep 1; done; curl -fsS http://localhost:9090/-/ready &amp;&amp; curl -fsS http://localhost:9093/-/ready &amp;&amp; test $(curl -s http://localhost:9090/api/v1/rules | jq -r '.data.groups[].rules[].name' | wc -l) -ge 5 &amp;&amp; curl -s -X POST http://localhost:9093/api/v2/alerts -H "Content-Type: application/json" -d "[{\"labels\":{\"alertname\":\"TesteSMOKEPlan02\",\"severity\":\"critical\",\"team\":\"fiscal\"},\"annotations\":{\"summary\":\"smoke\",\"description\":\"smoke\",\"runbook_url\":\"/docs/runbooks/dpy4011-consecutivos.md\"}}]" &amp;&amp; sleep 3 &amp;&amp; test $(curl -s http://localhost:9093/api/v2/alerts | jq "[.[] | select(.labels.alertname==\"TesteSMOKEPlan02\")] | length") -ge 1 &amp;&amp; docker compose logs alertmanager 2&gt;&amp;1 | grep -v "level=error"</automated>
  </verify>
  <acceptance_criteria>
    - curl -fsS http://localhost:9090/-/ready retorna exit 0 (Prometheus pronto)
    - curl -fsS http://localhost:9093/-/ready retorna exit 0 (Alertmanager pronto)
    - curl -s http://localhost:9090/api/v1/rules | jq -r ".data.groups[].rules[].name" lista pelo menos 5 nomes de alerta correspondentes aos da Task 1
    - POST /api/v2/alerts retorna exit 0 (Alertmanager aceitou o alerta)
    - GET /api/v2/alerts encontra o alerta TesteSMOKEPlan02
    - docker compose logs alertmanager NÃO contém "level=error" relacionado a config parsing
    - SUMMARY do plan documenta: (a) lista de jobs/health de Prometheus, (b) lista de regras carregadas, (c) latência entre POST alert e tentativa de notificação SMTP, (d) status da entrega SMTP (sucesso/falha-com-motivo)
    - Behavior: matar o stack após verificação (docker compose down) deixa o ambiente limpo — nenhum container de monitoramento órfão
  </acceptance_criteria>
  <done>Pipeline end-to-end validado: Prometheus carrega regras, Alertmanager aceita alertas, tentativa de entrega SMTP registrada nos logs. Plan 02 entrega OBS-02 com evidência operacional documentada no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alertmanager → SMTP relay externo | Saída de email pode vazar conteúdo de alertas se logada/interceptada |
| Prometheus → Alertmanager | Tráfego interno fb_net; alerta payload contém labels e annotations |
| Email → caixa do destinatário | Conteúdo de alertas chega em texto/html — pode conter dados operacionais |
| Endpoint POST /api/v2/alerts do Alertmanager | Sem autenticação na rede fb_net — qualquer container pode injetar alertas falsos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | Email de alerta com description detalhado | accept | Conteúdo de alerta contém apenas agregados (counts, severities, alertnames). Não inclui dados de NF-e, CPF ou valores tributários. Destinatário fixo: claudio.bezerra@ferreiracosta.com.br |
| T-05-02-02 | Spoofing | Endpoint POST /api/v2/alerts do Alertmanager | mitigate | Alertmanager fica na rede fb_net apenas (expose, não ports). Containers externos não atingem 9093. Em prod, Traefik NÃO roteia para 9093 |
| T-05-02-03 | Denial of Service | Flood de alertas estoura caixa de email | mitigate | group_by alertname + severity com repeat_interval 1h (critical) e 12h (warning) limita volume. inhibit_rules suprime alerta correlato (BridgeDaemonDown inibe DPY-4011) |
| T-05-02-04 | Information Disclosure | Credenciais SMTP no alertmanager.yml | mitigate | Usar envsubst com ${SMTP_*} renderizado em runtime do arquivo .tpl; valores reais vêm do env do container (Coolify secret). Arquivo versionado NÃO contém senhas |
| T-05-02-05 | Tampering | Operador edita regras manualmente em /etc/prometheus/rules/ no container | mitigate | Volume bind-mount com :ro (read-only) garantido no docker-compose; única forma de mudar regras é commit + redeploy |
| T-05-02-06 | Repudiation | Alguém recebe email e nega ter sido alertado | accept | Alertmanager mantém log de notificação enviada via docker logs; ELK/Loki opcional para retenção longa (FUTURO em RESEARCH.md) |
| T-05-02-07 | Information Disclosure | Runbook reset-banco-executado.md menciona /backups/reset-*.sql | accept | Caminho do backup já documentado em STAB-02 (Phase 1). Runbook é interno do repo; não exposto publicamente |
</threat_model>

<verification>
Pós-execução de todas as 4 tasks:

1. promtool check rules monitoring/prometheus/rules/fiscal.yml (>= 5 regras, todas com runbook_url)
2. amtool check-config (com envsubst renderizado) sobre monitoring/alertmanager/alertmanager.yml.tpl
3. docker compose up -d prometheus alertmanager + curl /-/ready em ambos
4. curl /api/v1/rules conta >= 5 alertas
5. POST /api/v2/alerts com payload de teste retorna 200 e o alerta aparece no GET
6. for each url in $(grep runbook_url monitoring/prometheus/rules/fiscal.yml | grep -oP '/docs/runbooks/\S+'); test -f ".${url}" — todos os 5 runbooks referenciados existem
7. Cada runbook crítico tem >= 30 linhas com seções Sintomas/Causa/Passos
8. reset-banco-executado.md cita 2026-05-07 explicitamente
9. docker compose logs alertmanager sem ERROR de config
10. Latência medida no SUMMARY: POST alert → log de notif SMTP em <30s
</verification>

<success_criteria>
- 5 alertas críticos definidos em fiscal.yml validados por promtool
- Cada alerta linkado a um runbook existente via runbook_url
- Alertmanager configurado com 3 receivers, sub-rotas por severity, inhibit_rules para correlação BridgeDaemonDown ↔ DPY-4011
- SMTP usa as 5 env vars ${SMTP_*} já existentes no stack, sem duplicação de credenciais
- 5 runbooks Markdown em pt-BR com Sintomas/Causa/Passos concretos (comandos executáveis, não prosa abstrata)
- Runbook reset-banco-executado.md referencia o incidente 2026-05-07 explicitamente
- Pipeline end-to-end validado: POST sintético em /api/v2/alerts gera tentativa de notificação SMTP em <30s
- Plan entrega OBS-02 e completa os 4 success criteria da Phase 5 (dashboards do Plan 01 + alertas <1min + runbooks + acesso fiscal sem SSH)
</success_criteria>

<output>
Após completion criar .planning/phases/05-observabilidade-e-alertas/05-02-SUMMARY.md incluindo:
- Output dos comandos de validação da Task 4 (targets, regras, POST alert, log SMTP)
- Status final dos 4 success criteria da Phase 5 (todos true)
- Nota sobre o bug pendente CR-01 (xml_conciliacao.go IPI) — Phase 5 NÃO o resolve; gap closure separado
</output>
