---
phase: 11-motor-de-execu-o-do-pacote-fiscal-backend
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/handlers/fiscal_group_lookup.go
  - backend/migrations/147_fiscal_execution_items.sql
autonomous: true
requirements: [TPF-01, TPF-04]
must_haves:
  truths:
    - "Dado o código de produto + cod_empresa resolvido pelo CNPJ raiz do emitente, o sistema resolve grupo_fiscal/origem/ncm via Oracle prod/PRODB"
    - "Filial não mapeada (fora de Recife/PE) retorna erro explícito, nunca cod_empresa adivinhado"
    - "Produto ausente em prod/PRODB retorna erro não-fatal (status sem_grupo_fiscal), nunca derruba o lote"
    - "Existe a tabela fiscal_execution_items com status por item, colunas típicas indexáveis e full_result JSONB com os ~88 campos"
  artifacts:
    - path: "backend/handlers/fiscal_group_lookup.go"
      provides: "resolveCodEmpresa + lookupGrupoFiscal + mapa codEmpresaPorCNPJRaiz"
      contains: "func lookupGrupoFiscal"
    - path: "backend/migrations/147_fiscal_execution_items.sql"
      provides: "tabela fiscal_execution_items (schema híbrido + colunas IBS/CBS)"
      contains: "CREATE TABLE IF NOT EXISTS fiscal_execution_items"
  key_links:
    - from: "lookupGrupoFiscal"
      to: "Oracle prod/PRODB"
      via: "SELECT com sql.Named (:codigoProduto, :codEmpresa)"
      pattern: "sql.Named"
    - from: "fiscal_execution_items.nfe_item_id"
      to: "nfe_saidas_itens(id)"
      via: "FK ON DELETE CASCADE + UNIQUE(nfe_item_id)"
      pattern: "REFERENCES nfe_saidas_itens"
---

<objective>
Cobrir TPF-01 (lookup de grupo fiscal via Oracle) e TPF-04 (tabela
`fiscal_execution_items`). Ambos os artefatos são portados verbatim do
FB_TESTESFC e independem do driver go-ora (usam apenas database/sql /
DDL puro), então rodam em paralelo na Wave 1.

Purpose: Entregar (a) a resolução de grupo fiscal por item com o gap conhecido
de cod_empresa tratado como erro explícito (nunca adivinhado), e (b) o destino
de persistência dos ~88 campos calculados no modelo híbrido validado
(colunas típicas indexáveis + full_result JSONB), acrescido das colunas
IBS/CBS que a Fase 12 vai precisar.
Output: backend/handlers/fiscal_group_lookup.go + migration 147.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-CONTEXT.md
@.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-RESEARCH.md
@.planning/.continue-here.md

<interfaces>
<!-- Contratos e fatos confirmados no codebase FB_APU04. -->

Helper já existente (mesmo package handlers — chamada direta):
  func onlyDigits(s string) string   // handlers/icms_fronteira_prodepe.go:116

Query de lookup JÁ VALIDADA contra Oracle real (ver .continue-here.md secao 2 e 11-RESEARCH.md):
  SELECT pb.grupo_fiscal, p.especial AS origem, p.ncm
  FROM prodb pb, prod p
  WHERE p.codigo = pb.codigo AND pb.codigo = :codigoProduto AND pb.cod_empresa = :codEmpresa

Mapa de cod_empresa (unica raiz confirmada): "10230480" => 2 (Ferreira Costa Recife/PE).
sql.ErrNoRows do lookup => errSemGrupoFiscal (NAO fatal -> item vira status "sem_grupo_fiscal").

Tipos de coluna no FB_APU04: companies.id = UUID; nfe_saidas_itens.id = UUID; UNIQUE(nfe_id, n_item) ja existe em nfe_saidas_itens.
Esquema hibrido validado: FB_TESTESFC backend/migrations/008_fiscal_execution_items.sql (11 colunas tipicas + full_result JSONB).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portar fiscal_group_lookup.go (resolveCodEmpresa + lookupGrupoFiscal)</name>
  <files>backend/handlers/fiscal_group_lookup.go</files>
  <read_first>
    - /home/claudiobezerra/projetos/FB_TESTESFC/backend/handlers/fiscal_group_lookup.go  (arquivo original completo — porte verbatim; se apagado, usar 11-RESEARCH.md "Grupo fiscal lookup (TPF-01)" + .planning/.continue-here.md secao 2)
    - backend/handlers/icms_fronteira_prodepe.go  (confirmar assinatura de onlyDigits para reuso — NAO redefinir)
  </read_first>
  <action>
    Criar backend/handlers/fiscal_group_lookup.go no package handlers, portando do FB_TESTESFC:
    (a) errSemGrupoFiscal via errors.New("produto nao encontrado em prod/PRODB");
    (b) var codEmpresaPorCNPJRaiz = map[string]int{"10230480": 2} com comentario sobre Recife/PE ser a unica raiz confirmada e Garanhuns/PE (cod_empresa=1) sem raiz confirmada;
    (c) func resolveCodEmpresa(emitCNPJ, emitUF string) (int, error) que pega os 8 primeiros digitos (via onlyDigits, reusando o helper existente — NAO redefinir onlyDigits), busca no mapa, e em miss retorna erro explicito citando a raiz e a UF (nunca um cod_empresa fabricado — anti-pattern critico);
    (d) func lookupGrupoFiscal(ctx context.Context, oracleDB *sql.DB, codigoProduto string, codEmpresa int) (grupoFiscal, origem, ncm string, err error) usando a query validada acima com sql.Named para :codigoProduto e :codEmpresa, escaneando em sql.NullString; sql.ErrNoRows -> errSemGrupoFiscal; nunca expor scanErr cru ao cliente (retornar o erro para o caller logar/tratar).
    Adaptar apenas package name/imports para fb_apu04. Nao reescrever a logica.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./handlers/ && grep -q "func lookupGrupoFiscal" handlers/fiscal_group_lookup.go && grep -q "func resolveCodEmpresa" handlers/fiscal_group_lookup.go</automated>
  </verify>
  <acceptance_criteria>
    - fiscal_group_lookup.go define resolveCodEmpresa, lookupGrupoFiscal, errSemGrupoFiscal e codEmpresaPorCNPJRaiz com a raiz "10230480":2.
    - Usa a query exata FROM prodb pb, prod p WHERE p.codigo = pb.codigo com sql.Named.
    - `grep -c "func onlyDigits" backend/handlers/fiscal_group_lookup.go` retorna 0 (reusa o helper existente, nao redefine).
    - resolveCodEmpresa retorna erro (nao valor) para raiz nao mapeada.
    - `cd backend && go build ./...` sai com codigo 0.
  </acceptance_criteria>
  <done>Lookup de grupo fiscal portado; gap de cod_empresa vira erro explicito; produto ausente vira errSemGrupoFiscal; build/vet passam.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 147 — tabela fiscal_execution_items (schema hibrido + IBS/CBS)</name>
  <files>backend/migrations/147_fiscal_execution_items.sql</files>
  <read_first>
    - /home/claudiobezerra/projetos/FB_TESTESFC/backend/migrations/008_fiscal_execution_items.sql  (schema hibrido validado original; se apagado, usar 11-RESEARCH.md "Pattern 3")
    - backend/migrations/075_create_nfe_itens_tables.sql  (confirmar que nfe_saidas_itens.id e UUID para a FK)
  </read_first>
  <action>
    Criar backend/migrations/147_fiscal_execution_items.sql com CREATE TABLE IF NOT EXISTS fiscal_execution_items (D-04 / TPF-04), portando o modelo hibrido do FB_TESTESFC:
    - id UUID PK DEFAULT gen_random_uuid();
    - company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE;
    - nfe_item_id UUID NOT NULL REFERENCES nfe_saidas_itens(id) ON DELETE CASCADE;
    - status TEXT NOT NULL DEFAULT 'pending' (valores esperados: pending|ok|error|sem_grupo_fiscal), error_message TEXT, executed_at TIMESTAMPTZ;
    - grupo_fiscal_codigo TEXT, input_params JSONB (snapshot dos 23 IN);
    - colunas tipicas NUMERIC(15,2): base_calculo_icms, valor_icms, base_substituicao, valor_substituicao, base_calculo_pis, valor_pis, base_calculo_cofins, valor_cofins, valor_icms_partilha_destino, valor_icms_pobreza; percentual_difal NUMERIC(7,4);
    - colunas ADICIONAIS (alem do original FB_TESTESFC) para IBS/CBS da Fase 12: valor_ibs_uf NUMERIC(15,2), valor_ibs_mun NUMERIC(15,2), valor_cbs NUMERIC(15,2);
    - full_result JSONB NOT NULL (os ~88 campos completos para auditoria/detalhe);
    - created_at TIMESTAMPTZ NOT NULL DEFAULT now();
    - CONSTRAINT uq_fiscal_execution_item UNIQUE (nfe_item_id)  (habilita INSERT ... ON CONFLICT (nfe_item_id) DO UPDATE — upsert por item, TPF-05/D-04).
    Adicionar CREATE INDEX IF NOT EXISTS idx_fiscal_execution_status ON fiscal_execution_items(company_id, status) e idx_fiscal_execution_nfe_item ON fiscal_execution_items(nfe_item_id).
    Numeracao 147 (proxima apos 146). Nao ha colisao de nome (fiscal_calculations e outra tabela nao relacionada).
  </action>
  <verify>
    <automated>grep -q "CREATE TABLE IF NOT EXISTS fiscal_execution_items" backend/migrations/147_fiscal_execution_items.sql && grep -q "UNIQUE (nfe_item_id)" backend/migrations/147_fiscal_execution_items.sql && grep -q "full_result JSONB NOT NULL" backend/migrations/147_fiscal_execution_items.sql && grep -q "valor_cbs" backend/migrations/147_fiscal_execution_items.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo contem CREATE TABLE IF NOT EXISTS fiscal_execution_items.
    - FK nfe_item_id REFERENCES nfe_saidas_itens(id) ON DELETE CASCADE e company_id REFERENCES companies(id) ON DELETE CASCADE.
    - CONSTRAINT UNIQUE (nfe_item_id) presente (habilita upsert por item).
    - full_result JSONB NOT NULL presente + colunas valor_ibs_uf/valor_ibs_mun/valor_cbs presentes.
    - Dois indices idempotentes criados.
  </acceptance_criteria>
  <done>Migration 147 cria fiscal_execution_items no modelo hibrido validado + colunas IBS/CBS, com FKs CASCADE e UNIQUE(nfe_item_id) para upsert por item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend Go -> Oracle prod/PRODB | codigoProduto/codEmpresa viram bind params |
| batch job -> Postgres fiscal_execution_items | escrita exclusiva do backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | lookupGrupoFiscal query | mitigate | Todos os valores via sql.Named (:codigoProduto, :codEmpresa); zero concatenacao de string na query |
| T-11-08 | Information Disclosure | scanErr do lookup | mitigate | Nunca expor scanErr.Error() cru ao cliente; erro devolvido ao caller para log server-side |
| T-11-09 | Spoofing/Integrity | resolveCodEmpresa | mitigate | Retorno de erro explicito para raiz nao mapeada; proibido cod_empresa adivinhado (anti-pattern documentado) |
| T-11-10 | Information Disclosure | fiscal_execution_items | mitigate | company_id NOT NULL + FK; leitura futura (Fase 12) sempre escopada por company_id (indice idx_fiscal_execution_status inclui company_id) |
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./handlers/` passam.
- fiscal_group_lookup.go define resolveCodEmpresa/lookupGrupoFiscal sem redefinir onlyDigits.
- Migration 147 cria a tabela hibrida com UNIQUE(nfe_item_id) e colunas IBS/CBS.
</verification>

<success_criteria>
- TPF-01: grupo fiscal resolvido via Oracle prod/PRODB por item; filial nao mapeada e produto ausente viram erro explicito nao-fatal.
- TPF-04: fiscal_execution_items existe com status por item, colunas tipicas indexaveis, IBS/CBS e full_result JSONB (~88 campos).
</success_criteria>

<output>
Create `.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-03-SUMMARY.md` when done
</output>
