---
phase: 11-motor-de-execu-o-do-pacote-fiscal-backend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/146_nfe_itens_desc_outro.sql
  - backend/handlers/nfe_saidas.go
autonomous: true
requirements: [TPF-02]
must_haves:
  truths:
    - "Cada item de nfe_saidas_itens carrega v_desc (desconto) e v_outro (despesas acessórias), disponíveis como pDesconto/pDespesas do pacote fiscal"
    - "Novos uploads de XML populam v_desc e v_outro por item; reimport atualiza via ON CONFLICT"
  artifacts:
    - path: "backend/migrations/146_nfe_itens_desc_outro.sql"
      provides: "colunas v_desc/v_outro em nfe_saidas_itens E nfe_entradas_itens"
      contains: "ADD COLUMN IF NOT EXISTS v_outro"
    - path: "backend/handlers/nfe_saidas.go"
      provides: "parsing de vOutro no struct prod + persistência de v_desc/v_outro em insertNFeItens"
      contains: "vOutro"
  key_links:
    - from: "struct prod (nfe_saidas.go)"
      to: "insertNFeItens INSERT/ON CONFLICT"
      via: "toDecimal(d.Prod.VDesc), toDecimal(d.Prod.VOutro)"
      pattern: "v_outro"
---

<objective>
Cobrir TPF-02 (D-02): persistir desconto (`v_desc`) e despesas acessórias
(`v_outro`) por item de nota, que são inputs `pDesconto`/`pDespesas` do pacote
fiscal. Hoje o struct `prod` já parseia `VDesc` (`xml:"vDesc"`) mas `insertNFeItens`
não grava `v_desc`, e `VOutro` (`xml:"vOutro"`) nem existe no struct item-level.

Purpose: Fechar o único gap de dados de entrada identificado (confirmado por
inspeção de código em .continue-here.md) para que o motor da Fase 11 tenha
todos os 23 parâmetros IN disponíveis a partir de nfe_saidas_itens.
Output: Migration 146 (colunas nas DUAS tabelas de itens) + struct/INSERT atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-CONTEXT.md
@.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-RESEARCH.md

<interfaces>
<!-- Fatos do codebase FB_APU04 confirmados por inspeção direta. -->

insertNFeItens é COMPARTILHADO entre as duas tabelas de itens:
  handlers/nfe_entradas.go:302 → insertNFeItens(..., "nfe_entradas_itens")
  handlers/nfe_saidas.go:676    → insertNFeItens(..., "nfe_saidas_itens")
  handlers/xml_upload.go:391/463 → ambas
=> Como o INSERT é o MESMO texto SQL para os dois tableName, adicionar v_desc/v_outro
   ao INSERT OBRIGA que AMBAS as tabelas tenham as colunas, senão o INSERT falha em runtime.
   Isto torna a simetria entradas+saídas um REQUISITO, não uma escolha de estilo.

struct prod (nfe_saidas.go:82) já tem: VDesc string `xml:"vDesc"` (linha 89). NÃO tem VOutro item-level.
insertNFeItens (nfe_saidas.go:373) usa ON CONFLICT (nfe_id, n_item) DO UPDATE SET ...
helper toDecimal(s string) float64 já existe em nfe_saidas.go:221 (mesmo package) — reusar.
Estilo idempotente de migration (ver 094/095/141): ALTER TABLE ... ADD COLUMN IF NOT EXISTS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 146 — v_desc/v_outro em nfe_saidas_itens E nfe_entradas_itens</name>
  <files>backend/migrations/146_nfe_itens_desc_outro.sql</files>
  <read_first>
    - backend/migrations/141_nfe_itens_st.sql  (estilo idempotente de ALTER TABLE ADD COLUMN IF NOT EXISTS)
    - backend/migrations/075_create_nfe_itens_tables.sql  (schema base das duas tabelas de itens)
  </read_first>
  <action>
    Criar backend/migrations/146_nfe_itens_desc_outro.sql adicionando, de forma idempotente,
    as colunas `v_desc NUMERIC(15,2) DEFAULT 0` e `v_outro NUMERIC(15,2) DEFAULT 0` em AMBAS as
    tabelas: `nfe_saidas_itens` (requisito direto de TPF-02) e `nfe_entradas_itens` (obrigatório
    porque insertNFeItens é compartilhado e o mesmo INSERT roda para as duas tabelas — sem as
    colunas em nfe_entradas_itens o INSERT de entradas quebraria em runtime). Usar
    `ALTER TABLE ... ADD COLUMN IF NOT EXISTS ...` em dois statements (um por tabela).
    Incluir comentário no topo explicando a origem (pDesconto/pDespesas do PKG_FISCAL_FCTAX).
    Numeração 146 é a próxima sequencial (última existente = 145).
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS v_outro" backend/migrations/146_nfe_itens_desc_outro.sql | grep -q 2 && grep -q "nfe_entradas_itens" backend/migrations/146_nfe_itens_desc_outro.sql && grep -q "nfe_saidas_itens" backend/migrations/146_nfe_itens_desc_outro.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo contém ADD COLUMN IF NOT EXISTS v_desc e v_outro para nfe_saidas_itens.
    - O arquivo contém ADD COLUMN IF NOT EXISTS v_desc e v_outro para nfe_entradas_itens.
    - Ambas colunas são NUMERIC(15,2) DEFAULT 0.
    - Arquivo é idempotente (só usa IF NOT EXISTS).
  </acceptance_criteria>
  <done>Migration 146 adiciona v_desc/v_outro (NUMERIC(15,2) DEFAULT 0) idempotentemente às duas tabelas de itens.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Parsear vOutro e persistir v_desc/v_outro em insertNFeItens</name>
  <files>backend/handlers/nfe_saidas.go</files>
  <read_first>
    - backend/handlers/nfe_saidas.go  (struct prod ~linha 82-90; insertNFeItens ~linha 373-459; helper toDecimal ~linha 221)
  </read_first>
  <action>
    Em nfe_saidas.go:
    (1) No struct `prod` (item-level, ~linha 82), adicionar o campo `VOutro string `xml:"vOutro"`` ao lado do já existente `VDesc string `xml:"vDesc"``.
    (2) Em `insertNFeItens` (~linha 373), estender o INSERT: adicionar as colunas `v_desc` e `v_outro` à lista de colunas, os placeholders posicionais correspondentes ($23 e $24, ajustando a numeração), a cláusula `ON CONFLICT (nfe_id, n_item) DO UPDATE SET` com `v_desc = EXCLUDED.v_desc, v_outro = EXCLUDED.v_outro`, e os args finais `toDecimal(d.Prod.VDesc), toDecimal(d.Prod.VOutro)`.
    NÃO alterar a assinatura de insertNFeItens nem os callers. Como a função é compartilhada, as colunas já existem em ambas as tabelas (Task 1).
  </action>
  <verify>
    <automated>cd backend && go build ./... && grep -q 'xml:"vOutro"' handlers/nfe_saidas.go && grep -q "v_outro" handlers/nfe_saidas.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'VOutro string `xml:"vOutro"`' handlers/nfe_saidas.go` (campo adicionado ao struct prod).
    - insertNFeItens inclui `v_desc` e `v_outro` na lista de colunas, no ON CONFLICT DO UPDATE, e passa toDecimal(d.Prod.VDesc)/toDecimal(d.Prod.VOutro) como args.
    - `cd backend && go build ./...` sai com código 0.
  </acceptance_criteria>
  <done>Struct prod parseia vOutro; insertNFeItens grava e atualiza v_desc/v_outro; build passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| XML SEFAZ (upload) → parser Go | valores vDesc/vOutro vêm do XML, não confiáveis |
| parser → Postgres | INSERT parametrizado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | insertNFeItens INSERT | mitigate | Valores vão via placeholders posicionais ($n) parametrizados; tableName é literal fixo controlado pelo caller (nunca input do usuário) — sem concatenação de valores |
| T-11-06 | Denial of Service (malformed input) | toDecimal(vDesc/vOutro) | accept | toDecimal já trata strings inválidas devolvendo 0; XML inválido rejeitado antes na validação de schema NFe v4.00 existente |
</threat_model>

<verification>
- Migration 146 idempotente e cobrindo as duas tabelas.
- `cd backend && go build ./...` compila.
- v_desc/v_outro presentes no INSERT e no struct.
</verification>

<success_criteria>
- TPF-02 atendido: v_desc/v_outro persistidos por item em nfe_saidas_itens (e nfe_entradas_itens por simetria obrigatória do INSERT compartilhado), disponíveis como pDesconto/pDespesas do pacote fiscal.
</success_criteria>

<output>
Create `.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-02-SUMMARY.md` when done
</output>
