---
phase: 11-motor-de-execu-o-do-pacote-fiscal-backend
plan: 05
type: execute
wave: 3
depends_on: [11-01, 11-03, 11-04]
files_modified:
  - backend/handlers/fiscal_execution.go
  - backend/handlers/fiscal_execution_guards_test.go
  - backend/main.go
autonomous: true
requirements: [TPF-05]
must_haves:
  truths:
    - "Um admin dispara execucao em lote para uma nfe_id (escopada por company_id) e cada item vira um registro em fiscal_execution_items"
    - "Um item que falha (sem grupo fiscal, timeout, erro Oracle, panic) nao impede o processamento dos demais itens"
    - "A execucao respeita cap de concorrencia 5 e timeout de 15s por item"
    - "Cada item e seu proprio INSERT ... ON CONFLICT (nfe_item_id) DO UPDATE, nunca uma transacao unica do lote"
  artifacts:
    - path: "backend/handlers/fiscal_execution.go"
      provides: "FiscalExecutionRunHandler, processFiscalBatch, persistFiscalItemResult, defaults dos parametros"
      contains: "func FiscalExecutionRunHandler"
    - path: "backend/handlers/fiscal_execution_guards_test.go"
      provides: "guard tests 405/401 sem tocar o banco"
      contains: "func TestFiscalExecution"
    - path: "backend/main.go"
      provides: "rota admin /api/fiscal/execute"
      contains: "withAuth(handlers.FiscalExecutionRunHandler"
  key_links:
    - from: "processFiscalBatch"
      to: "semaforo chan struct{} cap 5 + context.WithTimeout 15s por item + defer recover()"
      via: "fan-out de goroutines por item"
      pattern: "make\\(chan struct\\{\\}, 5\\)"
    - from: "persistFiscalItemResult"
      to: "fiscal_execution_items"
      via: "INSERT ... ON CONFLICT (nfe_item_id) DO UPDATE"
      pattern: "ON CONFLICT \\(nfe_item_id\\)"
    - from: "FiscalExecutionRunHandler"
      to: "nfe_saidas / nfe_saidas_itens"
      via: "WHERE id = $1 AND company_id = $2 (guard IDOR)"
      pattern: "company_id"
---

<objective>
Cobrir TPF-05: o endpoint admin de execucao em lote que orquestra tudo —
carrega o cabecalho nfe_saidas e os itens de nfe_saidas_itens (escopados por
company_id), abre a conexao Oracle dedicada (Plan 01), resolve o grupo fiscal
por item (Plan 03), chama o pacote fiscal (Plan 04) e persiste cada resultado
em fiscal_execution_items (Plan 03) com concorrencia limitada, timeout por item
e isolamento de erro.

Purpose: E a wave final que costura os artefatos anteriores no fluxo end-to-end.
Introduz o padrao novo de fan-out com semaforo (inedito no FB_APU04).
Output: handler + processFiscalBatch + persistFiscalItemResult + guard tests + rota.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-CONTEXT.md
@.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-RESEARCH.md
@.planning/.continue-here.md

<interfaces>
<!-- Contratos entregues pelas waves anteriores + convencoes FB_APU04. -->

De Plan 01 (backend/handlers/fiscal_oracle_conn.go, package handlers):
  func openFiscalOracleConn(db *sql.DB, companyID string) (*sql.DB, error)   // ja com SetMaxOpenConns(5)

De Plan 03 (backend/handlers/fiscal_group_lookup.go, package handlers):
  func resolveCodEmpresa(emitCNPJ, emitUF string) (int, error)
  func lookupGrupoFiscal(ctx, oracleDB, codigoProduto string, codEmpresa int) (grupoFiscal, origem, ncm string, err error)
  var errSemGrupoFiscal error   // -> status "sem_grupo_fiscal"

De Plan 04 (backend/services/oracle_fiscal.go, package services):
  services.FiscalInput{...23 campos...}
  services.FiscalResult{...~88 campos...}
  func services.CallFiscalPackage(ctx, oracleDB *sql.DB, in services.FiscalInput) (*services.FiscalResult, error)

Tabela destino: fiscal_execution_items (UNIQUE(nfe_item_id)) — colunas em migration 147.
Colunas de entrada em nfe_saidas_itens: id, c_prod, cfop, v_prod, v_ipi, v_desc, v_outro (v_desc/v_outro de Plan 02).
Cabecalho nfe_saidas: emit_cnpj, emit_uf, emit_municipio (id UUID).

Convencoes de handler/auth/rota (ver Plan 01 interfaces + handlers/admin_nf_cancelamento.go + handlers/icms_fronteira_st_itens_guards_test.go):
  - factory func(*sql.DB) http.HandlerFunc; POST only (405); claims 401; GetEffectiveCompanyID(db,userID,X-Company-ID)
  - rota: http.HandleFunc("/api/fiscal/execute", withAuth(handlers.FiscalExecutionRunHandler, "admin"))
  - guard test: 405 em metodo errado, 401 sem auth, passando nil *sql.DB (checks curto-circuitam antes do DB)

Defaults de parametros (ver .continue-here.md secao 3): defaultTipoContribuinte="N", defaultTipoCentroFiscal="VRJNE",
  defaultTipoOperacao=1, defaultIndicadorServico="N", defaultFornecedorSimplesNacional="N".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handler de lote + processFiscalBatch + persistFiscalItemResult</name>
  <files>backend/handlers/fiscal_execution.go, backend/main.go</files>
  <read_first>
    - /home/claudiobezerra/projetos/FB_TESTESFC/backend/handlers/fiscal_execution.go  (arquivo original 415 linhas — porte do handler, processFiscalBatch, persistFiscalItemResult, defaults; se apagado, usar 11-RESEARCH.md Pattern 4 + .continue-here.md secao 3)
    - backend/handlers/admin_nf_cancelamento.go  (padrao claims/userID/GetEffectiveCompanyID + guard de metodo)
    - backend/main.go  (factory withAuth e bloco de rotas admin ~linha 519)
    - backend/handlers/fiscal_group_lookup.go  (assinaturas resolveCodEmpresa/lookupGrupoFiscal — ja existentes de Plan 03)
  </read_first>
  <action>
    Criar backend/handlers/fiscal_execution.go no package handlers:
    (a) consts de default (defaultTipoContribuinte="N", defaultTipoCentroFiscal="VRJNE", defaultTipoOperacao=1, defaultIndicadorServico="N", defaultFornecedorSimplesNacional="N") — documentar que so o caminho normal de venda foi validado contra Oracle real;
    (b) FiscalExecutionRunHandler(db *sql.DB) http.HandlerFunc: exige POST (405), le claims (401), resolve companyID via GetEffectiveCompanyID; le nfe_id do corpo; carrega cabecalho nfe_saidas e itens nfe_saidas_itens com guard IDOR WHERE id/nfe_id = $1 AND company_id = $2 (T-11-14); abre conexao via openFiscalOracleConn (defer Close); chama processFiscalBatch; responde JSON com o summary {total, ok, sem_grupo_fiscal, error};
    (c) processFiscalBatch(ctx, oracleDB, pgDB *sql.DB, companyID string, cabecalho, itens): semaforo make(chan struct{}, 5), sync.WaitGroup, sync.Mutex para o summary; por item: wg.Add(1), sem<-, goroutine com defer wg.Done()/defer <-sem/defer recover() (panic isolado vira status="error", nunca aborta o lote), context.WithTimeout(ctx, 15*time.Second) POR ITEM; dentro: resolveCodEmpresa -> lookupGrupoFiscal (errSemGrupoFiscal -> status sem_grupo_fiscal, nao fatal) -> montar services.FiscalInput (mapear v_prod->pPrecoTotal, v_ipi->pIPI, v_desc->pDesconto, v_outro->pDespesas, c_prod->pProduto, cfop->pCFOPOperacao, emit_cnpj->pCnpjEmpresa, emit_uf->pUFOrigem/Destino conforme original, grupo->pCodigoGrupoFiscal, emit_municipio->pCodigoIbge, + defaults) -> services.CallFiscalPackage -> persistFiscalItemResult;
    (d) persistFiscalItemResult(ctx, pgDB, companyID, nfeItemID, status, errMsg, grupo string, in services.FiscalInput, res *services.FiscalResult): INSERT INTO fiscal_execution_items (...) VALUES (...) ON CONFLICT (nfe_item_id) DO UPDATE SET ... — grava status/error_message/executed_at/grupo_fiscal_codigo, input_params (JSON de in), colunas tipicas (base_calculo_icms etc.), IBS/CBS, e full_result (JSON de res). Cada item e seu proprio statement — NUNCA uma transacao unica do lote (TPF-05/D-04).
    Definir o outer ctx do handler com timeout amplo (ex. 10min) so como backstop — o timeout real e por item.
    Nunca vazar err.Error() cru de Oracle ao cliente; logar server-side e responder mensagem generica.
    Registrar em main.go: http.HandleFunc("/api/fiscal/execute", withAuth(handlers.FiscalExecutionRunHandler, "admin")) no bloco admin.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./handlers/ && grep -q "make(chan struct{}, 5)" handlers/fiscal_execution.go && grep -q "ON CONFLICT (nfe_item_id)" handlers/fiscal_execution.go && grep -q 'withAuth(handlers.FiscalExecutionRunHandler, "admin")' main.go</automated>
  </verify>
  <acceptance_criteria>
    - FiscalExecutionRunHandler exige POST, valida claims, escopa nfe_id por company_id (guard IDOR WHERE ... AND company_id = $2).
    - processFiscalBatch usa semaforo cap 5, context.WithTimeout(15s) por item, defer recover() por goroutine, e nao usa transacao unica do lote.
    - persistFiscalItemResult usa INSERT ... ON CONFLICT (nfe_item_id) DO UPDATE e grava full_result JSON + colunas tipicas + IBS/CBS.
    - errSemGrupoFiscal produz status "sem_grupo_fiscal" (nao "error", nao aborta lote).
    - Rota /api/fiscal/execute registrada com role "admin".
    - `cd backend && go build ./...` sai com codigo 0.
  </acceptance_criteria>
  <done>Endpoint de lote costura conexao+lookup+pacote+persistencia com concorrencia 5, timeout 15s/item, isolamento por item e upsert por item; build/vet passam.</done>
</task>

<task type="auto">
  <name>Task 2: Guard tests (405/401) do endpoint de lote</name>
  <files>backend/handlers/fiscal_execution_guards_test.go</files>
  <read_first>
    - backend/handlers/icms_fronteira_st_itens_guards_test.go  (convencao de guard test: guardMethod 405, guardNoAuth 401, passando nil *sql.DB)
  </read_first>
  <action>
    Criar backend/handlers/fiscal_execution_guards_test.go no package handlers seguindo a convencao existente:
    uma funcao TestFiscalExecution_Guards que verifica que FiscalExecutionRunHandler(nil) retorna 405 para metodo errado (ex. GET quando exige POST) e 401 quando nao ha claims de auth — ambos os checks curto-circuitam antes de qualquer uso do *sql.DB, entao passar nil e seguro. Sem testify (usar stdlib testing + net/http/httptest). Nao tentar cobrir o caminho Oracle (sem test double no codebase).
  </action>
  <verify>
    <automated>cd backend && go test ./handlers/ -run TestFiscalExecution_Guards -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./handlers/ -run TestFiscalExecution_Guards` passa.
    - O teste passa nil *sql.DB e valida 405 (metodo) e 401 (sem auth).
    - Nao usa testify nem depende de conexao Oracle/Postgres.
  </acceptance_criteria>
  <done>Guard tests de metodo/auth passam sem tocar banco, seguindo a convencao handlers_guards_test.go.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP (admin) -> FiscalExecutionRunHandler | nfe_id no corpo; company_id derivado do JWT server-side |
| backend Go -> Oracle prod/PRODB + FCCORP_BKP | ate 5 sessoes concorrentes |
| batch -> Postgres fiscal_execution_items | upsert por item |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14 | Tampering / Information Disclosure (IDOR) | carga de nfe_id | mitigate | WHERE id/nfe_id = $1 AND company_id = $2 em toda leitura; company_id sempre do JWT via GetEffectiveCompanyID, nunca do corpo (ASVS V5) |
| T-11-15 | Elevation of Privilege | rota /api/fiscal/execute | mitigate | withAuth(handler, "admin"); guard 405/401 coberto por teste |
| T-11-16 | Denial of Service | fan-out do lote | mitigate | semaforo cap 5 + SetMaxOpenConns(5) (Plan 01) + timeout 15s por item; um item travado nao segura os demais |
| T-11-17 | Availability (falha por item) | processFiscalBatch | mitigate | defer recover() por goroutine + upsert por item (sem transacao unica); item com falha vira status error/sem_grupo_fiscal e o lote continua |
| T-11-18 | Information Disclosure | erro Oracle na resposta | mitigate | Mensagens genericas ao cliente; detalhe so em log.Printf server-side e em error_message da linha (nao no corpo agregado exposto) |
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./handlers/` passam.
- `go test ./handlers/ -run TestFiscalExecution_Guards` passa.
- Semaforo cap 5, timeout 15s/item, recover por item, upsert por item, guard IDOR presentes.
- Rota admin /api/fiscal/execute registrada.
</verification>

<success_criteria>
- TPF-05: execucao em lote com concorrencia limitada, timeout por item e isolamento de erro; um item que falha nao derruba o lote; cada item persistido com status em fiscal_execution_items.
</success_criteria>

<output>
Create `.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-05-SUMMARY.md` when done
</output>
