---
phase: 11-motor-de-execu-o-do-pacote-fiscal-backend
plan: 04
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - backend/services/oracle_fiscal.go
autonomous: true
requirements: [TPF-03]
must_haves:
  truths:
    - "O pacote PKG_FISCAL_FCTAX.calcula_imposto_produto e chamado via bloco PL/SQL anonimo 100% estatico, gerado apenas de tabelas de metadados fixas"
    - "Todos os 23 valores IN e ~88 destinos OUT trafegam por bind (sql.Named / go_ora.Out), nunca concatenados no texto do bloco"
    - "Campos OUT string usam go_ora.Out com Size explicito; campos IdRegraCalculo* sao string (VARCHAR2)"
  artifacts:
    - path: "backend/services/oracle_fiscal.go"
      provides: "FiscalInput, FiscalResult, fiscalInParams, fiscalOutFields, BuildCalculaImpostoBlock, CallFiscalPackage"
      contains: "func CallFiscalPackage"
  key_links:
    - from: "buildBindArgs"
      to: "go_ora.Out{Dest, Size}"
      via: "bind de OUT string com buffer explicito"
      pattern: "go_ora.Out"
    - from: "BuildCalculaImpostoBlock"
      to: "fiscalInParams / fiscalOutFields"
      via: "geracao por reflection de tabelas fixas (nunca input do usuario)"
      pattern: "fiscalInParams"
---

<objective>
Cobrir TPF-03: portar o servico de execucao do pacote Oracle
PKG_FISCAL_FCTAX.calcula_imposto_produto como novo arquivo no package
services (que ja existe no FB_APU04), gerando um bloco PL/SQL anonimo
100% estatico via reflection sobre duas tabelas de metadados fixas
(23 parametros IN, ~88 campos OUT) e executando com bind seguro.

Purpose: Esta e a peca de maior densidade tecnica da fase e o coracao do
modelo de seguranca (zero concatenacao no PL/SQL). Depende de go-ora estar
instalado (Plan 01), pois usa o tipo nativo go_ora.Out para os binds OUT string.
Output: backend/services/oracle_fiscal.go (package services).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-CONTEXT.md
@.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-RESEARCH.md
@.planning/.continue-here.md

<interfaces>
<!-- Contrato do pacote Oracle (ver .continue-here.md secao 1 e 11-RESEARCH.md Pattern 1). -->

23 parametros IN (ordem exata, notacao nomeada):
  pCnpjEmpresa, pUFOrigem, pUFDestino, pTipoContribuinte, pTipoCentroFiscal,
  pTipoOperacao, pEntradaSaida, pProduto, pCodigoGrupoFiscal, pCnpjExcecao,
  pIndicadorServico, pPrecoTotal, pDespesas, pDesconto, pIPI,
  pAliquotaSimplesNacional, FornecedorSimplesNacional (SEM prefixo 'p'!),
  pTipoIsencaoPedidoBonificado, pCFOPOperacao, pTipoContribuinteSecundario,
  pSimulacaoCalculo, pDataReferenciaFiscal, pCodigoIbge

Retorno: objeto PKG_FISCAL_FCTAX.RDADOS_FISCAIS_PRODUTO (~88 campos OUT: ICMS classico,
ICMS-ST, PIS/COFINS, IPI, DIFAL/partilha, IBS/CBS UF+MUN).

Pitfall 1: sql.Out{Dest:ptr} generico manda size=0 -> ORA-06502 buffer too small.
  Fix obrigatorio: go_ora.Out{Dest: ptr, Size: 4000} (import go_ora "github.com/sijms/go-ora/v2") para TODO campo OUT string. Numericos podem usar sql.Out.
Pitfall 2: IdRegraCalculoIcms/PisCofins/Ipi/Ibs/Cbs sao VARCHAR2 -> tipar string, nunca float64/int (senao ORA-06502 char-to-number).

go-ora ja instalado no Plan 01 (go.mod). Package alvo: services (ja existe: ai.go/crypto.go/email.go/rfb.go).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structs de contrato + tabelas de metadados</name>
  <files>backend/services/oracle_fiscal.go</files>
  <read_first>
    - /home/claudiobezerra/projetos/FB_TESTESFC/backend/services/oracle_fiscal.go  (arquivo original 380 linhas — porte verbatim das structs e das duas tabelas de metadados; se apagado, usar 11-RESEARCH.md Pattern 1 + .continue-here.md secao 1)
    - backend/services/crypto.go  (confirmar convencao de package services)
  </read_first>
  <action>
    Criar backend/services/oracle_fiscal.go no package services. Portar do FB_TESTESFC, 1:1:
    (a) struct FiscalInput com os 23 campos Go correspondentes aos 23 parametros IN (respeitando FornecedorSimplesNacional SEM prefixo p);
    (b) struct FiscalResult com os ~88 campos OUT mapeando RDADOS_FISCAIS_PRODUTO — tipar os cinco campos IdRegraCalculo* (Icms/PisCofins/Ipi/Ibs/Cbs) como string (Pitfall 2), demais numericos como float64 e textos como string;
    (c) var fiscalInParams com as 23 entradas na ordem exata, cada uma mapeando OracleParam para GoField;
    (d) var fiscalOutFields com as ~88 entradas, cada uma mapeando OracleField para GoField;
    (e) const fiscalOutStringBufSize = 4000.
    Esta task e interface-first: entrega apenas os contratos/tabelas; o builder e a execucao vem na Task 2. NAO inventar campos — copiar os nomes reais do arquivo original.
  </action>
  <verify>
    <automated>cd backend && go build ./services/ && grep -q "type FiscalInput struct" services/oracle_fiscal.go && grep -q "type FiscalResult struct" services/oracle_fiscal.go && grep -q "fiscalInParams" services/oracle_fiscal.go && grep -q "fiscalOutFields" services/oracle_fiscal.go</automated>
  </verify>
  <acceptance_criteria>
    - FiscalInput tem 23 campos; FiscalResult mapeia os ~88 campos OUT.
    - Os 5 campos IdRegraCalculo* sao tipados como string (nao numerico).
    - fiscalInParams tem 23 entradas na ordem exata do contrato; fiscalOutFields cobre os ~88 campos.
    - const fiscalOutStringBufSize = 4000 presente.
    - `cd backend && go build ./services/` sai com codigo 0.
  </acceptance_criteria>
  <done>Contratos FiscalInput/FiscalResult e tabelas de metadados portados; build do package services passa.</done>
</task>

<task type="auto">
  <name>Task 2: Builder do bloco PL/SQL estatico + CallFiscalPackage (bind seguro)</name>
  <files>backend/services/oracle_fiscal.go</files>
  <read_first>
    - /home/claudiobezerra/projetos/FB_TESTESFC/backend/services/oracle_fiscal.go  (funcoes BuildCalculaImpostoBlock, buildBindArgs, CallFiscalPackage originais)
    - .planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-RESEARCH.md  (Pattern 1 + Anti-Patterns to Avoid)
  </read_first>
  <action>
    Acrescentar ao mesmo arquivo services/oracle_fiscal.go:
    (a) func BuildCalculaImpostoBlock() string — monta o bloco PL/SQL anonimo (declare result RDADOS_FISCAIS_PRODUTO; begin result := calcula_imposto_produto(<param => :param> por reflection sobre fiscalInParams); depois :oField := result.<OracleField> por fiscalOutFields; end;). O texto e gerado SOMENTE das tabelas fixas — NUNCA concatenar valores de entrada (anti-pattern critico T-11-11);
    (b) func buildBindArgs(in FiscalInput, result *FiscalResult) []interface{} — via reflect, para cada IN faz sql.Named(OracleParam, valorDoCampo); para cada OUT, se o campo Go for string usa go_ora.Out{Dest: ptr, Size: fiscalOutStringBufSize} (Pitfall 1), senao sql.Out{Dest: ptr};
    (c) func CallFiscalPackage(ctx context.Context, oracleDB *sql.DB, in FiscalInput) (*FiscalResult, error) — executa oracleDB.ExecContext(ctx, BuildCalculaImpostoBlock(), buildBindArgs(...)...), devolve *FiscalResult preenchido ou erro. Import go_ora "github.com/sijms/go-ora/v2".
    Manter a assinatura exportada CallFiscalPackage para o handler de lote (Plan 05) consumir via services.CallFiscalPackage.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./services/ && grep -q "func CallFiscalPackage" services/oracle_fiscal.go && grep -q "go_ora.Out" services/oracle_fiscal.go</automated>
  </verify>
  <acceptance_criteria>
    - BuildCalculaImpostoBlock gera o bloco apenas de fiscalInParams/fiscalOutFields (sem fmt.Sprintf de valores de entrada no texto SQL).
    - buildBindArgs usa go_ora.Out{Dest,Size} para OUT string e sql.Named para todos os IN.
    - CallFiscalPackage e exportada e retorna (*FiscalResult, error).
    - `cd backend && go build ./...` e `go vet ./services/` saem com codigo 0.
    - `grep -c "fmt.Sprintf" services/oracle_fiscal.go` nao aparece dentro da construcao do bloco com valores de entrada (revisao: qualquer Sprintf usa apenas OracleParam/OracleField das tabelas fixas, nunca valores IN).
  </acceptance_criteria>
  <done>Bloco PL/SQL estatico gerado por reflection; binds seguros (sql.Named + go_ora.Out); CallFiscalPackage exportada; build/vet passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| valores de entrada (FiscalInput) -> texto do bloco PL/SQL | fronteira critica: valores NUNCA entram no texto SQL |
| backend Go -> Oracle FCCORP_BKP | execucao do pacote via ExecContext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering (injection) | BuildCalculaImpostoBlock | mitigate | Bloco 100% estatico gerado so das tabelas fixas fiscalInParams/fiscalOutFields; todos os valores via sql.Named/go_ora.Out; proibido fmt.Sprintf de valor de entrada no texto (ASVS V5) |
| T-11-12 | Denial of Service (buffer/erro Oracle) | buildBindArgs OUT string | mitigate | go_ora.Out{Size:4000} evita ORA-06502 buffer too small; IdRegraCalculo* como string evita ORA-06502 char-to-number |
| T-11-13 | Information Disclosure | CallFiscalPackage error | mitigate | Erro Oracle retornado ao caller para log server-side; caller (Plan 05) nao expoe detalhe cru ao cliente |
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./services/` passam.
- Bloco PL/SQL gerado apenas de metadados fixos (sem concatenacao de valores).
- go_ora.Out usado para OUT string; IdRegraCalculo* como string.
</verification>

<success_criteria>
- TPF-03: calcula_imposto_produto executado via bloco PL/SQL estatico com bind seguro; ~88 campos OUT capturados em FiscalResult; ambas as pegadinhas do go-ora tratadas.
</success_criteria>

<output>
Create `.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-04-SUMMARY.md` when done
</output>
