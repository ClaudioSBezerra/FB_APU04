---
phase: 12-tela-compara-o-fiscal-navega-o
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/src/components/NfeSearchCombobox.tsx
  - frontend/src/pages/ComparacaoFiscal.tsx
autonomous: true
requirements: [TPF-06, TPF-07]

must_haves:
  truths:
    - "Admin busca uma NF-e no combobox (>=3 chars, debounce) e seleciona um resultado"
    - "Clicar 'Executar' chama POST /api/fiscal/execute e a tabela recarrega automaticamente na mesma tela (sem navegação extra)"
    - "Tabela mostra esperado/calculado/diferença para ICMS, ICMS-ST, PIS, COFINS, IBS, CBS item a item"
    - "Célula/linha divergente é destacada por cor (tolerância zero: abs(esperado-calculado) != 0)"
    - "Item status != 'ok' aparece como 'Não calculado'; item sem execução aparece como 'Nunca executado' (4º badge distinto)"
    - "Filtro 'Só divergentes' oculta itens sem nenhuma divergência"
    - "Resumo agregado exibe 4 cards + 6 chips por imposto com contagem/percentual de divergências"
    - "Botões Exportar Excel (client-side) e Exportar CSV (backend) funcionam"
  artifacts:
    - path: "frontend/src/components/NfeSearchCombobox.tsx"
      provides: "Combobox de busca server-side debounced"
      contains: "shouldFilter={false}"
    - path: "frontend/src/pages/ComparacaoFiscal.tsx"
      provides: "Tela Comparação Fiscal completa"
      contains: "fiscal-comparacao"
  key_links:
    - from: "frontend/src/components/NfeSearchCombobox.tsx"
      to: "/api/fiscal/comparacao/search"
      via: "useQuery debounced, enabled: debounced.length >= 3"
      pattern: "comparacao/search"
    - from: "frontend/src/pages/ComparacaoFiscal.tsx"
      to: "POST /api/fiscal/execute → invalidateQueries(['fiscal-comparacao', nfeId])"
      via: "useMutation onSuccess"
      pattern: "invalidateQueries"
---

<objective>
Construir a tela React Comparação Fiscal e o combobox de busca de NF-e que a alimenta. A tela busca uma nota, dispara a execução do pacote fiscal (endpoint da Fase 11) e recarrega automaticamente os itens, exibindo esperado×calculado das 6 impostos com divergências destacadas, filtro "só divergentes", resumo agregado e exportação Excel/CSV.

Purpose: Fechar o ciclo end-to-end do módulo Teste Pacote Fiscal — primeira e única interface humana de validação do pacote fiscal.
Output: `NfeSearchCombobox.tsx`, `ComparacaoFiscal.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-tela-compara-o-fiscal-navega-o/12-UI-SPEC.md
@.planning/phases/12-tela-compara-o-fiscal-navega-o/12-RESEARCH.md
@.planning/phases/12-tela-compara-o-fiscal-navega-o/12-PATTERNS.md

<interfaces>
<!-- Endpoints do Plan 12-01 (Wave 1) que esta tela consome -->
GET /api/fiscal/comparacao/search?q=...   → [{ id, chave_nfe, numero_nfe, serie, dest_nome, data_emissao }]  (>=3 chars)
GET /api/fiscal/comparacao?nfe_id=...      → [ ComparacaoRow ] com campos esperado (v_bc_icms, v_icms, v_bc_st, v_st,
     v_bc_pis, v_pis, v_bc_cofins, v_cofins, v_ibs, v_cbs), calculado (base_calculo_icms, valor_icms,
     base_substituicao, valor_substituicao, base_calculo_pis, valor_pis, base_calculo_cofins, valor_cofins,
     valor_ibs_total, valor_cbs, percentual_difal, valor_icms_partilha_destino, valor_icms_pobreza),
     status ('ok'|'error'|'sem_grupo_fiscal'|'pending'|'not_executed'), error_message, n_item, c_prod, x_prod, ncm, cfop
GET /api/fiscal/comparacao/csv?nfe_id=...  → blob CSV (attachment)
POST /api/fiscal/execute { nfe_id }         → { total, ok, sem_grupo_fiscal, error }   (Fase 11, existente)

Reuso frontend confirmado:
- @/components/ui/command (Command, CommandInput, CommandList, CommandEmpty, CommandGroup, CommandItem) — cmdk vendored
- @/components/ui/popover (Popover, PopoverContent, PopoverTrigger)
- @/lib/exportToExcel → exportToExcel(data, fileName, sheetName)   (client-side xlsx)
- @/contexts/AuthContext → useAuth() expõe { token, companyId }   (fetch global já injeta Authorization + X-Company-ID)
- @tanstack/react-query: useQuery, useMutation, useQueryClient
- lucide-react: Search, HelpCircle, AlertTriangle, CheckCircle, Download, FileSpreadsheet, Loader2, Send
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: NfeSearchCombobox — busca server-side debounced</name>
  <files>frontend/src/components/NfeSearchCombobox.tsx</files>
  <read_first>
    - frontend/src/components/FilialSelector.tsx (shell Command/Popover — hoje filtra array em memória)
    - frontend/src/components/ui/command.tsx e popover.tsx (primitivas vendored)
    - .planning/phases/12-tela-compara-o-fiscal-navega-o/12-RESEARCH.md (Pattern 1 — código completo já redigido, é a fonte autoritativa)
  </read_first>
  <action>
    Criar o componente `NfeSearchCombobox({ onSelect }: { onSelect: (nfe: NfeSearchResult) => void })` seguindo verbatim o Pattern 1 de RESEARCH.md (linhas 164-241). Tipo `NfeSearchResult` com id, chave_nfe, numero_nfe, serie, dest_nome, data_emissao. Estado local query/debounced com debounce de 300ms; `useQuery` keyed em ['nfe-saidas-search', debounced, companyId], `enabled: debounced.length >= 3`, fetch de `/api/fiscal/comparacao/search?q=${encodeURIComponent(debounced)}`. Popover + Command com `shouldFilter={false}` (DESVIO CRÍTICO de FilialSelector: desabilita o filtro client-side do cmdk porque a filtragem agora é server-side). CommandEmpty com as 3 mensagens condicionais (isFetching → 'Buscando...', <3 chars → 'Digite ao menos 3 caracteres.', senão 'Nenhuma nota encontrada.'). CommandItem chama onSelect(nfe) e fecha o popover. Trigger é um Button outline com ícone Search e texto 'Buscar NF-e por número ou chave...'.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | (! grep -i "NfeSearchCombobox") && grep -q "shouldFilter={false}" src/components/NfeSearchCombobox.tsx && grep -q "debounced.length >= 3" src/components/NfeSearchCombobox.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` não reporta erro em NfeSearchCombobox.tsx
    - Componente usa `shouldFilter={false}` (filtragem server-side)
    - useQuery `enabled` gate em `debounced.length >= 3`
    - onSelect propaga o NfeSearchResult e fecha o popover
  </acceptance_criteria>
  <done>Combobox digita→debounce→fetch→lista resultados server-side e seleciona uma nota, tipando limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ComparacaoFiscal.tsx — tela completa (busca, execução, comparação, filtro, resumo, export)</name>
  <files>frontend/src/pages/ComparacaoFiscal.tsx</files>
  <read_first>
    - frontend/src/pages/ConciliacaoBridgeXML.tsx (shell: header+tooltip, cards, filtro, tabela densa, badges, botões export, estados vazio/erro)
    - frontend/src/pages/ImportarViaERP.tsx (linhas 188-247: useMutation trigger-then-invalidate + botão com spinner Loader2/Send)
    - frontend/src/pages/ConsultaNFeSaidas.tsx (linhas 108-205: componente DetalheNFe — shell do Dialog de detalhe 3 seções)
    - frontend/src/lib/exportToExcel.ts
    - .planning/phases/12-tela-compara-o-fiscal-navega-o/12-UI-SPEC.md (contrato de design travado — copy, cores, layout de 8 blocos, Divergence Visual Rules)
    - .planning/phases/12-tela-compara-o-fiscal-navega-o/12-PATTERNS.md (excertos por bloco)
  </read_first>
  <action>
    Criar a página `ComparacaoFiscal` (default export). Compor 3 análogos: shell de ConciliacaoBridgeXML.tsx, mutation de ImportarViaERP.tsx, Dialog de ConsultaNFeSaidas.tsx.

    Fluxo (D-01/D-02): NfeSearchCombobox (Task 1) no topo; ao selecionar, guarda nfeId em estado. Botão 'Executar' (Loader2 quando isPending, senão Send) chama `useMutation` POST /api/fiscal/execute { nfe_id }, e no `onSuccess` faz `queryClient.invalidateQueries({ queryKey: ['fiscal-comparacao', nfeId] })` + toast `Execução concluída: ${ok}/${total} OK`. A leitura é um `useQuery` keyed ['fiscal-comparacao', nfeId] contra GET /api/fiscal/comparacao?nfe_id=, `enabled: !!nfeId`, que refetch automaticamente na invalidação — reload na mesma tela, sem navegação (D-01). Loading da execução: spinner inline no botão (Claude's Discretion, convenção do UI-SPEC).

    Regra de divergência (BINDING, UI-SPEC Divergence Visual Rules — tolerância ZERO, NÃO reusar o `> 0.01` de ConciliacaoBridgeXML): por par base/valor de cada imposto, `divergente = Math.abs((esperado ?? 0) - (calculado ?? 0)) !== 0`. Para IBS comparar `v_ibs` contra `valor_ibs_total` (já somado no backend). Precedência de status: se `status !== 'ok'` NÃO avaliar divergência — renderizar badge de estado. QUATRO estados de badge (resolvido na open-question do planejamento — estende o modelo de 3 do UI-SPEC): 'OK' (verde/muted), 'Divergente' (bg-red-50 text-red-700 border-red-200), 'Não calculado' para status error/sem_grupo_fiscal/pending (bg-slate-100 text-slate-600 border-slate-200, tooltip com status+error_message), e 'Nunca executado' para status === 'not_executed' (estilo neutro DISTINTO, ex.: bg-gray-50 text-gray-400 border-gray-200 border-dashed, tooltip 'Nota ainda não executada — clique em Executar'). Nunca renderizar not_executed/não-ok como 'Divergente' (falso positivo).

    Layout (UI-SPEC 8 blocos): (1) Header H1 'Comparação Fiscal' + HelpCircle tooltip + subtítulo verbatim. (2) 4 cards `grid grid-cols-1 sm:grid-cols-4 gap-4`: Total de Itens, Sem Divergência, Divergentes, Não Calculados — computados client-side com reduce() sobre o payload. (3) 6 chips por imposto (ICMS, ICMS-ST, PIS, COFINS, IBS, CBS) mostrando `{count} ({pct}%)`, Badge red-50/red-700 quando count>0 senão gray-50/muted. (4) Filtro 'Só divergentes' (Switch+Label, off por default) que oculta itens sem nenhuma divergência. (5) Tabela densa (`text-[11px]`, células `py-1 px-2`): identificação (nº/chave truncada mono, produto, CFOP), para cada imposto 3 sub-colunas Esperado(600)/Calculado(400 muted)/Diferença(Badge red se !=0), coluna Status (badge de 4 estados), tint de linha bg-red-50 se divergente / bg-slate-50 se não-ok / sem tint se OK, e ícone 'Ver detalhes'. (6) Dialog de detalhe 3 seções (Identificação / Comparação full precision 6 impostos / 'Só calculado' com DIFAL %, valor_icms_partilha_destino, valor_icms_pobreza + nota 'Campos abaixo só existem no retorno do pacote fiscal — sem valor esperado correspondente no XML'). (7) Botões Exportar Excel (exportToExcel client-side, mapeando 6 impostos × esperado/calculado/diferença) + Exportar CSV (blob download de /api/fiscal/comparacao/csv?nfe_id=). (8) Footnote da legenda de threshold: '(divergência = qualquer diferença ≠ R$ 0,00 — sem tolerância de arredondamento)'.

    IBS/CBS (D-05): tooltip mandatório nos headers das colunas IBS/CBS com a copy do UI-SPEC ('Campo v_ibs/v_cbs do XML pode aparecer zerado...'). IBS e CBS CONTINUAM contando normalmente no filtro e no resumo — NÃO esconder nem excluir.

    Estados vazio/erro: usar copy verbatim do UI-SPEC (nenhum item comparado / filtro sem divergências com CheckCircle verde / erro com retry inline). Data fetching: @tanstack/react-query (NÃO fetch+useState — convenção desta família de telas per UI-SPEC).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | (! grep -i "ComparacaoFiscal") && npm run build >/dev/null 2>&1 && grep -q "invalidateQueries" src/pages/ComparacaoFiscal.tsx && grep -q "not_executed" src/pages/ComparacaoFiscal.tsx && grep -q "!== 0" src/pages/ComparacaoFiscal.tsx && ! grep -q "> 0.01" src/pages/ComparacaoFiscal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` conclui sem erro e `tsc --noEmit` não reporta erro em ComparacaoFiscal.tsx
    - onSuccess da mutation chama `invalidateQueries(['fiscal-comparacao', nfeId])` (reload automático, D-01)
    - Regra de divergência usa `!== 0` (tolerância zero); NÃO existe `> 0.01` no arquivo
    - Os 4 estados de badge presentes, incluindo 'not_executed'/'Nunca executado' distinto de 'Não calculado'
    - 4 cards + 6 chips por imposto computados client-side; filtro 'Só divergentes' presente
    - Botões Exportar Excel e Exportar CSV presentes; tooltip IBS/CBS presente
  </acceptance_criteria>
  <done>Tela completa: busca→executa→recarrega→compara 6 impostos com 4 estados, filtro, resumo e export, buildando limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (React) → API | os gates de UI (esconder nav/rota) são cosméticos; o limite real está no backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information Disclosure | ComparacaoFiscal.tsx / NfeSearchCombobox.tsx | accept | Frontend não é limite de segurança; toda proteção real (admin gate + IDOR + company scope) está nos handlers do Plan 12-01 (`withAuth "admin"` + `company_id = $N`). O AdminRoute (Plan 12-03) é defesa em profundidade de UX, não a fronteira |
| T-12-06 | Tampering | fetch de search/read/csv/execute | accept | Requisições usam o fetch global do AuthContext que injeta Authorization+X-Company-ID; nenhum company_id de confiança é enviado pelo corpo — o backend re-resolve via JWT (T-12-01) |
| T-12-SC | Tampering | npm installs | accept | Zero pacotes novos: todas as libs (react-query, xlsx, cmdk vendored, lucide-react) já são dependências do projeto (RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `cd frontend && npm run build` conclui sem erro
- `tsc --noEmit` limpo para os dois arquivos novos
- Grep confirma reload automático, tolerância zero, 4º estado e ausência de `> 0.01`
</verification>

<success_criteria>
- Fluxo busca→executar→recarrega funciona na mesma tela
- 6 impostos comparados com divergência destacada (tolerância zero) e 4 estados de badge
- Filtro 'só divergentes' + resumo agregado (4 cards + 6 chips)
- Exportar Excel e CSV disponíveis
</success_criteria>

<output>
Create `.planning/phases/12-tela-compara-o-fiscal-navega-o/12-02-SUMMARY.md` when done
</output>
