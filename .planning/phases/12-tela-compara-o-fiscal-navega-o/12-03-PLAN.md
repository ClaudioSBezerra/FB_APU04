---
phase: 12-tela-compara-o-fiscal-navega-o
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - frontend/src/lib/navigation.ts
  - frontend/src/components/AppSidebar.tsx
  - frontend/src/App.tsx
autonomous: false
requirements: [TPF-08]

must_haves:
  truths:
    - "Usuário com role admin vê o item de navegação 'Teste Pacote Fiscal' → 'Comparação Fiscal'"
    - "Usuário sem role admin NÃO vê essa seção de navegação"
    - "Navegar direto para /pacote-fiscal/comparacao como não-admin redireciona (AdminRoute)"
    - "getActiveModule('/pacote-fiscal/...') retorna 'pacotefiscal'"
  artifacts:
    - path: "frontend/src/lib/navigation.ts"
      provides: "módulo pacotefiscal + getActiveModule"
      contains: "pacotefiscal"
    - path: "frontend/src/components/AppSidebar.tsx"
      provides: "NavSection adminOnly 'Teste Pacote Fiscal'"
      contains: "pacotefiscal"
    - path: "frontend/src/App.tsx"
      provides: "rota AdminRoute /pacote-fiscal/comparacao"
      contains: "/pacote-fiscal/comparacao"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "ComparacaoFiscal (Plan 12-02)"
      via: "<AdminRoute><ComparacaoFiscal /></AdminRoute>"
      pattern: "AdminRoute><ComparacaoFiscal"
    - from: "frontend/src/components/AppSidebar.tsx"
      to: "gate section.adminOnly && !isAdmin"
      via: "NavSection adminOnly: true"
      pattern: "adminOnly: true"
---

<objective>
Fiar o item de navegação novo "Teste Pacote Fiscal → Comparação Fiscal" com gate `adminOnly` em três arquivos (navigation.ts, AppSidebar.tsx, App.tsx), reusando o padrão idêntico já em produção — e verificar o fluxo end-to-end visualmente.

Purpose: Entregar TPF-08 (item de navegação admin-gated) e fechar a fase com verificação humana do fluxo completo busca→executar→comparar.
Output: 3 arquivos de config editados + verificação end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-tela-compara-o-fiscal-navega-o/12-UI-SPEC.md
@.planning/phases/12-tela-compara-o-fiscal-navega-o/12-PATTERNS.md

<interfaces>
<!-- Pontos de inserção verificados contra o conteúdo atual dos arquivos -->
navigation.ts: módulo `auditoria` (linhas 66-71, shape single-tab) é o análogo; getActiveModule() tem cadeia de `if (pathname.startsWith(...))` (linha ~104)
AppSidebar.tsx: seção `"malha"` (linhas 113-123, adminOnly: true) é o análogo; gate de seção já implementado (linha 273: `if (section.adminOnly && !isAdmin) return null`); precisa importar FlaskConical, GitCompare de lucide-react
App.tsx: AdminRoute (linhas 77-84) e rota /importacoes/erp-bridge-xml (linha 197) são o análogo; imports de página perto da linha 30
Página consumida: frontend/src/pages/ComparacaoFiscal.tsx (Plan 12-02, default export)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wiring de navegação nos 3 arquivos (adminOnly gate)</name>
  <files>frontend/src/lib/navigation.ts, frontend/src/components/AppSidebar.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/lib/navigation.ts (módulo auditoria + função getActiveModule)
    - frontend/src/components/AppSidebar.tsx (seção "malha" adminOnly + imports lucide-react + gate linha 273)
    - frontend/src/App.tsx (AdminRoute + bloco de rotas /importacoes/erp-bridge* + imports de página)
    - .planning/phases/12-tela-compara-o-fiscal-navega-o/12-UI-SPEC.md (seção Navigation Entry — código exato já redigido)
  </read_first>
  <action>
    navigation.ts: adicionar o módulo `pacotefiscal` ao mapa de módulos (mesmo shape de `auditoria`): label 'Teste Pacote Fiscal', tabs [{ label: 'Comparação Fiscal', path: '/pacote-fiscal/comparacao' }]. Estender getActiveModule() adicionando `if (pathname.startsWith('/pacote-fiscal')) return 'pacotefiscal'` na cadeia existente.

    AppSidebar.tsx: importar `FlaskConical` e `GitCompare` de 'lucide-react' (adicionar aos imports existentes). Adicionar ao array `sections` um NavSection novo (mesmo shape de "malha"): id 'pacotefiscal', title 'Teste Pacote Fiscal', sectionIcon FlaskConical, adminOnly: true, items [{ title: 'Comparação Fiscal', url: '/pacote-fiscal/comparacao', icon: GitCompare }]. Não alterar os tipos NavSection/NavItem (adminOnly já existe) nem a lógica de gate (linha 273 já cobre).

    App.tsx: adicionar `import ComparacaoFiscal from './pages/ComparacaoFiscal'` junto aos demais imports de página; adicionar a rota `<Route path="/pacote-fiscal/comparacao" element={<AdminRoute><ComparacaoFiscal /></AdminRoute>} />` no mesmo bloco das rotas /importacoes/erp-bridge* (envolver no AdminRoute existente — defesa em profundidade além de esconder o item de nav).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | (! grep -iE "navigation.ts|AppSidebar|App.tsx|ComparacaoFiscal") && npm run build >/dev/null 2>&1 && grep -q "pacotefiscal" src/lib/navigation.ts && grep -q "startsWith('/pacote-fiscal')" src/lib/navigation.ts && grep -q "adminOnly: true" src/components/AppSidebar.tsx && grep -q "AdminRoute><ComparacaoFiscal" src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` conclui sem erro e `tsc --noEmit` limpo
    - navigation.ts contém o módulo `pacotefiscal` e o branch getActiveModule para '/pacote-fiscal'
    - AppSidebar.tsx tem a NavSection 'pacotefiscal' com `adminOnly: true` e ícones FlaskConical/GitCompare importados
    - App.tsx tem a rota `/pacote-fiscal/comparacao` envolvida em `<AdminRoute>` e o import de ComparacaoFiscal
  </acceptance_criteria>
  <done>Item de nav 'Teste Pacote Fiscal' visível só para admin, rota gated por AdminRoute, build limpo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação end-to-end do fluxo Comparação Fiscal</name>
  <files>(nenhum — verificação manual, sem edição de arquivo)</files>
  <what-built>
    Fluxo completo do módulo Teste Pacote Fiscal: item de navegação admin-gated (TPF-08), tela Comparação Fiscal (TPF-06/07) que busca uma NF-e, dispara a execução do pacote fiscal (endpoint da Fase 11) e recarrega a comparação item a item das 6 impostos com divergências destacadas, filtro 'só divergentes', resumo agregado e exportação.
  </what-built>
  <action>
    Tarefa de verificação humana — nenhuma alteração de código. O executor apresenta os passos de verificação abaixo e pausa aguardando o sinal de retorno do usuário. Ver <how-to-verify>.
  </action>
  <how-to-verify>
    1. Rodar o frontend (`cd frontend && npm run dev`) e o backend, logar como usuário admin.
    2. Confirmar que a seção 'Teste Pacote Fiscal' aparece na sidebar; abrir 'Comparação Fiscal' (/pacote-fiscal/comparacao).
    3. Buscar uma NF-e de saída existente (digitar >=3 chars do número ou chave) e selecionar um resultado do autocomplete.
    4. Clicar 'Executar' — o botão mostra spinner, e ao concluir a tabela recarrega automaticamente na mesma tela (sem navegação extra).
    5. Verificar: 4 cards de resumo, 6 chips por imposto, tabela com Esperado/Calculado/Diferença para ICMS/ICMS-ST/PIS/COFINS/IBS/CBS, células divergentes em vermelho (tolerância zero).
    6. Confirmar os badges de estado: itens com status != 'ok' = 'Não calculado'; itens de uma nota nunca executada (buscar sem clicar Executar) = 'Nunca executado' (badge distinto).
    7. Ativar 'Só divergentes' e confirmar que itens sem divergência somem.
    8. Passar o mouse nos headers IBS/CBS e confirmar o tooltip de aviso de dado zerado.
    9. Clicar 'Exportar Excel' e 'Exportar CSV' e confirmar os downloads.
    10. (Opcional/segurança) logar como usuário NÃO-admin e confirmar que a seção não aparece e que /pacote-fiscal/comparacao redireciona.
  </how-to-verify>
  <verify><human-check>Usuário confirma os 10 passos de verificação end-to-end</human-check></verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados</resume-signal>
  <done>Fluxo end-to-end aprovado pelo usuário (ou problemas descritos para correção).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (rota/nav) → página admin | esconder nav é UX; o bloqueio real de rota é o AdminRoute + o admin gate do backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Elevation of Privilege | AppSidebar/navigation (item de nav) | mitigate | Gate `adminOnly: true` na NavSection + lógica `if (section.adminOnly && !isAdmin) return null` (AppSidebar linha 273) escondem o item para não-admin |
| T-12-08 | Elevation of Privilege | App.tsx rota /pacote-fiscal/comparacao | mitigate | Rota envolta em `<AdminRoute>` (redireciona não-admin) — defesa em profundidade; o limite real permanece o `withAuth "admin"` dos endpoints (Plan 12-01 T-12-02) |
| T-12-SC | Tampering | npm installs | accept | Zero pacotes novos — apenas edição de config e reuso de ícones lucide-react já instalados |
</threat_model>

<verification>
- `cd frontend && npm run build` conclui sem erro
- Grep confirma o módulo pacotefiscal, o branch getActiveModule, a NavSection adminOnly e a rota AdminRoute
- Checkpoint humano aprova o fluxo end-to-end
</verification>

<success_criteria>
- Admin vê e acessa 'Teste Pacote Fiscal → Comparação Fiscal'; não-admin não vê e é redirecionado
- Fluxo busca→executar→comparar validado visualmente
</success_criteria>

<output>
Create `.planning/phases/12-tela-compara-o-fiscal-navega-o/12-03-SUMMARY.md` when done
</output>
