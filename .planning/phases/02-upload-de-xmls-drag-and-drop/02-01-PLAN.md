---
phase: 02-upload-de-xmls-drag-and-drop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/074_add_source_to_nfe_tables.sql
  - backend/migrations/075_create_nfe_itens_tables.sql
  - backend/migrations/076_create_xml_upload_batches.sql
  - backend/migrations/077_add_regime_tributario_to_companies.sql
  - backend/migrations/078_create_vw_xml_panels.sql
autonomous: true
requirements:
  - XML-03
  - XML-04
  - XML-05
  - XML-06
  - XML-08

must_haves:
  truths:
    - "Coluna source existe em nfe_entradas, nfe_saidas e cte_entradas com DEFAULT 'oracle_bridge'"
    - "Tabelas nfe_entradas_itens e nfe_saidas_itens existem com todos os campos de item (NCM, CFOP, CST, valores tributários, IBS/CBS, cclasstrib)"
    - "Tabela xml_upload_batches existe com campos de histórico (company_id, uploaded_by, tipo, status, contagens)"
    - "Coluna regime_tributario existe em companies (lucro_real | lucro_presumido | simples_nacional)"
    - "Views vw_xml_entradas_resumo, vw_xml_saidas_resumo, vw_xml_ctes_resumo, vw_xml_itens_ncm existem"
  artifacts:
    - path: "backend/migrations/074_add_source_to_nfe_tables.sql"
      provides: "Coluna source com DEFAULT oracle_bridge + CHECK constraint + índices"
    - path: "backend/migrations/075_create_nfe_itens_tables.sql"
      provides: "Tabelas nfe_entradas_itens e nfe_saidas_itens com UNIQUE (nfe_id, n_item)"
    - path: "backend/migrations/076_create_xml_upload_batches.sql"
      provides: "Tabela xml_upload_batches para histórico de uploads"
    - path: "backend/migrations/077_add_regime_tributario_to_companies.sql"
      provides: "Coluna regime_tributario em companies (per D-03)"
    - path: "backend/migrations/078_create_vw_xml_panels.sql"
      provides: "4 views PostgreSQL para painel XML (per D-10, D-11)"
  key_links:
    - from: "nfe_entradas_itens.nfe_id"
      to: "nfe_entradas.id"
      via: "FOREIGN KEY ON DELETE CASCADE"
      pattern: "REFERENCES nfe_entradas"
    - from: "xml_upload_batches.company_id"
      to: "companies.id"
      via: "FOREIGN KEY"
      pattern: "REFERENCES companies"
    - from: "vw_xml_entradas_resumo"
      to: "nfe_entradas"
      via: "VIEW SELECT com GROUP BY company_id, filial, mes_ano"
      pattern: "FROM nfe_entradas"
---

<objective>
Criar todas as migrations de banco de dados necessárias para a Phase 02: coluna source nas tabelas NFe/CTe, tabelas de itens por linha de nota, tabela de histórico de uploads, campo de regime tributário em companies, e as views PostgreSQL para os painéis XML.

Purpose: Estabelecer o schema antes de qualquer código de handler ou frontend. Todas as migrations seguem o padrão IF NOT EXISTS e são idempotentes — seguras para re-execução no startup do servidor.

Output: 5 arquivos SQL de migration (074 a 078) prontos para aplicação automática pelo migrator em backend/main.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-upload-de-xmls-drag-and-drop/02-RESEARCH.md
@.planning/phases/02-upload-de-xmls-drag-and-drop/02-CONTEXT.md

Última migration existente: 073_admin_destructive_actions.sql
Próximos números disponíveis: 074, 075, 076, 077, 078

Schema atual das tabelas (conforme RESEARCH.md Q9):
- nfe_entradas: tem id, company_id, chave_nfe, forn_cnpj/nome, dest_cnpj/nome, v_bc/v_icms/v_pis/v_cofins (ICMSTot), IBS/CBS. NÃO tem source, NÃO tem itens.
- nfe_saidas: mesma estrutura com emit_cnpj/nome. NÃO tem source.
- cte_entradas: estrutura mais simples. NÃO tem source.
- companies: tem id, name, cnpj. NÃO tem regime_tributario.
- forn_simples: tem cnpj (PK), created_at. Sem company_id (comportamento global intencional).

Constraint existente em nfe_entradas: uq_nfe_entradas_company_chave (usada no ON CONFLICT DO UPDATE).
Constraint existente em nfe_saidas: verificar nome similar em erp_bridge_batch.go.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 074 — Coluna source em nfe_entradas, nfe_saidas, cte_entradas</name>
  <files>backend/migrations/074_add_source_to_nfe_tables.sql</files>
  <action>
Criar migration 074 que adiciona a coluna source às três tabelas de notas. Regras:

1. Usar ADD COLUMN IF NOT EXISTS para idempotência.
2. Tipo TEXT NOT NULL DEFAULT 'oracle_bridge' — dados históricos ficam classificados como oracle_bridge sem reescrever linhas (PostgreSQL 15 usa virtual default).
3. Adicionar CHECK constraint em cada tabela: source IN ('oracle_bridge', 'xml_upload', 'manual').
4. Criar índices compostos (company_id, source) nas três tabelas para suportar o relatório de cobertura da Phase 4.
5. Adicionar COMMENT ON COLUMN explicando os valores válidos.

Estrutura do arquivo:
- Cabeçalho de comentário explicando o propósito (per D-17, D-12).
- ALTER TABLE nfe_entradas ADD COLUMN IF NOT EXISTS source TEXT NOT NULL DEFAULT 'oracle_bridge'.
- ALTER TABLE nfe_entradas ADD CONSTRAINT IF NOT EXISTS chk_nfe_entradas_source CHECK (source IN ('oracle_bridge', 'xml_upload', 'manual')).
- Repetir para nfe_saidas e cte_entradas.
- CREATE INDEX IF NOT EXISTS idx_nfe_entradas_source ON nfe_entradas(company_id, source).
- Repetir para nfe_saidas e cte_entradas.
- COMMENT ON COLUMN para cada tabela/coluna source.

NÃO usar BEGIN/COMMIT — o migrator do projeto aplica migrations individualmente.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS source" /home/claudiobezerra/projetos/FB_APU04/backend/migrations/074_add_source_to_nfe_tables.sql</automated>
  </verify>
  <done>Arquivo existe com 3 ALTER TABLE ADD COLUMN source, 3 CHECK constraints, 3 índices, 3 COMMENTs.</done>
</task>

<task type="auto">
  <name>Task 2: Migrations 075, 076, 077 — Tabelas de itens, histórico de uploads, regime tributário</name>
  <files>
    backend/migrations/075_create_nfe_itens_tables.sql,
    backend/migrations/076_create_xml_upload_batches.sql,
    backend/migrations/077_add_regime_tributario_to_companies.sql
  </files>
  <action>
Criar três migrations independentes:

**075_create_nfe_itens_tables.sql** (per D-07, D-09):
Criar nfe_entradas_itens e nfe_saidas_itens. Cada tabela deve ter:
- id UUID PRIMARY KEY DEFAULT gen_random_uuid()
- nfe_id UUID NOT NULL REFERENCES nfe_entradas(id) ON DELETE CASCADE (ou nfe_saidas para saidas)
- company_id UUID NOT NULL (desnormalizado para queries eficientes sem JOIN)
- n_item SMALLINT NOT NULL (número sequencial do item, 1..N)
- c_prod VARCHAR(60) (código do produto — <cProd>)
- x_prod VARCHAR(120) NOT NULL (descrição — <xProd>)
- ncm VARCHAR(8) (<NCM>)
- cfop VARCHAR(4) (<CFOP>)
- cst_icms VARCHAR(3) (<CST> ou <CSOSN> do grupo ICMS)
- cst_pis VARCHAR(2) (<CST> do grupo PIS)
- cst_cofins VARCHAR(2) (<CST> do grupo COFINS)
- v_prod NUMERIC(15,2) DEFAULT 0 (valor do produto)
- v_total_item NUMERIC(15,2) DEFAULT 0 (valor total do item com impostos)
- v_bc_icms NUMERIC(15,2) DEFAULT 0
- v_icms NUMERIC(15,2) DEFAULT 0
- v_ipi NUMERIC(15,2) DEFAULT 0
- v_bc_pis NUMERIC(15,2) DEFAULT 0
- v_pis NUMERIC(15,2) DEFAULT 0
- v_bc_cofins NUMERIC(15,2) DEFAULT 0
- v_cofins NUMERIC(15,2) DEFAULT 0
- v_ibs NUMERIC(15,2) DEFAULT 0 (IBS quando presente)
- v_cbs NUMERIC(15,2) DEFAULT 0 (CBS quando presente)
- cclasstrib VARCHAR(20) (classificação tributária, nullable — per D-07)
- UNIQUE (nfe_id, n_item) — evita duplicatas em re-upload

Criar índices: (company_id, ncm) e (nfe_id) para as queries do relatório CCLASSTRIB.
IMPORTANTE: Não criar tabela cte_entradas_itens nesta migration — CT-es de carga não têm itens no formato NFe.

**076_create_xml_upload_batches.sql** (per D-13):
Criar xml_upload_batches:
- id UUID PRIMARY KEY DEFAULT gen_random_uuid()
- company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE
- uploaded_by UUID REFERENCES users(id) ON DELETE SET NULL
- tipo TEXT NOT NULL — 'entradas' | 'saidas' | 'ctes'
- filename TEXT (nome original do arquivo ZIP ou XML)
- total_count INT NOT NULL DEFAULT 0
- processed_count INT NOT NULL DEFAULT 0
- imported_count INT NOT NULL DEFAULT 0
- rejected_count INT NOT NULL DEFAULT 0
- status TEXT NOT NULL DEFAULT 'pending' — CHECK (status IN ('pending','processing','done','failed'))
- error_details JSONB (detalhes dos erros por XML rejeitado)
- xml_data BYTEA — armazena bytes dos XMLs compactados para batches assíncronos (>50 XMLs); NULL para batches síncronos
- created_at TIMESTAMPTZ DEFAULT NOW()
- completed_at TIMESTAMPTZ
Índice: (company_id, created_at DESC) para consulta de histórico.
Índice: (status) WHERE status IN ('pending','processing') — partial index para o worker.

**077_add_regime_tributario_to_companies.sql** (per D-03):
- ALTER TABLE companies ADD COLUMN IF NOT EXISTS regime_tributario TEXT DEFAULT 'nao_informado'
- CHECK constraint: regime_tributario IN ('lucro_real', 'lucro_presumido', 'simples_nacional', 'nao_informado')
- COMMENT ON COLUMN companies.regime_tributario para documentar os valores.
  </action>
  <verify>
    <automated>ls /home/claudiobezerra/projetos/FB_APU04/backend/migrations/075_create_nfe_itens_tables.sql /home/claudiobezerra/projetos/FB_APU04/backend/migrations/076_create_xml_upload_batches.sql /home/claudiobezerra/projetos/FB_APU04/backend/migrations/077_add_regime_tributario_to_companies.sql 2>&1</automated>
  </verify>
  <done>3 arquivos existem. 075 tem CREATE TABLE nfe_entradas_itens e nfe_saidas_itens com UNIQUE (nfe_id, n_item). 076 tem CREATE TABLE xml_upload_batches. 077 tem ALTER TABLE companies ADD COLUMN regime_tributario.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 078 — Views PostgreSQL para painel XML</name>
  <files>backend/migrations/078_create_vw_xml_panels.sql</files>
  <action>
Criar 4 views regulares (não materializadas) para o painel de XMLs importados (per D-10, D-11).
Seguir o padrão de vw_nfe_entradas_impostos (migration 072) — GROUP BY com SUM(COALESCE(...)).

**vw_xml_entradas_resumo** — agrega nfe_entradas por filial/mês para o painel de Entradas:
```sql
CREATE OR REPLACE VIEW vw_xml_entradas_resumo AS
SELECT
    ne.company_id,
    ne.forn_cnpj,
    ne.forn_nome,
    ne.mes_ano,
    ne.source,
    COUNT(*) AS qtd_notas,
    SUM(COALESCE(ne.v_nf, 0)) AS v_total,
    SUM(COALESCE(ne.v_bc, 0)) AS v_bc_icms,
    SUM(COALESCE(ne.v_icms_dest, 0) + COALESCE(ne.v_icms_remet, 0)) AS v_icms,
    SUM(COALESCE(ne.v_pis, 0)) AS v_pis,
    SUM(COALESCE(ne.v_cofins, 0)) AS v_cofins,
    SUM(COALESCE(ne.v_ipi, 0)) AS v_ipi,
    SUM(COALESCE(ne.v_ibs, 0)) AS v_ibs,
    SUM(COALESCE(ne.v_cbs, 0)) AS v_cbs
FROM nfe_entradas ne
GROUP BY ne.company_id, ne.forn_cnpj, ne.forn_nome, ne.mes_ano, ne.source;
```

**vw_xml_saidas_resumo** — mesma lógica para nfe_saidas com emit_cnpj/emit_nome.

**vw_xml_ctes_resumo** — agrega cte_entradas por transportadora/mês:
```sql
CREATE OR REPLACE VIEW vw_xml_ctes_resumo AS
SELECT
    ce.company_id,
    ce.emit_cnpj,
    ce.emit_nome,
    ce.mes_ano,
    ce.source,
    COUNT(*) AS qtd_ctes,
    SUM(COALESCE(ce.v_rec, 0)) AS v_total_frete,
    SUM(COALESCE(ce.v_bc_icms, 0)) AS v_bc_icms,
    SUM(COALESCE(ce.v_icms, 0)) AS v_icms,
    SUM(COALESCE(ce.v_ibs, 0)) AS v_ibs,
    SUM(COALESCE(ce.v_cbs, 0)) AS v_cbs
FROM cte_entradas ce
GROUP BY ce.company_id, ce.emit_cnpj, ce.emit_nome, ce.mes_ano, ce.source;
```

**vw_xml_itens_ncm** — agrega nfe_entradas_itens por NCM para o relatório CCLASSTRIB (per D-16a):
```sql
CREATE OR REPLACE VIEW vw_xml_itens_ncm AS
SELECT
    ei.company_id,
    ei.ncm,
    COUNT(DISTINCT ei.cst_pis) AS variantes_cst_pis,
    COUNT(DISTINCT ei.cst_cofins) AS variantes_cst_cofins,
    COUNT(DISTINCT ei.cclasstrib) FILTER (WHERE ei.cclasstrib IS NOT NULL) AS variantes_cclasstrib,
    COUNT(*) AS qtd_itens,
    SUM(COALESCE(ei.v_pis, 0)) AS v_pis_total,
    SUM(COALESCE(ei.v_cofins, 0)) AS v_cofins_total,
    BOOL_OR(ei.cclasstrib IS NULL) AS tem_cclasstrib_nulo
FROM nfe_entradas_itens ei
GROUP BY ei.company_id, ei.ncm;
```

IMPORTANTE: Verificar colunas reais existentes nas tabelas de produção antes de referenciar nomes como v_nf, v_bc, v_icms_dest — usar os nomes confirmados no schema (RESEARCH.md Q9). Se nome exato for incerto, usar COALESCE com o campo mais provável e 0 como fallback. Usar CREATE OR REPLACE VIEW para idempotência.
  </action>
  <verify>
    <automated>grep -c "CREATE OR REPLACE VIEW" /home/claudiobezerra/projetos/FB_APU04/backend/migrations/078_create_vw_xml_panels.sql</automated>
  </verify>
  <done>Arquivo existe com 4 views (vw_xml_entradas_resumo, vw_xml_saidas_resumo, vw_xml_ctes_resumo, vw_xml_itens_ncm). Cada view usa CREATE OR REPLACE e referencia as tabelas corretas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Migration runner → PostgreSQL | SQL aplicado em banco de produção no startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Migration runner | accept | Migrations são idempotentes (IF NOT EXISTS); re-aplicação em produção é segura |
| T-02-01-02 | Denial of Service | ADD COLUMN em tabelas grandes | mitigate | Usar ADD COLUMN IF NOT EXISTS com DEFAULT — PostgreSQL 15 usa virtual default sem reescrever rows. Executar em janela de manutenção se tabelas > 10M rows |
| T-02-01-03 | Information Disclosure | CHECK constraint source | accept | Valores válidos documentados em COMMENT ON COLUMN; sem exposição de dados |
</threat_model>

<verification>
Verificação pós-execução (após restart do servidor Go com as migrations aplicadas):

1. Confirmar que as 5 migrations aparecem em schema_migrations:
```sql
SELECT filename FROM schema_migrations WHERE filename LIKE '07%' ORDER BY filename;
```
Deve retornar: 074, 075, 076, 077, 078.

2. Confirmar coluna source:
```sql
SELECT column_name, column_default FROM information_schema.columns
WHERE table_name = 'nfe_entradas' AND column_name = 'source';
```
Deve retornar source com DEFAULT 'oracle_bridge'.

3. Confirmar tabela de itens:
```sql
SELECT COUNT(*) FROM information_schema.columns WHERE table_name = 'nfe_entradas_itens';
```
Deve retornar >= 20 colunas.

4. Confirmar campo regime_tributario:
```sql
SELECT column_name FROM information_schema.columns
WHERE table_name = 'companies' AND column_name = 'regime_tributario';
```

5. Confirmar views:
```sql
SELECT table_name FROM information_schema.views WHERE table_name LIKE 'vw_xml%';
```
Deve retornar 4 views.
</verification>

<success_criteria>
- 5 arquivos .sql existem em backend/migrations/ com números 074-078
- Todos os arquivos são idempotentes (IF NOT EXISTS / CREATE OR REPLACE)
- Nenhum arquivo usa BEGIN/COMMIT explícito (o migrator controla a transação)
- Coluna source em 3 tabelas com DEFAULT 'oracle_bridge' e CHECK constraint
- 2 tabelas de itens (nfe_entradas_itens, nfe_saidas_itens) com UNIQUE (nfe_id, n_item)
- Tabela xml_upload_batches com campo status e partial index para o worker
- Campo regime_tributario em companies com valores válidos
- 4 views vw_xml_* criadas com CREATE OR REPLACE
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-upload-de-xmls-drag-and-drop/02-01-SUMMARY.md` com:
- Lista de migrations criadas (074-078) e o que cada uma adiciona
- Nomes exatos das constraints e índices criados
- Schema completo de nfe_entradas_itens (para o executor do plano 02-02 usar sem precisar ler o SQL)
- Schema de xml_upload_batches (para o executor de 02-02)
- Qualquer ajuste feito nos nomes de colunas ao verificar schema real
</output>
