---
phase: 09-modulos-2x-analytics-dimensional
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - frontend/src/pages/Reforma22CfopAnalysis.tsx
  - frontend/src/pages/Reforma21NcmAnalysis.tsx
  - frontend/src/pages/Reforma23UfDestino.tsx
  - frontend/src/pages/Reforma24B2bB2c.tsx
  - frontend/src/lib/navigation.ts
  - frontend/src/App.tsx
autonomous: true
requirements: [RFMC-01, RFMC-02, RFMC-03, RFMC-04]

must_haves:
  truths:
    - "Reforma22CfopAnalysis.tsx consome GET /api/reforma/modulo2/cfop e renderiza tabela + gráfico de barras por natureza CFOP"
    - "Reforma21NcmAnalysis.tsx consome GET /api/reforma/modulo2/ncm e renderiza tabela com alíquota efetiva ICMS vs IBS+CBS projetada + badge IS"
    - "Reforma23UfDestino.tsx consome GET /api/reforma/modulo2/uf-destino e renderiza tabela + mapa coroplético react-simple-maps colorido por valor_total"
    - "Reforma24B2bB2c.tsx consome GET /api/reforma/modulo2/b2b-b2c e renderiza PieChart + tabela com alerta sobre qtd_sem_ind_final"
    - "navigation.ts remove disabled:true das 4 tabs (Análise CFOP, Análise NCM, UF Destino, B2B vs B2C)"
    - "App.tsx adiciona 4 novas rotas /reforma/cfop, /reforma/ncm, /reforma/uf-destino, /reforma/b2b-b2c"
  artifacts:
    - path: "frontend/src/pages/Reforma22CfopAnalysis.tsx"
      provides: "Módulo 2.2 — Análise por CFOP"
      contains: "CfopAnalysis"
      min_lines: 80
    - path: "frontend/src/pages/Reforma21NcmAnalysis.tsx"
      provides: "Módulo 2.1 — Análise por NCM"
      contains: "NcmAnalysis"
      min_lines: 80
    - path: "frontend/src/pages/Reforma23UfDestino.tsx"
      provides: "Módulo 2.3 — UF Destino com mapa coroplético"
      contains: "ComposableMap"
      min_lines: 100
    - path: "frontend/src/pages/Reforma24B2bB2c.tsx"
      provides: "Módulo 2.4 — Segmentação B2B vs B2C"
      contains: "B2bB2c"
      min_lines: 80
  key_links:
    - from: "frontend/src/App.tsx"
      to: "Reforma22CfopAnalysis, Reforma21NcmAnalysis, Reforma23UfDestino, Reforma24B2bB2c"
      via: "import + <Route path=...>"
      pattern: "path=\"/reforma/cfop\""
    - from: "frontend/src/lib/navigation.ts"
      to: "4 tabs habilitadas no módulo reforma"
      via: "remoção de disabled:true"
      pattern: "{ label: 'Análise CFOP', path: '/reforma/cfop' }"
---

<objective>
Implementar o frontend da Phase 9 — 4 páginas de análise dimensional (Módulos 2.1 a 2.4) + ativação das 4 tabs de navegação + 4 novas rotas em App.tsx.

Purpose: Tornar os endpoints da Phase 9 backend visíveis e interativos para o usuário fiscal. O mapa coroplético (Módulo 2.3) usa react-simple-maps que já está instalado e brazil-states.json já está em /public/.

Output: 4 páginas React + atualização de navigation.ts (remove `disabled:true`) + 4 rotas em App.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
Padrão de página existente (ler como referência obrigatória):
  frontend/src/pages/Reforma11CreditosBloqueados.tsx (tabela + BarChart + CSV download + fmtBRL + Skeleton)
  frontend/src/pages/Reforma14SplitPayment.tsx (sem fetch manual — useQuery pattern; sem CSV)

Fetch pattern (NÃO usar tanstack query — usar fetch direto igual Reforma11):
  const res = await fetch('/api/reforma/modulo2/cfop')
  if (!res.ok) throw new Error(...)
  const data: Modulo22Response = await res.json()

Auth: Não é necessário passar Authorization header nas rotas /api/reforma — o cookie de sessão cuida disso (confirmar olhando Reforma11, que não passa headers manualmente).

Componentes UI disponíveis: Card, CardContent, CardHeader, CardTitle, Badge, Button, Separator, Alert, AlertDescription, Skeleton, Table, TableBody, TableCell, TableHead, TableHeader, TableRow (todos em @/components/ui/).

Charts: recharts (BarChart, PieChart, Cell, Tooltip, Legend, ResponsiveContainer, XAxis, YAxis, CartesianGrid) — já instalado.

Mapa: react-simple-maps já instalado (v3). Padrão:
  import { ComposableMap, Geographies, Geography } from 'react-simple-maps'
  const geoUrl = '/brazil-states.json'
  <ComposableMap projection="geoMercator" projectionConfig={{ center: [-54, -15], scale: 800 }}>
    <Geographies geography={geoUrl}>
      {({ geographies }) => geographies.map(geo => {
        const uf = geo.properties.sigla  // campo sigla no brazil-states.json
        const row = data.rows.find(r => r.dest_uf === uf)
        const fill = row ? colorScale(row.valor_total) : '#e5e7eb'
        return <Geography key={geo.rsmKey} geography={geo} fill={fill} stroke="#fff" />
      })}
    </Geographies>
  </ComposableMap>

Color scale para mapa: interpolação linear entre '#dbeafe' (mínimo) e '#1d4ed8' (máximo) usando valor_total. Implementar como função JS pura (não instalar d3-scale).

navigation.ts (linhas atuais com disabled:true que devem ser habilitadas):
  { label: 'Análise CFOP',   path: '/reforma/cfop',         disabled: true },
  { label: 'Análise NCM',    path: '/reforma/ncm',          disabled: true },
  { label: 'UF Destino',     path: '/reforma/uf-destino',   disabled: true },
  { label: 'B2B vs B2C',    path: '/reforma/b2b-b2c',      disabled: true },
  Correção: remover `disabled: true` dessas 4 entradas (NÃO mudar label ou path).

App.tsx (padrão de adição de rotas — linhas 180-185):
  import Reforma22CfopAnalysis from './pages/Reforma22CfopAnalysis'
  // ... outros imports
  <Route path="/reforma/cfop"        element={<Reforma22CfopAnalysis />} />
  <Route path="/reforma/ncm"         element={<Reforma21NcmAnalysis />} />
  <Route path="/reforma/uf-destino"  element={<Reforma23UfDestino />} />
  <Route path="/reforma/b2b-b2c"     element={<Reforma24B2bB2c />} />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reforma22CfopAnalysis.tsx — Módulo 2.2 Análise por CFOP</name>
  <files>frontend/src/pages/Reforma22CfopAnalysis.tsx</files>
  <read_first>
    - frontend/src/pages/Reforma11CreditosBloqueados.tsx (padrão COMPLETO: fetch, useState, loading/error states, fmtBRL helper, Skeleton, BarChart, Table, CSV download com fetch+blob)
    - frontend/src/lib/utils.ts (import cn se necessário)
  </read_first>
  <action>
    Criar `frontend/src/pages/Reforma22CfopAnalysis.tsx`.

    Types:
    ```typescript
    interface Modulo22Row {
      natureza_cfop: string
      qtd_notas: number
      valor_total: number
      ibs_projetado: number
      cbs_projetado: number
    }
    interface Modulo22Response {
      rows: Modulo22Row[]
      total_ibs: number
      total_cbs: number
      aliq_ibs_pct: number
      aliq_cbs_pct: number
    }
    ```

    Helpers: `function fmtBRL(v: number | null | undefined): string` (mesmo padrão de Reforma11).

    Componente `export default function Reforma22CfopAnalysis()`:
    - useState para data (Modulo22Response | null), loading (true), error (string | null).
    - useEffect → fetch('/api/reforma/modulo2/cfop') → setData / setError. Sempre setLoading(false) no finally.
    - Render: `<h1>Módulo 2.2 — Análise por CFOP</h1>` + parágrafo disclaimer "CFOPs de transferência são exibidos para contexto mas excluídos do cálculo de IBS/CBS."
    - Skeleton durante loading (mesmo padrão Reforma11).
    - Alert se error.
    - Se data: mostrar 2 KPI cards (Total IBS projetado, Total CBS projetado) usando Card.
    - BarChart (recharts ResponsiveContainer altura 300): XAxis=natureza_cfop, 3 barras: "Valor Total (R$)", "IBS Proj (R$)", "CBS Proj (R$)" com cores azul/verde/laranja.
    - Table com colunas: Natureza, Qtd Notas, Valor Total, IBS Projetado, CBS Projetado.
    - Botão "Exportar CSV" → fetch('/api/reforma/modulo2/cfop/csv') → download via blob (mesmo padrão Reforma11 com Authorization header se necessário; confirmar olhando Reforma11).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -i "reforma22\|modulo22" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `Reforma22CfopAnalysis.tsx` existe e contém `export default function Reforma22CfopAnalysis`
    - Arquivo contém `fetch('/api/reforma/modulo2/cfop')`
    - Arquivo contém `fetch('/api/reforma/modulo2/cfop/csv')` (CSV download)
    - Arquivo contém `natureza_cfop` (campo da response)
    - `npx tsc --noEmit` passa sem erros referentes a este arquivo
  </acceptance_criteria>
  <done>Reforma22CfopAnalysis.tsx criado; TypeScript compila; fetch para cfop e cfop/csv implementado.</done>
</task>

<task type="auto">
  <name>Task 2: Reforma21NcmAnalysis.tsx — Módulo 2.1 Análise por NCM</name>
  <files>frontend/src/pages/Reforma21NcmAnalysis.tsx</files>
  <read_first>
    - frontend/src/pages/Reforma11CreditosBloqueados.tsx (padrão fetch, tabela, BarChart, badges)
    - frontend/src/pages/Reforma12Reprecificacao.tsx (padrão de tabela com badge IS e variação percentual, se existir)
  </read_first>
  <action>
    Criar `frontend/src/pages/Reforma21NcmAnalysis.tsx`.

    Types:
    ```typescript
    interface Modulo21Row {
      ncm: string
      x_prod: string
      vl_prod: number
      vl_icms: number
      aliq_icms_efet: number
      ibs_projetado: number
      cbs_projetado: number
      is_flag: boolean
    }
    interface Modulo21Response {
      rows: Modulo21Row[]
      aliq_ibs_pct: number
      aliq_cbs_pct: number
    }
    ```

    Componente `export default function Reforma21NcmAnalysis()`:
    - Mesmo padrão de fetch/loading/error/Skeleton de Reforma22.
    - `<h1>Módulo 2.1 — Análise por NCM</h1>`.
    - Tabela com colunas: NCM, Descrição do Produto, VL Prod, VL ICMS, Alíq ICMS Efet (%), IBS Proj (R$), CBS Proj (R$), IS.
    - Coluna IS: `<Badge variant="destructive">IS</Badge>` se is_flag=true, senão `—`.
    - Alíq ICMS Efet: mostrar como `{row.aliq_icms_efet.toFixed(1)}%`.
    - BarChart com dados agrupados: XAxis = ncm (truncado a 8 chars), 2 barras: ICMS Atual vs IBS+CBS Proj.
    - Botão "Exportar CSV" → fetch('/api/reforma/modulo2/ncm/csv') → blob download.
    - Nota de rodapé: "Limitado aos 100 NCMs de maior volume. IS = Imposto Seletivo (conforme ncm_cclasstrib_reforma)."
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -i "reforma21\|modulo21" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `Reforma21NcmAnalysis.tsx` existe e contém `export default function Reforma21NcmAnalysis`
    - Arquivo contém `fetch('/api/reforma/modulo2/ncm')`
    - Arquivo contém `is_flag` e `badge` (renderização do IS flag)
    - `npx tsc --noEmit` passa sem erros referentes a este arquivo
  </acceptance_criteria>
  <done>Reforma21NcmAnalysis.tsx criado com tabela NCM, badge IS, BarChart e CSV download.</done>
</task>

<task type="auto">
  <name>Task 3: Reforma23UfDestino.tsx — Módulo 2.3 UF Destino com mapa coroplético</name>
  <files>frontend/src/pages/Reforma23UfDestino.tsx</files>
  <read_first>
    - frontend/src/pages/Reforma11CreditosBloqueados.tsx (padrão fetch/loading/Skeleton/Table)
    - Documentação local em node_modules/react-simple-maps se disponível; senão usar o padrão descrito em interfaces.
  </read_first>
  <action>
    Criar `frontend/src/pages/Reforma23UfDestino.tsx`.

    Types:
    ```typescript
    interface Modulo23Row {
      dest_uf: string
      qtd_notas: number
      valor_total: number
      vl_icms: number
      ibs_projetado: number
      cbs_projetado: number
    }
    interface Modulo23Response {
      rows: Modulo23Row[]
      aliq_ibs_pct: number
      aliq_cbs_pct: number
    }
    ```

    Componente `export default function Reforma23UfDestino()`:
    - Mesmo padrão fetch/loading/error/Skeleton.
    - `<h1>Módulo 2.3 — Análise por UF de Destino</h1>`.
    - Mapa coroplético react-simple-maps (layout 2 colunas em md: mapa à esquerda, tabela à direita):
      ```typescript
      import { ComposableMap, Geographies, Geography } from 'react-simple-maps'
      const geoUrl = '/brazil-states.json'
      ```
      - Calcular minVal e maxVal de valor_total dos rows.
      - Função `colorScale(val: number): string` — interpolação linear hexadecimal entre '#dbeafe' (mínimo) e '#1d4ed8' (máximo). Se val <= 0 retornar '#e5e7eb'.
      - ComposableMap com `projection="geoMercator"` e `projectionConfig={{ center: [-54, -15], scale: 800 }}`.
      - Geographies: para cada Geography, buscar row onde dest_uf === geo.properties.sigla; fill = colorScale(row?.valor_total ?? 0); tooltip simples com title do browser (geo.properties.name).
      - Legenda manual abaixo do mapa: "Cor mais escura = maior volume de vendas".
    - Tabela abaixo (ou ao lado): colunas UF, Qtd Notas, Valor Total (R$), ICMS Real (R$), IBS Proj (R$), CBS Proj (R$). Ordenar por valor_total DESC (já vem ordenado do backend).
    - NÃO implementar CSV nesta página.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -i "reforma23\|modulo23" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `Reforma23UfDestino.tsx` existe e contém `export default function Reforma23UfDestino`
    - Arquivo contém `import { ComposableMap, Geographies, Geography }` (react-simple-maps)
    - Arquivo contém `fetch('/api/reforma/modulo2/uf-destino')`
    - Arquivo contém `geoUrl` e `/brazil-states.json`
    - `npx tsc --noEmit` passa sem erros referentes a este arquivo
  </acceptance_criteria>
  <done>Reforma23UfDestino.tsx criado com mapa coroplético react-simple-maps + tabela de UF.</done>
</task>

<task type="auto">
  <name>Task 4: Reforma24B2bB2c.tsx — Módulo 2.4 Segmentação B2B vs B2C</name>
  <files>frontend/src/pages/Reforma24B2bB2c.tsx</files>
  <read_first>
    - frontend/src/pages/Reforma14SplitPayment.tsx (padrão PieChart / donut em recharts, se existir — caso contrário usar BarChart)
    - frontend/src/pages/Reforma11CreditosBloqueados.tsx (padrão tabela + alert)
  </read_first>
  <action>
    Criar `frontend/src/pages/Reforma24B2bB2c.tsx`.

    Types:
    ```typescript
    interface Modulo24Row {
      segmento: string      // 'b2b_credit' | 'b2c' | 'sem_classificacao'
      qtd_notas: number
      valor_total: number
      ibs_projetado: number
      cbs_projetado: number
    }
    interface Modulo24Response {
      rows: Modulo24Row[]
      qtd_sem_ind_final: number
      aliq_ibs_pct: number
      aliq_cbs_pct: number
    }
    ```

    Helper de label: `function segmentoLabel(s: string): string { return s === 'b2b_credit' ? 'B2B (Creditável)' : s === 'b2c' ? 'B2C (Consumidor Final)' : 'Sem Classificação' }`.

    Componente `export default function Reforma24B2bB2c()`:
    - Mesmo padrão fetch/loading/error/Skeleton.
    - `<h1>Módulo 2.4 — Segmentação B2B vs B2C</h1>`.
    - Se qtd_sem_ind_final > 0: mostrar `<Alert>` (variant="default", ícone Info): "**{qtd_sem_ind_final} nota(s)** sem campo `ind_final` foram classificadas pelo CPF/CNPJ do destinatário (fallback). Notas históricas importadas antes da Phase 6 podem ter classificação menos precisa."
    - PieChart (recharts) ou BarChart com 3 segmentos (b2b_credit, b2c, sem_classificacao) mostrando valor_total. Cores sugeridas: azul (#3b82f6), verde (#22c55e), cinza (#9ca3af).
    - Tabela com colunas: Segmento, Qtd Notas, Valor Total (R$), IBS Proj (R$), CBS Proj (R$).
    - Nota de rodapé: "B2B Creditável: destinatário CNPJ (não Simples). B2C: ind_final=1 ou CPF destinatário. b2b_nocredit não determinável sem dados do regime tributário do cliente."
    - NÃO implementar CSV nesta página.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -i "reforma24\|modulo24" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `Reforma24B2bB2c.tsx` existe e contém `export default function Reforma24B2bB2c`
    - Arquivo contém `fetch('/api/reforma/modulo2/b2b-b2c')`
    - Arquivo contém `qtd_sem_ind_final` (campo do aviso de notas históricas)
    - Arquivo contém `segmentoLabel` ou equivalente para exibição amigável dos segmentos
    - `npx tsc --noEmit` passa sem erros referentes a este arquivo
  </acceptance_criteria>
  <done>Reforma24B2bB2c.tsx criado com alerta ind_final, gráfico de segmentação e tabela.</done>
</task>

<task type="auto">
  <name>Task 5: Habilitar tabs em navigation.ts e adicionar rotas em App.tsx</name>
  <files>frontend/src/lib/navigation.ts, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/lib/navigation.ts (ver as 4 entradas com disabled:true no módulo reforma — linhas 53-56 aproximadamente)
    - frontend/src/App.tsx (ver onde ficam os imports Reforma1x e as Route do módulo reforma — linhas 31-35 e 180-185)
  </read_first>
  <action>
    Em `frontend/src/lib/navigation.ts`, localizar as 4 entradas do módulo `reforma` com `disabled: true`:
    ```
    { label: 'Análise CFOP',  path: '/reforma/cfop',       disabled: true },
    { label: 'Análise NCM',   path: '/reforma/ncm',         disabled: true },
    { label: 'UF Destino',    path: '/reforma/uf-destino',  disabled: true },
    { label: 'B2B vs B2C',   path: '/reforma/b2b-b2c',     disabled: true },
    ```
    Remover `disabled: true` de cada uma (4 edições cirúrgicas, NÃO alterar label nem path).
    Resultado esperado: `{ label: 'Análise CFOP', path: '/reforma/cfop' }`.

    Em `frontend/src/App.tsx`:
    1. Adicionar 4 imports após os imports Reforma1x existentes:
       ```typescript
       import Reforma22CfopAnalysis from './pages/Reforma22CfopAnalysis'
       import Reforma21NcmAnalysis  from './pages/Reforma21NcmAnalysis'
       import Reforma23UfDestino    from './pages/Reforma23UfDestino'
       import Reforma24B2bB2c       from './pages/Reforma24B2bB2c'
       ```
    2. Adicionar 4 rotas no bloco `{/* Análise Reforma Tributária */}` após as rotas Reforma1x:
       ```tsx
       <Route path="/reforma/cfop"        element={<Reforma22CfopAnalysis />} />
       <Route path="/reforma/ncm"         element={<Reforma21NcmAnalysis />} />
       <Route path="/reforma/uf-destino"  element={<Reforma23UfDestino />} />
       <Route path="/reforma/b2b-b2c"     element={<Reforma24B2bB2c />} />
       ```

    Verificar com `npx tsc --noEmit` que não há erros de TypeScript.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -c "disabled" src/lib/navigation.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/navigation.ts` NÃO contém `{ label: 'Análise CFOP'.*disabled` (nem as outras 3 tabs)
    - `frontend/src/App.tsx` contém `import Reforma22CfopAnalysis` e as 3 outras importações
    - `frontend/src/App.tsx` contém `path="/reforma/cfop"`, `path="/reforma/ncm"`, `path="/reforma/uf-destino"`, `path="/reforma/b2b-b2c"`
    - `cd frontend && npx tsc --noEmit` sai com código 0
    - `grep -c "disabled" frontend/src/lib/navigation.ts` retorna um número menor que antes (4 entradas removidas)
  </acceptance_criteria>
  <done>4 tabs habilitadas em navigation.ts; 4 imports e 4 rotas adicionadas em App.tsx; TypeScript compila sem erros.</done>
</task>

<task type="auto">
  <name>Task 6: Build de produção e verificação final</name>
  <files></files>
  <read_first></read_first>
  <action>
    Executar build de produção para verificar que não há erros de compilação:
    ```bash
    cd frontend && npm run build 2>&1 | tail -20
    ```

    Se o build falhar com erros de TypeScript ou import:
    - Ler o arquivo com erro
    - Corrigir o problema (import path errado, tipo incorreto, propriedade ausente)
    - Reexecutar build

    Executar também TypeScript check geral:
    ```bash
    cd frontend && npx tsc --noEmit 2>&1 | head -30
    ```

    O build deve completar com `✓ built in` sem erros.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` sai com código 0
    - Output contém `✓ built in` (ou equivalente Vite)
    - Nenhuma mensagem de erro TypeScript ou import missing nos 4 novos arquivos
  </acceptance_criteria>
  <done>Build de produção bem-sucedido; os 4 módulos 2.x estão compilados e prontos para deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | Fetch para /api/reforma/modulo2/* com mesma auth cookie do app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-F01 | Information Disclosure | react-simple-maps (mapa coroplético) | accept | Dados geográficos do GeoJSON são públicos (divisão política Brasil). Dados fiscais vêm do backend com auth JWT — mapa apenas coloriza por valor já autenticado. |
| T-09-F02 | Tampering | CSV download (cfop/csv, ncm/csv) | mitigate | Download via fetch com mesma auth — backend valida JWT e company_id. Não usar anchor href sem token. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` sem erros
- `cd frontend && npm run build` sai com código 0
- `grep -c "disabled" frontend/src/lib/navigation.ts` menor que antes (4 entradas removidas)
- `grep -c "reforma/cfop\|reforma/ncm\|reforma/uf-destino\|reforma/b2b" frontend/src/App.tsx` retorna 4
- Todos os 4 arquivos de páginas existem em frontend/src/pages/
</verification>

<success_criteria>
- 4 páginas implementadas: Reforma22CfopAnalysis, Reforma21NcmAnalysis, Reforma23UfDestino (com mapa coroplético), Reforma24B2bB2c
- Mapa coroplético com react-simple-maps + brazil-states.json funcionando (Módulo 2.3)
- Alerta de qtd_sem_ind_final no Módulo 2.4 (RFMC-04)
- 4 tabs de navegação habilitadas (sem disabled:true)
- 4 rotas registradas em App.tsx
- Build de produção sem erros
</success_criteria>

<output>
Create `.planning/phases/09-modulos-2x-analytics-dimensional/09-02-SUMMARY.md` when done
</output>
