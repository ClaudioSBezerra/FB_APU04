---
phase: 09-modulos-2x-analytics-dimensional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/handlers/reforma_modulo2.go
  - backend/handlers/reforma_modulo2_test.go
  - backend/main.go
autonomous: true
requirements: [RFMC-01, RFMC-02, RFMC-03, RFMC-04]

must_haves:
  truths:
    - "GET /api/reforma/modulo2/cfop retorna JSON agrupado por natureza CFOP com IBS/CBS projetado (excluindo tipo='T'), isolado por company_id"
    - "GET /api/reforma/modulo2/ncm retorna JSON com alíquota ICMS efetiva vs IBS+CBS projetada por NCM, usando LATERAL join em ncm_cclasstrib_reforma"
    - "GET /api/reforma/modulo2/uf-destino retorna JSON com volume de vendas, ICMS e IBS/CBS por UF de destino"
    - "GET /api/reforma/modulo2/b2b-b2c retorna JSON com segmentação em 3 vias usando ind_final + fallback LENGTH(dest_cnpj_cpf)=11"
    - "GET /api/reforma/modulo2/cfop/csv e GET /api/reforma/modulo2/ncm/csv retornam CSV com Content-Disposition attachment"
    - "Todos os handlers filtram cancelados (cancelado='N') e companyID via GetEffectiveCompanyID"
  artifacts:
    - path: "backend/handlers/reforma_modulo2.go"
      provides: "4 handlers JSON + 2 handlers CSV dos Módulos 2.x"
      contains: "func CfopAnalysisHandler"
      min_lines: 300
    - path: "backend/main.go"
      provides: "Registro de 6 rotas /api/reforma/modulo2/*"
      contains: "/api/reforma/modulo2/cfop"
  key_links:
    - from: "backend/handlers/reforma_modulo2.go"
      to: "reforma_parametros"
      via: "QueryRow com sql.ErrNoRows guard + defaults (aliq_ibs 26.5, aliq_cbs 9.9)"
      pattern: "FROM reforma_parametros WHERE company_id = \\$1"
    - from: "backend/handlers/reforma_modulo2.go"
      to: "ncm_cclasstrib_reforma"
      via: "LEFT JOIN LATERAL (... ORDER BY length(ncm_digits) DESC LIMIT 1) ncmr ON true"
      pattern: "LEFT JOIN LATERAL"
    - from: "backend/main.go"
      to: "handlers.CfopAnalysisHandler"
      via: "AuthMiddleware(handler, \"\")"
      pattern: "handlers.AuthMiddleware\\(handlers\\.CfopAnalysisHandler"
---

<objective>
Implementar o backend da Phase 9 — 4 handlers analíticos da Reforma Tributária (Módulos 2.x) em `backend/handlers/reforma_modulo2.go`, mais o registro de 6 rotas em `backend/main.go`.

Purpose: Expor a análise dimensional cruzada (por CFOP, NCM, UF/destino, B2B vs B2C) para complementar os módulos 1.x da Phase 7. Toda a lógica SQL/Go; o frontend (09-02) apenas renderiza.

Output: `reforma_modulo2.go` com 4 handlers JSON + 2 handlers CSV + 6 rotas registradas em `main.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
Auth + companyID (padrão do codebase):
  claims, ok := r.Context().Value(ClaimsKey).(jwt.MapClaims)  // ok==false → 401
  userID, _ := claims["user_id"].(string)
  companyID, err := GetEffectiveCompanyID(db, userID, r.Header.Get("X-Company-ID"))

Helpers já existentes no pacote handlers:
  func jsonErr(w http.ResponseWriter, status int, msg string, extra ...map[string]string)
  func GetEffectiveCompanyID(db *sql.DB, userID, headerCompanyID string) (string, error)

Schema verificado:
  nfe_entradas(id, company_id, cancelado, cfop, v_nf)
  nfe_entradas_itens(nfe_id, company_id, x_prod, ncm, cfop, cst_icms, v_prod, v_bc_icms, v_icms)
  nfe_saidas(id, company_id, cancelado, cfop, v_nf, v_icms, dest_uf, dest_cnpj_cpf, ind_final)
  cfop(cfop VARCHAR(4), descricao_cfop VARCHAR(255), tipo VARCHAR(1))  -- tipo='T' para transferências
  ncm_cclasstrib_reforma(ncm_digits VARCHAR(10), ibs_reducao_pct, cbs_reducao_pct, cclasstrib)
  reforma_parametros(company_id, target_ano, aliq_ibs_pct, aliq_cbs_pct, ...)

Registro de rota (padrão exato de main.go):
  http.HandleFunc("/api/reforma/modulo2/cfop", func(w http.ResponseWriter, r *http.Request) {
    database := getDB(); if database == nil { jsonServiceUnavailable(w); return }
    handlers.AuthMiddleware(handlers.CfopAnalysisHandler(database), "")(w, r)
  })
  // role "" = qualquer usuário autenticado (analytics read-only)

Referência: backend/handlers/reforma_modulo1.go (padrão de structs, handlers, CSV, auth+companyID)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handlers Módulo 2.2 (CFOP Analysis) e Módulo 2.1 (NCM Analysis) — JSON + CSV</name>
  <files>backend/handlers/reforma_modulo2.go</files>
  <read_first>
    - backend/handlers/reforma_modulo1.go (padrão completo: structs, auth, GetEffectiveCompanyID, scan de rows, empty-slice guard, leitura de reforma_parametros com sql.ErrNoRows, CSV com csv.NewWriter)
    - backend/handlers/reforma_config.go (struct ReformaParametros)
    - backend/handlers/xml_conciliacao.go (linhas 305-386: padrão CSV — Content-Disposition, charset=utf-8, Flush+Error)
  </read_first>
  <action>
    Criar `backend/handlers/reforma_modulo2.go` no package `handlers`, importando database/sql, encoding/csv, encoding/json, fmt, log, net/http, github.com/golang-jwt/jwt/v5.

    Structs (declarar no topo do arquivo, JSON tags snake_case):
    ```
    type Modulo22Row struct {
        NaturezaCFOP string  `json:"natureza_cfop"`
        QtdNotas     int     `json:"qtd_notas"`
        ValorTotal   float64 `json:"valor_total"`
        IBSProjetado float64 `json:"ibs_projetado"`
        CBSProjetado float64 `json:"cbs_projetado"`
    }
    type Modulo22Response struct {
        Rows       []Modulo22Row `json:"rows"`
        TotalIBS   float64       `json:"total_ibs"`
        TotalCBS   float64       `json:"total_cbs"`
        AliqIBSPct float64       `json:"aliq_ibs_pct"`
        AliqCBSPct float64       `json:"aliq_cbs_pct"`
    }
    type Modulo21Row struct {
        NCM          string  `json:"ncm"`
        XProd        string  `json:"x_prod"`
        VlProd       float64 `json:"vl_prod"`
        VlICMS       float64 `json:"vl_icms"`
        AliqICMSEfet float64 `json:"aliq_icms_efet"`  // vl_icms/vl_prod*100
        IBSProjetado float64 `json:"ibs_projetado"`
        CBSProjetado float64 `json:"cbs_projetado"`
        IsFlag       bool    `json:"is_flag"`          // ncmr.cclasstrib IS NOT NULL
    }
    type Modulo21Response struct {
        Rows       []Modulo21Row `json:"rows"`
        AliqIBSPct float64       `json:"aliq_ibs_pct"`
        AliqCBSPct float64       `json:"aliq_cbs_pct"`
    }
    ```

    Implementar `CfopAnalysisHandler` (GET /api/reforma/modulo2/cfop — RFMC-01):
    - Method guard: se r.Method != GET → jsonErr 405.
    - Auth padrão: claims → userID → GetEffectiveCompanyID → jsonErr 500 em erro.
    - Ler aliq_ibs_pct, aliq_cbs_pct de reforma_parametros (guard sql.ErrNoRows, defaults 26.5 e 9.9).
    - Query principal (fonte XML nfe_entradas, agrupando por natureza CFOP):
      ```sql
      SELECT
        CASE
          WHEN COALESCE(cf.tipo,'O') = 'T' THEN 'Transferência'
          WHEN LEFT(ne.cfop,2) IN ('12','22','52','62') THEN 'Revenda'
          WHEN LEFT(ne.cfop,2) IN ('13','23') THEN 'Uso e Consumo'
          WHEN LEFT(ne.cfop,2) IN ('14','24') THEN 'Ativo Permanente'
          WHEN LEFT(ne.cfop,1) = '7' THEN 'Exportação'
          ELSE 'Outras Operações'
        END AS natureza_cfop,
        COUNT(*) AS qtd_notas,
        SUM(ne.v_nf) AS valor_total
      FROM nfe_entradas ne
      LEFT JOIN cfop cf ON cf.cfop = ne.cfop
      WHERE ne.company_id = $1 AND ne.cancelado = 'N'
      GROUP BY natureza_cfop
      ORDER BY valor_total DESC
      ```
      Nota: NÃO filtrar tipo='T' da query principal — mostrar todos incluindo Transferência para contexto, conforme RFMC-01 "CFOPs de transferência excluídos [do cálculo de IBS/CBS]". Calcular IBSProjetado e CBSProjetado somente quando natureza != 'Transferência': se natureza == 'Transferência' → 0.0, senão valor_total * aliqIBS/100.0.
    - Acumular TotalIBS e TotalCBS excluindo transferências.
    - Empty-slice guard: se rows == nil → rows = []Modulo22Row{}. Encode Modulo22Response.

    Implementar `CfopAnalysisCSVHandler` (GET /api/reforma/modulo2/cfop/csv):
    - Mesma auth e query; Content-Type "text/csv; charset=utf-8"; Content-Disposition `attachment; filename="analise-cfop.csv"`.
    - Header CSV: []string{"Natureza CFOP","Qtd Notas","Valor Total (R$)","IBS Projetado (R$)","CBS Projetado (R$)"}.
    - Para cada row: escrever com fmt.Sprintf("%.2f", ...) para floats. cw.Flush() + checar cw.Error().

    Implementar `NcmAnalysisHandler` (GET /api/reforma/modulo2/ncm — RFMC-02):
    - Method guard + auth padrão + leitura reforma_parametros (defaults 26.5, 9.9).
    - Query principal (fonte XML nfe_entradas_itens com LATERAL join NCM — mesmo padrão do ReprecificacaoHandler de reforma_modulo1.go):
      ```sql
      SELECT
        nit.ncm,
        COALESCE(nit.x_prod,'') AS x_prod,
        SUM(nit.v_prod) AS vl_prod,
        SUM(nit.v_icms) AS vl_icms,
        COALESCE(ncmr.ibs_reducao_pct, 0) AS ibs_reducao_pct,
        COALESCE(ncmr.cbs_reducao_pct, 0) AS cbs_reducao_pct,
        CASE WHEN ncmr.cclasstrib IS NOT NULL THEN true ELSE false END AS is_flag
      FROM nfe_entradas_itens nit
      JOIN nfe_entradas ne ON ne.id = nit.nfe_id
      LEFT JOIN cfop cf ON cf.cfop = nit.cfop
      LEFT JOIN LATERAL (
        SELECT ibs_reducao_pct, cbs_reducao_pct, cclasstrib
        FROM ncm_cclasstrib_reforma
        WHERE nit.ncm LIKE ncm_digits || '%'
        ORDER BY length(ncm_digits) DESC
        LIMIT 1
      ) ncmr ON true
      WHERE nit.company_id = $1 AND ne.cancelado = 'N' AND COALESCE(cf.tipo,'O') != 'T'
      GROUP BY nit.ncm, nit.x_prod, ncmr.ibs_reducao_pct, ncmr.cbs_reducao_pct, ncmr.cclasstrib
      ORDER BY vl_prod DESC
      LIMIT 100
      ```
    - Para cada row: AliqICMSEfet = se vl_prod > 0 → vl_icms/vl_prod*100.0, senão 0; IBSProjetado = vl_prod * aliqIBS/100.0 * (1 - ibs_reducao_pct/100.0); CBSProjetado = vl_prod * aliqCBS/100.0 * (1 - cbs_reducao_pct/100.0).
    - Empty-slice guard. Encode Modulo21Response.

    Implementar `NcmAnalysisCSVHandler` (GET /api/reforma/modulo2/ncm/csv):
    - Mesma auth e query; Content-Disposition `attachment; filename="analise-ncm.csv"`.
    - Header CSV: []string{"NCM","Descrição","VL Prod (R$)","VL ICMS (R$)","Alíq ICMS Efet (%)","IBS Proj (R$)","CBS Proj (R$)","IS Flag"}.
    - Coluna IS Flag: "Sim" se is_flag, senão "Não".

    Usar `jsonErr(w, status, msg)` para erros JSON e `http.Error(w, msg, status)` nos handlers CSV (consistente com xml_conciliacao.go). Logar erros de query com log.Printf antes de 500 genérico.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./handlers/</automated>
  </verify>
  <acceptance_criteria>
    - `backend/handlers/reforma_modulo2.go` contém `func CfopAnalysisHandler(db *sql.DB) http.HandlerFunc`
    - Arquivo contém `func CfopAnalysisCSVHandler(`, `func NcmAnalysisHandler(`, `func NcmAnalysisCSVHandler(`
    - Arquivo contém `LEFT JOIN LATERAL` e `ORDER BY length(ncm_digits) DESC` (LATERAL prefix-match NCM)
    - Arquivo contém `attachment; filename="analise-cfop.csv"` e `attachment; filename="analise-ncm.csv"`
    - Nenhuma interpolação de company_id em SQL: `grep -nE "Sprintf.*company_id|company_id.*%s" backend/handlers/reforma_modulo2.go` retorna 0 linhas
    - `cd backend && go build ./...` sai com código 0
  </acceptance_criteria>
  <done>Os 4 handlers de CFOP e NCM (JSON + CSV) compilam; auth, company_id parametrizado, LATERAL join e CSV content-disposition corretos.</done>
</task>

<task type="auto">
  <name>Task 2: Handlers Módulo 2.3 (UF Destino) e Módulo 2.4 (B2B vs B2C) + rotas em main.go</name>
  <files>backend/handlers/reforma_modulo2.go, backend/main.go</files>
  <read_first>
    - backend/handlers/reforma_modulo2.go (estado após Task 1 — structs, helpers, padrão dos handlers já criados)
    - backend/main.go (linhas onde ficam as rotas modulo1 — padrão exato de getDB/jsonServiceUnavailable/AuthMiddleware)
  </read_first>
  <action>
    Adicionar ao `backend/handlers/reforma_modulo2.go` (mesmo arquivo) as structs e handlers dos Módulos 2.3 e 2.4.

    Structs:
    ```
    type Modulo23Row struct {
        DestUF       string  `json:"dest_uf"`
        QtdNotas     int     `json:"qtd_notas"`
        ValorTotal   float64 `json:"valor_total"`
        VlICMS       float64 `json:"vl_icms"`
        IBSProjetado float64 `json:"ibs_projetado"`
        CBSProjetado float64 `json:"cbs_projetado"`
    }
    type Modulo23Response struct {
        Rows       []Modulo23Row `json:"rows"`
        AliqIBSPct float64       `json:"aliq_ibs_pct"`
        AliqCBSPct float64       `json:"aliq_cbs_pct"`
    }
    type Modulo24Row struct {
        Segmento     string  `json:"segmento"`      // "b2b_credit" | "b2c" | "sem_classificacao"
        QtdNotas     int     `json:"qtd_notas"`
        ValorTotal   float64 `json:"valor_total"`
        IBSProjetado float64 `json:"ibs_projetado"`
        CBSProjetado float64 `json:"cbs_projetado"`
    }
    type Modulo24Response struct {
        Rows            []Modulo24Row `json:"rows"`
        QtdSemIndFinal  int           `json:"qtd_sem_ind_final"`
        AliqIBSPct      float64       `json:"aliq_ibs_pct"`
        AliqCBSPct      float64       `json:"aliq_cbs_pct"`
    }
    ```

    Implementar `UfDestinoHandler` (GET /api/reforma/modulo2/uf-destino — RFMC-03):
    - Method guard + auth padrão + leitura reforma_parametros (defaults 26.5, 9.9).
    - Query principal (fonte XML nfe_saidas, agrupando por dest_uf):
      ```sql
      SELECT
        COALESCE(NULLIF(ns.dest_uf,''), 'N/A') AS dest_uf,
        COUNT(*) AS qtd_notas,
        SUM(ns.v_nf) AS valor_total,
        SUM(ns.v_icms) AS vl_icms
      FROM nfe_saidas ns
      LEFT JOIN cfop cf ON cf.cfop = ns.cfop
      WHERE ns.company_id = $1 AND ns.cancelado = 'N' AND COALESCE(cf.tipo,'O') != 'T'
      GROUP BY dest_uf
      ORDER BY valor_total DESC
      ```
    - Para cada row: IBSProjetado = valor_total * aliqIBS/100.0; CBSProjetado = valor_total * aliqCBS/100.0.
    - Empty-slice guard. Encode Modulo23Response.
    - NÃO implementar CSV para UF destino (dados vão para mapa, não planilha; B2C não tem CSV por padrão).

    Implementar `B2bB2cHandler` (GET /api/reforma/modulo2/b2b-b2c — RFMC-04):
    - Method guard + auth padrão + leitura reforma_parametros (defaults 26.5, 9.9).
    - Query principal (fonte XML nfe_saidas, segmentação com ind_final + fallback CPF):
      ```sql
      SELECT
        CASE
          WHEN ns.ind_final = '1' THEN 'b2c'
          WHEN ns.ind_final = '0' THEN 'b2b_credit'
          WHEN ns.ind_final IS NULL AND LENGTH(COALESCE(ns.dest_cnpj_cpf,'')) = 11 THEN 'b2c'
          WHEN ns.ind_final IS NULL AND LENGTH(COALESCE(ns.dest_cnpj_cpf,'')) = 14 THEN 'b2b_credit'
          ELSE 'sem_classificacao'
        END AS segmento,
        COUNT(*) AS qtd_notas,
        SUM(ns.v_nf) AS valor_total,
        SUM(CASE WHEN ns.ind_final IS NULL THEN 1 ELSE 0 END) AS qtd_sem_ind_final
      FROM nfe_saidas ns
      LEFT JOIN cfop cf ON cf.cfop = ns.cfop
      WHERE ns.company_id = $1 AND ns.cancelado = 'N' AND COALESCE(cf.tipo,'O') != 'T'
      GROUP BY segmento
      ORDER BY valor_total DESC
      ```
    - Para cada row: IBSProjetado = valor_total * aliqIBS/100.0; CBSProjetado = valor_total * aliqCBS/100.0.
    - QtdSemIndFinal: somar qtd_sem_ind_final de todas as rows (para o aviso de UI sobre notas históricas).
    - Empty-slice guard. Encode Modulo24Response.
    - NÃO implementar CSV para B2B/B2C.

    Em `backend/main.go`, após o bloco das rotas modulo1, adicionar comentário `// ── Reforma Tributária — Módulos 2.x (Phase 9) ──` e registrar 6 rotas:
    - /api/reforma/modulo2/cfop → CfopAnalysisHandler
    - /api/reforma/modulo2/cfop/csv → CfopAnalysisCSVHandler
    - /api/reforma/modulo2/ncm → NcmAnalysisHandler
    - /api/reforma/modulo2/ncm/csv → NcmAnalysisCSVHandler
    - /api/reforma/modulo2/uf-destino → UfDestinoHandler
    - /api/reforma/modulo2/b2b-b2c → B2bB2cHandler
    Todas com role "" (AuthMiddleware(handler, "")).
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./handlers/ && grep -c "api/reforma/modulo2/" main.go</automated>
  </verify>
  <acceptance_criteria>
    - `reforma_modulo2.go` contém `func UfDestinoHandler(` e `func B2bB2cHandler(`
    - `reforma_modulo2.go` contém `dest_uf` e `COALESCE(ns.dest_uf,` (query UF destino)
    - `reforma_modulo2.go` contém `ind_final = '1'` e `LENGTH(COALESCE(ns.dest_cnpj_cpf,'')) = 11` (fallback CPF)
    - `reforma_modulo2.go` contém `qtd_sem_ind_final` (contador para aviso de UI)
    - `backend/main.go` contém `/api/reforma/modulo2/cfop`, `/api/reforma/modulo2/ncm`, `/api/reforma/modulo2/uf-destino`, `/api/reforma/modulo2/b2b-b2c`
    - `grep -c "api/reforma/modulo2/" backend/main.go` retorna 6 (4 JSON + 2 CSV)
    - `cd backend && go build ./...` sai com código 0; `go vet ./handlers/` sem erros
  </acceptance_criteria>
  <done>Os handlers UF Destino e B2B/B2C compilam; as 6 rotas estão registradas em main.go; build/vet passa.</done>
</task>

<task type="auto">
  <name>Task 3: Guard tests para os 6 handlers do Módulo 2.x</name>
  <files>backend/handlers/reforma_modulo2_test.go</files>
  <read_first>
    - backend/handlers/reforma_modulo1_test.go (padrão exato de guard tests: handler creation + method-not-allowed)
    - backend/handlers/reforma_modulo2.go (assinaturas dos 6 handlers)
  </read_first>
  <action>
    Criar `backend/handlers/reforma_modulo2_test.go` no package `handlers`, importando net/http, net/http/httptest, testing.

    Para cada um dos 6 handlers, escrever teste de criação: `func TestCfopAnalysisHandler_Creation(t *testing.T) { h := CfopAnalysisHandler(nil); if h == nil { t.Error("expected non-nil handler") } }`. Repetir para CfopAnalysisCSVHandler, NcmAnalysisHandler, NcmAnalysisCSVHandler, UfDestinoHandler, B2bB2cHandler.

    Adicionar teste de method-not-allowed para um handler JSON:
    ```go
    func TestCfopAnalysisHandler_MethodNotAllowed(t *testing.T) {
        h := CfopAnalysisHandler(nil)
        req := httptest.NewRequest(http.MethodPost, "/api/reforma/modulo2/cfop", nil)
        rr := httptest.NewRecorder()
        h(rr, req)
        if rr.Code != http.StatusMethodNotAllowed {
            t.Errorf("got %d, want 405", rr.Code)
        }
    }
    ```

    Não escrever testes que executem queries (exigiriam DB real).
  </action>
  <verify>
    <automated>cd backend && go test ./handlers/ -run "TestCfopAnalysisHandler|TestNcmAnalysisHandler|TestUfDestinoHandler|TestB2bB2cHandler" -v</automated>
  </verify>
  <acceptance_criteria>
    - `reforma_modulo2_test.go` contém testes de criação para os 6 handlers
    - Arquivo contém `func TestCfopAnalysisHandler_MethodNotAllowed(` assertando `http.StatusMethodNotAllowed`
    - `cd backend && go test ./handlers/ -run "TestCfopAnalysisHandler|TestNcmAnalysisHandler|TestUfDestinoHandler|TestB2bB2cHandler"` sai com código 0
  </acceptance_criteria>
  <done>Guard tests dos 6 handlers passam; build final sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → API (Go handler) | JWT do usuário atravessa; X-Company-ID pode ser manipulado |
| API → PostgreSQL | queries com company_id derivado do JWT, nunca do body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | Todos os 6 handlers de reforma_modulo2.go | mitigate | company_id via GetEffectiveCompanyID — IDOR protection; nunca usar company_id do body/query. company_id sempre via $1 parametrizado. |
| T-09-02 | Tampering (SQL injection) | Queries SQL dos handlers | mitigate | company_id e demais filtros via placeholders $N; cfop.tipo lido do banco; nenhum valor concatenado em string SQL. |
| T-09-03 | Information Disclosure | CSV handlers (cfop/csv, ncm/csv) | mitigate | Mesma proteção JWT dos handlers JSON — impossível exportar dados de outra empresa. |
| T-09-04 | Information Disclosure | Endpoints analytics (dados fiscais sensíveis) | mitigate | Todos protegidos por AuthMiddleware(handler, "") — exigem JWT válido; 401 para não autenticados. |
</threat_model>

<verification>
- `cd backend && go build ./...` compila sem erros
- `cd backend && go vet ./handlers/` sem warnings
- `cd backend && go test ./handlers/ -run "TestCfopAnalysisHandler|TestNcmAnalysisHandler|TestUfDestinoHandler|TestB2bB2cHandler"` todos PASS
- `grep -c "api/reforma/modulo2/" backend/main.go` retorna 6
- Nenhuma interpolação de company_id: `grep -nE "Sprintf.*company_id" backend/handlers/reforma_modulo2.go` retorna 0 linhas
</verification>

<success_criteria>
- 4 handlers JSON (cfop, ncm, uf-destino, b2b-b2c) + 2 handlers CSV (cfop, ncm) implementados em reforma_modulo2.go
- Agrupamento por natureza CFOP via CASE SQL (Módulo 2.2)
- LATERAL join NCM com longest-prefix-wins e IS flag (Módulo 2.1)
- Agrupamento por dest_uf com ICMS real de nfe_saidas (Módulo 2.3)
- Segmentação B2B/B2C com ind_final + fallback CPF + contador qtd_sem_ind_final (Módulo 2.4)
- 6 rotas registradas em main.go com role "" (read-only analytics)
- Guard tests passam para os 6 handlers
- company_id sempre parametrizado (IDOR protection)
</success_criteria>

<output>
Create `.planning/phases/09-modulos-2x-analytics-dimensional/09-01-SUMMARY.md` when done
</output>
