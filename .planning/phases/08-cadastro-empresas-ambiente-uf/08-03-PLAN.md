---
phase: 08-cadastro-empresas-ambiente-uf
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - frontend/src/pages/GestaoAmbiente.tsx
  - frontend/src/pages/IcmsFronteira.tsx
  - frontend/src/lib/navigation.ts
autonomous: false
requirements: [CADU-03, CADU-07]

must_haves:
  truths:
    - "Modal de Nova Empresa em GestaoAmbiente.tsx exibe campos cnpj, inscricao_estadual, cnae_principal, municipio, segmento_economico (além dos existentes name, trade_name, regime_tributario)"
    - "Edição inline de empresa em GestaoAmbiente.tsx permite atualizar todos os 7 campos novos (não só regime_tributario)"
    - "Aba Regras NCM em IcmsFronteira.tsx exibe Tabs aninhadas PE/BA/CE; selecionar UF filtra a tabela via queryKey ['fronteira-regras', companyId, selectedUF] e ?uf_estado=<UF> no fetch"
    - "Upload de planilha de regras envia uf_estado no FormData (campo do formulário) e cria regras na UF selecionada"
    - "Criação de regra via POST envia uf_estado no body JSON com a UF selecionada"
    - "navigation.ts: label do módulo fronteira muda de 'ICMS Fronteira — PE' para 'ICMS Fronteira' (sem UF específica)"
    - "Checkpoint humano confirma renderização visual: modal expandido, tabs UF funcionando, troca de UF muda dados, upload por UF, edição inline expandida"
  artifacts:
    - path: "frontend/src/pages/GestaoAmbiente.tsx"
      provides: "Modal Nova Empresa expandido + edição inline com 7 novos campos + interface Company expandida"
      contains: "inscricao_estadual"
    - path: "frontend/src/pages/IcmsFronteira.tsx"
      provides: "Tabs UF aninhadas em RegrasTab + queryKey por UF + FormData com uf_estado + Body POST com uf_estado"
      contains: "selectedUF"
    - path: "frontend/src/lib/navigation.ts"
      provides: "Label do módulo fronteira sem UF específica"
      contains: "label: 'ICMS Fronteira'"
  key_links:
    - from: "frontend/src/pages/GestaoAmbiente.tsx"
      to: "PATCH /api/config/companies?id=UUID"
      via: "fetch com body JSON contendo 7 campos novos"
      pattern: "inscricao_estadual.*cnae_principal"
    - from: "frontend/src/pages/IcmsFronteira.tsx (RegrasTab)"
      to: "GET /api/icms-fronteira/regras?uf_estado=<UF>"
      via: "useQuery queryKey + queryFn fetch"
      pattern: "uf_estado=\\$\\{selectedUF\\}"
    - from: "frontend/src/pages/IcmsFronteira.tsx (upload de planilha)"
      to: "POST /api/icms-fronteira/regras/importar"
      via: "FormData.append('uf_estado', selectedUF)"
      pattern: "FormData.*uf_estado"
---

<objective>
Tornar visíveis e operáveis no UI as duas funcionalidades entregues pelos planos anteriores: (1) cadastro completo de empresa em `GestaoAmbiente.tsx` com os 7 campos novos no modal de criação e na edição inline (CADU-03); (2) ambiente administrativo de ICMS-Fronteira por UF em `IcmsFronteira.tsx` com Tabs aninhadas PE/BA/CE dentro da aba "Regras NCM", incluindo filtro de dados por UF, criação de regras por UF e upload de planilha por UF (CADU-07).

Purpose: Sem este plano, o backend expandido em Plan 02 não tem consumidor — o usuário continua vendo apenas o regime tributário editável e somente regras PE. Esta é a entrega de valor visível da fase: a equipe fiscal cadastra empresa completa (CNPJ/IE/CNAE/município/segmento) e configura regras ICMS-Fronteira para os 3 estados (PE, BA, CE) na mesma tela.

Output: 3 arquivos frontend modificados + checkpoint humano que valida renderização, troca de UF, criação por UF, upload por UF e edição expandida de empresa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cadastro-empresas-ambiente-uf/08-RESEARCH.md
@.planning/phases/08-cadastro-empresas-ambiente-uf/08-PATTERNS.md
@.planning/phases/08-cadastro-empresas-ambiente-uf/08-01-PLAN.md
@.planning/phases/08-cadastro-empresas-ambiente-uf/08-02-PLAN.md

<interfaces>
<!-- Contratos consumidos pelo frontend, do backend (Plan 02). Verificado 2026-05-23. -->

Endpoints relevantes (todos atrás do AuthMiddleware existente):
  GET    /api/config/companies                        → Company[] (agora com 7 campos novos)
  POST   /api/config/companies                        → cria Company (body inclui 7 campos novos opcionais)
  PUT|PATCH /api/config/companies?id=<UUID>           → atualiza Company (body inclui 7 campos novos)
  DELETE /api/config/companies?id=<UUID>              → deleta Company
  GET    /api/icms-fronteira/regras?uf_estado=<UF>    → FronteiraRegraRow[] filtrado por UF (default PE)
  POST   /api/icms-fronteira/regras                   → cria regra (body inclui uf_estado)
  PUT|PATCH /api/icms-fronteira/regras/<id>           → atualiza regra (NOVO em Plan 02)
  DELETE /api/icms-fronteira/regras/<id>              → deleta regra
  POST   /api/icms-fronteira/regras/importar          → upload CSV/XLSX (FormData inclui uf_estado)

Tipos JSON (snake_case):
  Company { id, group_id, name, trade_name, regime_tributario, cnpj?, inscricao_estadual?,
            cnae_principal?, cnae_secundario?: string[], municipio?, segmento_economico?,
            incentivos_fiscais?: unknown, created_at }
  FronteiraRegraRow { id, ncm_prefixo, descricao, regime, aliquota_interna, mva_original|null,
            mva_ajustado_4pct|null, mva_ajustado_7pct|null, mva_ajustado_12pct|null,
            reducao_bc_pct, uf_estado, is_global }

Estado atual GestaoAmbiente.tsx (verificado 2026-05-23):
  - linha 86: const [isCompanyModalOpen, setIsCompanyModalOpen] = useState(false);
  - linhas 93-96: useState para newCompanyCNPJ, newCompanyName, newCompanyTradeName, newCompanyRegime
  - linha 98: const [editingCompany, setEditingCompany] = useState<Company | null>(null);
  - linhas 54-62: interface Company { id, group_id, cnpj, name, trade_name, regime_tributario, created_at }
  - linhas 280-283: body do POST inclui cnpj, name, trade_name, regime_tributario
  - linhas 335-350: handleUpdateCompanyRegime envia APENAS { regime_tributario } (será substituído)
  - linhas 617-664: bloco Dialog "Nova Empresa" com 4 campos
  - linhas 723-748: bloco edição inline com APENAS Select de regime

Estado atual IcmsFronteira.tsx (verificado em 08-RESEARCH/08-PATTERNS):
  - RegrasTab linhas 617-944: useQuery queryKey ['icms-fronteira/regras'], fetch sem uf_estado
  - useMutation createMutation linhas 643-663: body sem uf_estado
  - handleImport linhas 700-718: FormData append('file', importFile) — sem uf_estado
  - Tabs externas (linhas 2273-2285): Resumo/Antecipação/ST/DIFAL/Itens/Divergências/Regras NCM/Extrato/Contestações

Estado atual navigation.ts:
  - linha ~60 dentro do bloco `fronteira`: label: 'ICMS Fronteira — PE' (precisa virar 'ICMS Fronteira')
  - NÃO adicionar novas tabs no navigation — UF é aba aninhada dentro de RegrasTab no componente IcmsFronteira.tsx

Padrão Tabs shadcn já em uso no IcmsFronteira.tsx (Pattern 4 do PATTERNS.md):
  <Tabs value={selectedUF} onValueChange={(v) => setSelectedUF(v as 'PE' | 'BA' | 'CE')}>
    <TabsList>
      <TabsTrigger value="PE">PE — Pernambuco</TabsTrigger>
      <TabsTrigger value="BA">BA — Bahia</TabsTrigger>
      <TabsTrigger value="CE">CE — Ceará</TabsTrigger>
    </TabsList>
    <TabsContent value="PE">...</TabsContent>
    ...
  </Tabs>

Padrão queryKey por UF (Pattern 5 do PATTERNS.md):
  queryKey: ['fronteira-regras', companyId, selectedUF]
  queryFn: () => fetch(`/api/icms-fronteira/regras?uf_estado=${selectedUF}`, ...)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expandir GestaoAmbiente.tsx — modal de criação e edição inline com os 7 campos novos de empresa</name>
  <files>frontend/src/pages/GestaoAmbiente.tsx</files>
  <read_first>
    - frontend/src/pages/GestaoAmbiente.tsx (estado atual completo — interface Company linhas 54-62; useState das newCompany_* linhas 93-96; bloco Dialog Nova Empresa linhas 617-664; bloco edição inline linhas 723-748; handleCreateCompany linhas 266-313; handleUpdateCompanyRegime linhas 335-350)
    - .planning/phases/08-cadastro-empresas-ambiente-uf/08-PATTERNS.md (seção "GestaoAmbiente.tsx" — padrão useState por campo, Dialog expandido, edição inline expandida, PATCH com todos os campos)
    - .planning/phases/08-cadastro-empresas-ambiente-uf/08-02-PLAN.md (interface Company expandida vinda do backend — tags JSON snake_case)
  </read_first>
  <action>
    Modificar `frontend/src/pages/GestaoAmbiente.tsx`:

    1. Expandir `interface Company` (linhas 54-62) adicionando os 6 campos novos como opcionais (CNPJ já existe; demais opcionais):
       - inscricao_estadual?: string
       - cnae_principal?: string
       - cnae_secundario?: string[]
       - municipio?: string
       - segmento_economico?: string
       - incentivos_fiscais?: unknown
       Não alterar cnpj (já existe).

    2. Adicionar 5 useState para os campos novos do modal Nova Empresa (após as 4 existentes nas linhas 93-96):
       - `const [newCompanyIE, setNewCompanyIE] = useState("");`
       - `const [newCompanyCNAE, setNewCompanyCNAE] = useState("");`
       - `const [newCompanyMunicipio, setNewCompanyMunicipio] = useState("");`
       - `const [newCompanySegmento, setNewCompanySegmento] = useState("");`

    3. Adicionar useState equivalentes para edição inline (após o `editingCompany` da linha 98), para o formulário de edição (não-vazios quando `editingCompany` está setado):
       - `const [editCNPJ, setEditCNPJ] = useState("");`
       - `const [editIE, setEditIE] = useState("");`
       - `const [editCNAE, setEditCNAE] = useState("");`
       - `const [editMunicipio, setEditMunicipio] = useState("");`
       - `const [editSegmento, setEditSegmento] = useState("");`
       (manter o `editRegime` existente para regime_tributario)

    4. No handler que abre o painel de edição inline (procurar onde `setEditingCompany(company)` é chamado — botão de editar com pencil icon na lista de empresas), inicializar os useState de edição a partir do objeto `company` selecionado (`setEditCNPJ(company.cnpj || "")`, `setEditIE(company.inscricao_estadual || "")`, etc.). Manter o `setEditRegime(company.regime_tributario || "lucro_real")` existente.

    5. Expandir o bloco Dialog "Nova Empresa" (linhas 617-664). Após o campo CNPJ existente (linha 629) e antes/após os campos name/trade_name/regime, adicionar 4 grupos de `<div className="space-y-2"><Label>...</Label><Input .../></div>` com:
       - Label "Inscrição Estadual" + Input value={newCompanyIE} onChange={(e) => setNewCompanyIE(e.target.value)} placeholder="Opcional" maxLength={30}
       - Label "CNAE Principal" + Input value={newCompanyCNAE} onChange={(e) => setNewCompanyCNAE(e.target.value)} placeholder="Ex: 4711301" maxLength={7}
       - Label "Município" + Input value={newCompanyMunicipio} onChange={(e) => setNewCompanyMunicipio(e.target.value)} placeholder="Opcional" maxLength={100}
       - Label "Segmento Econômico" + Input value={newCompanySegmento} onChange={(e) => setNewCompanySegmento(e.target.value)} placeholder="Ex: Varejo de móveis" maxLength={100}

    6. Expandir `handleCreateCompany` (linhas 266-313): adicionar ao body do POST os 4 novos campos: `inscricao_estadual: newCompanyIE, cnae_principal: newCompanyCNAE, municipio: newCompanyMunicipio, segmento_economico: newCompanySegmento`. Após sucesso, resetar os 4 novos useState para "" (acompanhando o reset existente de cnpj/name/trade_name/regime).

    7. Renomear `handleUpdateCompanyRegime` para `handleUpdateCompany` (linhas 335-350) e expandir o body do PATCH para incluir todos os 7 campos editáveis: `body: JSON.stringify({ regime_tributario: editRegime, cnpj: editCNPJ, inscricao_estadual: editIE, cnae_principal: editCNAE, municipio: editMunicipio, segmento_economico: editSegmento })`. Manter o restante da função (toast.success, setEditingCompany(null), fetchCompanies). Atualizar o onClick do botão "Salvar" no bloco de edição inline para chamar `handleUpdateCompany`.

    8. Expandir o bloco de edição inline (linhas 723-748). O bloco atual tem apenas o Select de regime; substituir o conteúdo de `<div className="mt-2 p-3 border border-blue-200 rounded-md bg-blue-50">` por um formulário compacto com 6 inputs em duas colunas (`grid grid-cols-2 gap-2 mb-2`) — CNPJ (maxLength=14), IE (maxLength=30), CNAE (maxLength=7), Município, Segmento, e o Select de regime ocupando linha inteira (já existente). Cada input/select com Label inline pequeno (`text-[10px]` ou similar para manter compacto). Manter os 2 botões finais "Salvar"/"Cancelar" exatamente como estão.

    NÃO adicionar campo `cnae_secundario` (TEXT[]) ao formulário desta task — campo de array é UX complexa; será deferido para versão futura via upload bulk. Não exibir `incentivos_fiscais` (JSONB) — campo livre sem schema fixo, fora do escopo desta UI.
  </action>
  <verify>
    <automated>cd /home/claudiobezerra/projetos/FB_APU04/frontend && npx tsc --noEmit 2>&1 | grep -E "GestaoAmbiente" | head -20 || echo "no type errors in GestaoAmbiente.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Interface Company contém os 5 campos novos: `grep -cE "(inscricao_estadual|cnae_principal|cnae_secundario|municipio|segmento_economico)\??:\s*(string|string\[\]|unknown)" frontend/src/pages/GestaoAmbiente.tsx` retorna ao menos 5
    - useState newCompany* expandido: `grep -cE "useState.*newCompany(IE|CNAE|Municipio|Segmento)" frontend/src/pages/GestaoAmbiente.tsx` retorna 4
    - useState edit* novos para edição inline: `grep -cE "useState.*edit(CNPJ|IE|CNAE|Municipio|Segmento)" frontend/src/pages/GestaoAmbiente.tsx` retorna 5
    - handleCreateCompany inclui os 4 campos novos no body: `grep -E "inscricao_estadual:\s*newCompanyIE" frontend/src/pages/GestaoAmbiente.tsx` retorna match
    - handleUpdateCompany (renomeado) inclui todos os 5 campos editáveis no body: `grep -cE "(cnpj|inscricao_estadual|cnae_principal|municipio|segmento_economico):\s*edit" frontend/src/pages/GestaoAmbiente.tsx` retorna ao menos 5
    - Bloco Dialog Nova Empresa contém os 4 Inputs novos: `grep -cE "value=\{newCompany(IE|CNAE|Municipio|Segmento)\}" frontend/src/pages/GestaoAmbiente.tsx` retorna 4
    - Bloco de edição inline contém os 5 inputs novos: `grep -cE "value=\{edit(CNPJ|IE|CNAE|Municipio|Segmento)\}" frontend/src/pages/GestaoAmbiente.tsx` retorna 5
    - `cd frontend && npx tsc --noEmit` não reporta erros de tipo no GestaoAmbiente.tsx
  </acceptance_criteria>
  <done>Modal de Nova Empresa exibe 7 campos (cnpj, name, trade_name, regime, IE, CNAE, Município, Segmento — 8 inputs no total). Edição inline permite atualizar todos os 5 campos novos + regime. Sem erros de tipo TS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adicionar Tabs UF (PE/BA/CE) em IcmsFronteira.tsx + filtros por UF em queries/mutations/upload + label navigation.ts</name>
  <files>frontend/src/pages/IcmsFronteira.tsx, frontend/src/lib/navigation.ts</files>
  <read_first>
    - frontend/src/pages/IcmsFronteira.tsx (estado atual — RegrasTab linhas 617-944: useQuery linhas 632-641, createMutation linhas 643-663, handleImport linhas 700-718; estrutura Tabs externa linhas 2273-2285 — padrão a replicar; interface RegraNCM ou similar próximo ao topo do arquivo)
    - frontend/src/lib/navigation.ts (linha ~60: bloco fronteira com label 'ICMS Fronteira — PE')
    - .planning/phases/08-cadastro-empresas-ambiente-uf/08-PATTERNS.md (seção "IcmsFronteira.tsx" — Tabs aninhadas, queryKey com selectedUF, FormData com uf_estado, body POST com uf_estado, interface RegraNCM expandida)
  </read_first>
  <action>
    Modificar `frontend/src/pages/IcmsFronteira.tsx`:

    1. Localizar a interface da regra NCM (procurar `interface RegraNCM` ou similar). Expandir com os 4 campos novos vindos do backend:
       - mva_ajustado_4pct: number | null
       - mva_ajustado_7pct: number | null
       - mva_ajustado_12pct: number | null
       - uf_estado: string
       Manter os campos existentes.

    2. Dentro do componente RegrasTab (linhas 617-944), adicionar no topo:
       - `const [selectedUF, setSelectedUF] = useState<'PE' | 'BA' | 'CE'>('PE');`

    3. Atualizar o `useQuery` (linhas 632-641):
       - queryKey: alterar de `['icms-fronteira/regras']` para `['icms-fronteira/regras', selectedUF]` (incluir selectedUF; companyId já implícito via header).
       - queryFn: alterar a URL do fetch de `'/api/icms-fronteira/regras'` para `` `/api/icms-fronteira/regras?uf_estado=${selectedUF}` `` (template literal). Manter o `headers: { Authorization: ... }` existente e o tratamento de erro.

    4. Atualizar o `createMutation` (linhas 643-663):
       - mutationFn body deve incluir `uf_estado: selectedUF` no JSON. Se o body atual é `JSON.stringify(body)` onde `body` é o parâmetro, ajustar para `JSON.stringify({ ...body, uf_estado: selectedUF })`.
       - onSuccess: alterar `queryClient.invalidateQueries({ queryKey: ['icms-fronteira/regras'] })` para `queryClient.invalidateQueries({ queryKey: ['icms-fronteira/regras', selectedUF] })`. (Invalida apenas a UF atual; UF não afetada permanece em cache.)

    5. Atualizar o `handleImport` (linhas 700-718):
       - Antes do fetch, adicionar `fd.append('uf_estado', selectedUF);` (linha entre o `fd.append('file', importFile)` existente e o fetch).
       - Após sucesso, invalidar a query da UF atual via `queryClient.invalidateQueries({ queryKey: ['icms-fronteira/regras', selectedUF] })` (se não existir já).

    6. Adicionar as Tabs aninhadas dentro do render do RegrasTab, ANTES do bloco que renderiza a tabela atual de regras. Estrutura:
       - Importar `Tabs, TabsContent, TabsList, TabsTrigger` de `@/components/ui/tabs` se ainda não estiverem importados no topo do arquivo (verificar — o padrão external Tabs já está em uso, então o import provavelmente já existe).
       - Inserir JSX:
         ```
         <Tabs value={selectedUF} onValueChange={(v) => setSelectedUF(v as 'PE' | 'BA' | 'CE')}>
           <TabsList>
             <TabsTrigger value="PE">PE — Pernambuco</TabsTrigger>
             <TabsTrigger value="BA">BA — Bahia</TabsTrigger>
             <TabsTrigger value="CE">CE — Ceará</TabsTrigger>
           </TabsList>
           {/* O conteúdo (tabela + upload + form de criação) é renderizado FORA do TabsContent, porque é o mesmo componente; o filtro é feito via selectedUF na query. */}
         </Tabs>
         ```
       Importante: NÃO replicar o JSX da tabela 3 vezes dentro de TabsContent. A tabela atual permanece após o Tabs e é re-renderizada automaticamente quando selectedUF muda (useQuery refaz fetch). Os TabsTrigger são apenas controles de estado.

       Alternativa mais explícita (se preferir para acessibilidade): manter os 3 `<TabsContent value="PE/BA/CE">` mas dentro de cada um colocar o MESMO JSX da tabela/upload (extraído para um sub-componente local se necessário). A versão simples acima é aceitável.

    7. Ajustar o botão/UI de criação de regra para exibir a UF atual ("Nova regra para BA") ou apenas usar selectedUF implicitamente no body do POST. Sem mudança visual obrigatória além do escopo do mutation já atualizado.

    Modificar `frontend/src/lib/navigation.ts`:

    8. Localizar o bloco `fronteira` (linha ~60). Alterar a propriedade `label` de `'ICMS Fronteira — PE'` para `'ICMS Fronteira'` (sem UF específica; o módulo agora cobre múltiplas UFs). Não alterar as tabs internas.
  </action>
  <verify>
    <automated>cd /home/claudiobezerra/projetos/FB_APU04/frontend && npx tsc --noEmit 2>&1 | grep -E "IcmsFronteira|navigation" | head -20 || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - IcmsFronteira.tsx contém `const [selectedUF, setSelectedUF] = useState<'PE' | 'BA' | 'CE'>('PE')`: `grep -c "selectedUF.*'PE' | 'BA' | 'CE'" frontend/src/pages/IcmsFronteira.tsx` retorna ao menos 1
    - queryKey inclui selectedUF: `grep -E "queryKey:.*\[.*'icms-fronteira/regras'.*selectedUF\]|queryKey:.*\['icms-fronteira/regras', selectedUF\]" frontend/src/pages/IcmsFronteira.tsx` retorna match
    - URL do fetch usa template literal com uf_estado: `grep -E "uf_estado=\\\$\{selectedUF\}" frontend/src/pages/IcmsFronteira.tsx` retorna match
    - FormData append uf_estado: `grep -c "fd.append.*uf_estado.*selectedUF" frontend/src/pages/IcmsFronteira.tsx` retorna 1
    - JSX contém os 3 TabsTrigger UF: `grep -cE "TabsTrigger value=\"(PE|BA|CE)\"" frontend/src/pages/IcmsFronteira.tsx` retorna 3
    - Interface de regra inclui campos novos: `grep -cE "mva_ajustado_(4|7|12)pct\s*:\s*number\s*\|\s*null" frontend/src/pages/IcmsFronteira.tsx` retorna 3
    - Interface inclui uf_estado: `grep -E "uf_estado\s*:\s*string" frontend/src/pages/IcmsFronteira.tsx` retorna match
    - navigation.ts label atualizado: `grep -E "label:\s*'ICMS Fronteira'" frontend/src/lib/navigation.ts` retorna match
    - navigation.ts NÃO contém mais o sufixo PE: `grep -c "'ICMS Fronteira — PE'" frontend/src/lib/navigation.ts` retorna 0
    - `cd frontend && npx tsc --noEmit` não reporta erros de tipo nos 2 arquivos alterados
  </acceptance_criteria>
  <done>Aba Regras NCM em IcmsFronteira.tsx exibe Tabs aninhadas PE/BA/CE; trocar UF muda dados (via queryKey); criar regra envia uf_estado no body; upload envia uf_estado no FormData; label do módulo é "ICMS Fronteira" sem UF.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual end-to-end — cadastro completo de empresa + ambiente UF</name>
  <what-built>
    Integração frontend + backend das duas funcionalidades:
    - Cadastro de empresa: modal com 7 campos (CNPJ, IE, CNAE, Município, Segmento + name/trade_name/regime) na Gestão de Ambiente; edição inline expandida para alterar todos os 7 campos.
    - Ambiente UF ICMS-Fronteira: Tabs PE/BA/CE dentro da aba "Regras NCM" do IcmsFronteira; trocar UF filtra dados; criação e upload de planilha respeitam a UF selecionada.
    - Label do menu do módulo agora é "ICMS Fronteira" (sem "— PE").
  </what-built>
  <action>
    Tarefa de verificação humana — sem código a escrever. O executor garante que backend (`cd backend && go build && ./apu04` ou container) e frontend (`cd frontend && npm run dev`) estão rodando com o schema das migrations 096-098 aplicado, então pausa para o usuário inspecionar os fluxos em <how-to-verify>.
  </action>
  <how-to-verify>
    1. Aplicar migrations: confirmar que 096, 097, 098 rodaram com sucesso (verificar tabela `schema_migrations` ou logs do migration runner; verificar via psql que `companies` tem `cnpj` e que `icms_fronteira_regras_ncm` tem `uf_estado`).
    2. Rodar backend e frontend localmente. Logar como admin de uma empresa.
    3. **Cadastro de empresa**:
       a. Ir em "Gestão de Ambiente" (ou caminho equivalente atual). Clicar em "Nova Empresa".
       b. Confirmar que o modal exibe TODOS estes campos visíveis: CNPJ, Nome, Nome Fantasia, Regime Tributário, Inscrição Estadual, CNAE Principal, Município, Segmento Econômico.
       c. Preencher uma empresa de teste (ex: CNPJ 12345678000190, IE 123456789, CNAE 4711301, Município "Recife", Segmento "Varejo"). Salvar.
       d. Confirmar que a empresa aparece na lista após o save.
       e. Clicar no botão de editar (lápis) da empresa criada. Confirmar que TODOS os 7 campos aparecem no painel inline azul, populados com os valores salvos.
       f. Alterar o município para "Olinda" e clicar Salvar. Confirmar via reload da página que a alteração persistiu.
       g. Tentar criar empresa com CNPJ "123" → confirmar erro 400 "CNPJ deve ter 14 dígitos numéricos" (toast vermelho).
    4. **Ambiente UF ICMS-Fronteira**:
       a. Navegar para "ICMS Fronteira" no menu. Confirmar que o label do módulo é "ICMS Fronteira" (NÃO "ICMS Fronteira — PE").
       b. Clicar na aba "Regras NCM".
       c. Confirmar que aparecem 3 TabsTriggers: "PE — Pernambuco", "BA — Bahia", "CE — Ceará".
       d. Aba PE selecionada por padrão — confirmar que as regras existentes (do seed da migration 091) aparecem.
       e. Clicar em "BA — Bahia". Confirmar que aparecem APENAS as 7 regras inseridas pela migration 098 com uf_estado='BA' (NCMs 2202, 2203, 3004, 3303, 4011, 2523, 8517).
       f. Clicar em "CE — Ceará". Confirmar que aparecem APENAS as 7 regras CE (mesmos NCMs do BA, mas com aliquota_interna diferente em 2202/2203/2523).
       g. Voltar para BA. Criar nova regra (NCM 9999, descricao "Teste UF", regime ST, aliq 18.0, MVA 50.0). Salvar. Confirmar que aparece na aba BA mas NÃO em PE nem CE.
       h. Trocar para PE e confirmar que a regra Teste UF NÃO aparece.
       i. (Opcional, se houver planilha de teste disponível) Em BA: clicar em "Importar Planilha", selecionar um CSV/XLSX com regras de teste. Confirmar que as regras importadas aparecem APENAS em BA.
    5. **Edge cases**:
       a. Confirmar que regras com company_id IS NULL (globais, do seed) aparecem em BA/CE sem dependência da empresa selecionada.
       b. Confirmar que troca de empresa (selector global) não afeta os dados globais por UF.
  </how-to-verify>
  <resume-signal>Digite "approved" se o cadastro completo de empresa funciona, a validação de CNPJ rejeita formato inválido, as Tabs UF (PE/BA/CE) filtram corretamente, criação/upload respeitam a UF selecionada, e regras criadas em uma UF não vazam para outra. Ou descreva os problemas encontrados.</resume-signal>
  <verify>
    <human-check>Usuário confirma fluxo end-to-end: cadastro completo, validação CNPJ, troca de UF, isolamento de regras por UF, persistência.</human-check>
  </verify>
  <done>Usuário respondeu "approved"; cadastro completo de empresa (CADU-03) e ambiente UF ICMS-Fronteira (CADU-07) funcionam end-to-end com isolamento correto entre UFs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (React) → API Go | fetch envia JWT + X-Company-ID injetados pelo AuthContext (window.fetch interceptado); dados do formulário enviados como JSON ou FormData |
| usuário → UI | dados fiscais sensíveis exibidos no modal e nas tabs; CNPJ inserido manualmente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Tampering | uf_estado manipulável no client (URL/body/FormData) | mitigate | Whitelist de uf_estado no backend (Plan 02 Task 2) protege independentemente do client; o frontend é cooperativo, não fonte de verdade |
| T-08-13 | Information Disclosure | Dados de empresa de outra organização via troca de UUID | mitigate | UpdateCompanyHandler valida que o usuário pertence à empresa via GetEffectiveCompanyID (Plan 02 Task 1); frontend não decide a empresa |
| T-08-14 | Tampering | XSS via campos de texto do formulário (nome, município, segmento) | mitigate | React escapa por padrão todo conteúdo em JSX `{value}`; nenhum uso de dangerouslySetInnerHTML nas alterações desta task; backend não confia no que o frontend envia (validações no Plan 02) |
| T-08-15 | Tampering | CNPJ inválido aceito no frontend e enviado ao backend | mitigate | maxLength=14 no Input limita digitação; validação real está no backend (regexp `^\d{14}$`); UI exibe erro do backend via toast — sem fonte de verdade duplicada |
| T-08-16 | Spoofing | Acesso direto a rotas /icms-fronteira/regras?uf_estado=BA sem auth | mitigate | App.tsx aninha sob guard de autenticação (padrão existente); backend valida JWT antes de qualquer ação |
| T-08-SC | Tampering | npm installs | mitigate | Nenhuma dependência nova nesta fase (RESEARCH "Sem novas dependências a instalar") — Tabs shadcn, useQuery, fetch já em uso |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` sem erros nos 3 arquivos alterados
- Interface Company expandida com os 5 campos novos (cnpj já existia)
- 4 novos useState no fluxo Nova Empresa + 5 no fluxo de edição inline
- handleCreateCompany e handleUpdateCompany enviam todos os campos editáveis no body
- useQuery em IcmsFronteira.tsx usa queryKey incluindo selectedUF
- handleImport em IcmsFronteira.tsx faz FormData.append('uf_estado', selectedUF)
- navigation.ts label atualizado para "ICMS Fronteira"
- Checkpoint humano aprovado: cadastro completo + ambiente UF funcionando end-to-end
</verification>

<success_criteria>
- Modal Nova Empresa exibe 8 inputs (CNPJ, IE, CNAE, Município, Segmento + name, trade_name, regime)
- Edição inline permite alterar todos os 7 campos editáveis (cnpj + 4 novos + regime; cnae_secundario e incentivos_fiscais deferidos)
- handleCreateCompany e handleUpdateCompany enviam o body completo ao backend
- Tabs PE/BA/CE renderizam dentro da aba Regras NCM com troca funcional de estado
- useQuery refaz fetch ao trocar UF (queryKey inclui selectedUF)
- createMutation envia uf_estado no body
- handleImport envia uf_estado no FormData
- Label do módulo fronteira em navigation.ts é "ICMS Fronteira" (sem UF)
- TypeScript compila sem erros (`npx tsc --noEmit`)
- Checkpoint humano aprova fluxo end-to-end com isolamento correto entre UFs
</success_criteria>

<output>
Create `.planning/phases/08-cadastro-empresas-ambiente-uf/08-03-SUMMARY.md` when done
</output>
