---
phase: 08-cadastro-empresas-ambiente-uf
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - backend/handlers/environment.go
  - backend/handlers/icms_fronteira_regras.go
  - backend/main.go
autonomous: true
requirements: [CADU-02, CADU-06]

must_haves:
  truths:
    - "Struct Company em environment.go inclui CNPJ, InscricaoEstadual, CNAEPrincipal, CNAESecundario, Municipio, SegmentoEconomico, IncentivosFiscais"
    - "CreateCompanyHandler aceita e persiste os 7 novos campos com validação de CNPJ (14 dígitos numéricos quando fornecido)"
    - "UpdateCompanyHandler aceita e persiste os 7 novos campos via struct nomeado (não struct anônimo restrito a regime_tributario)"
    - "GetCompaniesHandler retorna os 7 novos campos no JSON via SELECT expandido com COALESCE"
    - "IcmsFronteiraRegrasListHandler filtra por uf_estado (query param, whitelist PE/BA/CE, default PE)"
    - "IcmsFronteiraRegraCreateHandler persiste uf_estado vindo do body (whitelist obrigatória)"
    - "IcmsFronteiraRegrasImportarHandler aceita uf_estado via FormValue e usa em todos os INSERTs do batch"
    - "Novo IcmsFronteiraRegraUpdateHandler atende PUT/PATCH /api/icms-fronteira/regras/{id}"
    - "main.go roteia PUT/PATCH para o novo Update handler no path /api/icms-fronteira/regras/"
  artifacts:
    - path: "backend/handlers/environment.go"
      provides: "Company struct expandido + Create/Update/Get handlers com 7 campos novos + validação CNPJ"
      contains: "InscricaoEstadual"
    - path: "backend/handlers/icms_fronteira_regras.go"
      provides: "FronteiraRegraRow expandido + filtro uf_estado em List/Create/Importar + IcmsFronteiraRegraUpdateHandler"
      contains: "IcmsFronteiraRegraUpdateHandler"
    - path: "backend/main.go"
      provides: "Roteamento PUT/PATCH para Update handler de regras"
      contains: "IcmsFronteiraRegraUpdateHandler"
  key_links:
    - from: "backend/handlers/environment.go"
      to: "tabela companies (após migration 096)"
      via: "INSERT/UPDATE com 7 novos campos + COALESCE no SELECT"
      pattern: "cnpj.*inscricao_estadual.*cnae_principal"
    - from: "backend/handlers/icms_fronteira_regras.go"
      to: "tabela icms_fronteira_regras_ncm (após migration 097)"
      via: "WHERE uf_estado = $2 + INSERT incluindo uf_estado"
      pattern: "uf_estado\\s*=\\s*\\$"
    - from: "backend/main.go"
      to: "handlers.IcmsFronteiraRegraUpdateHandler"
      via: "switch r.Method case PUT, PATCH"
      pattern: "IcmsFronteiraRegraUpdateHandler"
---

<objective>
Expandir o backend Go para suportar o cadastro completo de empresas (CADU-02) e o ambiente administrativo de ICMS-Fronteira por UF (CADU-06). Duas frentes:

1. `environment.go`: expandir struct `Company` com 7 campos novos; expandir `CreateCompanyHandler`, `UpdateCompanyHandler` e `GetCompaniesHandler` para aceitar, validar (CNPJ 14 dígitos) e retornar os novos campos.
2. `icms_fronteira_regras.go`: adicionar filtro por `uf_estado` (query param com whitelist PE/BA/CE) em `IcmsFronteiraRegrasListHandler`, persistir uf_estado em `IcmsFronteiraRegraCreateHandler`, aceitar uf_estado como campo de formulário em `IcmsFronteiraRegrasImportarHandler`, e criar novo `IcmsFronteiraRegraUpdateHandler` para PUT/PATCH.
3. `main.go`: rotear PUT/PATCH `/api/icms-fronteira/regras/{id}` para o novo Update handler (DELETE já existe; padrão switch r.Method).

Purpose: Tornar o backend capaz de consumir o schema entregue em Plan 01 e expor a API que o frontend (Plan 03) vai consumir para multi-empresa cadastro completo e multi-UF para regras ICMS-Fronteira.

Output: 3 arquivos Go modificados, build limpo (`go build ./...`), todos os handlers aplicando `GetEffectiveCompanyID` (isolamento por empresa) e whitelist de uf_estado (proteção contra Tampering).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cadastro-empresas-ambiente-uf/08-RESEARCH.md
@.planning/phases/08-cadastro-empresas-ambiente-uf/08-PATTERNS.md
@.planning/phases/08-cadastro-empresas-ambiente-uf/08-01-PLAN.md

<interfaces>
<!-- Contratos extraídos do codebase (2026-05-23). -->

Schema esperado após Plan 01 (migrations 096-098):
  companies: id, group_id, name, trade_name, regime_tributario, cnpj VARCHAR(18),
             inscricao_estadual VARCHAR(30), cnae_principal VARCHAR(7),
             cnae_secundario TEXT[], municipio VARCHAR(100), segmento_economico VARCHAR(100),
             incentivos_fiscais JSONB, created_at, updated_at
  icms_fronteira_regras_ncm: id, company_id (nullable), ncm_prefixo, descricao, regime,
             aliquota_interna, mva_original, mva_ajustado_4pct/7pct/12pct, reducao_bc_pct,
             uf_estado VARCHAR(2) NOT NULL DEFAULT 'PE', created_at
             CONSTRAINT uq_icms_fronteira_regras_uf UNIQUE NULLS NOT DISTINCT (company_id, ncm_prefixo, uf_estado)

Struct Company atual (backend/handlers/environment.go linhas 28-36):
  type Company struct {
      ID, GroupID, Name, TradeName, RegimeTributario, CreatedAt string
  }

Handlers atuais com linhas exatas (verificado via grep):
  GetCompaniesHandler   -> linha 234 (SELECT + Scan atual lê 6 campos)
  CreateCompanyHandler  -> linha 270 (aceita name, trade_name, group_id, regime_tributario via JSON)
  UpdateCompanyHandler  -> linha 317 (struct anônimo restrito a RegimeTributario — BUG: silenciosamente descarta demais campos)
  DeleteCompanyHandler  -> linha 360

Padrão Auth + companyID (de backend/handlers/icms_fronteira_regras.go linhas 51-62):
  claims, ok := r.Context().Value(ClaimsKey).(jwt.MapClaims)
  if !ok { jsonErr(w, http.StatusUnauthorized, "Unauthorized"); return }
  userID, _ := claims["user_id"].(string)
  companyID, err := GetEffectiveCompanyID(db, userID, r.Header.Get("X-Company-ID"))

Helpers já no pacote handlers:
  func jsonErr(w http.ResponseWriter, status int, msg string, extra ...map[string]string)
  func GetEffectiveCompanyID(db *sql.DB, userID, headerCompanyID string) (string, error)
  func AuthMiddleware(handler http.HandlerFunc, requiredRole string) http.HandlerFunc

Rota atual `/api/icms-fronteira/regras/` em backend/main.go linha 686 (só DELETE):
  http.HandleFunc("/api/icms-fronteira/regras/", func(w, r) {
      database := getDB(); if database == nil { jsonServiceUnavailable(w); return }
      handlers.AuthMiddleware(handlers.IcmsFronteiraRegraDeleteHandler(database), "")(w, r)
  })

Rota `/api/icms-fronteira/regras` em backend/main.go linhas 691-702 (switch GET/POST):
  http.HandleFunc("/api/icms-fronteira/regras", func(w, r) {
      switch r.Method {
      case "GET":  handlers.AuthMiddleware(handlers.IcmsFronteiraRegrasListHandler(database), "")(w, r)
      case "POST": handlers.AuthMiddleware(handlers.IcmsFronteiraRegraCreateHandler(database), "")(w, r)
      default: w.WriteHeader(http.StatusMethodNotAllowed)
      }
  })

Whitelist UF a usar em TODOS os handlers que aceitam uf_estado:
  validUFs := map[string]bool{"PE": true, "BA": true, "CE": true}
  if !validUFs[ufEstado] { jsonErr(w, http.StatusBadRequest, "uf_estado inválido"); return }

Validação CNPJ a usar em Create/Update Company (apenas quando CNPJ != ""):
  re := regexp.MustCompile(`^\d{14}$`)
  if payload.CNPJ != "" && !re.MatchString(payload.CNPJ) {
      http.Error(w, "CNPJ deve ter 14 dígitos numéricos", http.StatusBadRequest); return
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expandir struct Company + Create/Update/Get handlers em environment.go com 7 campos novos + validação CNPJ</name>
  <files>backend/handlers/environment.go</files>
  <read_first>
    - backend/handlers/environment.go (estado atual completo — struct Company linhas 28-36, GetCompaniesHandler linhas 234-267, CreateCompanyHandler linhas 270-314, UpdateCompanyHandler linhas 317-358, DeleteCompanyHandler linhas 360-...)
    - .planning/phases/08-cadastro-empresas-ambiente-uf/08-PATTERNS.md (seção "environment.go" — struct expandido, snippet UpdateCompanyHandler com NULLIF, validação CNPJ regexp)
    - .planning/phases/08-cadastro-empresas-ambiente-uf/08-RESEARCH.md (Pitfall 4 — interface Company no frontend já tem cnpj; Pitfall 5 — UpdateCompanyHandler usa struct literal anônimo)
    - backend/migrations/077_add_regime_tributario_to_companies.sql (whitelist regime_tributario — manter compatibilidade)
  </read_first>
  <action>
    Modificar `backend/handlers/environment.go`:

    1. Adicionar `"encoding/json"` e `"regexp"` aos imports existentes no topo do arquivo (junto com database/sql, log, net/http, github.com/golang-jwt/jwt/v5 já presentes). NÃO remover imports existentes.

    2. Substituir o struct Company (atualmente linhas 28-36) por struct expandido com tags JSON (snake_case) e `omitempty` para os campos novos opcionais:
       - ID string `json:"id"`
       - GroupID string `json:"group_id"`
       - Name string `json:"name"`
       - TradeName string `json:"trade_name"`
       - RegimeTributario string `json:"regime_tributario"`
       - CNPJ string `json:"cnpj,omitempty"`
       - InscricaoEstadual string `json:"inscricao_estadual,omitempty"`
       - CNAEPrincipal string `json:"cnae_principal,omitempty"`
       - CNAESecundario []string `json:"cnae_secundario,omitempty"`
       - Municipio string `json:"municipio,omitempty"`
       - SegmentoEconomico string `json:"segmento_economico,omitempty"`
       - IncentivosFiscais *json.RawMessage `json:"incentivos_fiscais,omitempty"`
       - CreatedAt string `json:"created_at"`

       Remover o comentário `// CNPJ string \`json:"cnpj"\` // Deprecated` (não é mais deprecated).

    3. Expandir `GetCompaniesHandler` (linha 234):
       - Atualizar query SELECT para incluir os 7 campos novos com COALESCE para tratar NULL: `SELECT id, group_id, name, COALESCE(trade_name, ''), COALESCE(regime_tributario, 'nao_informado'), COALESCE(cnpj, ''), COALESCE(inscricao_estadual, ''), COALESCE(cnae_principal, ''), COALESCE(cnae_secundario, '{}'::text[]), COALESCE(municipio, ''), COALESCE(segmento_economico, ''), incentivos_fiscais, created_at FROM companies` (manter WHERE atual). `cnae_secundario` é TEXT[] → COALESCE com array vazio `'{}'::text[]`. `incentivos_fiscais` é JSONB → scan via `*json.RawMessage` sem COALESCE (pode ficar nil).
       - Atualizar `rows.Scan(...)` para ler os campos adicionais. Usar `pq.Array(&c.CNAESecundario)` para TEXT[] se o driver `lib/pq` estiver em uso (verificar imports atuais — se não estiver, importar `github.com/lib/pq`). Para `incentivos_fiscais` declarar variável local `var inc sql.NullString` ou `*json.RawMessage` e atribuir após scan.
       - Se driver `lib/pq` não estiver importado, alternativa é trocar `cnae_secundario` por `array_to_string(cnae_secundario, ',') AS cnae_secundario_str` no SELECT, ler como string e fazer split por vírgula em Go. **Verificar via `grep -l "lib/pq" backend/handlers/`** antes de decidir; o projeto já usa `lib/pq` em outros lugares (verificado).
       - Empty-slice guard: se rows == nil → `companies = []Company{}` antes do encode JSON (manter padrão do handler atual).

    4. Expandir `CreateCompanyHandler` (linha 270):
       - Expandir o struct de decode do body para incluir os 7 campos novos com mesmos JSON tags do struct Company.
       - Adicionar validação CNPJ: `if payload.CNPJ != ""` → regexp `^\d{14}$` → erro 400 "CNPJ deve ter 14 dígitos numéricos" se não corresponder.
       - Expandir o INSERT para incluir os 7 campos: `INSERT INTO companies (group_id, name, trade_name, regime_tributario, cnpj, inscricao_estadual, cnae_principal, cnae_secundario, municipio, segmento_economico, incentivos_fiscais) VALUES ($1, $2, $3, $4, NULLIF($5,''), NULLIF($6,''), NULLIF($7,''), $8, NULLIF($9,''), NULLIF($10,''), $11)`. Usar `pq.Array(payload.CNAESecundario)` para o array; `NULLIF` para strings vazias virarem NULL; passar `payload.IncentivosFiscais` (que é *json.RawMessage, pode ser nil → JSONB NULL).
       - Manter o RETURNING id existente + retornar Company completo via QueryRow.Scan ou re-select.

    5. Substituir o struct anônimo restrito em `UpdateCompanyHandler` (linha 317) por struct nomeado incluindo todos os 7 campos novos + RegimeTributario:
       - Validar regime_tributario via whitelist existente (manter o bloco map `allowed := map[string]bool{"lucro_real": true, "lucro_presumido": true, "simples_nacional": true, "nao_informado": true}` em linhas ~338-345 — REUSAR sem alterar).
       - Adicionar validação CNPJ regexp se CNPJ != "".
       - Substituir a query UPDATE atual (`UPDATE companies SET regime_tributario = $1, updated_at = NOW() WHERE id = $2`) por UPDATE expandido: `UPDATE companies SET regime_tributario = $1, cnpj = NULLIF($2, ''), inscricao_estadual = NULLIF($3, ''), cnae_principal = NULLIF($4, ''), cnae_secundario = $5, municipio = NULLIF($6, ''), segmento_economico = NULLIF($7, ''), incentivos_fiscais = $8, updated_at = NOW() WHERE id = $9` passando `pq.Array(payload.CNAESecundario)` para $5 e `payload.IncentivosFiscais` para $8.

    6. NÃO modificar `DeleteCompanyHandler` — não é afetado por CADU-02.

    Não criar testes nesta task (cobertos por Plan 03 frontend smoke + execução manual). Não tocar em main.go (rotas /api/config/companies já existem com switch Method).
  </action>
  <verify>
    <automated>cd /home/claudiobezerra/projetos/FB_APU04/backend && go build ./... 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `backend/handlers/environment.go` compila: `cd backend && go build ./...` retorna código 0
    - Struct Company contém os 7 novos campos: `grep -cE "(CNPJ|InscricaoEstadual|CNAEPrincipal|CNAESecundario|Municipio|SegmentoEconomico|IncentivosFiscais)\s+(string|\[\]string|\*json\.RawMessage)" backend/handlers/environment.go` retorna ao menos 7
    - Imports incluem encoding/json e regexp: `grep -cE "\"encoding/json\"|\"regexp\"" backend/handlers/environment.go` retorna ao menos 2
    - GetCompaniesHandler SELECT inclui COALESCE para os 7 novos campos: `grep -cE "COALESCE\((cnpj|inscricao_estadual|cnae_principal|cnae_secundario|municipio|segmento_economico)" backend/handlers/environment.go` retorna ao menos 6
    - CreateCompanyHandler INSERT inclui os 7 campos novos: `grep -E "INSERT INTO companies.*cnpj.*inscricao_estadual" backend/handlers/environment.go` retorna match (regex em uma linha pode falhar com SQL multi-linha — alternativa: `grep -c "cnpj" backend/handlers/environment.go` retorna ao menos 4 ocorrências distintas: struct, Create payload, Create INSERT, Update UPDATE)
    - UpdateCompanyHandler NÃO usa mais struct anônimo restrito: `grep -A2 "var payload struct" backend/handlers/environment.go | grep -c "RegimeTributario string" ` retorna ao menos 1 mas o bloco contém também `CNPJ string`
    - UpdateCompanyHandler UPDATE inclui NULLIF para os campos opcionais: `grep -c "NULLIF" backend/handlers/environment.go` retorna ao menos 6 (Create + Update somados)
    - Validação CNPJ regexp presente: `grep -E "regexp.MustCompile\([\"\`]+\^\\\\d\{14\}\\\$[\"\`]+\)" backend/handlers/environment.go` retorna match (regexp `^\d{14}$`)
    - Whitelist regime_tributario preservada: `grep -c "lucro_real" backend/handlers/environment.go` retorna ao menos 1
    - `go vet ./handlers/` sem warnings: `cd backend && go vet ./handlers/` sai com código 0
  </acceptance_criteria>
  <done>Struct Company expandido para 13 campos (6 antigos + 7 novos). Create/Update/Get persistem e retornam os campos novos. Validação CNPJ via regexp ativa. Build e vet limpos.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Expandir icms_fronteira_regras.go com uf_estado em List/Create/Importar + novo UpdateHandler + roteamento PUT em main.go</name>
  <files>backend/handlers/icms_fronteira_regras.go, backend/main.go</files>
  <read_first>
    - backend/handlers/icms_fronteira_regras.go (estado atual completo — struct FronteiraRegraRow linhas 22-31, IcmsFronteiraRegrasListHandler, IcmsFronteiraRegraCreateHandler, IcmsFronteiraRegraDeleteHandler, IcmsFronteiraRegrasImportarHandler)
    - backend/main.go (linhas 686-702: bloco regras/ e regras — padrão exato a expandir; linha 681: rota importar separada)
    - .planning/phases/08-cadastro-empresas-ambiente-uf/08-PATTERNS.md (seção "icms_fronteira_regras.go" — struct expandido com MVA ajustado e UFEstado, padrão filtro UF, novo Update handler)
    - .planning/phases/08-cadastro-empresas-ambiente-uf/08-RESEARCH.md (Code Examples — filtro por UF no ListHandler)
  </read_first>
  <action>
    Modificar `backend/handlers/icms_fronteira_regras.go`:

    1. Expandir struct `FronteiraRegraRow` (linhas 22-31) adicionando 4 campos novos antes do IsGlobal:
       - MVAAjustado4pct  *float64 `json:"mva_ajustado_4pct"`
       - MVAAjustado7pct  *float64 `json:"mva_ajustado_7pct"`
       - MVAAjustado12pct *float64 `json:"mva_ajustado_12pct"`
       - UFEstado         string   `json:"uf_estado"`

    2. Modificar `IcmsFronteiraRegrasListHandler`:
       - Após o bloco GetEffectiveCompanyID existente, adicionar leitura de query param: `ufEstado := r.URL.Query().Get("uf_estado")`; se vazio → `ufEstado = "PE"` (default preserva compatibilidade com frontend atual ainda não atualizado).
       - Adicionar whitelist: `validUFs := map[string]bool{"PE": true, "BA": true, "CE": true}`; se `!validUFs[ufEstado]` → `jsonErr(w, http.StatusBadRequest, "uf_estado inválido: deve ser PE, BA ou CE"); return`.
       - Atualizar a query SQL principal: adicionar colunas `mva_ajustado_4pct, mva_ajustado_7pct, mva_ajustado_12pct, uf_estado` ao SELECT; adicionar `AND uf_estado = $2` à cláusula WHERE; passar `ufEstado` como segundo parâmetro ao db.Query (junto com companyID).
       - Atualizar o Scan dos rows para ler os 4 campos novos. Para os 3 MVA ajustado usar 3 variáveis locais `sql.NullFloat64` (padrão do MVAOriginal nas linhas 88-104) e atribuir aos ponteiros após Scan quando .Valid. Para uf_estado scan direto em `row.UFEstado` (string NOT NULL).

    3. Modificar `IcmsFronteiraRegraCreateHandler`:
       - Adicionar `UFEstado string \`json:"uf_estado"\`` ao struct de decode do body (junto com NCMPrefixo, Descricao, Regime, AliquotaInterna, MVAOriginal, ReducaoBCPct).
       - Default + whitelist: se UFEstado vazio → "PE"; aplicar o mesmo `validUFs := map[string]bool{...}` + erro 400.
       - Atualizar query INSERT para incluir `uf_estado`: `INSERT INTO icms_fronteira_regras_ncm (company_id, ncm_prefixo, descricao, regime, aliquota_interna, mva_original, reducao_bc_pct, uf_estado) VALUES ($1, $2, $3, $4, $5, $6, $7, $8) ON CONFLICT (company_id, ncm_prefixo, uf_estado) DO UPDATE SET descricao = EXCLUDED.descricao, regime = EXCLUDED.regime, aliquota_interna = EXCLUDED.aliquota_interna, mva_original = EXCLUDED.mva_original, reducao_bc_pct = EXCLUDED.reducao_bc_pct RETURNING id`. ATENÇÃO: ON CONFLICT target precisa bater com a nova constraint `uq_icms_fronteira_regras_uf` criada na migration 097.

    4. Criar novo `IcmsFronteiraRegraUpdateHandler` no mesmo arquivo (após o DeleteHandler):
       - Mesma assinatura `func IcmsFronteiraRegraUpdateHandler(db *sql.DB) http.HandlerFunc`.
       - Auth padrão (claims + userID + GetEffectiveCompanyID) idêntico aos demais handlers.
       - Extrair id do path: `id := strings.TrimPrefix(r.URL.Path, "/api/icms-fronteira/regras/")` + `strings.TrimSpace(id)`; se vazio → 400 "ID não informado".
       - Decode body com campos editáveis: Descricao, Regime, AliquotaInterna (float64), MVAOriginal (*float64), MVAAjustado4pct/7pct/12pct (*float64), ReducaoBCPct (float64). NÃO permitir editar NCMPrefixo nem UFEstado (esses são chaves da constraint UNIQUE — para mudar, deletar e recriar).
       - Validar regime via whitelist `{"ST", "ANTECIPACAO", "DIFAL", "ISENTO", "NORMAL"}` (mesma do CHECK da migration 091).
       - Executar `UPDATE icms_fronteira_regras_ncm SET descricao = $1, regime = $2, aliquota_interna = $3, mva_original = $4, mva_ajustado_4pct = $5, mva_ajustado_7pct = $6, mva_ajustado_12pct = $7, reducao_bc_pct = $8 WHERE id = $9::uuid AND (company_id = $10 OR company_id IS NULL)`. O `(company_id = $10 OR company_id IS NULL)` permite editar regras globais (NULL) e da própria empresa, mas NÃO regras de outras empresas. Verificar RowsAffected: se 0 → 404 "Regra não encontrada ou sem permissão".
       - Retornar JSON `{"updated": true, "id": id}`.

    5. Modificar `IcmsFronteiraRegrasImportarHandler`:
       - Após `ParseMultipartForm` (padrão atual), ler `ufEstado := r.FormValue("uf_estado")` e aplicar default "PE" + whitelist (mesmo bloco).
       - No loop de INSERT/UPSERT de cada linha do CSV/XLSX, incluir `uf_estado` como coluna no INSERT e usar `ufEstado` (a variável capturada do FormValue, não da linha do arquivo — o arquivo pode opcionalmente conter coluna uf_estado, mas se ausente a FormValue prevalece). Atualizar ON CONFLICT target para `(company_id, ncm_prefixo, uf_estado)`.

    Modificar `backend/main.go`:

    6. Localizar o bloco da rota `/api/icms-fronteira/regras/` (linha 686 — atualmente roteia apenas DeleteHandler). Substituir o conteúdo do handler por switch `r.Method`:
       - case http.MethodDelete → IcmsFronteiraRegraDeleteHandler (mantém comportamento atual)
       - case http.MethodPut, http.MethodPatch → IcmsFronteiraRegraUpdateHandler (novo)
       - default → `w.WriteHeader(http.StatusMethodNotAllowed)`
       - Manter o guard `database := getDB(); if database == nil { jsonServiceUnavailable(w); return }` antes do switch.
       - Manter `handlers.AuthMiddleware(handler, "")(w, r)` em cada case (role "" = qualquer autenticado).
       - NÃO modificar a rota `/api/icms-fronteira/regras` (sem barra final, linha 691) — esta já trata GET/POST e deve continuar inalterada.
       - NÃO modificar a rota `/api/icms-fronteira/regras/importar` (linha 681) — handler de importar é separado.
  </action>
  <verify>
    <automated>cd /home/claudiobezerra/projetos/FB_APU04/backend && go build ./... && go vet ./handlers/ 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && go build ./...` retorna código 0
    - `cd backend && go vet ./handlers/` sem warnings
    - Struct FronteiraRegraRow contém os 4 campos novos: `grep -cE "(MVAAjustado4pct|MVAAjustado7pct|MVAAjustado12pct|UFEstado)\s+(\*float64|string)" backend/handlers/icms_fronteira_regras.go` retorna 4
    - Whitelist UF presente: `grep -c 'map\[string\]bool{"PE": true, "BA": true, "CE": true}' backend/handlers/icms_fronteira_regras.go` retorna ao menos 3 (List, Create, Importar)
    - ListHandler tem filtro WHERE uf_estado: `grep -E "AND uf_estado\s*=\s*\\\$2" backend/handlers/icms_fronteira_regras.go` retorna match
    - CreateHandler ON CONFLICT inclui uf_estado: `grep -E "ON CONFLICT \(company_id, ncm_prefixo, uf_estado\)" backend/handlers/icms_fronteira_regras.go` retorna match
    - Novo handler exportado: `grep -c "func IcmsFronteiraRegraUpdateHandler" backend/handlers/icms_fronteira_regras.go` retorna 1
    - UpdateHandler valida ownership via WHERE: `grep -E "WHERE id\s*=\s*\\\$\d+::uuid AND \(company_id" backend/handlers/icms_fronteira_regras.go` retorna match
    - main.go: rota /regras/ com switch PUT/PATCH: `grep -A5 'http.HandleFunc."/api/icms-fronteira/regras/"' backend/main.go | grep -c "MethodPut\|MethodPatch"` retorna ao menos 1
    - main.go contém referência ao novo handler: `grep -c "IcmsFronteiraRegraUpdateHandler" backend/main.go` retorna 1
    - ImportarHandler usa FormValue uf_estado: `grep -c 'r.FormValue("uf_estado")' backend/handlers/icms_fronteira_regras.go` retorna 1
  </acceptance_criteria>
  <done>FronteiraRegraRow expandido com MVA ajustado + UFEstado; List filtra por uf_estado; Create persiste uf_estado; novo UpdateHandler aceita PUT/PATCH com proteção de ownership; main.go roteia PUT/PATCH; Importar respeita uf_estado via FormValue; build e vet limpos.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Guard tests para IcmsFronteiraRegraUpdateHandler (criação + método não permitido)</name>
  <files>backend/handlers/icms_fronteira_regras_update_test.go</files>
  <read_first>
    - backend/handlers/icms_fronteira_regras.go (assinatura do novo IcmsFronteiraRegraUpdateHandler criado na Task 2)
    - backend/handlers/handlers_guards13_test.go (padrão exato dos guard tests do projeto: HandlerName(nil) retorna non-nil; httptest.NewRequest/NewRecorder; StatusMethodNotAllowed test)
  </read_first>
  <action>
    Criar `backend/handlers/icms_fronteira_regras_update_test.go` no package `handlers`, importando net/http, net/http/httptest, testing.

    1. `func TestIcmsFronteiraRegraUpdateHandler_Creation(t *testing.T)`: instanciar `h := IcmsFronteiraRegraUpdateHandler(nil)`; assertar `h == nil` → t.Error("expected non-nil handler").

    2. `func TestIcmsFronteiraRegraUpdateHandler_MethodNotAllowed(t *testing.T)`: criar handler com nil DB, httptest.NewRequest(http.MethodGet, "/api/icms-fronteira/regras/abc", nil) + httptest.NewRecorder, invocar handler(rr, req), assertar `rr.Code == http.StatusMethodNotAllowed`. (GET é rejeitado antes de tocar o DB, por isso nil é seguro — o handler só aceita PUT/PATCH.)

    Não escrever testes que executem queries (exigiriam DB real ou sqlmock — fora do escopo desta fase). Os 2 guard tests seguem o padrão dos demais handlers do projeto.
  </action>
  <verify>
    <automated>cd /home/claudiobezerra/projetos/FB_APU04/backend && go test ./handlers/ -run "TestIcmsFronteiraRegraUpdateHandler" -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `backend/handlers/icms_fronteira_regras_update_test.go` existe
    - Contém função `TestIcmsFronteiraRegraUpdateHandler_Creation`: `grep -c "func TestIcmsFronteiraRegraUpdateHandler_Creation" backend/handlers/icms_fronteira_regras_update_test.go` retorna 1
    - Contém função `TestIcmsFronteiraRegraUpdateHandler_MethodNotAllowed`: `grep -c "func TestIcmsFronteiraRegraUpdateHandler_MethodNotAllowed" backend/handlers/icms_fronteira_regras_update_test.go` retorna 1
    - Testa o status code 405: `grep -c "StatusMethodNotAllowed" backend/handlers/icms_fronteira_regras_update_test.go` retorna ao menos 1
    - `cd backend && go test ./handlers/ -run "TestIcmsFronteiraRegraUpdateHandler"` sai com código 0 e indica "PASS" para os 2 testes
  </acceptance_criteria>
  <done>Os 2 guard tests do IcmsFronteiraRegraUpdateHandler passam, confirmando que o handler é criável e rejeita GET com 405 antes de tocar o banco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → API Go | JWT do usuário + header X-Company-ID podem ser manipulados pelo cliente |
| API → PostgreSQL | queries com company_id derivado do JWT, nunca do body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Spoofing | UpdateCompanyHandler aceita id via query param | mitigate | Padrão existente do handler já valida que o usuário pertence à empresa via JWT + GetEffectiveCompanyID (manter); novos campos seguem o mesmo guard |
| T-08-06 | Elevation of Privilege | IcmsFronteiraRegraUpdateHandler pode editar regra de outra empresa via id forjado | mitigate | UPDATE com cláusula `WHERE id = $1 AND (company_id = $2 OR company_id IS NULL)` — usuário só edita regras da própria empresa ou globais; RowsAffected==0 → 404 |
| T-08-07 | Tampering | uf_estado forjado no body/query (ex: "XX" para bypass filtro) | mitigate | Whitelist `map[string]bool{"PE":true,"BA":true,"CE":true}` em List/Create/Importar antes de qualquer uso em SQL; erro 400 se inválido |
| T-08-08 | Tampering (SQL injection) | uf_estado interpolado em SQL | mitigate | uf_estado sempre via `$N` placeholder no Query/Exec; valor já filtrado pela whitelist antes de chegar à query |
| T-08-09 | Tampering | CNPJ malformado ou com caracteres especiais | mitigate | regexp `^\d{14}$` em CreateCompanyHandler e UpdateCompanyHandler quando CNPJ != ""; tipo VARCHAR(18) acomoda apenas dígitos; sem máscara |
| T-08-10 | Information Disclosure | Upload XLSX com fórmulas maliciosas em IcmsFronteiraRegrasImportarHandler | accept | excelize.OpenReader não executa macros (verificado em RESEARCH); risco baixo para usuário interno autenticado |
| T-08-11 | Denial of Service | Upload de arquivo enorme em /regras/importar | accept | Padrão atual do handler já trata limite via ParseMultipartForm com maxMemory; risco residual aceito (operação admin) |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova nesta fase (RESEARCH: `excelize/v2` já em go.mod, `lib/pq` já em uso) — sem superfície de supply chain |
</threat_model>

<verification>
- `cd backend && go build ./...` compila sem erros
- `cd backend && go vet ./handlers/` sem warnings
- `cd backend && go test ./handlers/ -run "TestIcmsFronteiraRegraUpdateHandler" -v` PASS para os 2 testes
- Whitelist UF presente em 3 handlers (List, Create, Importar)
- ON CONFLICT atualizado para (company_id, ncm_prefixo, uf_estado)
- Validação CNPJ regexp ^\d{14}$ presente
- main.go roteia PUT/PATCH para o novo Update handler
</verification>

<success_criteria>
- Struct Company expandido para 13 campos (6 antigos + 7 novos) com tags JSON corretas
- GetCompaniesHandler retorna os 7 novos campos via COALESCE
- CreateCompanyHandler aceita e persiste os 7 novos campos com validação CNPJ
- UpdateCompanyHandler substitui struct anônimo por struct nomeado completo
- FronteiraRegraRow expandido com MVAAjustado4/7/12pct + UFEstado
- IcmsFronteiraRegrasListHandler filtra por uf_estado (whitelist PE/BA/CE)
- IcmsFronteiraRegraCreateHandler persiste uf_estado (whitelist + ON CONFLICT atualizado)
- IcmsFronteiraRegrasImportarHandler usa FormValue("uf_estado") com whitelist
- Novo IcmsFronteiraRegraUpdateHandler atende PUT/PATCH com proteção de ownership
- main.go roteia PUT/PATCH para o novo handler
- 2 guard tests passam
- Build e vet limpos
</success_criteria>

<output>
Create `.planning/phases/08-cadastro-empresas-ambiente-uf/08-02-SUMMARY.md` when done
</output>
