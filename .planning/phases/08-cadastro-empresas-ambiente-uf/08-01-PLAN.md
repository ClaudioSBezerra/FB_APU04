---
phase: 08-cadastro-empresas-ambiente-uf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/096_add_fields_to_companies.sql
  - backend/migrations/097_add_uf_estado_to_fronteira_regras.sql
  - backend/migrations/098_seed_ba_ce_fronteira.sql
autonomous: true
requirements: [CADU-01, CADU-04, CADU-05]

must_haves:
  truths:
    - "Tabela companies possui cnpj, inscricao_estadual, cnae_principal, cnae_secundario, municipio, segmento_economico, incentivos_fiscais (todos opcionais — nullable, sem UNIQUE em cnpj)"
    - "Tabela icms_fronteira_regras_ncm possui uf_estado VARCHAR(2) NOT NULL DEFAULT 'PE' + mva_ajustado_4pct/7pct/12pct NUMERIC(8,4)"
    - "Constraint uq_icms_fronteira_regras foi removida e substituída por uq_icms_fronteira_regras_uf incluindo uf_estado"
    - "Tabela icms_fronteira_inaplicabilidades existe com (id, ncm_digits, uf_estado, motivo, vigencia_inicio, vigencia_fim)"
    - "Seed insere regras NCM para uf_estado='BA' e uf_estado='CE' usando ON CONFLICT DO NOTHING (idempotente)"
    - "Migrations rodam na ordem 096 → 097 → 098 e são idempotentes (re-execução não causa erro)"
  artifacts:
    - path: "backend/migrations/096_add_fields_to_companies.sql"
      provides: "ADD COLUMN IF NOT EXISTS para 7 campos novos em companies"
      contains: "ADD COLUMN IF NOT EXISTS cnpj"
    - path: "backend/migrations/097_add_uf_estado_to_fronteira_regras.sql"
      provides: "uf_estado + MVA ajustado + nova constraint UNIQUE + tabela inaplicabilidades"
      contains: "DROP CONSTRAINT IF EXISTS uq_icms_fronteira_regras"
    - path: "backend/migrations/098_seed_ba_ce_fronteira.sql"
      provides: "Seed inicial regras BA e CE com ON CONFLICT DO NOTHING"
      contains: "INSERT INTO icms_fronteira_regras_ncm"
  key_links:
    - from: "backend/migrations/097_add_uf_estado_to_fronteira_regras.sql"
      to: "icms_fronteira_regras_ncm (existente da migration 091)"
      via: "ALTER TABLE ADD COLUMN + DROP CONSTRAINT + ADD CONSTRAINT"
      pattern: "uq_icms_fronteira_regras_uf"
    - from: "backend/migrations/098_seed_ba_ce_fronteira.sql"
      to: "icms_fronteira_regras_ncm (com uf_estado já presente)"
      via: "INSERT com coluna uf_estado='BA'|'CE'"
      pattern: "VALUES.*'BA'|VALUES.*'CE'"
---

<objective>
Criar 3 migrations SQL que (1) re-adicionam CNPJ + 6 campos de cadastro mestre à tabela `companies` (CADU-01), (2) expandem `icms_fronteira_regras_ncm` com `uf_estado` + MVA ajustado (4/7/12%) e criam a tabela `icms_fronteira_inaplicabilidades` (CADU-04), (3) inserem o seed inicial de regras para BA e CE (CADU-05).

Purpose: Estabelecer o schema de banco que desbloqueia o backend (08-02) e o frontend (08-03). Todas as migrations seguem o padrão idempotente do projeto (`IF NOT EXISTS` para colunas/tabelas, `DO $$ ... EXCEPTION WHEN duplicate_object THEN NULL` para constraints, `ON CONFLICT DO NOTHING` para seeds). A ordem 096 → 097 → 098 é crítica: a 097 precisa rodar antes da 098 para que `uf_estado` exista quando os INSERTs de BA/CE ocorrerem.

Output: 3 arquivos SQL em `backend/migrations/` rodáveis em sequência sem erro, deixando o schema preparado para os handlers Go (Plan 02) e a UI (Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cadastro-empresas-ambiente-uf/08-RESEARCH.md
@.planning/phases/08-cadastro-empresas-ambiente-uf/08-PATTERNS.md

<interfaces>
<!-- Padrões de migration idempotente verificados no codebase (2026-05-23). -->

Padrão ADD COLUMN IF NOT EXISTS (de backend/migrations/077_add_regime_tributario_to_companies.sql):
  ALTER TABLE companies ADD COLUMN IF NOT EXISTS regime_tributario TEXT NOT NULL DEFAULT 'nao_informado';

Padrão DO block idempotente para constraint (de backend/migrations/077):
  DO $$ BEGIN
      ALTER TABLE companies ADD CONSTRAINT chk_companies_regime_tributario CHECK (...);
  EXCEPTION WHEN duplicate_object THEN NULL;
  END $$;

Constraint atual em icms_fronteira_regras_ncm (de backend/migrations/091_icms_fronteira.sql linha 16):
  CONSTRAINT uq_icms_fronteira_regras UNIQUE NULLS NOT DISTINCT (company_id, ncm_prefixo)

Padrão seed idempotente (de backend/migrations/091_icms_fronteira.sql linhas 24-68):
  INSERT INTO icms_fronteira_regras_ncm (ncm_prefixo, descricao, regime, aliquota_interna, mva_original)
  VALUES ('2202', 'Refrigerantes...', 'ST', 20.5, 140.00), ...
  ON CONFLICT DO NOTHING;

Schema atual de companies (após migrations 013/014/017/023/077):
  id UUID PK, group_id UUID FK, name VARCHAR(255), trade_name VARCHAR(255),
  owner_id UUID FK, regime_tributario TEXT, created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ
  -- CNPJ foi removido em migration 023, deve ser re-adicionado como VARCHAR(18) nullable sem UNIQUE

Próxima migration livre: 096 (última existente é 095_add_cest_fronteira.sql) — verificado via ls.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 096 — re-adicionar CNPJ + 6 campos de cadastro mestre em companies</name>
  <files>backend/migrations/096_add_fields_to_companies.sql</files>
  <read_first>
    - backend/migrations/077_add_regime_tributario_to_companies.sql (padrão idempotente ADD COLUMN + DO block para constraint + COMMENT ON COLUMN)
    - backend/migrations/023_remove_cnpj_from_companies.sql (histórico — confirma que CNPJ foi removido, justifica re-adição sem UNIQUE)
    - backend/migrations/013_create_environment_hierarchy.sql (schema original de companies — referência da estrutura mantida)
    - .planning/phases/08-cadastro-empresas-ambiente-uf/08-PATTERNS.md (seção "096_add_fields_to_companies.sql")
  </read_first>
  <action>
    Criar `backend/migrations/096_add_fields_to_companies.sql` com cabeçalho de comentário descritivo (3-4 linhas) no padrão da migration 077: identificar o arquivo, citar o requirement CADU-01, e marcar como idempotente.

    Emitir 7 statements `ALTER TABLE companies ADD COLUMN IF NOT EXISTS` para os campos abaixo, todos nullable (sem NOT NULL), sem UNIQUE, sem DEFAULT:
    - cnpj VARCHAR(18) — re-adição do campo removido na migration 023. NÃO usar VARCHAR(14) (histórico) nem adicionar UNIQUE (multi-filial pode compartilhar CNPJs relacionados; threat model do phase exige NÃO criar UNIQUE).
    - inscricao_estadual VARCHAR(30)
    - cnae_principal VARCHAR(7)
    - cnae_secundario TEXT[] — array nativo PostgreSQL para múltiplos CNAEs secundários
    - municipio VARCHAR(100)
    - segmento_economico VARCHAR(100)
    - incentivos_fiscais JSONB — schema livre nesta fase (CADU-01 não exige estrutura fixa)

    Adicionar 7 statements `COMMENT ON COLUMN companies.<campo> IS '...';` explicando cada campo em PT-BR, seguindo o padrão da migration 077 linha 23.

    Não emitir CREATE INDEX para CNPJ — buscas por CNPJ serão raras na UI atual. Não criar nenhuma constraint além do que já existe na tabela.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS" /home/claudiobezerra/projetos/FB_APU04/backend/migrations/096_add_fields_to_companies.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `backend/migrations/096_add_fields_to_companies.sql` existe
    - Arquivo contém exatamente 7 linhas com `ADD COLUMN IF NOT EXISTS` (verificável: `grep -c "ADD COLUMN IF NOT EXISTS" .../096_*.sql` retorna 7)
    - Arquivo contém `cnpj VARCHAR(18)` (sem UNIQUE, sem NOT NULL): `grep -E "cnpj VARCHAR\(18\)" .../096_*.sql` retorna match
    - Arquivo contém `cnae_secundario TEXT\[\]`: `grep -E "cnae_secundario\s+TEXT\[\]" .../096_*.sql` retorna match
    - Arquivo contém `incentivos_fiscais JSONB`: `grep -E "incentivos_fiscais\s+JSONB" .../096_*.sql` retorna match
    - Arquivo NÃO contém `UNIQUE` aplicado a cnpj: `grep -iE "cnpj.*UNIQUE|UNIQUE.*cnpj" .../096_*.sql` retorna 0 linhas
    - Arquivo NÃO contém `NOT NULL` em qualquer dos 7 campos novos: `grep -E "(cnpj|inscricao_estadual|cnae_principal|cnae_secundario|municipio|segmento_economico|incentivos_fiscais).*NOT NULL" .../096_*.sql` retorna 0 linhas
    - Arquivo contém 7 statements `COMMENT ON COLUMN companies.`: `grep -c "COMMENT ON COLUMN companies\." .../096_*.sql` retorna 7
  </acceptance_criteria>
  <done>Migration 096 cria 7 colunas novas em `companies` de forma idempotente, todas opcionais, sem constraints adicionais, com comentários descritivos.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 097 — uf_estado + MVA ajustado em icms_fronteira_regras_ncm + tabela inaplicabilidades</name>
  <files>backend/migrations/097_add_uf_estado_to_fronteira_regras.sql</files>
  <read_first>
    - backend/migrations/091_icms_fronteira.sql (estrutura original da tabela icms_fronteira_regras_ncm, constraint uq_icms_fronteira_regras na linha 16, padrão CREATE TABLE IF NOT EXISTS, CREATE INDEX IF NOT EXISTS)
    - backend/migrations/077_add_regime_tributario_to_companies.sql (padrão DO block para constraint idempotente)
    - .planning/phases/08-cadastro-empresas-ambiente-uf/08-PATTERNS.md (seção "097_add_uf_estado_to_fronteira_regras.sql" — SQL completo)
    - .planning/phases/08-cadastro-empresas-ambiente-uf/08-RESEARCH.md (Pitfall 1 — constraint UNIQUE precisa ser recriada com uf_estado)
  </read_first>
  <action>
    Criar `backend/migrations/097_add_uf_estado_to_fronteira_regras.sql` com cabeçalho descritivo citando CADU-04 e destacando explicitamente: "CRÍTICO: DROP da constraint uq_icms_fronteira_regras é obrigatório antes de adicionar uf_estado, senão regras para BA/CE com mesma NCM colidem com regras PE existentes (Pitfall 1 do RESEARCH)".

    Bloco 1 — adicionar coluna uf_estado: `ALTER TABLE icms_fronteira_regras_ncm ADD COLUMN IF NOT EXISTS uf_estado VARCHAR(2) NOT NULL DEFAULT 'PE';`. DEFAULT 'PE' preserva os registros existentes da migration 091 que são todos para Pernambuco.

    Bloco 2 — adicionar 3 colunas MVA ajustado nullable: 3 statements ADD COLUMN IF NOT EXISTS para `mva_ajustado_4pct NUMERIC(8,4)`, `mva_ajustado_7pct NUMERIC(8,4)`, `mva_ajustado_12pct NUMERIC(8,4)`. Todos NUMERIC(8,4), nullable — cálculo dinâmico via fórmula Convênio ICMS 110/07 possível quando NULL.

    Bloco 3 — recriar constraint UNIQUE incluindo uf_estado:
    1. `ALTER TABLE icms_fronteira_regras_ncm DROP CONSTRAINT IF EXISTS uq_icms_fronteira_regras;` (nome exato verificado em 091_icms_fronteira.sql:16)
    2. Dentro de `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN NULL; END $$;`: `ALTER TABLE icms_fronteira_regras_ncm ADD CONSTRAINT uq_icms_fronteira_regras_uf UNIQUE NULLS NOT DISTINCT (company_id, ncm_prefixo, uf_estado);`. Nome novo (`_uf` sufixo) distingue da antiga; NULLS NOT DISTINCT preserva semântica do projeto.

    Bloco 4 — criar tabela inaplicabilidades:
    `CREATE TABLE IF NOT EXISTS icms_fronteira_inaplicabilidades (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), ncm_digits VARCHAR(8) NOT NULL, uf_estado VARCHAR(2) NOT NULL, motivo TEXT NOT NULL, vigencia_inicio DATE, vigencia_fim DATE, created_at TIMESTAMPTZ NOT NULL DEFAULT now());`

    Bloco 5 — índice composto: `CREATE INDEX IF NOT EXISTS idx_icms_fronteira_inapl_uf_ncm ON icms_fronteira_inaplicabilidades(uf_estado, ncm_digits);`

    Bloco 6 — comentários: COMMENT ON COLUMN para uf_estado e as 3 colunas mva_ajustado_* + COMMENT ON TABLE para icms_fronteira_inaplicabilidades, todos em PT-BR.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS" /home/claudiobezerra/projetos/FB_APU04/backend/migrations/097_add_uf_estado_to_fronteira_regras.sql && grep -c "DROP CONSTRAINT IF EXISTS uq_icms_fronteira_regras" /home/claudiobezerra/projetos/FB_APU04/backend/migrations/097_add_uf_estado_to_fronteira_regras.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `backend/migrations/097_add_uf_estado_to_fronteira_regras.sql` existe
    - Contém `uf_estado VARCHAR(2) NOT NULL DEFAULT 'PE'`: `grep -E "uf_estado\s+VARCHAR\(2\)\s+NOT NULL DEFAULT 'PE'" .../097_*.sql` retorna match
    - Contém as 3 colunas MVA ajustado: `grep -cE "mva_ajustado_(4|7|12)pct\s+NUMERIC\(8,4\)" .../097_*.sql` retorna 3
    - Contém `DROP CONSTRAINT IF EXISTS uq_icms_fronteira_regras` (constraint antiga): `grep -c "DROP CONSTRAINT IF EXISTS uq_icms_fronteira_regras\b" .../097_*.sql` retorna 1
    - Contém `ADD CONSTRAINT uq_icms_fronteira_regras_uf`: `grep -c "uq_icms_fronteira_regras_uf" .../097_*.sql` retorna ao menos 1
    - Contém `UNIQUE NULLS NOT DISTINCT (company_id, ncm_prefixo, uf_estado)`: `grep -E "UNIQUE NULLS NOT DISTINCT\s*\(company_id,\s*ncm_prefixo,\s*uf_estado\)" .../097_*.sql` retorna match
    - Contém `CREATE TABLE IF NOT EXISTS icms_fronteira_inaplicabilidades`: `grep -c "CREATE TABLE IF NOT EXISTS icms_fronteira_inaplicabilidades" .../097_*.sql` retorna 1
    - Tabela inaplicabilidades tem todos os 6 campos esperados: `grep -cE "(ncm_digits|uf_estado|motivo|vigencia_inicio|vigencia_fim)" .../097_*.sql` retorna ao menos 5
    - Contém `CREATE INDEX IF NOT EXISTS idx_icms_fronteira_inapl_uf_ncm`: `grep -c "idx_icms_fronteira_inapl_uf_ncm" .../097_*.sql` retorna 1
    - Contém bloco `DO $$` com `EXCEPTION WHEN duplicate_object`: `grep -c "EXCEPTION WHEN duplicate_object" .../097_*.sql` retorna ao menos 1
  </acceptance_criteria>
  <done>Migration 097 expande icms_fronteira_regras_ncm com uf_estado + MVA ajustado, recria a constraint UNIQUE incluindo uf_estado, e cria a tabela icms_fronteira_inaplicabilidades — tudo idempotente.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 098 — seed inicial de regras NCM para BA e CE</name>
  <files>backend/migrations/098_seed_ba_ce_fronteira.sql</files>
  <read_first>
    - backend/migrations/091_icms_fronteira.sql (linhas 24-68 — padrão do seed PE original: INSERT com ON CONFLICT DO NOTHING; valores PE como referência de estrutura)
    - .planning/phases/08-cadastro-empresas-ambiente-uf/08-RESEARCH.md (seção "Seed BA e CE — Referência", tabelas BA/CE com alíquotas e MVA originais — todos marcados [ASSUMED])
    - .planning/phases/08-cadastro-empresas-ambiente-uf/08-PATTERNS.md (seção "098_seed_ba_ce_fronteira.sql")
  </read_first>
  <action>
    Criar `backend/migrations/098_seed_ba_ce_fronteira.sql` com cabeçalho descritivo identificando CADU-05 e explicitando duas notas: (1) "Valores [ASSUMED] baseados em legislação estadual conhecida — devem ser revisados contra RICMS/BA e RICMS/CE antes da execução em produção" e (2) "Deve rodar APÓS migration 097 que adiciona uf_estado e recria constraint UNIQUE".

    Bloco 1 — seed BA: emitir um único `INSERT INTO icms_fronteira_regras_ncm (ncm_prefixo, descricao, regime, aliquota_interna, mva_original, uf_estado) VALUES (...) ON CONFLICT DO NOTHING;` com as 7 linhas abaixo (company_id IS NULL = regra global aplicável a todas as empresas; uf_estado='BA'):
    - ('2202', 'Refrigerantes e bebidas não alcoólicas', 'ST', 26.0, 140.00, 'BA')
    - ('2203', 'Cervejas de malte', 'ST', 26.0, 140.00, 'BA')
    - ('3004', 'Medicamentos humanos', 'ST', 12.0, 33.00, 'BA')
    - ('3303', 'Cosméticos e produtos de higiene', 'ST', 20.5, 45.00, 'BA')
    - ('4011', 'Pneumáticos novos', 'ST', 20.5, 42.00, 'BA')
    - ('2523', 'Cimento', 'ST', 17.5, 25.00, 'BA')
    - ('8517', 'Aparelhos telefônicos', 'ST', 20.5, 15.00, 'BA')

    Bloco 2 — seed CE: segundo INSERT idêntico em estrutura, com 7 linhas usando uf_estado='CE' e valores da tabela "Regras Ceará" do RESEARCH (apenas aliquota_interna de 2202/2203/2523 muda em relação ao BA):
    - ('2202', 'Refrigerantes e bebidas não alcoólicas', 'ST', 25.0, 140.00, 'CE')
    - ('2203', 'Cervejas de malte', 'ST', 25.0, 140.00, 'CE')
    - ('3004', 'Medicamentos humanos', 'ST', 12.0, 33.00, 'CE')
    - ('3303', 'Cosméticos e produtos de higiene', 'ST', 20.5, 45.00, 'CE')
    - ('4011', 'Pneumáticos novos', 'ST', 20.5, 42.00, 'CE')
    - ('2523', 'Cimento', 'ST', 17.0, 25.00, 'CE')
    - ('8517', 'Aparelhos telefônicos', 'ST', 20.5, 15.00, 'CE')

    NÃO inserir valor em `mva_ajustado_4pct`/`mva_ajustado_7pct`/`mva_ajustado_12pct` — permanecem NULL e podem ser preenchidos pela UI/upload de planilha em Plan 02/03 (decisão A1/Open Question 1 do RESEARCH: armazenar pré-calculados quando disponíveis, calcular dinamicamente como fallback).

    Não inserir seed em `icms_fronteira_inaplicabilidades` — tabela vazia inicialmente (CADU-04 menciona "seed para inaplicabilidades conhecidas" mas não lista quais; deixar para configuração via UI quando o ambiente identificar casos específicos).
  </action>
  <verify>
    <automated>grep -c "'BA'" /home/claudiobezerra/projetos/FB_APU04/backend/migrations/098_seed_ba_ce_fronteira.sql && grep -c "'CE'" /home/claudiobezerra/projetos/FB_APU04/backend/migrations/098_seed_ba_ce_fronteira.sql && grep -c "ON CONFLICT DO NOTHING" /home/claudiobezerra/projetos/FB_APU04/backend/migrations/098_seed_ba_ce_fronteira.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `backend/migrations/098_seed_ba_ce_fronteira.sql` existe
    - Contém ao menos 2 statements `INSERT INTO icms_fronteira_regras_ncm`: `grep -c "INSERT INTO icms_fronteira_regras_ncm" .../098_*.sql` retorna ao menos 2
    - Contém ao menos 7 ocorrências de `'BA'` (uma por NCM no seed BA): `grep -c "'BA'" .../098_*.sql` retorna ao menos 7
    - Contém ao menos 7 ocorrências de `'CE'` (uma por NCM no seed CE): `grep -c "'CE'" .../098_*.sql` retorna ao menos 7
    - Contém ao menos 2 ocorrências de `ON CONFLICT DO NOTHING` (uma por INSERT): `grep -c "ON CONFLICT DO NOTHING" .../098_*.sql` retorna ao menos 2
    - Contém colunas (ncm_prefixo, descricao, regime, aliquota_interna, mva_original, uf_estado) na lista de INSERT: `grep -cE "\(ncm_prefixo,\s*descricao,\s*regime,\s*aliquota_interna,\s*mva_original,\s*uf_estado\)" .../098_*.sql` retorna ao menos 2
    - Arquivo NÃO insere em mva_ajustado_* (deixa NULL): `grep -c "mva_ajustado_" .../098_*.sql` retorna 0
    - NCMs esperados aparecem no seed: `grep -cE "'(2202|2203|3004|3303|4011|2523|8517)'" .../098_*.sql` retorna ao menos 14 (7 por UF)
  </acceptance_criteria>
  <done>Migration 098 insere 14 regras NCM (7 BA + 7 CE) globalmente como seed inicial, idempotente via ON CONFLICT DO NOTHING, sem preencher mva_ajustado_* (deixa para UI).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → PostgreSQL | DDL/DML executado pelo sistema de migrations do projeto (admin DB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Constraint UNIQUE em icms_fronteira_regras_ncm | mitigate | DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT (com nome diferente _uf) garante migração segura mesmo se 097 rodar duas vezes; constraint nova inclui uf_estado para permitir mesma NCM em UFs distintas (CADU-04) |
| T-08-02 | Information Disclosure | CNPJ no schema (campo identificador fiscal) | accept | CNPJ é dado público no Brasil (CADASTUR/Receita); não-único permite multi-filial; ASVS V5 sem requisito de criptografia adicional |
| T-08-03 | Denial of Service | Re-execução acidental das migrations | mitigate | Todos os DDL usam IF NOT EXISTS / DO block com EXCEPTION; INSERTs usam ON CONFLICT DO NOTHING — re-execução é idempotente |
| T-08-04 | Tampering | Seed de valores fiscais [ASSUMED] | mitigate | Cabeçalho de 098 explicita risco; valores são editáveis via UI (Plan 03) e upload de planilha (Plan 02); revisão por especialista fiscal antes do uso em produção |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova nesta fase (RESEARCH "Nenhuma dependência nova a instalar") — sem superfície de supply chain |
</threat_model>

<verification>
- 3 arquivos SQL criados em `backend/migrations/` com numeração sequencial 096, 097, 098
- Cada arquivo usa padrão idempotente do projeto (IF NOT EXISTS, DO block, ON CONFLICT DO NOTHING)
- Constraint `uq_icms_fronteira_regras` é dropada antes de adicionar uf_estado em 097
- Seed 098 não insere mva_ajustado_* (permanece NULL para preenchimento via UI)
- Re-execução das 3 migrations não causa erro (cada uma é idempotente)
</verification>

<success_criteria>
- 3 migrations criadas: 096 (companies fields), 097 (fronteira uf_estado + inaplicabilidades), 098 (seed BA/CE)
- Todas as colunas novas em companies são nullable, sem UNIQUE
- Constraint uq_icms_fronteira_regras_uf substitui uq_icms_fronteira_regras incluindo uf_estado
- Tabela icms_fronteira_inaplicabilidades criada com índice composto (uf_estado, ncm_digits)
- 14 INSERTs (7 BA + 7 CE) com ON CONFLICT DO NOTHING
- Schema pronto para handlers Go (Plan 02) e UI (Plan 03)
</success_criteria>

<output>
Create `.planning/phases/08-cadastro-empresas-ambiente-uf/08-01-SUMMARY.md` when done
</output>
