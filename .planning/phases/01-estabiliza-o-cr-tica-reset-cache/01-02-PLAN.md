---
phase: 01-estabiliza-o-cr-tica-reset-cache
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - frontend/src/components/ResetDatabaseDialog.tsx
  - frontend/src/pages/ImportarEFD.tsx
autonomous: false
requirements:
  - STAB-01

must_haves:
  truths:
    - "Botão 'Zerar Tudo (Admin)' não dispara fetch direto — abre dialog modal"
    - "Dialog mostra explicitamente o nome do banco conectado e a contagem aproximada de registros que serão destruídos"
    - "Botão 'Confirmar destruição' permanece desabilitado até o usuário digitar EXATAMENTE 'DELETE-FB_APU04' no input"
    - "Submit envia DELETE /api/admin/reset-db com body {\"confirmation\":\"DELETE-FB_APU04\"}"
    - "Resposta 429 mostra toast vermelho explicando rate limit; resposta 503 (DB allowlist) mostra toast com nome do DB conectado e ALLOWED_DESTRUCTIVE_DBS"
    - "Após sucesso, dialog fecha e jobs são recarregados (mesma UX atual)"
  artifacts:
    - path: "frontend/src/components/ResetDatabaseDialog.tsx"
      provides: "Componente modal de confirmação destrutiva reutilizável"
      exports:
        - "ResetDatabaseDialog"
      contains: "DELETE-FB_APU04"
    - path: "frontend/src/pages/ImportarEFD.tsx"
      provides: "Tela onde o botão admin 'Zerar Tudo' agora abre o dialog"
      contains: "ResetDatabaseDialog"
  key_links:
    - from: "frontend/src/pages/ImportarEFD.tsx button 'Zerar Tudo (Admin)'"
      to: "ResetDatabaseDialog open state"
      via: "onClick → setResetOpen(true)"
      pattern: "setResetOpen\\(true\\)"
    - from: "ResetDatabaseDialog confirm button"
      to: "/api/admin/reset-db"
      via: "fetch DELETE com body { confirmation: 'DELETE-FB_APU04' }"
      pattern: "confirmation:\\s*['\"]DELETE-FB_APU04['\"]"
    - from: "ResetDatabaseDialog typed input"
      to: "confirm button disabled state"
      via: "input.value === 'DELETE-FB_APU04' habilita; qualquer outra coisa desabilita"
      pattern: "value\\s*===\\s*['\"]DELETE-FB_APU04['\"]"
---

<objective>
Substituir o `window.confirm` atual em `ImportarEFD.tsx:476` por um modal explícito que:
- Avisa visualmente (cor vermelha, ícone de alerta) o caráter irreversível
- Lista o que será destruído (tabelas e contagens aproximadas) — fetch a um novo endpoint NÃO necessário neste plan; usar lista estática de tabelas igual a `ResetTables` do backend, com contagem opcional pulled de `/api/jobs` se disponível
- **Exige** que o usuário digite literalmente `DELETE-FB_APU04` em um input antes de habilitar o botão "Confirmar destruição"
- Envia o token no corpo da requisição conforme contrato do plan 01-01 (`{"confirmation":"DELETE-FB_APU04"}`)
- Trata respostas estruturadas: 400 (token), 429 (rate limit), 503 (DB allowlist), 500 (backup falhou)

Purpose: alinhar o fluxo de UI com a defesa server-side. Sem isto, a UI envia DELETE sem body → o handler rejeita 400 → admin frustrado. Com isto, o admin entende **por que** o passo é difícil e o token chega correto.

Output: novo componente `ResetDatabaseDialog.tsx`, alteração mínima em `ImportarEFD.tsx` para acoplá-lo. Não toca `handleResetCompanyData` (escopo é apenas reset GLOBAL — `ResetCompanyDataHandler` é tema de outro plan se for endurecer também).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-estabiliza-o-cr-tica-reset-cache/01-01-SUMMARY.md
@frontend/src/pages/ImportarEFD.tsx
@frontend/src/components/ui/alert-dialog.tsx
@frontend/src/components/ui/input.tsx
@frontend/src/components/ui/button.tsx

<interfaces>
<!-- Primitives já presentes em frontend/src/components/ui/ — usar sem instalar nada -->

AlertDialog (Radix wrapper, shadcn) em `@/components/ui/alert-dialog`:
```tsx
import {
  AlertDialog,
  AlertDialogContent,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogAction,
  AlertDialogCancel,
} from '@/components/ui/alert-dialog';
```
Padrão controlado: `<AlertDialog open={open} onOpenChange={setOpen}>`. NÃO usar `AlertDialogTrigger` (queremos abrir programaticamente do botão existente em ImportarEFD).

Input em `@/components/ui/input`:
```tsx
import { Input } from '@/components/ui/input';
<Input value={typed} onChange={(e) => setTyped(e.target.value)} />
```

Button em `@/components/ui/button` — usar `variant="destructive"` para CTA de confirmação.

Toast (sonner) — pattern existente em ImportarEFD.tsx:
```tsx
import { toast } from 'sonner';
toast.error('mensagem');
toast.success('mensagem');
```

Auth — fetch interceptor injeta automaticamente Authorization e X-Company-ID (frontend/src/contexts/AuthContext.tsx:47). NÃO precisa adicionar headers manualmente além de Content-Type.

Token literal (do plan 01-01):
- Constante `DELETE-FB_APU04` (sensível a caso, sem espaços, sem aspas)
- Body: `JSON.stringify({ confirmation: 'DELETE-FB_APU04' })`

Status codes esperados do backend (plan 01-01 contrato):
- 200: sucesso, body `{ message, backup_path, rows_before }`
- 400: token ausente ou errado, body texto
- 403: não-admin, body texto
- 429: rate limit, body texto explicando "1 reset per hour per user"
- 503: DB não está em ALLOWED_DESTRUCTIVE_DBS, body explicando qual DB conectado
- 500: backup falhou ou TRUNCATE falhou, body texto genérico

Trecho atual a remover (ImportarEFD.tsx:475-494):
```tsx
const handleResetDatabase = async () => {
  if (!window.confirm('ATENÇÃO: Tem certeza que deseja APAGAR TODOS os dados importados? Essa ação não pode ser desfeita.')) {
    return;
  }
  try {
    const res = await fetch('/api/admin/reset-db', { method: 'DELETE' });
    if (res.ok) { toast.success('Base de dados limpa com sucesso!'); setJobs([]); }
    else { toast.error('Erro ao limpar base de dados.'); }
  } catch (error) {
    console.error('Error resetting database:', error);
    toast.error('Erro de conexão.');
  }
};
```
E o botão (ImportarEFD.tsx:546):
```tsx
<Button variant="destructive" size="sm" onClick={handleResetDatabase} className="gap-2">
  <Trash2 className="h-4 w-4" />
  Zerar Tudo (Admin)
</Button>
```

Tabelas afetadas (espelhar ResetTables do backend para mostrar no dialog):
```ts
const AFFECTED_TABLES = [
  'import_jobs (SPED + dependentes via CASCADE)',
  'filial_apelidos',
  'nfe_entradas', 'nfe_saidas', 'cte_entradas',
  'parceiros',
  'erp_bridge_run_items', 'erp_bridge_runs',
];
```
NÃO consultar contagens via API neste plan (evita adicionar endpoint só para isso). Mostrar a lista textualmente é suficiente para o objetivo "usuário entende o que será destruído".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar componente ResetDatabaseDialog.tsx</name>
  <files>
    frontend/src/components/ResetDatabaseDialog.tsx
    frontend/src/components/ResetDatabaseDialog.test.tsx
  </files>
  <behavior>
    - Test 1: Renderiza fechado quando `open={false}` — sem nó `[role=alertdialog]` no DOM
    - Test 2: Renderiza aberto quando `open={true}` — título contém "Zerar TODA a base"
    - Test 3: Botão "Confirmar destruição" começa desabilitado
    - Test 4: Após digitar "DELETE-FB_APU04" no input, botão fica habilitado
    - Test 5: Após digitar "delete-fb_apu04" (lowercase), botão CONTINUA desabilitado (case-sensitive)
    - Test 6: Após digitar "DELETE-FB_APU04 " (com espaço final), botão CONTINUA desabilitado (literal exato)
    - Test 7: Click em "Confirmar destruição" com input correto chama `onConfirm` UMA vez com body `{ confirmation: 'DELETE-FB_APU04' }`
    - Test 8: Click em "Cancelar" chama `onOpenChange(false)` e NÃO chama `onConfirm`
  </behavior>
  <action>
Criar `frontend/src/components/ResetDatabaseDialog.tsx`:

```tsx
import { useState, useEffect } from 'react';
import { AlertTriangle } from 'lucide-react';
import {
  AlertDialog,
  AlertDialogContent,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogDescription,
  AlertDialogFooter,
} from '@/components/ui/alert-dialog';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';

const REQUIRED_TOKEN = 'DELETE-FB_APU04';

const AFFECTED_TABLES = [
  'import_jobs (e dependentes via CASCADE)',
  'filial_apelidos',
  'nfe_entradas',
  'nfe_saidas',
  'cte_entradas',
  'parceiros',
  'erp_bridge_run_items',
  'erp_bridge_runs',
];

export interface ResetDatabaseDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  /** Chamado SOMENTE quando o usuário digita o token correto e clica Confirmar.
   *  Recebe o body que deve ser enviado ao backend. */
  onConfirm: (body: { confirmation: string }) => Promise<void> | void;
  /** Indica que a requisição está em voo — desabilita CTA e mostra spinner */
  loading?: boolean;
}

export function ResetDatabaseDialog({
  open,
  onOpenChange,
  onConfirm,
  loading = false,
}: ResetDatabaseDialogProps) {
  const [typed, setTyped] = useState('');

  // Reset input quando abre/fecha (evita estado vazado entre aberturas)
  useEffect(() => {
    if (!open) setTyped('');
  }, [open]);

  const tokenOk = typed === REQUIRED_TOKEN;

  return (
    <AlertDialog open={open} onOpenChange={onOpenChange}>
      <AlertDialogContent className="max-w-lg">
        <AlertDialogHeader>
          <AlertDialogTitle className="flex items-center gap-2 text-destructive">
            <AlertTriangle className="h-5 w-5" />
            Zerar TODA a base de dados
          </AlertDialogTitle>
          <AlertDialogDescription asChild>
            <div className="space-y-3 text-sm">
              <p className="font-semibold text-destructive">
                Esta ação é IRREVERSÍVEL e destruirá os dados de TODAS as empresas.
              </p>
              <p>Tabelas que serão truncadas:</p>
              <ul className="list-disc pl-5 space-y-0.5 font-mono text-xs">
                {AFFECTED_TABLES.map((t) => (
                  <li key={t}>{t}</li>
                ))}
              </ul>
              <p className="text-muted-foreground">
                Um backup automático <code>/backups/reset-&lt;timestamp&gt;.sql</code> será
                gerado antes do TRUNCATE; se o backup falhar, a operação é abortada.
              </p>
              <p className="pt-2">
                Para confirmar, digite exatamente{' '}
                <code className="rounded bg-muted px-1 py-0.5 font-mono text-destructive">
                  {REQUIRED_TOKEN}
                </code>{' '}
                abaixo:
              </p>
              <Input
                aria-label="Token de confirmação"
                value={typed}
                onChange={(e) => setTyped(e.target.value)}
                placeholder={REQUIRED_TOKEN}
                autoComplete="off"
                spellCheck={false}
                disabled={loading}
              />
            </div>
          </AlertDialogDescription>
        </AlertDialogHeader>
        <AlertDialogFooter>
          <Button
            variant="outline"
            onClick={() => onOpenChange(false)}
            disabled={loading}
          >
            Cancelar
          </Button>
          <Button
            variant="destructive"
            disabled={!tokenOk || loading}
            onClick={() => onConfirm({ confirmation: REQUIRED_TOKEN })}
          >
            {loading ? 'Executando...' : 'Confirmar destruição'}
          </Button>
        </AlertDialogFooter>
      </AlertDialogContent>
    </AlertDialog>
  );
}

export default ResetDatabaseDialog;
```

Criar `frontend/src/components/ResetDatabaseDialog.test.tsx` usando Vitest + Testing Library:

```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { ResetDatabaseDialog } from './ResetDatabaseDialog';

describe('ResetDatabaseDialog', () => {
  it('not rendered in DOM when open=false', () => {
    render(<ResetDatabaseDialog open={false} onOpenChange={() => {}} onConfirm={() => {}} />);
    expect(screen.queryByRole('alertdialog')).toBeNull();
  });

  it('renders title when open=true', () => {
    render(<ResetDatabaseDialog open={true} onOpenChange={() => {}} onConfirm={() => {}} />);
    expect(screen.getByText(/Zerar TODA a base/i)).toBeInTheDocument();
  });

  it('confirm button starts disabled', () => {
    render(<ResetDatabaseDialog open={true} onOpenChange={() => {}} onConfirm={() => {}} />);
    expect(screen.getByRole('button', { name: /Confirmar destruição/i })).toBeDisabled();
  });

  it('confirm button enables when exact token is typed', () => {
    render(<ResetDatabaseDialog open={true} onOpenChange={() => {}} onConfirm={() => {}} />);
    fireEvent.change(screen.getByLabelText('Token de confirmação'), {
      target: { value: 'DELETE-FB_APU04' },
    });
    expect(screen.getByRole('button', { name: /Confirmar destruição/i })).toBeEnabled();
  });

  it('confirm button stays disabled for case-mismatched token', () => {
    render(<ResetDatabaseDialog open={true} onOpenChange={() => {}} onConfirm={() => {}} />);
    fireEvent.change(screen.getByLabelText('Token de confirmação'), {
      target: { value: 'delete-fb_apu04' },
    });
    expect(screen.getByRole('button', { name: /Confirmar destruição/i })).toBeDisabled();
  });

  it('confirm button stays disabled with trailing space', () => {
    render(<ResetDatabaseDialog open={true} onOpenChange={() => {}} onConfirm={() => {}} />);
    fireEvent.change(screen.getByLabelText('Token de confirmação'), {
      target: { value: 'DELETE-FB_APU04 ' },
    });
    expect(screen.getByRole('button', { name: /Confirmar destruição/i })).toBeDisabled();
  });

  it('clicking Confirm calls onConfirm with body once', () => {
    const onConfirm = vi.fn();
    render(<ResetDatabaseDialog open={true} onOpenChange={() => {}} onConfirm={onConfirm} />);
    fireEvent.change(screen.getByLabelText('Token de confirmação'), {
      target: { value: 'DELETE-FB_APU04' },
    });
    fireEvent.click(screen.getByRole('button', { name: /Confirmar destruição/i }));
    expect(onConfirm).toHaveBeenCalledTimes(1);
    expect(onConfirm).toHaveBeenCalledWith({ confirmation: 'DELETE-FB_APU04' });
  });

  it('clicking Cancelar calls onOpenChange(false) and not onConfirm', () => {
    const onOpenChange = vi.fn();
    const onConfirm = vi.fn();
    render(<ResetDatabaseDialog open={true} onOpenChange={onOpenChange} onConfirm={onConfirm} />);
    fireEvent.click(screen.getByRole('button', { name: /Cancelar/i }));
    expect(onOpenChange).toHaveBeenCalledWith(false);
    expect(onConfirm).not.toHaveBeenCalled();
  });
});
```

**Pré-requisito de testes:** Vitest, @testing-library/react, jsdom precisam estar disponíveis. Verificar `frontend/package.json`. Se não houver:
- Adicionar como devDeps: `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`
- Adicionar script `"test": "vitest"` em `frontend/package.json` se ausente
- Adicionar `vitest.config.ts` mínimo configurando `environment: 'jsdom'` e `setupFiles` com `@testing-library/jest-dom`

Se o projeto NÃO tiver setup de teste (provável — CONCERNS.md indica "Only test file currently in the repo: utils.test.ts"), criar:
- `frontend/vitest.config.ts` com `environment: 'jsdom'`
- `frontend/src/test/setup.ts` importando `@testing-library/jest-dom`
- Atualizar `frontend/package.json` `"scripts": { "test": "vitest run" }`
- `npm install --save-dev vitest @testing-library/react @testing-library/jest-dom jsdom`

NOTA: STAB-08 (bootstrap completo de testes React) é phase 3. Aqui adicionamos só o mínimo para testar este componente. Se isso for inviável (npm install em offline mode), marcar `<automated>MISSING — vitest setup required by Wave 0 of Phase 3 (STAB-08)</automated>` para `verify` e prosseguir sem testes para o componente; documentar lacuna no SUMMARY.
  </action>
  <verify>
    <automated>
# Build TypeScript não falha
cd frontend && npx tsc --noEmit
# Lint não introduz novos erros
cd frontend && npm run lint -- src/components/ResetDatabaseDialog.tsx
# Testes (se vitest setup foi feito; senão, gate com MISSING)
cd frontend && npm test -- ResetDatabaseDialog
    </automated>
  </verify>
  <done>
    Componente compila com TypeScript strict; testes passam (se setup viável); botão "Confirmar destruição" só habilita com token EXATO (case-sensitive, sem padding); component fechado não fica no DOM.
  </done>
</task>

<task type="auto">
  <name>Task 2: Acoplar ResetDatabaseDialog em ImportarEFD.tsx</name>
  <files>
    frontend/src/pages/ImportarEFD.tsx
  </files>
  <action>
Em `frontend/src/pages/ImportarEFD.tsx`:

1. **Adicionar import** no topo (junto com outros imports de `@/components/...`):
   ```tsx
   import { ResetDatabaseDialog } from '@/components/ResetDatabaseDialog';
   ```

2. **Adicionar estado** dentro do componente (junto dos outros `useState`):
   ```tsx
   const [resetOpen, setResetOpen] = useState(false);
   const [resetLoading, setResetLoading] = useState(false);
   ```

3. **Substituir `handleResetDatabase`** atual (linhas 475-494) por versão que chama o backend com body e trata códigos novos:
   ```tsx
   const handleResetDatabaseConfirm = async (body: { confirmation: string }) => {
     setResetLoading(true);
     try {
       const res = await fetch('/api/admin/reset-db', {
         method: 'DELETE',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify(body),
       });
       const text = await res.text();
       if (res.ok) {
         toast.success('Base de dados limpa com sucesso. Backup gerado em /backups/.');
         setJobs([]);
         setResetOpen(false);
         return;
       }
       // Mensagens específicas por status
       if (res.status === 400) {
         toast.error('Token de confirmação rejeitado pelo servidor.');
       } else if (res.status === 403) {
         toast.error('Apenas administradores globais podem zerar a base.');
       } else if (res.status === 429) {
         toast.error('Limite de 1 reset por hora atingido. Aguarde antes de tentar novamente.');
       } else if (res.status === 503) {
         toast.error(`Servidor recusou: ${text || 'banco conectado fora do allowlist'}.`);
       } else {
         toast.error(`Erro ${res.status}: ${text || 'falha ao zerar base'}.`);
       }
     } catch (error) {
       console.error('Error resetting database:', error);
       toast.error('Erro de conexão.');
     } finally {
       setResetLoading(false);
     }
   };
   ```

4. **Remover** `handleResetDatabase` antigo (linhas 475-494). NÃO remover `handleResetCompanyData` (escopo é apenas reset GLOBAL).

5. **Trocar o botão admin** (linhas 545-550) para abrir o dialog em vez de chamar handler direto:
   ```tsx
   {user?.role === 'admin' && (
     <Button
       variant="destructive"
       size="sm"
       onClick={() => setResetOpen(true)}
       className="gap-2"
     >
       <Trash2 className="h-4 w-4" />
       Zerar Tudo (Admin)
     </Button>
   )}
   ```

6. **Adicionar o componente** no JSX (perto do final do return, antes do fechamento do container):
   ```tsx
   <ResetDatabaseDialog
     open={resetOpen}
     onOpenChange={setResetOpen}
     onConfirm={handleResetDatabaseConfirm}
     loading={resetLoading}
   />
   ```

7. NÃO mexer em `handleResetCompanyData` nem no botão "Limpar {company}".
  </action>
  <verify>
    <automated>
cd frontend && npx tsc --noEmit
cd frontend && npm run lint -- src/pages/ImportarEFD.tsx
# window.confirm para reset GLOBAL foi removido (mas pode permanecer em handleResetCompanyData — checar contexto)
# Verificar: o handleResetDatabase antigo (com window.confirm + APAGAR TODOS) foi removido
! grep -E "window\.confirm.*APAGAR TODOS os dados importados" frontend/src/pages/ImportarEFD.tsx
# Novo handler está presente
grep -c "handleResetDatabaseConfirm" frontend/src/pages/ImportarEFD.tsx | awk '$1>=2 {ok=1} END {exit !ok}'
# Body envia confirmation
grep -E "confirmation:\s*['\"]DELETE-FB_APU04['\"]|JSON\.stringify\(body\)" frontend/src/pages/ImportarEFD.tsx
# Componente foi montado
grep -c "<ResetDatabaseDialog" frontend/src/pages/ImportarEFD.tsx | grep -q "^1$"
# Build production passa
cd frontend && npm run build
    </automated>
  </verify>
  <done>
    `tsc --noEmit` passa; `npm run build` passa; o botão admin "Zerar Tudo" abre o dialog em vez de disparar fetch direto; nenhum `window.confirm` para reset GLOBAL no arquivo; novo handler envia body com `confirmation: 'DELETE-FB_APU04'`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana — fluxo destrutivo end-to-end em ambiente de homolog</name>
  <what-built>
    Modal de confirmação destrutiva com input de token, integrado ao botão "Zerar Tudo (Admin)" em /importar-efd. Backend (plan 01-01) com 5 gates já está em produção/homolog assumido (este plan depende de 01-01).
  </what-built>
  <how-to-verify>
    Em ambiente de homolog (NUNCA em produção), com banco descartável onde `ALLOWED_DESTRUCTIVE_DBS` está configurado para o nome desse banco:

    1. **Login como admin global** em https://homolog.simu.fcxlabs.com (ou local em http://localhost:3000)
    2. Navegar para /importar-efd
    3. Clicar **"Zerar Tudo (Admin)"** — verificar que abre um modal vermelho com:
       - Título "Zerar TODA a base de dados"
       - Lista de 8 tabelas em fonte monospace
       - Texto sobre backup automático
       - Input de token vazio
       - Botão "Confirmar destruição" desabilitado
    4. **Digitar token errado** ("delete-fb_apu04" minúsculo) — botão permanece desabilitado
    5. **Apagar input e digitar "DELETE-FB_APU04 "** (com espaço) — botão permanece desabilitado
    6. **Apagar e digitar "DELETE-FB_APU04"** exato — botão habilita
    7. Clicar **Cancelar** — modal fecha, NADA acontece. Verificar via psql/admin que `import_jobs` count está intacto.
    8. Reabrir o modal, digitar token novamente, clicar **Confirmar destruição**:
       - Botão muda para "Executando..." e desabilita
       - Toast verde "Base de dados limpa com sucesso. Backup gerado em /backups/."
       - Modal fecha
       - Lista de jobs na página esvazia
    9. **Verificar backup**:
       ```bash
       docker exec fb_apu04-api ls -la /backups/
       # Esperado: reset-<TS>.sql com >0 bytes
       ```
    10. **Verificar audit log**:
       ```sql
       SELECT created_at, status, scope, tables_affected, backup_path
         FROM admin_destructive_actions ORDER BY created_at DESC LIMIT 5;
       -- Esperado: linhas com status='success', backup_path preenchido
       ```
    11. **Tentar 2ª chamada** imediatamente: clicar Zerar Tudo de novo, digitar token, confirmar
        - Esperado: toast vermelho "Limite de 1 reset por hora atingido"
        - Verificar audit: linha `status='rejected_rate'`
    12. **Setar `ALLOWED_DESTRUCTIVE_DBS` para um valor errado** e reiniciar o container API; tentar reset:
        - Esperado: toast vermelho "Servidor recusou: ..." mostrando nome do DB
        - Verificar audit: linha `status='rejected_db'`
    13. **Limpar a alteração** de `ALLOWED_DESTRUCTIVE_DBS` para valor correto e restartar.
  </how-to-verify>
  <resume-signal>Type "approved" se todos os 13 passos comportam-se conforme descrito; describe issues caso contrário (ex: "passo 6 não habilitou botão", "passo 11 retornou 200 em vez de 429")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM ↔ Dialog state | Estado React local; nenhum dado sensível persistido |
| Dialog ↔ /api/admin/reset-db | Mesma boundary do plan 01-01 (autenticada via JWT, body conta com token) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | Atacante via XSS dispara reset programaticamente | mitigate | Mesmo via XSS, atacante precisa: (a) ter JWT admin (CSP atual permite roubar), (b) enviar body correto. Defense em profundidade real é o backend (plan 01-01); este plan só evita auto-disparo acidental. CSP hardening tracked em STAB-06 (phase 3). |
| T-01-02-02 | Spoofing | Botão "Confirmar" habilita com input que parece correto mas tem char Unicode invisível | mitigate | Comparação `===` JavaScript é byte-exact em strings UTF-16; chars zero-width quebram a comparação → botão NÃO habilita (comportamento desejado, não bug) |
| T-01-02-03 | Repudiation | Usuário clica e depois nega | accept | Audit no backend (plan 01-01) registra com timestamp, IP, user_id; UI não precisa registrar nada extra |
| T-01-02-04 | DoS | Usuário spam-clica botão habilitado | mitigate | `loading` desabilita botão durante fetch; backend rate limit (1/h) é o gate real |

</threat_model>

<verification>
- TypeScript compila sem novos erros: `cd frontend && npx tsc --noEmit`
- Build de produção passa: `cd frontend && npm run build`
- Testes do componente passam (se setup viável): `cd frontend && npm test -- ResetDatabaseDialog`
- Em ambiente real (Task 3): fluxo end-to-end funciona com 13 passos verificados
</verification>

<success_criteria>
1. Botão "Zerar Tudo (Admin)" abre modal e NÃO dispara fetch direto.
2. Botão "Confirmar destruição" só habilita quando input digita exatamente `DELETE-FB_APU04` (case-sensitive, sem padding).
3. Submit envia `DELETE /api/admin/reset-db` com body `{"confirmation":"DELETE-FB_APU04"}` e Content-Type: application/json.
4. Toasts diferenciados para 400, 403, 429, 503, 5xx — cada caminho tem mensagem útil.
5. Após sucesso, modal fecha e lista de jobs recarrega.
6. Após cancel, NADA é destruído (verificável no audit: nenhuma linha nova).
7. `npm run build` passa; nenhum erro novo de TypeScript ou ESLint introduzido.
</success_criteria>

<output>
Após completar, criar `.planning/phases/01-estabiliza-o-cr-tica-reset-cache/01-02-SUMMARY.md` documentando:
- Componente `ResetDatabaseDialog` exportado de `@/components/ResetDatabaseDialog`
- Contrato de uso (props, eventos)
- Por que fluxo similar **não foi aplicado** a `ResetCompanyDataHandler` neste plan (escopo: phase 1 cuida apenas do reset GLOBAL)
- Lacuna: testes Vitest podem ter sido pulados se o setup completo (STAB-08) ainda não está em curso — registrar em "TDD Notes"
</output>
</content>
</invoke>