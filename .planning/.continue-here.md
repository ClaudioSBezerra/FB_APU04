---
context: default
phase: Fase B — PRODEPE dispensa por CNPJ (ICMS Fronteira)
task: 11
total_tasks: 5
status: in_progress
last_updated: 2026-05-27T20:06:45.883Z
---

<current_state>
Implementando a Fase B do plano de benefícios fiscais do módulo ICMS Fronteira:
PRODEPE como DISPENSA de antecipação/ST na entrada, por ESTABELECIMENTO (CNPJ).
Fase A (base "por dentro" por UF) já está em PRD (commit 861248c). A Fase B começou:
a migration das tabelas foi escrita; o resto (cleanup, handlers, motor, frontend,
validação) está pendente. Nada da Fase B foi commitado.
</current_state>

<completed_work>
- Decisões fiscais fechadas com o contador (ver decisions abaixo).
- Lista de 175 NCMs do decreto da Rolimec extraída → /tmp/beneficios/rolimec_prodepe_ncms.csv
- migration `backend/migrations/124_prodepe_enquadramentos.sql` escrita (tabelas
  prodepe_enquadramentos + prodepe_ncms). Inócua; NÃO commitada (deixada no disco a pedido).
</completed_work>

<remaining_work>
- Task 11 (resto): adicionar `prodepe_enquadramentos` ao grupo "fronteira" em
  backend/handlers/admin_reset_helpers.go (limpar-dados → clean slate). prodepe_ncms sai por CASCADE.
- Task 12: handlers CRUD de enquadramento (por empresa) + import server-side de NCM
  (CSV, mesmo padrão de icms_fronteira_segmentos.go Importar) + rotas em main.go.
- Task 13: MOTOR — join prodepe_enquadramentos por CNPJ da filial recebedora +
  vigência cobrindo a data da nota, e zerar icms_devido_est para antecipação+ST:
    * SPED (icms_fronteira.go): join por `import_jobs.cnpj` (= j.cnpj), data c100.dt_doc.
    * Bloco C (icms_fronteira_nao_sped.go): join por `nfe_entradas.dest_cnpj_cpf`, data m.data_emissao.
    * Normalizar CNPJ para dígitos nos dois lados (regexp_replace).
    * DIFAL (2551/2556) NÃO entra na dispensa.
- Task 14: aba "PRODEPE" em AdministrativoTab (AdministrativoFronteira.tsx): listar
  enquadramentos, form para cadastrar (selecionar CNPJ das branches, nº ato, vigência,
  % crédito presumido) e importar CSV de NCMs.
- Task 15: validar em localhost na Rolimec.
</remaining_work>

<decisions_made>
- PRODEPE por CNPJ (estabelecimento), NÃO por UF — regime especial é do estabelecimento
  (Art. 11-A do Dec. 21.959/1999, incluído pelo Dec. 47.864/2019). Numa mesma UF coexistem
  CD com TARE e varejo normal.
- Leitura A (ampla): dispensa vale para TODAS as aquisições do CD, não apenas os NCMs da
  lista. Confirmado pelo contador via Art. 11-A. Os 175 NCMs são documentação + base do
  crédito presumido das saídas (apuração, fora do fronteira).
- Dispensa = ICMS fronteira 0 para antecipação (2101/2102/2152) e ST (2403/2409/2651/2652).
  DIFAL (2551/2556) FORA (a confirmar com contador — ajuste de 1 linha).
- Mantém o regime original na classificação, só zera o valor (transparência no relatório).
</decisions_made>

<blockers>
- Nenhum bloqueador técnico. Pendência humana não-bloqueante: confirmar DIFAL com contador.
</blockers>

## Required Reading (in order)
1. `memory/project_fases_beneficios_fronteira.md` — plano faseado completo (A feita; B em andamento; C/D pendentes) e decisões/dados da Rolimec.
2. `backend/migrations/124_prodepe_enquadramentos.sql` — schema das tabelas já desenhado.
3. `backend/handlers/icms_fronteira.go` (motor SPED) e `icms_fronteira_nao_sped.go` (Bloco C) — onde aplicar a dispensa (mesmo ponto onde a Fase A aplicou `base_por_dentro`).
4. `backend/handlers/icms_fronteira_segmentos.go` — padrão de import server-side (CSV) a reusar para os NCMs.

## Critical Anti-Patterns (do NOT repeat)
- [TSC MASCARADO]: nunca rodar `npx tsc --noEmit | head` — o pipe mascara o exit code e deixou passar um `AlertTriangle` não importado que quebrou PRD. Rodar `npx tsc --noEmit; echo "exit: $?"`.
- [IMPORT CLIENT-SIDE FRÁGIL]: parsing de CSV no browser não detectava TAB/BOM. Fazer parsing no SERVIDOR (multipart).

## Infrastructure State
- localhost Docker (stack fb_apu04): web:3003, api:8084, db (fiscal_apu04_db). Login admin: claudio_bezerra@hotmail.com / 123456.
- PRD em commit 861248c (Fase A). Migration 124 roda no startup do container quando commitada — ainda NÃO commitada.
- Empresa de teste = Rolimec ("MASTER", company_id 0fce5c57-e0f1-4554-9785-8fb8bcd040ff). CD PE = CNPJ 01612046000124; filial BA = 01612046000558.

<context>
Sessão muito longa; pausada por contexto quase esgotado, não por bloqueio. A Fase A foi
entregue e validada. A Fase B tem o desenho 100% fechado e a migration pronta — falta
a implementação (handlers, motor, frontend), que é direta seguindo os padrões existentes
(a Fase A é o molde de onde mexer no motor; os segmentos são o molde do import).
</context>

<next_action>
Começar adicionando `{Table: "prodepe_enquadramentos", ResultKey: "prodepe_enquadramentos"}`
ao grupo "fronteira" em backend/handlers/admin_reset_helpers.go. Depois implementar
handlers (task 12) e o motor (task 13). Validar em localhost antes de commit.
</next_action>
