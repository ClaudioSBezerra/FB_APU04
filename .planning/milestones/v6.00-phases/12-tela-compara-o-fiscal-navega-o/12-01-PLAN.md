---
phase: 12-tela-compara-o-fiscal-navega-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/handlers/fiscal_comparacao.go
  - backend/handlers/fiscal_comparacao_csv.go
  - backend/main.go
autonomous: true
requirements: [TPF-06, TPF-07]

must_haves:
  truths:
    - "Admin busca uma NF-e de saída por número ou chave de acesso e recebe até 20 candidatos com company_id da JWT"
    - "Admin lê a comparação item a item de uma nota: esperado (nfe_saidas_itens) vs calculado (fiscal_execution_items) para ICMS, ICMS-ST, PIS, COFINS, IBS, CBS"
    - "Itens nunca executados (sem linha em fiscal_execution_items) retornam status 'not_executed', distinto de 'error'/'sem_grupo_fiscal'/'pending'"
    - "IBS calculado vem somado (valor_ibs_uf + valor_ibs_mun) num único total, comparável a v_ibs"
    - "Admin baixa CSV com as 6 impostos × esperado/calculado/diferença da nota"
    - "Usuário sem role admin recebe 403/401 ao chamar qualquer um dos 3 endpoints diretamente"
  artifacts:
    - path: "backend/handlers/fiscal_comparacao.go"
      provides: "FiscalComparacaoSearchHandler + FiscalComparacaoReadHandler"
      contains: "func FiscalComparacaoReadHandler"
    - path: "backend/handlers/fiscal_comparacao_csv.go"
      provides: "FiscalComparacaoCSVHandler (encoding/csv stream)"
      contains: "func FiscalComparacaoCSVHandler"
    - path: "backend/main.go"
      provides: "3 rotas admin-gated"
      contains: "/api/fiscal/comparacao"
  key_links:
    - from: "backend/handlers/fiscal_comparacao.go"
      to: "nfe_saidas_itens LEFT JOIN fiscal_execution_items"
      via: "ON fei.nfe_item_id = nsi.id, WHERE nsi.nfe_id = $1 AND nsi.company_id = $2"
      pattern: "LEFT JOIN fiscal_execution_items"
    - from: "backend/main.go"
      to: "withAuth(..., \"admin\")"
      via: "registro das 3 rotas"
      pattern: "comparacao.*admin"
---

<objective>
Construir a superfície backend da tela Comparação Fiscal: três handlers HTTP novos (busca de NF-e por número/chave, leitura da comparação esperado×calculado item a item, exportação CSV) — todos admin-gated e company-scoped. Nenhuma lógica fiscal nova: reaproveita `fiscal_execution_items` (migration 147, Fase 11) e `nfe_saidas_itens` já existentes.

Purpose: Fornecer os dados que a tela React (Plan 12-02) vai buscar, disparar e comparar. Sem estes endpoints, `fiscal_execution_items` só é lida por curl manual.
Output: `fiscal_comparacao.go`, `fiscal_comparacao_csv.go`, 3 rotas em `main.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-tela-compara-o-fiscal-navega-o/12-RESEARCH.md
@.planning/phases/12-tela-compara-o-fiscal-navega-o/12-PATTERNS.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do codebase — não explorar de novo. -->

Helpers já existentes (backend/handlers):
- func GetEffectiveCompanyID(db *sql.DB, userID, requestedCompanyID string) (string, error)  // auth.go:274
- func jsonErr(w http.ResponseWriter, status int, msg string, extra ...map[string]string)     // ai_query.go:55
- ClaimsKey (context key), jwt.MapClaims em r.Context().Value(ClaimsKey)

Esperado — nfe_saidas_itens (migrations 075/141/146): id (UUID), nfe_id, company_id, n_item, c_prod, x_prod, ncm, cfop,
  v_bc_icms, v_icms, v_bc_st, v_st, v_bc_pis, v_pis, v_bc_cofins, v_cofins, v_ibs, v_cbs

Calculado — fiscal_execution_items (migration 147): nfe_item_id (FK→nfe_saidas_itens.id, UNIQUE), company_id,
  status ('pending'|'ok'|'error'|'sem_grupo_fiscal'), error_message, executed_at, grupo_fiscal_codigo,
  base_calculo_icms, valor_icms, base_substituicao, valor_substituicao, base_calculo_pis, valor_pis,
  base_calculo_cofins, valor_cofins, percentual_difal, valor_icms_partilha_destino, valor_icms_pobreza,
  valor_ibs_uf, valor_ibs_mun, valor_cbs, full_result (JSONB)

Header — nfe_saidas (migration 058): id, company_id, chave_nfe, numero_nfe, serie, dest_nome, data_emissao

Rotas existentes já admin-gated (main.go 532/535): withAuth(handlers.FiscalExecutionRunHandler, "admin")
Par JSON+CSV existente (main.go 1305-1306): ConciliacaoCSVHandler / ConciliacaoHandler
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handlers de busca e leitura da comparação (fiscal_comparacao.go)</name>
  <files>backend/handlers/fiscal_comparacao.go</files>
  <read_first>
    - backend/handlers/admin_nf_cancelamento.go (padrão exato de busca ILIKE server-side por parâmetro)
    - backend/handlers/fiscal_execution.go (guard IDOR duplo linhas 122-139 e query de itens 150-159)
    - backend/handlers/xml_conciliacao.go (shape do ConciliacaoHandler: método GET, claims, GetEffectiveCompanyID, sempre Encode de slice não-nil)
    - backend/migrations/147_fiscal_execution_items.sql (colunas do lado calculado)
    - .planning/phases/12-tela-compara-o-fiscal-navega-o/12-RESEARCH.md (Pattern 2 e Pattern 3 — SQL já redigido; Pitfall 1 e Pitfall 2)
  </read_first>
  <action>
    Criar `package handlers` com dois handlers `func(db *sql.DB) http.HandlerFunc`.

    FiscalComparacaoSearchHandler (GET /api/fiscal/comparacao/search?q=...): valida método GET (senão jsonErr 405 "Método não permitido"), extrai claims via ClaimsKey (senão jsonErr 401), resolve companyID via GetEffectiveCompanyID (senão jsonErr 500). Lê `q := strings.TrimSpace(r.URL.Query().Get("q"))`; se `len(q) < 3` responde `json.NewEncoder(w).Encode([]any{})` e retorna (nunca null, nunca roda a query). Query parametrizada contra nfe_saidas: `SELECT id, chave_nfe, COALESCE(numero_nfe,''), COALESCE(serie,''), COALESCE(dest_nome,''), TO_CHAR(data_emissao,'DD/MM/YYYY') FROM nfe_saidas WHERE company_id = $1 AND (numero_nfe ILIKE '%'||$2||'%' OR chave_nfe ILIKE '%'||$2||'%') ORDER BY data_emissao DESC LIMIT 20` com args (companyID, q). NUNCA usar fmt.Sprintf/concatenação de q no SQL (T-12-03). Scan para struct com json tags id/chave_nfe/numero_nfe/serie/dest_nome/data_emissao; Encode de slice inicializada `[]NfeSearchResult{}`.

    FiscalComparacaoReadHandler (GET /api/fiscal/comparacao?nfe_id=...): mesmo preâmbulo GET/claims/companyID. Lê `nfeID := r.URL.Query().Get("nfe_id")`; se vazio jsonErr 400. Query LEFT JOIN (base já redigida em RESEARCH.md Pattern 2), com estas duas adaptações OBRIGATÓRIAS: (1) selecionar `COALESCE(fei.status, 'not_executed')` para distinguir item nunca executado (linha ausente no LEFT JOIN → NULL) de erro real — este é o 4º estado resolvido na open-question do planejamento, NÃO colapsar em 'error'/'sem_grupo_fiscal'; (2) selecionar o IBS calculado já somado como `COALESCE(fei.valor_ibs_uf,0) + COALESCE(fei.valor_ibs_mun,0) AS valor_ibs_total` (Pitfall 2 — não existe coluna total; somar uma vez no SQL, reusável pelo CSV). WHERE `nsi.nfe_id = $1 AND nsi.company_id = $2` (guard IDOR duplo — nunca só por nfe_id), ORDER BY nsi.n_item ASC. Colunas esperado: n_item, c_prod, x_prod, ncm, cfop, v_bc_icms, v_icms, v_bc_st, v_st, v_bc_pis, v_pis, v_bc_cofins, v_cofins, v_ibs, v_cbs. Colunas calculado: status(coalesced), error_message, executed_at, base_calculo_icms, valor_icms, base_substituicao, valor_substituicao, base_calculo_pis, valor_pis, base_calculo_cofins, valor_cofins, valor_ibs_total, valor_cbs, percentual_difal, valor_icms_partilha_destino, valor_icms_pobreza, grupo_fiscal_codigo. Usar sql.NullFloat64/*float64 para os campos calculados (podem ser NULL no LEFT JOIN). Encode de slice inicializada. Extrair a função de query numa helper interna (ex.: `queryComparacaoRows(db, nfeID, companyID) ([]ComparacaoRow, error)`) para reuso pelo handler CSV do Task 2.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./handlers/ && grep -q "COALESCE(fei.status, 'not_executed')" handlers/fiscal_comparacao.go && grep -q "valor_ibs_uf,0) + COALESCE(fei.valor_ibs_mun,0)" handlers/fiscal_comparacao.go && grep -q "nsi.company_id = \$2" handlers/fiscal_comparacao.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` compila sem erro e `go vet ./handlers/` passa
    - Source contém `COALESCE(fei.status, 'not_executed')` (4º estado "nunca executado")
    - Source soma `valor_ibs_uf + valor_ibs_mun` no SQL (Pitfall 2)
    - Toda query com dados da nota inclui `company_id = $N` (guard IDOR); `q` entra apenas como placeholder parametrizado
    - Search retorna `[]` (não null) quando len(q) < 3
    - Helper de query compartilhável exportada/interna disponível para o handler CSV
  </acceptance_criteria>
  <done>Os dois handlers compilam, buscam por número/chave company-scoped, e retornam a comparação item a item com o 4º estado e o IBS somado.</done>
</task>

<task type="auto">
  <name>Task 2: Handler CSV + registro das 3 rotas admin-gated</name>
  <files>backend/handlers/fiscal_comparacao_csv.go, backend/main.go</files>
  <read_first>
    - backend/handlers/xml_conciliacao.go (ConciliacaoCSVHandler linhas 310-386: headers Content-Type/Content-Disposition, csv.NewWriter, loop de linhas, Flush + Error)
    - backend/handlers/fiscal_comparacao.go (helper de query criada no Task 1 — reusar, não duplicar a aritmética do IBS)
    - backend/main.go (bloco de rotas fiscais linhas 532/535 e par CSV+JSON 1305-1306)
    - .planning/phases/12-tela-compara-o-fiscal-navega-o/12-RESEARCH.md (Open Question 2 — escopo do CSV)
  </read_first>
  <action>
    Criar FiscalComparacaoCSVHandler (GET /api/fiscal/comparacao/csv?nfe_id=...) em fiscal_comparacao_csv.go. Mesmo preâmbulo de auth/companyID/nfe_id do read handler; reusar a helper `queryComparacaoRows` do Task 1 (NÃO reimplementar a soma IBS). Setar `Content-Type: text/csv; charset=utf-8` e `Content-Disposition: attachment; filename="comparacao-fiscal.csv"`. `csv.NewWriter(w)`, escrever header PT-BR e uma linha por item.

    Escopo das colunas do CSV (resolvido na open-question do planejamento): espelhar SOMENTE as 6 impostos × esperado/calculado/diferença da tabela visível + identificação — NÃO incluir DIFAL (percentual_difal/valor_icms_partilha_destino), FCP (valor_icms_pobreza), grupo_fiscal_codigo nem full_result (esses são dialog-only). Header: Item, Produto, NCM, CFOP, Status, e para cada imposto (ICMS, ICMS-ST, PIS, COFINS, IBS, CBS) três colunas Esperado/Calculado/Diferença. Diferença = esperado - calculado. Valores numéricos via `fmt.Sprintf("%.2f", ...)`. Para IBS usar o total já somado (valor_ibs_total). `cw.Flush()` e checar `cw.Error()` no fim (log em erro, padrão do análogo).

    Em main.go, registrar as 3 rotas no mesmo bloco de /api/fiscal/execute (linha 535), TODAS com role "admin" (nunca ""; Pitfall 4 — a rota é o único limite real de acesso, o gate de nav é cosmético): `/api/fiscal/comparacao/search` → withAuth(handlers.FiscalComparacaoSearchHandler, "admin"); `/api/fiscal/comparacao` → withAuth(handlers.FiscalComparacaoReadHandler, "admin"); `/api/fiscal/comparacao/csv` → withAuth(handlers.FiscalComparacaoCSVHandler, "admin"). Registrar a rota mais específica (/csv, /search) antes da genérica, conforme convenção do mux.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./handlers/ && grep -c "withAuth(handlers.FiscalComparacao" main.go | grep -qx 3 && grep -E "FiscalComparacao(Search|Read|CSV)Handler, \"admin\"" main.go | grep -vc '"admin"' | grep -qx 0 && ! grep -qi "valor_icms_pobreza\|percentual_difal" handlers/fiscal_comparacao_csv.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` compila e `go vet ./handlers/` passa
    - Exatamente 3 rotas `withAuth(handlers.FiscalComparacao*, "admin")` registradas — nenhuma com `""`
    - CSV handler reusa a helper de query do Task 1 (sem re-somar IBS)
    - CSV NÃO contém colunas DIFAL/FCP (grep por valor_icms_pobreza/percentual_difal falha no arquivo CSV)
    - Header CSV cobre as 6 impostos × Esperado/Calculado/Diferença + identificação
  </acceptance_criteria>
  <done>CSV baixável com o escopo das 6 impostos da tabela; 3 rotas registradas exclusivamente com admin gate; build e vet limpos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (novos 3 endpoints) | `q`, `nfe_id`, `X-Company-ID` são input não-confiável do cliente |
| API → Postgres | queries parametrizadas cruzam para o banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | FiscalComparacaoReadHandler / SearchHandler / CSVHandler | mitigate | Toda query filtra `company_id = $N` resolvido de `GetEffectiveCompanyID(JWT)`; leitura da nota usa guard duplo `WHERE nsi.nfe_id=$1 AND nsi.company_id=$2` — nunca confia em nfe_id/company_id do corpo/cliente (padrão T-11-14) |
| T-12-02 | Elevation of Privilege | main.go registro das 3 rotas | mitigate | `withAuth(handler, "admin")` nas 3 rotas — role string explícita "admin", nunca "" (Pitfall 4). É o único limite de acesso real; o gate de nav (Plan 12-03) é apenas UX |
| T-12-03 | Tampering | FiscalComparacaoSearchHandler (`q`) | mitigate | `ILIKE '%'||$2||'%'` parametrizado; `q` jamais concatenado/`fmt.Sprintf` no SQL |
| T-12-04 | Tampering | nfe_id param (read/csv) | mitigate | nfe_id passado como placeholder `$1` contra coluna UUID — type-check do Postgres falha com segurança em input malformado; sem interpolação de string |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Esta fase instala ZERO pacotes novos (RESEARCH.md Package Legitimacy Audit: N/A); nada a verificar |
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./handlers/` limpos
- 3 rotas admin-gated presentes em main.go
- Grep confirma 4º estado (not_executed), soma IBS, guard company_id, e ausência de DIFAL/FCP no CSV
</verification>

<success_criteria>
- Search retorna até 20 NF-e por número/chave, company-scoped
- Read retorna comparação item a item das 6 impostos com status coalesced e IBS somado
- CSV baixável no escopo das 6 impostos
- Todos os 3 endpoints exigem role admin server-side
</success_criteria>

<output>
Create `.planning/phases/12-tela-compara-o-fiscal-navega-o/12-01-SUMMARY.md` when done
</output>
