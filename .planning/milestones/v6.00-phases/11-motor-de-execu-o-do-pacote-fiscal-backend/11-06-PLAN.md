---
phase: 11-motor-de-execu-o-do-pacote-fiscal-backend
plan: 06
type: execute
wave: 4
depends_on: [11-05]
files_modified: []
autonomous: false
requirements: [TPF-05]
must_haves:
  truths:
    - "Uma execucao real de lote contra Oracle produz linhas em fiscal_execution_items com status ok e valores numericos calculados"
    - "As duas pegadinhas do go-ora (buffer OUT string; IdRegraCalculo* VARCHAR2) NAO se manifestam com dados reais nao-vazios"
    - "Um item de filial nao mapeada (fora de Recife/PE) vira erro explicito sem derrubar o lote"
  artifacts: []
  key_links:
    - from: "POST /api/fiscal/execute"
      to: "fiscal_execution_items"
      via: "execucao real contra Oracle + persistencia"
      pattern: "status = 'ok'"
---

<objective>
Validacao end-to-end da fase inteira: rodar uma execucao real do endpoint de
lote contra o Oracle de producao e inspecionar fiscal_execution_items. Este e
o unico ponto onde as pegadinhas do go-ora (Pitfall 1 buffer OUT string e
Pitfall 2 IdRegraCalculo* VARCHAR2) podem se manifestar, pois so aparecem com
uma chamada real do pacote retornando dados nao-vazios — o Ping do Plan 01 nao
exercita os 88 binds OUT.

Purpose: Fechar o maior risco tecnico residual da fase de forma barata (uma
chamada real) antes de a Fase 12 depender destes dados. Este plano nao escreve
codigo — e um checkpoint de verificacao humana.
Output: Confirmacao de que o porte funciona end-to-end no ambiente real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-RESEARCH.md
@.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-05-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Rodar lote real e inspecionar fiscal_execution_items</name>
  <read_first>
    - .planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-RESEARCH.md  (Common Pitfalls 1 e 2 — o que observar)
  </read_first>
  <action>Rodar uma execucao real de lote (POST /api/fiscal/execute) para uma nfe_id de Recife/PE e inspecionar fiscal_execution_items, confirmando linhas status='ok' com valores calculados e ausencia das pegadinhas do go-ora. Nenhuma alteracao de codigo nesta task.</action>
  <what-built>
    Endpoint admin POST /api/fiscal/execute (Plan 05) que executa o pacote fiscal
    para os itens de uma nota de saida (nfe_saidas_itens) da company de Recife/PE
    e persiste os resultados em fiscal_execution_items.
  </what-built>
  <how-to-verify>
    1. Rodar o backend no ambiente com acesso Oracle (o mesmo validado no Plan 01), com as migrations 146/147 ja aplicadas.
    2. Escolher uma nfe_id de saida real da company de Recife/PE (CNPJ raiz 10230480) com varios itens.
    3. Autenticar como admin e chamar: curl -X POST https://<host>/api/fiscal/execute -H "Authorization: Bearer <token>" -H "X-Company-ID: <company>" -d '{"nfe_id":"<uuid>"}'.
    4. Conferir a resposta JSON: total > 0 e ok > 0 (ao menos um item calculado com sucesso).
    5. No Postgres: SELECT status, count(*) FROM fiscal_execution_items GROUP BY status; — confirmar linhas com status 'ok' e valores numericos preenchidos (base_calculo_icms, valor_icms etc.).
    6. Inspecionar full_result de uma linha 'ok': confirmar que campos OUT string vieram preenchidos (Pitfall 1 NAO ocorreu — sem ORA-06502 buffer) e que os IdRegraCalculo* aparecem como texto tipo "IVA_..." (Pitfall 2 NAO ocorreu — sem erro char-to-number).
    7. (Opcional, se houver) rodar para uma nota de filial fora de Recife/PE e confirmar status explicito (sem_grupo_fiscal/error) sem derrubar o lote.
  </how-to-verify>
  <acceptance_criteria>
    - Humano confirma resposta com ok > 0 e ao menos uma linha status='ok' em fiscal_execution_items com valores numericos calculados.
    - Nenhum ORA-06502 (buffer too small nem char-to-number) observado nos logs durante a execucao.
    - Campos IdRegraCalculo* aparecem como string preenchida em full_result.
    - Item de filial nao mapeada (se testado) vira status explicito sem abortar o lote.
  </acceptance_criteria>
  <resume-signal>Digite "ok - lote real validado" com um resumo da distribuicao de status, ou descreva a falha (ORA-xxxxx, timeout, etc.).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execucao real (admin) -> Oracle producao | verificacao manual controlada por humano autenticado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-19 | Availability | execucao real do pacote | mitigate | Checkpoint humano de-risca Pitfalls 1/2 do go-ora antes da Fase 12 depender dos dados; falha detectada aqui e barata |
| T-11-20 | Information Disclosure | inspecao de full_result | accept | Inspecao feita por admin em ambiente controlado; dados fiscais internos da propria company |
</threat_model>

<verification>
- Execucao real retorna ok > 0 e persiste linhas 'ok' em fiscal_execution_items.
- Nenhuma pegadinha do go-ora se manifesta com dados reais.
</verification>

<success_criteria>
- Porte end-to-end validado contra Oracle real no ambiente do FB_APU04: fundacao pronta para a Fase 12 exibir esperado vs. calculado.
</success_criteria>

<output>
Create `.planning/phases/11-motor-de-execu-o-do-pacote-fiscal-backend/11-06-SUMMARY.md` when done
</output>
