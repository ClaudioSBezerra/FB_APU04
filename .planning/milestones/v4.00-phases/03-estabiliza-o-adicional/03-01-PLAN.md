---
phase: 03-estabiliza-o-adicional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/.env
  - installer/.env
  - erp-bridge-aws/config-apu04.yaml
  - coolify-env-template.txt
autonomous: false
requirements:
  - STAB-06

must_haves:
  truths:
    - "Nenhum segredo real existe em qualquer arquivo .env ou config yaml versionado"
    - "backend/.env permanece no repositório mas contém apenas placeholders"
    - "O desenvolvedor sabe exatamente quais variáveis configurar no Coolify"
    - "A aplicação em produção continua funcionando pois os segredos reais vivem no Coolify"
  artifacts:
    - path: "backend/.env"
      provides: "Template de variáveis de ambiente para dev local — sem segredos reais"
      contains: "SMTP_PASSWORD=your_smtp_password_here"
    - path: "installer/.env"
      provides: "Template de instalação sem credenciais"
      contains: "SMTP_PASSWORD=your_smtp_password_here"
    - path: "erp-bridge-aws/config-apu04.yaml"
      provides: "Configuração do bridge sem senhas Oracle reais"
      contains: "senha: \"CHANGE_ME\""
    - path: "coolify-env-template.txt"
      provides: "Guia completo de variáveis a configurar no Coolify"
  key_links:
    - from: "backend/.env"
      to: "Coolify dashboard (simu.fcxlabs.com service)"
      via: "godotenv.Load() respeita vars já injetadas pelo Coolify"
      pattern: "os.Getenv\\(\"SMTP_PASSWORD\"\\)"
    - from: "erp-bridge-aws/config-apu04.yaml"
      to: "Docker container do bridge"
      via: "Arquivo montado na imagem — não deve conter senhas reais"
      pattern: "senha:.*CHANGE_ME"
---

<objective>
Remover todos os segredos reais dos arquivos versionados do repositório, substituindo-os por
placeholders descritivos. Os segredos reais (SMTP password, ZAI API key, senhas Oracle) já
existem no Coolify e no ambiente de produção — este plano apenas limpa os arquivos estáticos.

Purpose: Credenciais hardcoded em .env e config yaml representam risco de vazamento caso o
repositório seja clonado ou tornado público. A aplicação em produção já usa Coolify para injetar
as variáveis — o arquivo .env local é redundante e perigoso.

Output: backend/.env e installer/.env com placeholders; erp-bridge-aws/config-apu04.yaml sem
senha fcosta2013; coolify-env-template.txt atualizado como referência canônica; checkpoint de
confirmação que o Coolify tem os valores corretos antes de qualquer redeploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-estabiliza-o-adicional/03-RESEARCH.md
@.planning/phases/03-estabiliza-o-adicional/03-PATTERNS.md

Contexto específico deste plano:

backend/.env linha 16: SMTP_PASSWORD=Proxy#6939 (senha real de produção)
backend/.env linha 23: ZAI_API_KEY=985fc97cd618417dabaee4500d8f15d3.HYv6843AfzUl6tS3 (API key real)
installer/.env linha 17: SMTP_PASSWORD=teste123 (placeholder mas deve ser padronizado)
erp-bridge-aws/config-apu04.yaml: 12 entradas com senha: "fcosta2013" (senha Oracle real)
coolify-env-template.txt: já existe no projeto, já contém nomes corretos das variáveis

O Go lê os segredos via os.Getenv() em:
  backend/services/email.go linha 67: password := os.Getenv("SMTP_PASSWORD")
  backend/services/ai.go linha 66: apiKey := os.Getenv("ZAI_API_KEY")
  backend/main.go linha 257: _ = godotenv.Load() (não sobrescreve vars já injetadas pelo Coolify)

REGRA CRÍTICA: NÃO deletar os arquivos .env. Substituir apenas os valores secretos por
placeholders. O arquivo precisa existir para que o projeto funcione em dev local com
variáveis fakes configuradas.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir segredos reais por placeholders nos arquivos de configuração</name>
  <files>backend/.env, installer/.env, erp-bridge-aws/config-apu04.yaml</files>
  <action>
  Editar backend/.env: substituir o valor da linha 16 de "Proxy#6939" para "your_smtp_password_here"
  e o valor da linha 23 da ZAI_API_KEY para "your_zai_api_key_here". Todos os outros valores em
  backend/.env (DB_PASSWORD=postgres, JWT_SECRET, etc.) são valores de dev local aceitáveis e
  NÃO devem ser alterados.

  Editar installer/.env: substituir o valor da linha 17 de SMTP_PASSWORD de "teste123" para
  "your_smtp_password_here" para consistência com o padrão adotado em backend/.env.

  Editar erp-bridge-aws/config-apu04.yaml: encontrar todas as ocorrências de senha: "fcosta2013"
  (há 12 ocorrências, nas linhas aproximadamente 21, 27, 33, 39, 45, 51, 57, 63, 69, 75, 81, 87)
  e substituir CADA UMA por senha: "CHANGE_ME". Usar substituição global — não alterar nenhuma
  outra chave do arquivo (usuario, dsn, nome, etc.).

  Verificar que backend/.env ainda contém o arquivo completo com todas as outras variáveis
  intactas. Não remover comentários nem outras linhas.
  </action>
  <verify>
    <automated>
grep -c "Proxy#6939\|ZAI_API_KEY=985fc97\|fcosta2013" /home/claudiobezerra/projetos/FB_APU04/backend/.env /home/claudiobezerra/projetos/FB_APU04/installer/.env /home/claudiobezerra/projetos/FB_APU04/erp-bridge-aws/config-apu04.yaml 2>/dev/null | grep -v ":0" | wc -l
    </automated>
  </verify>
  <done>
- grep retorna 0 (nenhum arquivo contém segredos reais)
- backend/.env ainda possui SMTP_PASSWORD= e ZAI_API_KEY= mas com placeholders
- erp-bridge-aws/config-apu04.yaml possui 12 entradas com senha: "CHANGE_ME"
- installer/.env possui SMTP_PASSWORD=your_smtp_password_here
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar coolify-env-template.txt como referência canônica de configuração</name>
  <files>coolify-env-template.txt</files>
  <action>
  Ler o conteúdo atual de coolify-env-template.txt. Verificar se ele já contém as entradas para
  SMTP_PASSWORD e ZAI_API_KEY com descrição clara de onde obter os valores. Se já contiver,
  apenas adicionar ao final uma seção de instrução de uso com os passos:

  1. Acesse o Coolify dashboard para o serviço simu.fcxlabs.com (ou simu.fbtax.cloud)
  2. Vá em Environment Variables
  3. Confirme que SMTP_PASSWORD está definido com a senha real do Hostinger SMTP
  4. Confirme que ZAI_API_KEY está definido com a chave real do Z.AI GLM
  5. Após confirmar, o serviço não precisa ser reimplantado (vars já estão no container ativo)
  6. Para o bridge (fbtax-bridge-apu04 container no AWS): editar o arquivo
     /opt/apps/fbtax/erp-bridge-aws/config-apu04.yaml no servidor e restaurar as senhas Oracle
     reais que foram substituídas por CHANGE_ME neste plano

  Se o arquivo não contiver a entrada para ZAI_API_KEY, adicionar a linha com descrição
  "Z.AI GLM API key — obtida em https://bigmodel.cn -> API Keys".

  NÃO adicionar os valores reais ao arquivo — apenas nomes e instruções de onde obtê-los.
  </action>
  <verify>
    <automated>
grep -c "SMTP_PASSWORD\|ZAI_API_KEY" /home/claudiobezerra/projetos/FB_APU04/coolify-env-template.txt
    </automated>
  </verify>
  <done>
- coolify-env-template.txt contém ao menos 2 linhas (uma para cada variável)
- Arquivo contém instruções de onde obter cada valor
- Nenhum segredo real está no arquivo
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Task 1 substituiu segredos reais em backend/.env, installer/.env e erp-bridge-aws/config-apu04.yaml
  por placeholders. Task 2 atualizou coolify-env-template.txt com instruções de configuração.
  </what-built>
  <how-to-verify>
  1. Confirme que o Coolify tem as variáveis reais configuradas para o serviço do backend:
     - SMTP_PASSWORD: deve ser a senha real do Hostinger SMTP (não "your_smtp_password_here")
     - ZAI_API_KEY: deve ser a chave real do Z.AI GLM

  2. Confirme que o serviço de backend em produção (simu.fcxlabs.com) ainda está funcionando:
     - Acesse simu.fcxlabs.com/login — deve carregar normalmente
     - Se o Coolify tiver as vars corretas, o serviço não precisa ser reimplantado

  3. Para o bridge AWS: o arquivo config-apu04.yaml no servidor AWS
     (/opt/apps/fbtax/erp-bridge-aws/config-apu04.yaml) NÃO foi alterado por este plano
     (ele não é o arquivo versionado). Mas confirme que o container do bridge em produção
     usa seu próprio config, não o do repo.

  4. Execute no terminal local para confirmar que nenhum segredo real permanece:
     grep -r "Proxy#6939\|985fc97cd618417d\|fcosta2013" backend/.env installer/.env erp-bridge-aws/config-apu04.yaml
     (deve retornar vazio)
  </how-to-verify>
  <resume-signal>
  Digite "aprovado" para confirmar que: (a) Coolify tem os valores reais das variáveis,
  (b) produção ainda funciona, (c) grep retornou vazio. Ou descreva o problema encontrado.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositório git → ambiente de produção | Segredos NÃO devem transitar por git; devem existir apenas no Coolify |
| arquivo .env local → container Docker | godotenv.Load() não deve sobrescrever vars injetadas pelo Coolify |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | backend/.env | mitigate | Substituir segredos reais por placeholders neste plano |
| T-03-01-02 | Information Disclosure | erp-bridge-aws/config-apu04.yaml | mitigate | Substituir senha Oracle por CHANGE_ME neste plano |
| T-03-01-03 | Spoofing | Coolify env vars | accept | Coolify injeta vars no container; godotenv não sobrescreve vars já definidas |
| T-03-01-04 | Tampering | config-apu04.yaml no servidor AWS | accept | O arquivo no servidor AWS (/opt/apps/fbtax/erp-bridge-aws/) é separado do repo; não foi modificado |
</threat_model>

<verification>
# Verificar que nenhum segredo real permanece nos arquivos versionados
grep -r "Proxy#6939\|985fc97cd618417d\|fcosta2013" \
  /home/claudiobezerra/projetos/FB_APU04/backend/.env \
  /home/claudiobezerra/projetos/FB_APU04/installer/.env \
  /home/claudiobezerra/projetos/FB_APU04/erp-bridge-aws/config-apu04.yaml \
  && echo "FALHA: segredos ainda presentes" || echo "OK: nenhum segredo real encontrado"

# Verificar que os placeholders estão corretos
grep "SMTP_PASSWORD\|ZAI_API_KEY" /home/claudiobezerra/projetos/FB_APU04/backend/.env

# Verificar que config-apu04.yaml tem 12 ocorrências de CHANGE_ME
grep -c "CHANGE_ME" /home/claudiobezerra/projetos/FB_APU04/erp-bridge-aws/config-apu04.yaml
</verification>

<success_criteria>
- grep não encontra "Proxy#6939", "985fc97cd618417d" ou "fcosta2013" em nenhum arquivo versionado
- backend/.env contém SMTP_PASSWORD=your_smtp_password_here e ZAI_API_KEY=your_zai_api_key_here
- erp-bridge-aws/config-apu04.yaml contém 12 ocorrências de senha: "CHANGE_ME"
- coolify-env-template.txt documenta SMTP_PASSWORD e ZAI_API_KEY com instruções de onde obtê-los
- Produção em simu.fcxlabs.com continua funcionando (Coolify injeta os valores reais)
</success_criteria>

<output>
After completion, create `.planning/phases/03-estabiliza-o-adicional/03-01-SUMMARY.md`
</output>
